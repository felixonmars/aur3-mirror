BASIC=`basename $0 | sed "s/\.sh//"`

# Список директорий
DIR='/data/shells/bash'
SHELLS_DIR="/usr/bin"
LOG_DIR="/var/log"
LIB_DIR="/etc/money_counter"
mkdir "${DIR}/tmp/${BASIC}"
TMP_DIR="${DIR}/tmp/${BASIC}"
RES_DIR="${DIR}/res"

# Список файлов
LOG="${LOG_DIR}/${BASIC}.log"
TMP="${TMP_DIR}/${BASIC}.tmp"
RES="${RES_DIR}/${BASIC}.res"
PRN="${DIR}/print.txt"

# Прочее
CODE_ERROR='0'
COUNT='0'
STRING=''
DOING=''
