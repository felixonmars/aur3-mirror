diff -Naur ghostscript-8.64/base/gdevdjvu.c ghostscript-8.64.new/base/gdevdjvu.c
--- ghostscript-8.64/base/gdevdjvu.c	2007-11-29 03:32:27.000000000 +0100
+++ ghostscript-8.64.new/base/gdevdjvu.c	2009-02-05 22:35:11.472977938 +0100
@@ -153,9 +153,9 @@
 private void 
 lbassertfail(const char *file, int line)
 {
-    FILE *serr = fdopen(2,"a");
-    fprintf(serr,"Internal error at %s:%d\n", file, line);
-    fclose(serr);
+    //FILE *serr = fdopen(2,"a");
+    //fprintf(serr,"Internal error at %s:%d\n", file, line);
+    //fclose(serr);
     abort();
 }
 
@@ -5130,12 +5130,6 @@
                            0, cdev->width-1, 0, cdev->height-1, comment);
         if (code < 0) return code;
     }
-    /* Print message */
-    if (! cdev->quiet) {
-        fprintf(STDOUT, "Page %dx%d (%s )\n", 
-                cdev->width, cdev->height, comment);
-        fflush(STDOUT);
-    }
     /* Terminate */
     runmap_free(fgmap);
 #ifdef DEBUG
@@ -5547,12 +5541,6 @@
         if (code < 0) 
             return code; 
     }
-    /* Print message */
-    if (! cdev->quiet) {
-        fprintf(STDOUT,"Page %dx%d (%s )\n",
-                cdev->width, cdev->height, comment);
-        fflush(STDOUT);
-    }
     /* Terminate */
     runmap_free(fgmap);
 #ifdef DEBUG
diff -Naur ghostscript-8.64/gsdjvu ghostscript-8.64.new/gsdjvu
--- ghostscript-8.64/gsdjvu	2005-07-01 22:23:56.000000000 +0200
+++ ghostscript-8.64.new/gsdjvu	2009-02-05 22:31:57.385977935 +0100
@@ -56,4 +56,4 @@
 # Step 2 -- execute ghostscript
 
 gsdir="$progdir"
-GS_LIB="$gsdir/lib:$gsdir/fonts:$gsdir" exec "$gsdir/bin/gs" "$@"
+GS_LIB="$gsdir/lib:$gsdir/fonts:$gsdir" exec "/usr/bin/gs" "$@"
