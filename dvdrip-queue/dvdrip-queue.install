post_install() {
  echo "Because I am loath to spend much time on this,"
  echo "for now, all *.rip files must be in ~/dvdrip-data,"
  echo "and it seems dvd::rip must use that directory as its"
  echo "default data base directory. Other than that, it works."
}