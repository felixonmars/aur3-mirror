
# Pimper : M0Rf30

pkgname=akabeicore-git
pkgver=579.0c9c444
pkgrel=1
pkgdesc="Akabei, Chakra’s package manager. Consisting of two libraries, and a terminal interface."
arch=('i686' 'x86_64')
url="http://git.chakra-project.org/akabei"
license=('GPL')
depends=('kdelibs' 'libarchive' 'sqlite3')
makedepends=('cmake' 'automoc4' 'translate-toolkit')
source=("akabeicore::git://gitorious.org/chakra/akabeicore.git")


build() {
  cd akabeicore
  cmake . -DCMAKE_INSTALL_PREFIX=/usr 
  make || return 1
}
package() {
  cd akabeicore
  make DESTDIR=$pkgdir install
}

pkgver() {
  cd akabeicore
  echo $(git rev-list --count master).$(git rev-parse --short master)
}

md5sums=('SKIP')
