# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: Your Name <youremail@domain.com>
pkgname=jimtcl
pkgver=0.72
pkgrel=1
pkgdesc='A small-footprint implementation of the Tcl programming language'
arch=(i686 x86_64)
url="http://jim.berlios.de"
license=('BSD')
source=(https://github.com/msteveb/$pkgname/tarball/$pkgver)
md5sums=('a85cb7e07be192be517366295f438bb1')

build() {
  cd "$srcdir/msteveb-jimtcl-b9db846/"
  ./configure --prefix=/usr
  make
}

check() {
  cd "$srcdir/msteveb-jimtcl-b9db846"
  make -k check
}

package() {
  cd "$srcdir/msteveb-jimtcl-b9db846/"
  make DESTDIR="$pkgdir/" install
}

# vim:set ts=2 sw=2 et:
