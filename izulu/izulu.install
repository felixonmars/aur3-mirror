post_install() {
 echo "Edit the config file $HOME/.izulu/config and configure the wallpaper command (WPCOMMAND)"
 echo "For example in xfce:"
 echo "xfconf-query -c xfce4-desktop -p /backdrop/screen0/monitor0/image-path -s "
}

