# Maintainer: Limao Luo <luolimao+AUR@gmail.com>

pkgname=rescuetime-beta
pkgver=2.8.5.897
pkgrel=1
pkgdesc="RescueTime time-tracking for Linux. Beta version"
arch=(i686 x86_64)
url=https://www.rescuetime.com
license=(GPL2)
depends=(qt4 xorg-xprop xprintidle)
provides=(${pkgname%-*})
conflicts=(${pkgname%-*})
install=$pkgname.install
changelog=$pkgname.changelog

if [ $CARCH = "x86_64" ]
then
    _suffix=amd64
    sha1sums=('6131232206dabf7c5273a374945d6de0a3123ec6')
else
    _suffix=i386
    sha1sums=('27cd2e27abab1fb2d61b210ab76d21e77b9ec420')
fi

source=($url/installers/rescuetime_beta_$_suffix.deb)

package() {
    bsdtar -xf data.tar.gz
    install -Dm755 usr/bin/${pkgname%-*} "$pkgdir"/usr/bin/${pkgname%-*}
}
