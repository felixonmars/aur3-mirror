post_install() {
  echo "==> Make sure ogre-1.8 libraries have precedence in LD_LIBRARY_PATH."
  echo "==> Now you need to export some Gazebo variables:"
  echo "==>   echo 'source /usr/share/gazebo-3.0/setup.sh' >> ~/.bashrc"
  echo "==>   source ~/.bashrc"
}
