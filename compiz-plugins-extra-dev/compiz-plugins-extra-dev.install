pre_install() {
  echo "Pease note that the notification plugin is not compatible with newer version of libnotify and is thus disabled by default for the time being."
}
pre_upgrade() {
  pre_install $1
}
