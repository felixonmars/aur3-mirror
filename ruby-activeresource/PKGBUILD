# Contributor: Alexsandr Pavlov <kidoz at mail dot ru>
pkgname=ruby-activeresource
pkgver=3.0.5
pkgrel=1
pkgdesc="Wraps web resources in model classes that can be manipulated through XML over REST."
arch=(any)
url="http://www.rubyonrails.org/"
license=('MIT')
depends=('ruby' 'ruby-activemodel' 'ruby-activesupport')
source=(http://gems.rubyforge.org/gems/activeresource-$pkgver.gem)
noextract=(activeresource-$pkgver.gem)
md5sums=('d5f45c368f9d2011426fd3bd96bc9f08')

build() {
  cd $srcdir
  # _gemdir is defined inside build() because if ruby[gems] is not installed on the system
  # makepkg will exit with an error when sourcing the PKGBUILD
  local _gemdir="$(ruby -rubygems -e'puts Gem.default_dir')"

  gem install --ignore-dependencies -i "$pkgdir$_gemdir" activeresource-$pkgver.gem
}
