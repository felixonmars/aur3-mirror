pkgname=mingw32-apr-util
pkgver=1.4.1
pkgrel=2
pkgdesc="Apache Portable Runtime utilities (mingw32)"
arch=('any')
url="http://apr.apache.org/"
license=('Apache')
makedepends=('mingw32-gcc')
depends=('mingw32-apr')
options=('!strip' '!buildflags' '!libtool')
source=("http://ftp.wayne.edu/apache//apr/apr-util-$pkgver.tar.bz2")
md5sums=('52b31b33fb1aa16e65ddaefc76e41151')

build() {
  cd "${srcdir}/apr-util-$pkgver"

  unset LDFLAGS

  ./configure --prefix=/usr/i486-mingw32 --host=i486-mingw32 \
  --with-apr=/usr/i486-mingw32 --with-crypto \
  --without-freetds
  make
}

package() {
  cd "${srcdir}/apr-util-$pkgver"

  make DESTDIR=${pkgdir} install
  rm $pkgdir/usr/i486-mingw32/lib/*.exp
  i486-mingw32-strip -g $pkgdir/usr/i486-mingw32/lib/*.a
}