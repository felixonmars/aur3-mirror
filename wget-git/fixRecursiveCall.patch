diff --git a/src/retr.c b/src/retr.c
index c324434..e4f669b 100644
--- a/src/retr.c
+++ b/src/retr.c
@@ -681,6 +681,7 @@ calc_rate (wgint bytes, double secs, int *units)
   post_data_suspended = true;                   \
   saved_post_data = opt.body_data;              \
   saved_post_file_name = opt.body_file;         \
+  saved_method_name = opt.method;               \
   opt.body_data = NULL;                         \
   opt.body_file = NULL;                         \
   opt.method = NULL;                            \
@@ -692,7 +693,7 @@ calc_rate (wgint bytes, double secs, int *units)
       opt.body_data = saved_post_data;                  \
       opt.body_file = saved_post_file_name;             \
       post_data_suspended = false;                      \
-      opt.method = "POST";                              \
+      opt.method = saved_method_name;                   \
     }                                                   \
 } while (0)
 
@@ -723,6 +724,7 @@ retrieve_url (struct url * orig_parsed, const char *origurl, char **file,
   bool post_data_suspended = false;
   char *saved_post_data = NULL;
   char *saved_post_file_name = NULL;
+  char *saved_method_name = NULL;
 
   /* If dt is NULL, use local storage.  */
   if (!dt)
