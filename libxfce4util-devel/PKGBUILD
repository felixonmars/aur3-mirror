# Maintainer: Xavier Devlamynck <magicrhesus@ouranos.be>

pkgname=libxfce4util-devel
pkgver=4.8.1
pkgrel=3
pkgdesc="Basic utility non-GUI functions for Xfce (Development Version)"
arch=('i686' 'x86_64')
license=('GPL2')
url="http://www.xfce.org/"
groups=('xfce4-devel')
depends=("glib2")
makedepends=('pkgconfig' 'intltool' 'gtk-doc')
conflicts=('libxfce4util' 'libxfce4util-git')
options=('!libtool')
source=(http://archive.xfce.org/src/xfce/libxfce4util/4.8/libxfce4util-${pkgver}.tar.bz2)
md5sums=('2be3af4c7db5ad293a7525e1021e6f0f')

build() {
  cd ${srcdir}/libxfce4util-${pkgver}
  ./configure --prefix=/usr \
	--sysconfdir=/etc \
	--libexecdir=/usr/lib \
	--localstatedir=/var \
	--disable-static \
	--enable-gtk-doc \
	--disable-debug
  make
}

package() {
	cd ${srcdir}/libxfce4util-${pkgver}
	make DESTDIR=${pkgdir} install
}
