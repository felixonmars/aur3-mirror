# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Contributor: Bill Powell <bill@billpowellisalive.com>
pkgname=xkbset
pkgver=0.5
pkgrel=1
pkgdesc="Set accessx features in X, such as sticky keys."
arch=(i686)
url="http://www.math.missouri.edu/~stephen/software/"
license=('Other free license')
groups=()
depends=()
makedepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
source=(http://www.math.missouri.edu/~stephen/software/xkbset/$pkgname-$pkgver.tar.gz)
noextract=()
md5sums=('282c416a76a237fa84a7b513c9d7b2cf')


build() {
  cd "$srcdir/$pkgname-$pkgver"
  mkdir -p $pkgdir/usr/bin
  mkdir -p $pkgdir/usr/share/man/man1

  make || return 1
  make DESTDIR="$pkgdir/" INSTALL_BIN="$pkgdir/usr/bin" INSTALL_MAN1="$pkgdir/usr/share/man/man1" install
}

# vim:set ts=2 sw=2 et:
