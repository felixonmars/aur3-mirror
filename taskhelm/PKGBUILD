# Maintainer: Thomas Weißschuh <thomas_weissschuh || lavabit.com>
pkgname=taskhelm
pkgver=0.3.0
pkgrel=1
pkgdesc="A graphical shell that sits on top of TaskWarrior"
arch=(i686 x86_64)
url="http://taskwarrior.org/projects/taskwarrior/wiki/Taskhelm"
license=("GPL")
depends=('task' 'pygtk')
source=("http://www.bryceharrington.org/files/${pkgname}-${pkgver}.tar.gz")

#build() {
#  cd ${srcdir}/${pkgname}-${pkgver}
#  python2 setup.py build
#}

package() {
  cd ${srcdir}/${pkgname}-${pkgver}
  python2 setup.py install --root ${pkgdir} --optimize=1
}
# vim:set ts=2 sw=2 et:
sha1sums=('0cf79fdd183a6b8594b406b5ff012fe468a4544b')
