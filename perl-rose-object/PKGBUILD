# PKGBUILD generated by pacpan
pkgname=perl-rose-object
_realname=Rose-Object
pkgver=0.855
pkgrel=1
pkgdesc="A simple object base class."
arch=(i686 x86_64)
license=('perl')
url="http://search.cpan.org/~jsiracusa/Rose-Object"
options=(!emptydirs)

depends=('perl>=5.8.9')
makedepends=('perl')

#provides=('Rose-Object')
provides=('rose-object=0.855' 'Rose::Object::MakeMethods::DateTime=0.81' 'perl-rose-object-makemethods-datetime=0.81' 'Rose::Object=0.855' 'perl-rose-object=0.855' 'Rose::Object::MakeMethods=0.85' 'perl-rose-object-makemethods=0.85' 'Rose::Object::MakeMethods::Generic=0.855' 'perl-rose-object-makemethods-generic=0.855' 'Rose::Object::MixIn=0.852' 'perl-rose-object-mixin=0.852' 'Rose::Class=0.81' 'perl-rose-class=0.81' 'Rose::Class::MakeMethods::Set=0.81' 'perl-rose-class-makemethods-set=0.81' 'Rose::Class::MakeMethods::Generic=0.854' 'perl-rose-class-makemethods-generic=0.854')

source=(http://search.cpan.org/CPAN/authors/id/J/JS/JSIRACUSA/Rose-Object-0.855.tar.gz)
md5sums=('d9632687f60e78cf5b5ea4f2a0384c53')

build() {
  _expected_dir="${srcdir}/${_realname}-${pkgver}"
  if [ -d "$_expected_dir" ]; then
    cd "$_expected_dir"
  else
    _expected_dir="${srcdir}/$(bsdtar -t -f $(basename $source) | head -n1)"
    if [ -d "$_expected_dir" ]; then
      cd "$_expected_dir"
    else
      _makefile=$(find $srcdir -iname Makefile.PL)
      if [ ! -z "$_makefile" ]; then
        _expected_dir=$(dirname $_makefile)
        if [ -d "$_expected_dir" ]; then
          cd "$_expected_dir"
        else
          echo "[1;31mERROR[0m unable to detect source directory"
          echo "[1;34m-->[0m this is often due to CPAN's lack of standard naming conventions"
          echo "[1;34m-->[0m it may be possible to fix this by adjusting the build function in the PKGBUILD"
        fi
      fi
    fi
  fi
  # install module in vendor directories.
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1
  make  || return 1
  make install DESTDIR=${pkgdir} || return 1

  # remove perllocal.pod and .packlist
  find ${pkgdir} -name perllocal.pod -delete
  find ${pkgdir} -name .packlist -delete
}

