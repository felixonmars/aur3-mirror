--- transproxy-1.6/Makefile
+++ transproxy-1.6/Makefile
@@ -64,7 +64,7 @@
 #OPTIONS += -DHAVE_GETOPT_H
 
 # You may need to touch PREFIX, CC and CFLAGS.
-PREFIX = /usr/local
+#PREFIX = /usr/local
 INSTALL_PROGRAM = install -c -m 555 -o bin -g bin
 INSTALL_MAN = install -c -m 444 -o bin -g bin
 
