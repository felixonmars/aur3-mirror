post_install() {
    VERSION=${1%-*}
    dkms install -m zfs/${VERSION/_*/-}
}

post_upgrade() {
    NEW_VERSION=${1%-*}
    dkms install -m zfs/${NEW_VERSION/_*/-}
}

pre_remove() {
    VERSION=${1%-*}
    dkms remove -m zfs/${VERSION/_*/-} --all
}
