post_install() {
	echo "	==> It is necessary to add the custom installation directories to our environment settings, otherwise applications will not be able to find the headers, libraries, python scripts, etc."
	echo "	==> Run \"source /opt/gnuradio/setupenv\" before start GnuRadio."
}

post_upgrade() {
	post_install
}
