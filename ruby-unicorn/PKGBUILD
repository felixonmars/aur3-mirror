# Maintainer: Maxwell Pray a.k.a. Synthead <synthead@gmail.com>

pkgname=ruby-unicorn
pkgver=4.2.0
pkgrel=1
pkgdesc="An HTTP server for Rack applications designed to only serve fast clients on low-latency, high-bandwidth connections and take advantage of features in Unix/Unix-like kernels"
arch=('i686' 'x86_64')
url="http://unicorn.bogomips.org/"
license=("GPL")
depends=('ruby')
source=("http://gems.rubyforge.org/gems/unicorn-$pkgver.gem")
noextract=("unicorn-$pkgver.gem")
md5sums=('fb65211bc77acf0dcdbc903f252ff9a2')

package() {
	local _gemdir="$(ruby -rubygems -e'puts Gem.default_dir')"
	gem install --no-user-install --ignore-dependencies -Vi "$pkgdir$_gemdir" -n "$pkgdir/usr/bin" "$srcdir/unicorn-$pkgver.gem"
	sed -i "s/${pkgdir//\//\/}//" "$pkgdir$_gemdir/gems/unicorn-$pkgver/ext/unicorn_http/Makefile"
}
