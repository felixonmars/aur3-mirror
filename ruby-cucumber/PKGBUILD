# Maintainer:  Matthew Monaco <net 0x01b matt>
# Contributor: Rogof <com gmail fake.bios>

_gemname=cucumber
pkgname=ruby-$_gemname
pkgver=1.1.4
pkgrel=1
pkgdesc="Behaviour Driven Development with elegance and joy"
arch=(any)
url="http://cukes.info/"
license=('GPL')
depends=('ruby' 'ruby-builder>=2.1.2' 'ruby-diff-lcs>=1.1.2' 'ruby-gherkin' 'ruby-json>=1.4.6' 'ruby-term-ansicolor>=1.0.6')
makedepends=(rubygems)
source=(http://gems.rubyforge.org/gems/$_gemname-$pkgver.gem)
noextract=($_gemname-$pkgver.gem)
md5sums=('31778bc5035789f5377af9e916e5fe8f')

package() {
  cd "$srcdir"
  local _gemdir="$(ruby -rubygems -e'puts Gem.default_dir')"

  gem install --ignore-dependencies --verbose -i "$pkgdir$_gemdir" -n "$pkgdir/usr/bin" $_gemname-$pkgver.gem
}

# vim:set ts=2 sw=2 et:
