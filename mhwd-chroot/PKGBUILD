# Author: esclapion <esclapion[at]gmail[dot]com>
# Maintainer: esclapion <esclapion[at]gmail[dot]com>

pkgname=mhwd-chroot
pkgver=1.0
pkgrel=1
pkgdesc="chroot gui"
url="https://forum.manjaro.org/index.php?topic=21433"
arch=('any')
license=('GPL')
depends=('gksu')
source=("${pkgname}-$pkgver.tar.gz")
md5sums=('3d4da9dab0214fc9c0d8c81bdde33853')

package() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  install -Dm755 "${pkgname}" "${pkgdir}/usr/bin/${pkgname}"
  install -Dm755 "${pkgname}-shell" "${pkgdir}/usr/bin/${pkgname}-shell"
  install -Dm644 "${pkgname}.desktop" "${pkgdir}/usr/share/applications/${pkgname}.desktop"
}
