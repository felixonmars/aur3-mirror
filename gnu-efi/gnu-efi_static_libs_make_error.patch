diff --git a/gnuefi/Makefile b/gnuefi/Makefile
index 972be40..7ba0d05 100644
--- a/gnuefi/Makefile
+++ b/gnuefi/Makefile
@@ -38,7 +38,8 @@ TARGETS	= crt0-efi-$(ARCH).o libgnuefi.a
 
 all:	$(TARGETS)
 
-libgnuefi.a: libgnuefi.a($(OBJS))
+libgnuefi.a: $(OBJS)
+	ar -cq libgnuefi.a $(OBJS)
 
 clean:
 	rm -f $(TARGETS) *~ *.o $(OBJS)
diff --git a/lib/Makefile b/lib/Makefile
index 0c780ba..d5fade3 100644
--- a/lib/Makefile
+++ b/lib/Makefile
@@ -53,7 +53,8 @@ all: libsubdirs libefi.a
 libsubdirs:
 	for sdir in $(SUBDIRS); do mkdir -p $$sdir; done
 
-libefi.a: libefi.a($(OBJS))
+libefi.a: $(OBJS)
+	ar -cq libefi.a $(OBJS)
 
 clean:
 	rm -f libefi.a *~ $(OBJS) */*.o
