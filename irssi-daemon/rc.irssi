#!/bin/bash

. /etc/rc.conf
. /etc/rc.d/functions
. /etc/conf.d/irssi

# delete repeated users in configuration file
[ -n "$USERS" ] && USERS=`echo $USERS | tr " " "\n" | uniq`

# set defaults
[ -z "$USERS" ] && USERS="`ls -d -c1 /home/* | cut -d/ -f3`"

case "$1" in
  start)
    stat_busy "Starting irssi daemon"

    for user in $USERS ; do
      su $user -c "screen -d -m -S irssi irssi" &> /dev/null
    done

    if [ $? -gt 0 ]; then
      stat_fail
    else
      add_daemon irssi
      stat_done
    fi
    ;;
  stop)
    stat_busy "Stoping irssi daemon"

    for user in $USERS ; do
       su $user -c "killall -w /usr/bin/irssi" &> /dev/null
    done

    if [ $? -gt 0 ]; then
      stat_fail
    else
      rm_daemon irssi
      stat_done
    fi
    ;;
  restart)
    $0 stop
    sleep 1
    $0 start
    ;;
  *)
    echo "usage: $0 {start|stop|restart}"
esac
exit 0

# vim:set ts=2 sw=2 ft=sh et:
