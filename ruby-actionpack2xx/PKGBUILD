# Contributor: Alexsandr Pavlov <kidoz at mail dot ru>
pkgname=ruby-actionpack2xx
_gemname=actionpack
pkgver=2.3.12
pkgrel=1
pkgdesc="On rails from request to response."
arch=(any)
url="http://www.rubyonrails.org/"
license=('MIT')
depends=('ruby' 'ruby-rack110' 'ruby-activesupport2xx')
source=(http://gems.rubyforge.org/gems/${_gemname}-${pkgver}.gem)
noextract=(${_gemname}-${pkgver}.gem)
md5sums=('b1e21b3391f062712156f79a57dea6fd')

build() {
  cd "$srcdir"
  # _gemdir is defined inside build() because if ruby[gems] is not installed on the system
  # makepkg will exit with an error when sourcing the PKGBUILD
  local _gemdir="$(ruby -rubygems -e'puts Gem.default_dir')"

  gem install --ignore-dependencies -i "$pkgdir$_gemdir" ${_gemname}-${pkgver}.gem
}
