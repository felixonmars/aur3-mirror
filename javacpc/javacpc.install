post_install() {
  cat << EOF
==========================================================
Hint:
==========================================================

On 64-bit systems, you may experience low frame rates
and sound issues. The frame rate problem can be fixed
by enabling "64 bit OS" under Options -> System.
EOF
}
