post_install() {
  cat << EOF
==========================================================
Hint:
==========================================================

If there is no sound from the CPC and the emulator has
a low frame rate on startup, try changing the audio
sample rate, e.g. from 22 kHz to 44.1 kHz (under audio
settings).

EOF
}
