# Contributor: Ashok `ScriptDevil` Gautham <ScriptDevil@gmail.com>
# Maintainer: Laszlo Papp <djszapi2 at gmail.com>
pkgname=covered
pkgver=20090705
pkgrel=2
pkgdesc="A Verilog code coverage analyzer"
arch=('i686')
url="http://covered.sourceforge.net/"
license=('GPL')
depends=('zlib')
source=(http://downloads.sourceforge.net/sourceforge/${pkgname}/${pkgname}-${pkgver}.tar.gz)
md5sums=('2a68c56dae6adcbcf0619b4b6522c9e8')

build() {
  cd "$srcdir/$pkgname-$pkgver"
  ./configure --prefix=/usr
  make || return 1
  make DESTDIR="$pkgdir/" install || return 1
}

