post_install () {
        echo -e " >>> A server.conf simple file has been created in /opt/gateone/server.conf\n >>> Full access to those directories are required, so run it accordingly (commonly as root works)"
}
