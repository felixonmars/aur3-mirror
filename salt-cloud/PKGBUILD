# Maintainer: Christer Edwards <christer.edwards@gmail.com>

pkgname=salt-cloud
pkgver=0.8.2
pkgrel=1
pkgdesc="Salt Cloud is a generic cloud provisioning tool"
arch=(any)
url="https://github.com/saltstack/salt-cloud"
license=("APACHE")

depends=('apache-libcloud'
	 'python2-paramiko'
	 'python2'
         'python2-yaml'
         'salt')

makedepends=()
optdepends=()
options=()
conflicts=()

source=("https://github.com/downloads/saltstack/salt-cloud/${pkgname}-${pkgver}.tar.gz")

md5sums=('f1864f8cfc54c2cb913e290f0bd1c298')

package() {
  cd ${srcdir}/${pkgname}-${pkgver}
  python2 setup.py install --root=${pkgdir}/ --optimize=1
}

