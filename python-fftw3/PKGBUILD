#Maintainer: Paul Viren <paul dot viren at lmco dot com>

pkgname=python-fftw3
pkgver=0.2.1
pkgrel=1
pkgdesc="Python bindings to the FFTW3 C-library"
arch=(any)
url="http://launchpad.net/pyfftw"
license=('GPL3')
depends=('python2' 'python-numpy' 'fftw')
options=(!emptydirs)
source=(http://launchpad.net/pyfftw/trunk/${pkgver}/+download/PyFFTW3-${pkgver}.tar.gz)

build() {
    cd ${srcdir}/PyFFTW3-${pkgver}
    python2 setup.py install --root=${pkgdir}/ --optimize=1 || return 1
}

md5sums=('0066be45345591a6b4a97eb3779c7346')
