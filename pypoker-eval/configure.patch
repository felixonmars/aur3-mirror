--- pypoker-eval-138.0/configure	2010-09-04 10:10:30.000000000 +0200
+++ configure	2011-01-13 17:11:03.640165723 +0100
@@ -749,6 +749,15 @@
 PKG_CONFIG_LIBDIR
 PKG_CONFIG_PATH
 PKG_CONFIG
+PYTHON_2_7_FALSE
+PYTHON_2_7_TRUE
+PYTHON2_7_LIBS
+PYTHON2_7_CFLAGS
+python2_7linkflags
+python2_7clibdir
+python2_7includedir
+pkgpy2_7execdir
+py2_7execdir
 PYTHON_2_6_FALSE
 PYTHON_2_6_TRUE
 PYTHON2_6_LIBS
@@ -5466,13 +5475,13 @@
 else
   lt_cv_nm_interface="BSD nm"
   echo "int some_variable = 0;" > conftest.$ac_ext
-  (eval echo "\"\$as_me:5469: $ac_compile\"" >&5)
+  (eval echo "\"\$as_me:5478: $ac_compile\"" >&5)
   (eval "$ac_compile" 2>conftest.err)
   cat conftest.err >&5
-  (eval echo "\"\$as_me:5472: $NM \\\"conftest.$ac_objext\\\"\"" >&5)
+  (eval echo "\"\$as_me:5481: $NM \\\"conftest.$ac_objext\\\"\"" >&5)
   (eval "$NM \"conftest.$ac_objext\"" 2>conftest.err > conftest.out)
   cat conftest.err >&5
-  (eval echo "\"\$as_me:5475: output\"" >&5)
+  (eval echo "\"\$as_me:5484: output\"" >&5)
   cat conftest.out >&5
   if $GREP 'External.*some_variable' conftest.out > /dev/null; then
     lt_cv_nm_interface="MS dumpbin"
@@ -6677,7 +6686,7 @@
   ;;
 *-*-irix6*)
   # Find out which ABI we are using.
-  echo '#line 6680 "configure"' > conftest.$ac_ext
+  echo '#line 6689 "configure"' > conftest.$ac_ext
   if { { eval echo "\"\$as_me\":${as_lineno-$LINENO}: \"$ac_compile\""; } >&5
   (eval $ac_compile) 2>&5
   ac_status=$?
@@ -8466,11 +8475,11 @@
    -e 's:.*FLAGS}\{0,1\} :&$lt_compiler_flag :; t' \
    -e 's: [^ ]*conftest\.: $lt_compiler_flag&:; t' \
    -e 's:$: $lt_compiler_flag:'`
-   (eval echo "\"\$as_me:8469: $lt_compile\"" >&5)
+   (eval echo "\"\$as_me:8478: $lt_compile\"" >&5)
    (eval "$lt_compile" 2>conftest.err)
    ac_status=$?
    cat conftest.err >&5
-   echo "$as_me:8473: \$? = $ac_status" >&5
+   echo "$as_me:8482: \$? = $ac_status" >&5
    if (exit $ac_status) && test -s "$ac_outfile"; then
      # The compiler can only warn and ignore the option if not recognized
      # So say no if there are warnings other than the usual output.
@@ -8805,11 +8814,11 @@
    -e 's:.*FLAGS}\{0,1\} :&$lt_compiler_flag :; t' \
    -e 's: [^ ]*conftest\.: $lt_compiler_flag&:; t' \
    -e 's:$: $lt_compiler_flag:'`
-   (eval echo "\"\$as_me:8808: $lt_compile\"" >&5)
+   (eval echo "\"\$as_me:8817: $lt_compile\"" >&5)
    (eval "$lt_compile" 2>conftest.err)
    ac_status=$?
    cat conftest.err >&5
-   echo "$as_me:8812: \$? = $ac_status" >&5
+   echo "$as_me:8821: \$? = $ac_status" >&5
    if (exit $ac_status) && test -s "$ac_outfile"; then
      # The compiler can only warn and ignore the option if not recognized
      # So say no if there are warnings other than the usual output.
@@ -8910,11 +8919,11 @@
    -e 's:.*FLAGS}\{0,1\} :&$lt_compiler_flag :; t' \
    -e 's: [^ ]*conftest\.: $lt_compiler_flag&:; t' \
    -e 's:$: $lt_compiler_flag:'`
-   (eval echo "\"\$as_me:8913: $lt_compile\"" >&5)
+   (eval echo "\"\$as_me:8922: $lt_compile\"" >&5)
    (eval "$lt_compile" 2>out/conftest.err)
    ac_status=$?
    cat out/conftest.err >&5
-   echo "$as_me:8917: \$? = $ac_status" >&5
+   echo "$as_me:8926: \$? = $ac_status" >&5
    if (exit $ac_status) && test -s out/conftest2.$ac_objext
    then
      # The compiler can only warn and ignore the option if not recognized
@@ -8965,11 +8974,11 @@
    -e 's:.*FLAGS}\{0,1\} :&$lt_compiler_flag :; t' \
    -e 's: [^ ]*conftest\.: $lt_compiler_flag&:; t' \
    -e 's:$: $lt_compiler_flag:'`
-   (eval echo "\"\$as_me:8968: $lt_compile\"" >&5)
+   (eval echo "\"\$as_me:8977: $lt_compile\"" >&5)
    (eval "$lt_compile" 2>out/conftest.err)
    ac_status=$?
    cat out/conftest.err >&5
-   echo "$as_me:8972: \$? = $ac_status" >&5
+   echo "$as_me:8981: \$? = $ac_status" >&5
    if (exit $ac_status) && test -s out/conftest2.$ac_objext
    then
      # The compiler can only warn and ignore the option if not recognized
@@ -11349,7 +11358,7 @@
   lt_dlunknown=0; lt_dlno_uscore=1; lt_dlneed_uscore=2
   lt_status=$lt_dlunknown
   cat > conftest.$ac_ext <<_LT_EOF
-#line 11352 "configure"
+#line 11361 "configure"
 #include "confdefs.h"
 
 #if HAVE_DLFCN_H
@@ -11445,7 +11454,7 @@
   lt_dlunknown=0; lt_dlno_uscore=1; lt_dlneed_uscore=2
   lt_status=$lt_dlunknown
   cat > conftest.$ac_ext <<_LT_EOF
-#line 11448 "configure"
+#line 11457 "configure"
 #include "confdefs.h"
 
 #if HAVE_DLFCN_H
@@ -13401,11 +13410,11 @@
    -e 's:.*FLAGS}\{0,1\} :&$lt_compiler_flag :; t' \
    -e 's: [^ ]*conftest\.: $lt_compiler_flag&:; t' \
    -e 's:$: $lt_compiler_flag:'`
-   (eval echo "\"\$as_me:13404: $lt_compile\"" >&5)
+   (eval echo "\"\$as_me:13413: $lt_compile\"" >&5)
    (eval "$lt_compile" 2>conftest.err)
    ac_status=$?
    cat conftest.err >&5
-   echo "$as_me:13408: \$? = $ac_status" >&5
+   echo "$as_me:13417: \$? = $ac_status" >&5
    if (exit $ac_status) && test -s "$ac_outfile"; then
      # The compiler can only warn and ignore the option if not recognized
      # So say no if there are warnings other than the usual output.
@@ -13500,11 +13509,11 @@
    -e 's:.*FLAGS}\{0,1\} :&$lt_compiler_flag :; t' \
    -e 's: [^ ]*conftest\.: $lt_compiler_flag&:; t' \
    -e 's:$: $lt_compiler_flag:'`
-   (eval echo "\"\$as_me:13503: $lt_compile\"" >&5)
+   (eval echo "\"\$as_me:13512: $lt_compile\"" >&5)
    (eval "$lt_compile" 2>out/conftest.err)
    ac_status=$?
    cat out/conftest.err >&5
-   echo "$as_me:13507: \$? = $ac_status" >&5
+   echo "$as_me:13516: \$? = $ac_status" >&5
    if (exit $ac_status) && test -s out/conftest2.$ac_objext
    then
      # The compiler can only warn and ignore the option if not recognized
@@ -13552,11 +13561,11 @@
    -e 's:.*FLAGS}\{0,1\} :&$lt_compiler_flag :; t' \
    -e 's: [^ ]*conftest\.: $lt_compiler_flag&:; t' \
    -e 's:$: $lt_compiler_flag:'`
-   (eval echo "\"\$as_me:13555: $lt_compile\"" >&5)
+   (eval echo "\"\$as_me:13564: $lt_compile\"" >&5)
    (eval "$lt_compile" 2>out/conftest.err)
    ac_status=$?
    cat out/conftest.err >&5
-   echo "$as_me:13559: \$? = $ac_status" >&5
+   echo "$as_me:13568: \$? = $ac_status" >&5
    if (exit $ac_status) && test -s out/conftest2.$ac_objext
    then
      # The compiler can only warn and ignore the option if not recognized
@@ -14740,7 +14749,7 @@
       # VERSION.
       { $as_echo "$as_me:${as_lineno-$LINENO}: checking for a Python interpreter with version $required_version" >&5
 $as_echo_n "checking for a Python interpreter with version $required_version... " >&6; }
-      for am_cv_pathless_PYTHON in python2.6 python2.5 python2.4 python2.3 none; do
+      for am_cv_pathless_PYTHON in python2.7 python2.6 python2.5 python2.4 python2.3 none; do
 	  test "$am_cv_pathless_PYTHON" = none && break
 	  prog="import sys, string
 if '$required_version' == '': sys.exit(0)
@@ -15111,7 +15120,7 @@
       # VERSION.
       { $as_echo "$as_me:${as_lineno-$LINENO}: checking for a Python interpreter with version $required_version" >&5
 $as_echo_n "checking for a Python interpreter with version $required_version... " >&6; }
-      for am_cv_pathless_PYTHON in python2.6 python2.5 python2.4 python2.3 none; do
+      for am_cv_pathless_PYTHON in python2.7 python2.6 python2.5 python2.4 python2.3 none; do
 	  test "$am_cv_pathless_PYTHON" = none && break
 	  prog="import sys, string
 if '$required_version' == '': sys.exit(0)
@@ -15482,7 +15491,7 @@
       # VERSION.
       { $as_echo "$as_me:${as_lineno-$LINENO}: checking for a Python interpreter with version $required_version" >&5
 $as_echo_n "checking for a Python interpreter with version $required_version... " >&6; }
-      for am_cv_pathless_PYTHON in python2.6 python2.5 python2.4 python2.3 none; do
+      for am_cv_pathless_PYTHON in python2.7 python2.6 python2.5 python2.4 python2.3 none; do
 	  test "$am_cv_pathless_PYTHON" = none && break
 	  prog="import sys, string
 if '$required_version' == '': sys.exit(0)
@@ -15853,7 +15862,7 @@
       # VERSION.
       { $as_echo "$as_me:${as_lineno-$LINENO}: checking for a Python interpreter with version $required_version" >&5
 $as_echo_n "checking for a Python interpreter with version $required_version... " >&6; }
-      for am_cv_pathless_PYTHON in python2.6 python2.5 python2.4 python2.3 none; do
+      for am_cv_pathless_PYTHON in python2.7 python2.6 python2.5 python2.4 python2.3 none; do
 	  test "$am_cv_pathless_PYTHON" = none && break
 	  prog="import sys, string
 if '$required_version' == '': sys.exit(0)
@@ -16156,6 +16165,377 @@
 
 
 if test -f "$PYTHON" ; then found_one=$PYTHON ; PYTHONS="$PYTHON $PYTHONS" ; fi
+unset PYTHON
+
+
+
+
+
+    if expr "=2.7" : "[<>=]" > /dev/null ; then
+      required_version="=2.7"
+    elif test -z "=2.7" || expr "=2.7" : ' 	*$' > /dev/null ; then
+      required_version=""
+    else
+      required_version=">==2.7"
+    fi
+        if test -n "$PYTHON"; then
+      # If the user set $PYTHON, use it and don't search something else.
+      { $as_echo "$as_me:${as_lineno-$LINENO}: checking whether $PYTHON version $required_version" >&5
+$as_echo_n "checking whether $PYTHON version $required_version... " >&6; }
+      prog="import sys, string
+if '$required_version' == '': sys.exit(0)
+spec = string.replace('$required_version', ' ', '')
+if spec[:2] == '<=':
+  version_string = spec[2:]
+elif spec[:2] == '>=':
+  version_string = spec[2:]
+elif spec[:1] == '=':
+  version_string = spec[1:]
+elif spec[:1] == '>':
+  version_string = spec[1:]
+elif spec[:1] == '<':
+  version_string = spec[1:]
+ver = map(int, string.split(version_string, '.'))
+syshexversion = sys.hexversion >> (8 * (4 - l""en(ver)))
+verhex = 0
+for i in xrange(0, l""en(ver)): verhex = (verhex << 8) + ver[i]
+print 'sys.hexversion = 0x%08x, verhex = 0x%08x' % (syshexversion, verhex)
+if spec[:2] == '<=':
+  status = syshexversion <= verhex
+elif spec[:2] == '>=':
+  status = syshexversion >= verhex
+elif spec[:1] == '=':
+  status = syshexversion == verhex
+elif spec[:1] == '>':
+  status = syshexversion > verhex
+elif spec[:1] == '<':
+  status = syshexversion < verhex
+else:
+  status = syshexversion >= verhex
+
+if status:
+  sys.exit(0)
+else:
+  sys.exit(1)"
+  if { echo "$as_me:$LINENO: $PYTHON -c "$prog"" >&5
+   ($PYTHON -c "$prog") >&5 2>&5
+   ac_status=$?
+   echo "$as_me:$LINENO: \$? = $ac_status" >&5
+   (exit $ac_status); }; then :
+  { $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
+$as_echo "yes" >&6; }
+else
+  as_fn_error $? "too old" "$LINENO" 5
+fi
+      am_display_PYTHON=$PYTHON
+    else
+      # Otherwise, try each interpreter until we find one that satisfies
+      # VERSION.
+      { $as_echo "$as_me:${as_lineno-$LINENO}: checking for a Python interpreter with version $required_version" >&5
+$as_echo_n "checking for a Python interpreter with version $required_version... " >&6; }
+      for am_cv_pathless_PYTHON in python2.7 python2.6 python2.5 python2.4 python2.3 none; do
+	  test "$am_cv_pathless_PYTHON" = none && break
+	  prog="import sys, string
+if '$required_version' == '': sys.exit(0)
+spec = string.replace('$required_version', ' ', '')
+if spec[:2] == '<=':
+  version_string = spec[2:]
+elif spec[:2] == '>=':
+  version_string = spec[2:]
+elif spec[:1] == '=':
+  version_string = spec[1:]
+elif spec[:1] == '>':
+  version_string = spec[1:]
+elif spec[:1] == '<':
+  version_string = spec[1:]
+ver = map(int, string.split(version_string, '.'))
+syshexversion = sys.hexversion >> (8 * (4 - l""en(ver)))
+verhex = 0
+for i in xrange(0, l""en(ver)): verhex = (verhex << 8) + ver[i]
+print 'sys.hexversion = 0x%08x, verhex = 0x%08x' % (syshexversion, verhex)
+if spec[:2] == '<=':
+  status = syshexversion <= verhex
+elif spec[:2] == '>=':
+  status = syshexversion >= verhex
+elif spec[:1] == '=':
+  status = syshexversion == verhex
+elif spec[:1] == '>':
+  status = syshexversion > verhex
+elif spec[:1] == '<':
+  status = syshexversion < verhex
+else:
+  status = syshexversion >= verhex
+
+if status:
+  sys.exit(0)
+else:
+  sys.exit(1)"
+  if { echo "$as_me:$LINENO: $am_cv_pathless_PYTHON -c "$prog"" >&5
+   ($am_cv_pathless_PYTHON -c "$prog") >&5 2>&5
+   ac_status=$?
+   echo "$as_me:$LINENO: \$? = $ac_status" >&5
+   (exit $ac_status); }; then :
+  break
+fi
+      done
+      { $as_echo "$as_me:${as_lineno-$LINENO}: result: done" >&5
+$as_echo "done" >&6; }
+      # Set $PYTHON to the absolute path of $am_cv_pathless_PYTHON.
+      if test "$am_cv_pathless_PYTHON" = none; then
+	PYTHON=:
+      else
+        unset ac_cv_path_PYTHON
+        # Extract the first word of "$am_cv_pathless_PYTHON", so it can be a program name with args.
+set dummy $am_cv_pathless_PYTHON; ac_word=$2
+{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for $ac_word" >&5
+$as_echo_n "checking for $ac_word... " >&6; }
+if test "${ac_cv_path_PYTHON+set}" = set; then :
+  $as_echo_n "(cached) " >&6
+else
+  case $PYTHON in
+  [\\/]* | ?:[\\/]*)
+  ac_cv_path_PYTHON="$PYTHON" # Let the user override the test with a path.
+  ;;
+  *)
+  as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
+for as_dir in $PATH
+do
+  IFS=$as_save_IFS
+  test -z "$as_dir" && as_dir=.
+    for ac_exec_ext in '' $ac_executable_extensions; do
+  if { test -f "$as_dir/$ac_word$ac_exec_ext" && $as_test_x "$as_dir/$ac_word$ac_exec_ext"; }; then
+    ac_cv_path_PYTHON="$as_dir/$ac_word$ac_exec_ext"
+    $as_echo "$as_me:${as_lineno-$LINENO}: found $as_dir/$ac_word$ac_exec_ext" >&5
+    break 2
+  fi
+done
+  done
+IFS=$as_save_IFS
+
+  ;;
+esac
+fi
+PYTHON=$ac_cv_path_PYTHON
+if test -n "$PYTHON"; then
+  { $as_echo "$as_me:${as_lineno-$LINENO}: result: $PYTHON" >&5
+$as_echo "$PYTHON" >&6; }
+else
+  { $as_echo "$as_me:${as_lineno-$LINENO}: result: no" >&5
+$as_echo "no" >&6; }
+fi
+
+
+      fi
+      am_display_PYTHON=$am_cv_pathless_PYTHON
+    fi
+
+
+  if test "$PYTHON" = :; then
+      unset PYTHON
+  else
+
+
+  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $am_display_PYTHON version" >&5
+$as_echo_n "checking for $am_display_PYTHON version... " >&6; }
+  am_cv_python_version=`$PYTHON -c "import sys; print sys.version[:3]"`
+  { $as_echo "$as_me:${as_lineno-$LINENO}: result: done" >&5
+$as_echo "done" >&6; }
+  PYTHON_VERSION=$am_cv_python_version
+
+
+
+  PYTHON_PREFIX='${prefix}'
+
+  PYTHON_EXEC_PREFIX='${exec_prefix}'
+
+
+
+  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $am_display_PYTHON platform" >&5
+$as_echo_n "checking for $am_display_PYTHON platform... " >&6; }
+  am_cv_python_platform=`$PYTHON -c "import sys; print sys.platform"`
+  { $as_echo "$as_me:${as_lineno-$LINENO}: result: done" >&5
+$as_echo "done" >&6; }
+  PYTHON_PLATFORM=$am_cv_python_platform
+
+
+
+
+                { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $am_display_PYTHON script directory" >&5
+$as_echo_n "checking for $am_display_PYTHON script directory... " >&6; }
+  am_cv_python_pythondir=`$PYTHON -c "from distutils import sysconfig; print sysconfig.get_python_lib(0,0,prefix='$PYTHON_PREFIX')" 2>/dev/null ||
+     echo "$PYTHON_PREFIX/lib/python$PYTHON_VERSION/site-packages"`
+  { $as_echo "$as_me:${as_lineno-$LINENO}: result: done" >&5
+$as_echo "done" >&6; }
+  pythondir=$am_cv_python_pythondir
+
+
+
+  pkgpythondir=\${pythondir}/$PACKAGE
+
+
+            { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $am_display_PYTHON extension module directory" >&5
+$as_echo_n "checking for $am_display_PYTHON extension module directory... " >&6; }
+  am_cv_python_pyexecdir=`$PYTHON -c "from distutils import sysconfig; print sysconfig.get_python_lib(1,0,prefix='$PYTHON_EXEC_PREFIX')" 2>/dev/null ||
+     echo "${PYTHON_EXEC_PREFIX}/lib/python${PYTHON_VERSION}/site-packages"`
+  { $as_echo "$as_me:${as_lineno-$LINENO}: result: done" >&5
+$as_echo "done" >&6; }
+  pyexecdir=$am_cv_python_pyexecdir
+
+
+
+  pkgpyexecdir=\${pyexecdir}/$PACKAGE
+
+
+
+  fi
+unset am_cv_pathless_PYTHON
+unset am_cv_python_version
+unset am_cv_python_platform
+unset am_cv_python_pythondir
+unset am_cv_python_pyexecdir
+
+have_python="no"
+if test "$PYTHON" ; then
+
+    eval eval py2_7execdir=${pyexecdir}
+
+    eval eval pkgpy2_7execdir=${pkgpyexecdir}
+
+
+
+
+    { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $am_display_PYTHON2_7 include directory" >&5
+$as_echo_n "checking for $am_display_PYTHON2_7 include directory... " >&6; }
+if test "${am_cv_python2_7_includedir+set}" = set; then :
+  $as_echo_n "(cached) " >&6
+else
+  am_cv_python2_7_includedir=`$PYTHON -c "from distutils import sysconfig; print sysconfig.get_config_var('INCLUDEPY')" 2>/dev/null ||
+         echo "$PYTHON_PREFIX/include/python$PYTHON_VERSION"`
+fi
+{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $am_cv_python2_7_includedir" >&5
+$as_echo "$am_cv_python2_7_includedir" >&6; }
+      python2_7includedir=$am_cv_python2_7_includedir
+
+
+    { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $am_display_PYTHON C libraries directory" >&5
+$as_echo_n "checking for $am_display_PYTHON C libraries directory... " >&6; }
+if test "${am_cv_python2_7_clibdir+set}" = set; then :
+  $as_echo_n "(cached) " >&6
+else
+  am_cv_python2_7_clibdir=`$PYTHON -c "from distutils import sysconfig; print sysconfig.get_config_var('LIBPL')" 2>/dev/null ||
+         echo "$PYTHON_PREFIX/lib/python$PYTHON_VERSION/config"`
+fi
+{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $am_cv_python2_7_clibdir" >&5
+$as_echo "$am_cv_python2_7_clibdir" >&6; }
+      python2_7clibdir=$am_cv_python2_7_clibdir
+
+
+    { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $am_display_PYTHON link flags" >&5
+$as_echo_n "checking for $am_display_PYTHON link flags... " >&6; }
+if test "${am_cv_python2_7_linkflags+set}" = set; then :
+  $as_echo_n "(cached) " >&6
+else
+
+            am_cv_python2_7_linkflags=`$PYTHON -c "from distutils import sysconfig; print '-L' + sysconfig.get_config_var('LIBPL')" 2>/dev/null || echo "-L$PYTHON_PREFIX/lib/python$PYTHON_VERSION/config"`
+            am_cv_python2_7_linkflags="$am_cv_python2_7_linkflags -lpython$PYTHON_VERSION"
+
+fi
+{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $am_cv_python2_7_linkflags" >&5
+$as_echo "$am_cv_python2_7_linkflags" >&6; }
+      python2_7linkflags=$am_cv_python2_7_linkflags
+
+
+    PYTHON_CFLAGS="-I$python2_7includedir"
+    PYTHON_LIBS="-L$python2_7clibdir $python2_7linkflags"
+
+    _CPPFLAGS="$CPPFLAGS"
+    CPPFLAGS="$CPPFLAGS ${PYTHON_CFLAGS}"
+
+    echo checking python includes in $python2_7includedir
+
+    unset ac_cv_header_Python_h
+    ac_fn_c_check_header_mongrel "$LINENO" "Python.h" "ac_cv_header_Python_h" "$ac_includes_default"
+if test "x$ac_cv_header_Python_h" = x""yes; then :
+  have_python_headers="yes"
+else
+  have_python_headers="no"
+fi
+
+
+
+        { $as_echo "$as_me:${as_lineno-$LINENO}: checking for Python libraries" >&5
+$as_echo_n "checking for Python libraries... " >&6; }
+
+    cat confdefs.h - <<_ACEOF >conftest.$ac_ext
+/* end confdefs.h.  */
+
+int
+main ()
+{
+
+  ;
+  return 0;
+}
+_ACEOF
+if ac_fn_c_try_link "$LINENO"; then :
+  have_python_libraries="yes"
+else
+  have_python_libraries="no"
+fi
+rm -f core conftest.err conftest.$ac_objext \
+    conftest$ac_exeext conftest.$ac_ext
+
+    CPPFLAGS="$_CPPFLAGS"
+
+    if test "$have_python_libraries" = "yes"
+    then
+        if test "$python_libraries"
+        then
+            { $as_echo "$as_me:${as_lineno-$LINENO}: result: $python_libraries" >&5
+$as_echo "$python_libraries" >&6; }
+        else
+            { $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
+$as_echo "yes" >&6; }
+        fi
+    else
+        { $as_echo "$as_me:${as_lineno-$LINENO}: result: no" >&5
+$as_echo "no" >&6; }
+    fi
+
+    if test "$have_python_headers" = "yes" \
+       && test "$have_python_libraries" = "yes"
+    then
+        have_python="yes"
+    else
+        have_python="no"
+    fi
+
+    if test "$have_python" = "no"
+    then
+      { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: No python compilation environment for =2.7" >&5
+$as_echo "$as_me: WARNING: No python compilation environment for =2.7" >&2;}
+    else
+      PYTHON2_7_CFLAGS="${PYTHON_CFLAGS}"
+
+
+
+      PYTHON2_7_LIBS="${PYTHON_LIBS}"
+
+
+      { $as_echo "$as_me:${as_lineno-$LINENO}: Found working python compilation environment for =2.7" >&5
+$as_echo "$as_me: Found working python compilation environment for =2.7" >&6;}
+    fi
+fi
+ if test "$have_python" != "no"; then
+  PYTHON_2_7_TRUE=
+  PYTHON_2_7_FALSE='#'
+else
+  PYTHON_2_7_TRUE='#'
+  PYTHON_2_7_FALSE=
+fi
+
+
+if test -f "$PYTHON" ; then found_one=$PYTHON ; PYTHONS="$PYTHON $PYTHONS" ; fi
 PYTHON=$found_one
 if ! test "$found_one" ; then
    as_fn_error $? "No python development environments found" "$LINENO" 5
@@ -16553,6 +16933,10 @@
   as_fn_error $? "conditional \"PYTHON_2_6\" was never defined.
 Usually this means the macro was only invoked conditionally." "$LINENO" 5
 fi
+if test -z "${PYTHON_2_7_TRUE}" && test -z "${PYTHON_2_7_FALSE}"; then
+  as_fn_error $? "conditional \"PYTHON_2_7\" was never defined.
+Usually this means the macro was only invoked conditionally." "$LINENO" 5
+fi
 
 : ${CONFIG_STATUS=./config.status}
 ac_write_fail=0
