# Contributor: Calimero <calimeroteknik@free.fr>
pkgname=openbox-menu
pkgver=0.3.6.6
pkgrel=1
pkgdesc="Dynamic XDG menu for openbox"
arch=('i686' 'x86_64')
url="http://mimasgpc.free.fr/openbox-menu.html"
license=('GPL3')
depends=('menu-cache' 'lxmenu-data')
source=(http://mimarchlinux.googlecode.com/files/${pkgname}-${pkgver}.tar.bz2)
md5sums=('4b298e54ab03e80a5a22fae065531ddd')

build() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  make
  install -D -m755 openbox-menu "${pkgdir}/usr/bin/openbox-menu"
}
