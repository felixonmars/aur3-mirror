# Maintainer: Holden Cox <segrived@gmail.com>
pkgname=ftjerm
pkgver=0.12
pkgrel=1
pkgdesc="Quake-like terminal emulator, a fork of stjerm"
arch=('i686' 'x86_64')
url=('https://github.com/segrived/ftjerm')
license=('GPLv2')
depends=('vte>=0.16' 'gtk2>=2.10')
makedepends=('autoconf' 'pkg-config')
source=(https://github.com/downloads/segrived/ftjerm/$pkgname-$pkgver.tar.gz)
md5sums=('f62fa71dec173c1d91adbe2adc7bd239')

build() {
    cd "$srcdir/$pkgname"
    make
}

package() {
    cd "$srcdir/$pkgname"
    make DESTDIR="$pkgdir/" install
    install -D -m644 LICENSE "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}