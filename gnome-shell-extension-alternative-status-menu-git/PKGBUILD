# Maintainer: Alucryd <alucryd at gmail dot com>
# Contributor: Sebastian Lenz <sebastian@archusers.de>
pkgname=gnome-shell-extension-alternative-status-menu-git
pkgver=20120402
pkgrel=1
pkgdesc="For those who want a power off item visible at all the time, replaces GNOME Shell status menu with one featuring separate Suspend and Power Off. Adds the ability to hibernate as well."
arch=('i686' 'x86_64')
url="http://live.gnome.org/GnomeShell/Extensions"
license=('GPL' 'LGPL')
depends=('gnome-shell-extension-common-git')
makedepends=('git' 'gnome-common' 'intltool')
conflicts=('gnome-shell-extensions-git' 'gnome-shell-extension-alternative-status-menu')
install='gschemas.install'

_gitroot="git://git.gnome.org/gnome-shell-extensions"
_gitname="gnome-shell-extensions"

build() {
  cd "$srcdir"
  msg "Connecting to GIT server...."
  if [ -d $_gitname ] ; then
    cd $_gitname && git pull origin
    msg "The local files are updated."
  else
    git clone $_gitroot --depth=1
    cd $_gitname
#    git branch -f gnome-3-2 origin/gnome-3-2
#    git checkout gnome-3-2
  fi
  msg "GIT checkout done or server timeout"
  msg "Starting make..."
  cd ${srcdir}/${_gitname}
  ./autogen.sh --prefix=/usr --enable-extensions="alternative-status-menu"
}

package() {
  cd ${srcdir}/${_gitname}
  make DESTDIR=${pkgdir} install
  rm -rf $pkgdir/usr/share/locale
}

