--- Makefile.old	2013-11-04 13:44:03.090453125 +0100
+++ Makefile	2013-11-04 13:51:44.735832602 +0100
@@ -1,7 +1,7 @@
 
-CONFIG := clang-debug
+# CONFIG := clang-debug
 # CONFIG := gcc-debug
-# CONFIG := release
+CONFIG := release
 
 ENABLE_TCL := 1
 ENABLE_QT4 := 1
@@ -42,12 +42,12 @@
 
 ifeq ($(CONFIG),release)
 CXX = gcc
-CXXFLAGS += -std=gnu++0x -march=native -O3 -DNDEBUG
+CXXFLAGS += -std=gnu++0x -O3 -DNDEBUG
 endif
 
 ifeq ($(ENABLE_TCL),1)
-CXXFLAGS += -I/usr/include/tcl8.5 -DYOSYS_ENABLE_TCL
-LDLIBS += -ltcl8.5
+CXXFLAGS += -DYOSYS_ENABLE_TCL
+LDLIBS += -ltcl8.6
 endif
 
 ifeq ($(ENABLE_GPROF),1)
