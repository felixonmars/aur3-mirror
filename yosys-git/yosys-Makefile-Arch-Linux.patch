--- Makefile.orig	2013-10-31 17:28:51.069528185 +0100
+++ Makefile	2013-10-31 17:31:44.423137814 +0100
@@ -1,7 +1,9 @@
 
-CONFIG := clang-debug
+# CONFIG := clang-debug
 # CONFIG := gcc-debug
-# CONFIG := release
+CONFIG := release
+
+DESTDIR="/usr/local"
 
 ENABLE_TCL := 1
 ENABLE_QT4 := 1
@@ -40,12 +42,12 @@
 
 ifeq ($(CONFIG),release)
 CXX = gcc
-CXXFLAGS += -std=gnu++0x -march=native -O3 -DNDEBUG
+CXXFLAGS += -std=gnu++0x -O2 -DNDEBUG
 endif
 
 ifeq ($(ENABLE_TCL),1)
-CXXFLAGS += -I/usr/include/tcl8.5 -DYOSYS_ENABLE_TCL
-LDLIBS += -ltcl8.5
+CXXFLAGS += -DYOSYS_ENABLE_TCL
+LDLIBS += -ltcl8.6
 endif
 
 ifeq ($(ENABLE_GPROF),1)
@@ -105,12 +107,12 @@
 	cd tests/asicworld && bash run-test.sh
 
 install: $(TARGETS)
-	install $(TARGETS) /usr/local/bin/
-	mkdir -p /usr/local/share/yosys
-	cp -r share/. /usr/local/share/yosys/.
+	install $(TARGETS) $(DESTDIR)/bin/
+	mkdir -p $(DESTDIR)/share/yosys
+	cp -r share/. $(DESTDIR)/share/yosys/.
 
 install-abc:
-	install yosys-abc /usr/local/bin/
+	install yosys-abc $(DESTDIR)/bin/
 
 manual:
 	cd manual && bash make.sh
