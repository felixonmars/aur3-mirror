--- Makefile.orig	2013-11-21 08:26:22.883573473 +0100
+++ Makefile	2013-11-21 08:55:48.353245480 +0100
@@ -1,7 +1,7 @@
 
-CONFIG := clang-debug
+# CONFIG := clang-debug
 # CONFIG := gcc-debug
-# CONFIG := release
+CONFIG := release
 
 ENABLE_TCL := 1
 ENABLE_QT4 := 1
@@ -44,12 +44,12 @@
 
 ifeq ($(CONFIG),release)
 CXX = gcc
-CXXFLAGS += -std=gnu++0x -march=native -O3 -DNDEBUG
+CXXFLAGS += -std=gnu++0x -O3 -DNDEBUG
 endif
 
 ifeq ($(ENABLE_TCL),1)
-CXXFLAGS += -I/usr/include/tcl8.5 -DYOSYS_ENABLE_TCL
-LDLIBS += -ltcl8.5
+CXXFLAGS += -DYOSYS_ENABLE_TCL
+LDLIBS += -ltcl8.6
 endif
 
 ifeq ($(ENABLE_GPROF),1)
