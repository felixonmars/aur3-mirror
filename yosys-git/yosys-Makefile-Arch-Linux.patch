--- Makefile.old	2013-08-09 11:39:43.084293235 +0200
+++ Makefile	2013-08-09 13:50:56.470124968 +0200
@@ -1,7 +1,9 @@
 
-CONFIG := clang-debug
+# CONFIG := clang-debug
 # CONFIG := gcc-debug
-# CONFIG := release
+CONFIG := release
+
+DESTDIR="/usr/local"
 
 ENABLE_TCL := 1
 ENABLE_QT4 := 1
@@ -34,12 +36,12 @@
 
 ifeq ($(CONFIG),release)
 CXX = gcc
-CXXFLAGS += -std=gnu++0x -march=native -O3 -DNDEBUG
+CXXFLAGS += -std=gnu++0x -O2 -DNDEBUG
 endif
 
 ifeq ($(ENABLE_TCL),1)
-CXXFLAGS += -I/usr/include/tcl8.5 -DYOSYS_ENABLE_TCL
-LDLIBS += -ltcl8.5
+CXXFLAGS += -DYOSYS_ENABLE_TCL
+LDLIBS += -ltcl8.6
 endif
 
 ifeq ($(ENABLE_GPROF),1)
@@ -95,10 +97,10 @@
 	cd tests/asicworld && bash run-test.sh
 
 install: $(TARGETS)
-	install $(TARGETS) /usr/local/bin/
+	install $(TARGETS) $(DESTDIR)/bin/
 
 install-abc:
-	install yosys-abc /usr/local/bin/
+	install yosys-abc $(DESTDIR)/bin/
 
 manual:
 	cd manual && bash make.sh
