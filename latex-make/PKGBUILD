# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: Paulus Cyril <paulus.cyril@gmail.com>
pkgname=latex-make
pkgver=2.1.15
pkgrel=1
pkgdesc="latex-make is a collection of LaTeX packages, scipts and Makefile fragments that allows to easily compile LaTeX documents."
arch=('i686' 'x86_64')
url="https://gforge.inria.fr/projects/latex-utils/"
license=('GPL')
groups=()
depends=('make' 'texlive-core' 'texlive-latexextra')
makedepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
source=(https://gforge.inria.fr/frs/download.php/24735/$pkgname-$pkgver.tar.gz)
noextract=()
md5sums=('5d29e29fac08ca765ea18867c165e7ec')

build() {
  cd "$srcdir/$pkgname-$pkgver"
  make
}

package() {
  cd "$srcdir/$pkgname-$pkgver"

  make prefix="$pkgdir/usr" texmf_prefix="$pkgdir/usr/share/texmf" install
}

# vim:set ts=2 sw=2 et:
