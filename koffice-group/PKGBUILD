# Contributor: zertyz <zertyz@gmail.com>

# This package was made with dependencies from the homonimous package group
# in order to correctly solve package dependencies for automated installations.

pkgname=koffice-group

# To keep the package dependencies updated, issue the following command:
# 	echo `pacman -Qg $pkgname | sed 's/.* //'`

# Get the correct version and URL with:
#	pacman -Qi `pacman -Qg $pkgname | sed 's/.* //' | head -n 1`

pkgver=2.3.3
pkgrel=11
pkgdesc="Package created from the homonimous official package group in order to allow 'koffice' to be listed as dependencies for other packages"
arch=('i686' 'x86_64')
url="http://koffice.kde.org"
license="GPL"
depends=(koffice-filters koffice-handbook koffice-interfaces koffice-karbon koffice-karbon-doc koffice-kexi koffice-kexi-doc koffice-kformula koffice-kformula-doc koffice-kounavail koffice-kplato koffice-kpresenter koffice-kpresenter-doc koffice-krita koffice-kspread koffice-kspread-doc koffice-kword koffice-libs koffice-pics koffice-plugins koffice-servicetypes koffice-templates koffice-thesaurus-doc koffice-tools)
makedepends=()
conflicts=()
replaces=()
backup=()
install=
source=()
md5sums=()

build() {
  true
}
