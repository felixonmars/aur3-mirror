post_install() {
    echo -e "Edit the file in /usr/share/poezio/data/default_config.cfg and copy it to your ${XDG_CONFIG_HOME}/poezio/poezio.cfg\nIf you don't do so, poezio will create it at the first start\n"
}
