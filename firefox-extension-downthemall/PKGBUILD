# Maintainer: Tianjiao Yin <ytj000+aur@gmail.com>

pkgname=firefox-extension-downthemall
pkgver=2.0.15
pkgrel=1
pkgdesc="The first and only download manager/accelerator built inside Firefox!"
arch=('any')
url="http://www.downthemall.net/"
license=('GPL2')
depends=('firefox')
source=(https://addons.mozilla.org/firefox/downloads/file/165521/downthemall-$pkgver-fx+sm.xpi)


build() {
  cd $srcdir
  emid=$(sed -n '/.*<em:id>\(.*\)<\/em:id>.*/{s//\1/p;q}' install.rdf) || return 1
  local dstdir=$pkgdir/usr/lib/firefox/extensions/${emid}
  install -d $dstdir
  sed -i 's#<em:maxVersion>.*</em:maxVersion>#<em:maxVersion>11.*</em:maxVersion>#' install.rdf
  rm *.xpi
  mv * $dstdir
}

md5sums=('9648ff87ff1c650c70dc16e9d7c1b188')
