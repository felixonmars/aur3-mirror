# Maintainer: FadeMind <fademind@gmail.com>

pkgname=panelkadu
pkgver=1.0_11
pkgrel=1
pkgdesc='Panelkadu plugin for Kadu IM'
arch=('i686' 'x86_64')
url='http://www.kadu.im/w/Panelkadu'
license=('GPL')
depends=('kadu>=1.0')
makedepends=('cmake' 'git' 'chrpath')
source=($pkgname::'git://gitorious.org/ultr-kadu-plugins/panelkadu.git')
md5sums=('SKIP')

build() {
   mkdir build
   cd build
 
   cmake ../$pkgname \
     -DCMAKE_BUILD_TYPE=Release \
     -DCMAKE_INSTALL_PREFIX=/usr \
     -DCMAKE_INSTALL_LIBDIR=/usr/lib \
     -DQT_QMAKE_EXECUTABLE=qmake-qt4
}

package() {
   make -C build DESTDIR="$pkgdir" LIBDIR=/usr/lib install
   chrpath -d "$pkgdir"/usr/lib/kadu/plugins/*.so
}
