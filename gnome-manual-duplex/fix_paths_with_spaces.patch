--- gnome-manual-duplex	2014-01-20 23:55:44.474094607 +0100
+++ gnome-manual-duplex	2014-01-21 00:06:24.339121731 +0100
@@ -278,17 +278,17 @@
 	    if not self.SkipOddPages:
 		# Print out odd pages
 		# print self.filename
-		rc = os.system("file " + self.filename + " | grep -q PDF")
+		rc = os.system("file \"" + self.filename + "\" | grep -q PDF")
 		print "{ps,pdf}tops '2:0' '" + self.filename + \
 		    "' " + self.tempfile.name
 		if rc == 256:
 		    self.is_pdf = 0
-		    os.system("pstops 2:0 '"
-			+ self.filename + "' " + self.tempfile.name)
+		    os.system("pstops 2:0 \""
+			+ self.filename + "\" " + self.tempfile.name)
 		else:
 		    self.is_pdf = 1
-		    os.system("pdftops '" + self.filename
-			+ "' - | pstops 2:0 > "
+		    os.system("pdftops \"" + self.filename
+			+ "\" - | pstops 2:0 > "
 			+ self.tempfile.name)
 		# print "is_pdf ", self.is_pdf
 		
