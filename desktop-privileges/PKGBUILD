# Contributor: Ulf WInkelvos <ulf ät winkelvos dot de>
# Maintainer: anonymous_user <31337h4ck3r at gmail dot com>
pkgname=desktop-privileges
pkgver=0.3
pkgrel=1
pkgdesc="A collection (under development) of polkit configuration files suitable for desktop users: - Enables users of the storage group to mount local disks - Enables users of the power group to change the cpufreq govenor - Any
suggestions and ideas are welcome"
arch=('any')
url="http://aur.archlinux.org/packages.php?ID=41664"
license=('GPL')
depends=('polkit')
conflicts=('desptop-privileges')
replaces=('desptop-privileges')
#install=
#changelog=
source=(cpufreqselector.pkla udisks-internal.pkla udisks2-internal.pkla)
md5sums=('8169497e0438548ff5c8a2904b520631'
         'd606019d21171e54e729e5d147b96fea'
		 '0f683f5969d34b6a1e22ba6c4cdf83d4')

build() {
	echo "nothing to build" #nothing
}

package() {
	install -dm700 "${pkgdir}/var/lib/polkit-1/"
	install -dm755 "${pkgdir}/var/lib/polkit-1/localauthority/"
	install -dm755 "${pkgdir}/var/lib/polkit-1/localauthority/50-local.d/"
	install -D *.pkla ${pkgdir}/var/lib/polkit-1/localauthority/50-local.d/
}
