post_install() {
	echo "-- Use the alias.diff file to create an alias /cubes that will display all supported colors."
	echo "-- This alias is already hard coded into any *new* config irssi may generate"
}

post_remove() {
	echo "-- You may want to remove the alias /cubes from your config"
}
