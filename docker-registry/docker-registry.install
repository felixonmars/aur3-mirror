post_install() {
    echo "To get started go to /etc/docker-registry and configure the registry:"
    echo "# cp /etc/docker-registry/config{_sample.yml,.yml}"
    echo "Then start the registry service:"
    echo "# systemctl start docker-registry"
    echo "It will listen on 0.0.0.0:5000"
}
