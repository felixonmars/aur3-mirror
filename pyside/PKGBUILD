# Maintainer: Matthias Maennich <arch@maennich.net>
# Contributor: Massimiliano Torromeo <massimiliano.torromeo@gmail.com>
pkgname=pyside
pkgver=1.0.0
pkgrel=1
pkgdesc="Provides LGPL Qt bindings for Python and related tools for binding generation."
arch=('i686' 'x86_64')
license=('LGPL')
url="http://www.pyside.org"
depends=('libshiboken>=1.0.0' 'python2' 'qt>=4.7' 'phonon')
makedepends=('cmake' 'automoc4' 'shibokengenerator>=1.0.0')
source=("http://www.pyside.org/files/$pkgname-qt4.7+$pkgver.tar.bz2")
md5sums=('5589a883cebcb799a48b184a46db6386')

build(){
    cd $srcdir/$pkgname-qt4.7+$pkgver
    mkdir -p build && cd build
    cmake ../ -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=Release
    make
}

package(){
    cd $srcdir/$pkgname-qt4.7+$pkgver/build
	make DESTDIR=$pkgdir install
}
