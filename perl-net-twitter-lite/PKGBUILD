# Generated by Xyne::Arch::CPAN 0.07

pkgname=perl-net-twitter-lite
pkgver=0.08006
pkgrel=1
pkgdesc="A perl interface to the Twitter API"
arch=('i686 x86_64')
url="http://search.cpan.org/dist/Net-Twitter-Lite/"
license=('perl')
source=('http://search.cpan.org/CPAN/authors/id/M/MM/MMIMS/Net-Twitter-Lite-0.08006.tar.gz')
md5sums=('3cfeed07f256f9bfa6b17527914182c0')
sha256sums=('504a5765be88fefafef83d9cc95f6ae993e1b4b13fc9a4e82923965b567b3c65')
depends=('perl-encode' 'perl-json-any>=1.21' 'perl-lwp-useragent>=2.32.0' 'perl-uri>=1.40' 'perl>=5.0.5')
makedepends=('perl-extutils-makemaker>=6.42')
provides=('perl-net-twitter-lite')
options=(!emptydirs)

build() {
  _dir=$(find $srcdir -maxdepth 2 -type f -name 'Makefile.PL')
  if [ ! -z "$_dir" ]; then
    cd $(dirname "$_dir")
    PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1
    make  || return 1
    make install DESTDIR="${pkgdir}" || return 1

  else
  _dir=$(find $srcdir -maxdepth 2 -type f -name 'Build.PL')
  if [ ! -z "$_dir" ]; then
    cd $(dirname "$_dir")
    PERL_MM_USE_DEFAULT=1 perl Build.PL INSTALLDIRS=vendor || return 1
    ./Build  || return 1
    ./Build install destdir=${pkgdir} || return 1

  else
    echo "error: failed to detect build method for $pkgname"
    echo "you may be able to fix this by editing the PKGBUILD"
    return 1
  fi fi

  # remove perllocal.pod and .packlist
  find ${pkgdir} -name perllocal.pod -delete
  find ${pkgdir} -name .packlist -delete
}

# vim:set ts=2 sw=2 et:


