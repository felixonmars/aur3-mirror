# Contributor: Anonymous
# Generator  : CPANPLUS::Dist::Arch 1.12

pkgname='perl-zeromq'
pkgver='0.15'
pkgrel='1'
pkgdesc="A ZeroMQ2 wrapper for Perl"
arch=('any')
license=('PerlArtistic' 'GPL')
options=('!emptydirs')
depends=('perl-task-weaken' 'util-linux>=2.19.1' 'zeromq>=2.1.7')
makedepends=('perl-devel-checklib>=0.4' 'perl-test-exception>=0.29' 'perl-test-requires' 'perl-test-simple' 'perl-test-tcp' 'perl-task-weaken' 'zeromq>=2.1.7')
url='http://search.cpan.org/dist/ZeroMQ'
source=('http://search.cpan.org/CPAN/authors/id/D/DM/DMAKI/ZeroMQ-0.15.tar.gz')
md5sums=('68070a331b5d7bcea5abcca89f53920e')
_distdir="${srcdir}/ZeroMQ-0.15"

build() {
  ( export PERL_MM_USE_DEFAULT=1 PERL5LIB=""                 \
      PERL_AUTOINSTALL=--skipdeps                            \
      PERL_MM_OPT="INSTALLDIRS=vendor DESTDIR='$pkgdir'"     \
      PERL_MB_OPT="--installdirs vendor --destdir '$pkgdir'" \
      MODULEBUILDRC=/dev/null

    cd "$_distdir"
    /usr/bin/perl Makefile.PL
    make
  )
}
# temporary disabled
#check() {
#  cd "$_distdir"
#  ( export PERL_MM_USE_DEFAULT=1 PERL5LIB=""
#    make test
#  )
#}

package() {
  cd "$_distdir"
  make install
  find "$pkgdir" -name .packlist -o -name perllocal.pod -delete
}

# Local Variables:
# mode: shell-script
# sh-basic-offset: 2
# End:
# vim:set ts=2 sw=2 et:
