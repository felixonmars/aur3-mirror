# Maintainer: Skunnyk <skunnyk@archlinux.fr>
pkgname=gmvault
pkgver=1.6beta
_pkgver=1.6-beta
pkgrel=1
pkgdesc="Gmvault is a tool for backing up your gmail account and never lose email correspondence."
arch=(any)
url="http://gmvault.org"
license=('GPL')
depends=('python2' 'python2-logbook' 'python-gdata' 'python2-imapclient')
makedepends=(setuptools)
source=(https://github.com/downloads/gaubert/gmvault/$pkgname-$_pkgver-src.tar.gz use-python2.patch)

build() {
  cd "$srcdir/$pkgname-$_pkgver"
  patch -p1 < ../use-python2.patch
}
 
package() {
  cd "$srcdir/$pkgname-$_pkgver"
  python2 setup.py install --root "${pkgdir}" --optimize=1
}

# vim:set ts=2 sw=2 et:
md5sums=('3a67afbe231ec32e89218aabbd50cfca'
         '498f4f146b0522199901857c525f3043')
