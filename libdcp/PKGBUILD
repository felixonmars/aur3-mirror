# Maintainer: Markus Kalb <mk@filmkreis.tu-darmstadt.de>
# Contributor: Stefan Karner <stefan.karner@student.tuwien.ac.at>

pkgname=libdcp
pkgver=0.98.0
pkgrel=4
pkgdesc="A small C++ library which can create and read Digital Cinema Packages using JPEG2000 and WAV files"
arch=('i686' 'x86_64')
url="http://carlh.net/software/libdcp"
license=('GPL')
groups=
depends=('openssl' 'libxml++>=2.6' 'xmlsec' 'openjpeg' 'libcxml>=0.08' 'libsigc++>=2.0' 'boost-libs>=1.45')
makedepends=('python2' 'boost>=1.45')
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
source=(.AURINFO
        "${pkgname}.tar.gz::http://carlh.net/software/${pkgname}/${pkgname}-${pkgver}.tar.bz2")

build() {
  cd "${srcdir}/${pkgname}-${pkgver}"

  python2 waf configure --prefix=/usr
  python2 waf build
}

package() {
  cd "${srcdir}/${pkgname}-${pkgver}"

  python2 waf install --destdir=$pkgdir
}

sha512sums=('SKIP'
            '05ad249ee4ed467a7a42dcdd61249f63b9ed9e6021f873e783191c17e157c66939a2110182d7a0dd2e49969d8771f470c39f2e6f5d990242a991c041f3f5a516')
