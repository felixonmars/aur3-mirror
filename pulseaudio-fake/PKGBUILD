# Maintainer: Lara Maia <lara@craft.net.br>

pkgname=pulseaudio-fake
pkgdesc="A fake pulseaudio package (to bypass dependency)"
url='http://lara.craft.net.br'

pkgrel=1
pkgver=1
arch=(any)
license=('GPL')

provides=('pulseaudio=fake'
          'pulseaudio-alsa=fake')
          
conflicts=(pulseaudio
           pulseaudio-alsa)

package() {
	true
}
