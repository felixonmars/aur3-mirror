# Maintainer: Iñigo Alvarez<alvarezviu@gmail.com>
pkgname=rodent
pkgver=4.7.4
pkgrel=0
pkgdesc="Lightweight, yet full of options filemanager, compatible with console commands"
arch=('i686' 'x86_64')
url="http://xffm.org/"
license=('GPL')
depends=('libzip' 'libxml2' 'gtk2' 'intltool' 'file' 'gawk')
options=('!libtool')
source=(http://downloads.sourceforge.net/project/xffm/$pkgver/$pkgname-$pkgver.tar.bz2)
md5sums=('6f1d8ffbee134ac859fd1084a2c8d397')

build()
	{
  		cd "$srcdir/$pkgname-$pkgver"
		unset LDFLAGS
  		./configure --prefix=/usr
  		make 
	}


check()
	{
  		cd "$srcdir/$pkgname-$pkgver"
  		make -k check
	}	


package()
	{
  		cd "$srcdir/$pkgname-$pkgver"
  		make DESTDIR="$pkgdir/" install
 	}
