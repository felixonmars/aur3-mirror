diff -ur animal-0.15.0/animal/distmaps.c animal-0.15.0.ok/animal/distmaps.c
--- animal-0.15.0/animal/distmaps.c	2006-11-29 03:25:29.000000000 +0100
+++ animal-0.15.0.ok/animal/distmaps.c	2008-04-17 14:20:24.000000000 +0200
@@ -176,13 +176,14 @@
  *                                              squared distance transform.
  *----------------------------------------------------------------------------*/
 
+static bool edt_maurer_2D_from_1D(ImgPUInt32 *im);
+
 AnimalExport bool
 edt_maurer2003(ImgPUInt32 *im)
 {
    char *fname="edt_maurer2003";
    int i,r,c;
    bool stat;
-   inline bool edt_maurer_2D_from_1D(ImgPUInt32 *im);
    puint32 infty;
 
    assert(im->isbinary);
@@ -207,13 +208,14 @@
    return true;
 }
 
-inline bool
+static bool maurer_voronoi_edt_2D(ImgPUInt32 *im, puint32 *im_row, int *g, int *h);
+
+static bool
 edt_maurer_2D_from_1D(ImgPUInt32 *im)
 {
    bool stat;
    int i1, *g, *h; // same naming as in the paper
    char *fname="edt_maurer_2D_from_1D";
-   inline bool maurer_voronoi_edt_2D(ImgPUInt32 *im, puint32 *im_row, int *g, int *h);
 
    unsigned ncols = im->cols;
    unsigned nrows = im->rows;
@@ -248,10 +250,18 @@
    return true;
 }
 
-inline bool remove_edt(int du, int dv, int dw, 
-                      int u,  int v,  int w);
+inline bool 
+remove_edt(int du, int dv, int dw, 
+           int u,  int v,  int w)
+{
+    // 11 integer expressions
+    int a = v - u,
+        b = w - v,
+        c = w - u;
+    return ( (c * dv - b * du - a * dw) > (a * b * c) );
+}
 
-inline bool
+static bool
 maurer_voronoi_edt_2D(ImgPUInt32 *im, puint32 *im_row, int *g, int *h)
 {
    int l, i, ns, tmp0, tmp1, tmp2, r,c;
@@ -295,18 +305,6 @@
 }
 
 
-inline bool 
-remove_edt(int du, int dv, int dw, 
-           int u,  int v,  int w)
-{
-    // 11 integer expressions
-    int a = v - u,
-        b = w - v,
-        c = w - u;
-    return ( (c * dv - b * du - a * dw) > (a * b * c) );
-}
-
-
 /* 
  * ========================================
  *  ----------------------------------
@@ -840,13 +838,14 @@
  *    proc. of 5th Int. Conf. Mathematical Morphology and its Applications to
  *    Image and Signal Processing, 2000
  */
+static bool edt_meijster_2D_from_1D(ImgPUInt32 *im);
+
 AnimalExport bool
 edt_meijster2000(ImgPUInt32 *im)
 {
    char *fname="edt_meijster2000";
    int i,r,c;
    bool stat;
-   inline bool edt_meijster_2D_from_1D(ImgPUInt32 *im);
    puint32 infty;
 
    assert(im->isbinary);
@@ -872,7 +871,7 @@
 
 #define meijster_f(x,i, im_value) ( ( (x) - (i))*((x) - (i) ) + (im_value) )
 
-inline bool
+static bool
 edt_meijster_2D_from_1D(ImgPUInt32 *im)
 {
    char *fname="edt_meijster_2D_from_1D";
