# Maintainer: Andrzej Giniewicz <gginiu@gmail.com>
pkgname=python2-traitsbackendqt-git
pkgver=20110205
pkgrel=1
pkgdesc="PyQt backend for Traits and TraitsGUI"
arch=('i686' 'x86_64')
url="https://github.com/enthought/traitsbackendqt"
license=('BSD')
depends=('python2' 'python2-pyqt>=4' 'python2-traits' 'python2-enthoughtbase')
makedepends=('git' 'python2-distribute')
provides=('python2-traitsbackendqt' 'python2-traitsbackend' 'python2-traitsbackend-git')
conflicts=('python-enthought-traits' 'python2-traitsbackendqt' 'python-ets-traits-qt-svn')
replaces=('python-ets-traits-qt-svn')
options=(!emptydirs)

_gitroot="https://github.com/enthought/traitsbackendqt.git"
_gitname="traitsbackendqt"

build() {
  cd "$srcdir"
  msg "Connecting to GIT server...."

  if [ -d $_gitname ] ; then
    cd $_gitname && git pull origin
    msg "The local files are updated."
  else
    git clone $_gitroot $_gitname
  fi

  msg "GIT checkout done or server timeout"
  msg "Starting make..."

  rm -rf "$srcdir/$_gitname-build"
  git clone "$srcdir/$_gitname" "$srcdir/$_gitname-build"
  cd "$srcdir/$_gitname-build"

  python2 setup.py install --root="$pkgdir"/ --optimize=1 || return 1
}

