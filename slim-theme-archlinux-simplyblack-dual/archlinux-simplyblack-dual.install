post_install() {
  echo 'Edit your /etc/slim.conf and change the current_theme to archlinux-simplyblack-dual'
}

post_remove() {
  echo 'Edit your /etc/slim.conf and change the current_theme to something you still have installed'
}

op=$1
shift
$op $*
