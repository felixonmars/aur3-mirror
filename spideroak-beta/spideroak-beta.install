post_install() {
  echo "-----------------------------------------------------------"
  echo "SpiderOak uses inotify to find changed files."
  echo "Add fs.inotify.max_user_watches = 65536 in /etc/sysctl.conf"
  echo "when watching many files."
  echo "-----------------------------------------------------------"
}
