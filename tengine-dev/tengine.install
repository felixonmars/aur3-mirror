post_install() {
    if [[ -e var/log/tengine ]]; then
        chmod 750 var/log/tengine
        chown root:log var/log/tengine
    fi
}

post_upgrade() {
    [[ $(vercmp $2 1.4.3-2) -le 0 ]] && post_install
}
