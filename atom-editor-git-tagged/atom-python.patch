diff --git a/atom.sh b/atom.sh
index ecd7da0..d686159 100755
--- a/atom.sh
+++ b/atom.sh
@@ -76,6 +76,7 @@ elif [ $OS == 'Linux' ]; then
   USR_DIRECTORY=$(readlink -f $(dirname $SCRIPT)/..)
   ATOM_PATH="$USR_DIRECTORY/share/atom/atom"
   ATOM_HOME="${ATOM_HOME:-$HOME/.atom}"
+  PYTHON=python2
 
   mkdir -p "$ATOM_HOME"
 

