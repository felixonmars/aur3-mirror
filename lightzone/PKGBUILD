# Maintainer: Rikles <style.boubou@gmail.com>
# Contributer: N30N <archlinux@alunamation.com>

pkgname="lightzone"
pkgver=4.0.0
pkgrel=2
pkgdesc="A professional photo browser and editor, like Aperture or Lightroom"
url="http://lightzoneproject.org/"
license=("custom")
arch=("i686" "x86_64")

if [[ $CARCH == 'i686' ]]; then
  source=("${pkgname}-${pkgver}.deb::http://download.opensuse.org/repositories/home:/ktgw0316:/LightZone/Debian_7.0/i386/lightzone_4.0.0-0obs20130609_i386.deb")
  md5sums=('00f6539456a4d7c25a66f72e8911224e')
  depends+=("gcc-libs" "libstdc++5" "libx11" "libxtst" "java-runtime")
elif [[ $CARCH == 'x86_64' ]]; then
  source=("${pkgname}-${pkgver}.deb::http://download.opensuse.org/repositories/home:/ktgw0316:/LightZone/Debian_7.0/amd64/lightzone_4.0.0-0obs20130609_amd64.deb")
  md5sums=('7defbbf09f20c4306cb599a59aec76a9')
  depends+=("lib32-gcc-libs" "lib32-libstdc++5" "lib32-libx11" "lib32-libxcb" "lib32-libxtst" "java-runtime")
fi

build() {
  cd "${srcdir}"
  tar -xf data.tar.gz
}

package() {
  cd "${srcdir}"

# Install
  cp -dr --no-preserve=ownership {opt,usr} "${pkgdir}"/
}
