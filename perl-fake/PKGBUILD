# Maintainer: justbrowsing <developer4linux+aur@gmail.com>
pkgname=perl-fake
pkgdesc="Fake perl package (saves space)"
pkgver=1
pkgrel=2
arch=('any')
license=('none')
url="http://justbrowsing.info"
provides=('perl')
options=(!strip)
optdepends=('openssl: only helper script needs the real Perl package'
            'qpdf: only helper script needs the real Perl package')

package() {
  true
}
