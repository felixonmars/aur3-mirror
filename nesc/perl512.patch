diff -rupN nesc-1.3.1/tools/migdecode.pm ../nesc-1.3.1/tools/migdecode.pm
--- nesc-1.3.1/tools/migdecode.pm	2011-02-20 16:13:56.719381652 +0100
+++ ../nesc-1.3.1/tools/migdecode.pm	2006-05-02 23:42:19.000000000 +0200
@@ -47,14 +47,14 @@ sub decode() {
     $amtype = $4;
 
     $base = 0;
+    foreach $spec(@spec) {
-    for (@spec) {
 	# remove end of line stuff to avoid confusion w/ cygwin
 	chop;
 	s/\r$//;
 
 	/ *(.*)/;
+	@my_field = split(' ', $spec);
+	($field, $type, $offset, $bitlength) = @my_field;
-	split / /, $1;
-	($field, $type, $offset, $bitlength) = @_;
 
 	$basetype = &basetype($type);
 	@field_array_max = &arraymax($type);	
