# Maintainer: Stefano Facchini <stefano.facchini@gmail.com>
pkgname=libvirt-glib
pkgver=0.1.5
pkgrel=1
pkgdesc="GLib bindings for libvirt"
arch=('i686' 'x86_64')
url="http://libvirt.org"
license=('LGPL2.1')
depends=('libvirt' 'glib2' 'gobject-introspection' 'python2')
options=('!libtool')
source=(ftp://libvirt.org/libvirt/glib/$pkgname-$pkgver.tar.gz)
sha256sums=('b3a4fea273cb52890812a5d4033556c5221112c7fe47da2838de72641e547cbb')

build() {
  cd "$srcdir/$pkgname-$pkgver"

  PYTHON=/usr/bin/python2 ./configure --prefix=/usr
  make
}

package() {
  cd "$srcdir/$pkgname-$pkgver"
  make DESTDIR="$pkgdir/" install
}

# vim:set ts=2 sw=2 et:
