# Contributor: Gustavo Alvarez <sl1pkn07@gmail.com>

pkgname=dkms-vhba
_pkgname=vhba-module
_module=vhba
pkgver=20110416
pkgrel=4
pkgdesc="DKMS controlled drivers for VHBA module for CDEMU"
arch=('any')
url="http://cdemu.sourceforge.net/"
license=('GPL2')
depends=('dkms')
provides=('vhba-module')
conflicts=('vhba-module')
install="${pkgname}.install"
source=("http://downloads.sourceforge.net/cdemu/$_pkgname-$pkgver.tar.gz"
	vhba-kernel2.6.37.patch
	${pkgname}.install
	dkms.conf)
md5sums=('2f91dd4ee8648da92d625221d4275b60'
	 'f0499fc54f6ef9b8d6ca0b9e940c5906'
	 '45d9925e879ffc32b98647b3300932f5'
	 'f9116bc849811f965dca4e1723615e1f')

build() {
    cd ${srcdir}/${_pkgname}-${pkgver}
#    git apply -p2 ../vhba-kernel2.6.37.patch
}

package() {
    cd ${srcdir}/${_pkgname}-${pkgver}

    mkdir -p ${pkgdir}/usr/src/${_module}-${pkgver}
    cp -a * ${pkgdir}/usr/src/${_module}-${pkgver} 
    cp ${srcdir}/dkms.conf ${pkgdir}/usr/src/${_module}-${pkgver}
}  

