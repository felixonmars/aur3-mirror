# Maintainer: TDY <tdy@gmx.com>

pkgname=ttf-hanazono
pkgver=20101013
pkgrel=1
pkgdesc="A Japanese Kanji font"
arch=('any')
url="http://fonts.jp/hanazono/"
license=('custom')
depends=('fontconfig' 'xorg-font-utils')
install=ttf.install
source=(http://fonts.jp/hanazono/hanazono-$pkgver.zip)
md5sums=('e052b4215b4f9484637ec6408550bfa0')

build() {
  cd "$srcdir"
  install -Dm644 hanazono.ttf "$pkgdir/usr/share/fonts/TTF/hanazono.ttf"
  install -Dm644 LICENSE.txt "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
}
