post_install() {
	echo "==>> If you want to emulate real MSX systems and not only the free C-BIOS machines,"
	echo "==>> put the system ROMs in the following directory: ~/.openMSX/share/systemroms"
}

op=$1
shift
$op $*
