diff -upr sqldeveloper.orig/ide/bin/launcher.sh sqldeveloper/ide/bin/launcher.sh
--- sqldeveloper.orig/ide/bin/launcher.sh	2008-11-14 03:46:22.000000000 +0200
+++ sqldeveloper/ide/bin/launcher.sh	2009-02-05 19:20:05.000000000 +0200
@@ -365,22 +365,24 @@ GetDefaultJDK()
   # Once the new path for java has been determined, truncate the /bin/java
   # ending portion of it and set javahome with the trucated path
   #
-  if [ -f "$tmpvar" ]
-  then
-    while [ -h "$tmpvar" ]
-    do
-        tmpvar2=`ls -ls "$tmpvar"`
-        tmpvar2=`expr "$tmpvar2" : '.*-> \(.*\)$'`
-        if [ `expr "$tmpvar2" : "\.\/"` -gt 0 -o `expr "$tmpvar2" : "\.\.\/"` -gt 0 -o `expr "$tmpvar2" : ".*/.*"` -le 0 ]
-        then
-          tmpvar="`dirname "$tmpvar"`/$tmpvar2"
-        else
-          tmpvar="$tmpvar2"
-        fi
-    done
-    tmpvar=`expr "$tmpvar" : '\(.*\)\/bin\/[^\/]*$'`
-    SetJavaHome "$tmpvar"
-  fi
+#  if [ -f "$tmpvar" ]
+#  then
+#    while [ -h "$tmpvar" ]
+#    do
+#        tmpvar2=`ls -ls "$tmpvar"`
+#        tmpvar2=`expr "$tmpvar2" : '.*-> \(.*\)$'`
+#        if [ `expr "$tmpvar2" : "\.\/"` -gt 0 -o `expr "$tmpvar2" : "\.\.\/"` -gt 0 -o `expr "$tmpvar2" : ".*/.*"` -le 0 ]
+#        then
+#          tmpvar="`dirname "$tmpvar"`/$tmpvar2"
+#        else
+#          tmpvar="$tmpvar2"
+#        fi
+#    done
+#    tmpvar=`expr "$tmpvar" : '\(.*\)\/bin\/[^\/]*$'`
+#    SetJavaHome "$tmpvar"
+#  fi
+  tmpvar=`expr "$tmpvar" : '\(.*\)\/bin\/[^\/]*$'`
+  SetJavaHome "$tmpvar"
 }
 
 CheckJavaHome()
