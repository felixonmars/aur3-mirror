# CPAN Name  : CPAN-Meta
# Contributor: Caleb Cushing <xenoterracide@gmail.com>
# Generator  : CPANPLUS::Dist::Arch 1.04
# Template @ http://github.com/xenoterracide/AURpan/blob/master/perl-cpan-meta/PKGBUILD.tt
# File bugs @ http://github.com/xenoterracide/AURpan/issues

pkgname='perl-cpan-meta'
pkgver='2.101670'
pkgrel='1'
pkgdesc="the distribution metadata for a CPAN dist"
arch=('any')
url='http://search.cpan.org/dist/CPAN-Meta'
license=('PerlArtistic' 'GPL')
depends=('perl' 'perl-json>=2' 'perl-version-requirements>=0.101020')


options=('!emptydirs')

source=('http://search.cpan.org/CPAN/authors/id/D/DA/DAGOLDEN/CPAN-Meta-2.101670.tar.gz')
md5sums=('fa84bb39653a6901ff1fe6a99ed20d8a')

build() {
  DIST_DIR="${srcdir}/CPAN-Meta-2.101670"
  export PERL_MM_USE_DEFAULT=1 PERL5LIB=""                 \
    PERL_AUTOINSTALL=--skipdeps                            \
    PERL_MM_OPT="INSTALLDIRS=vendor DESTDIR='$pkgdir'"     \
    PERL_MB_OPT="--installdirs vendor --destdir '$pkgdir'" \
    MODULEBUILDRC=/dev/null
  {
	cd "$DIST_DIR" &&
    perl Makefile.PL &&
    make &&
    make test &&
    make DESTDIR="$pkgdir" install;
  } || return 1;

  find "$pkgdir" -name .packlist -o -name perllocal.pod -delete

}

