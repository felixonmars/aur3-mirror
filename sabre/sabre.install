post_install() {
  echo " > This game must be started from a console (e.g. Ctrl+Alt+F1)."
  echo " > Don't forget to load the svgalib module:"
  echo "     sudo mknod -m 666 /dev/svga c 209 0"
  echo "     sudo depmod -a"
  echo "     sudo modprobe svgalib_helper"
}
