# Maintainer: spider-mario <spidermario@free.fr>
# Contributor: Marcin Karpezo <sirmacik at gmail dot com>
pkgname=nqp
pkgver=2013.05
pkgrel=1
pkgdesc="High-level environment to create compilers and libraries for virtual machines"
arch=('i686' 'x86_64')
url='https://github.com/perl6/nqp#readme'
license=(PerlArtistic)
depends=('parrot>=5.4.0')
makedepends=('perl>=5.8')
options=('!makeflags')
source=(https://github.com/perl6/$pkgname/archive/${pkgver}.tar.gz)
sha512sums=('50e0033da948e4dec329f6ca38949a1122098cfcbcae6d801742104efce02bdb64be912b65caab5feebd24b5eba6b7779fa97dab846d9480d9ee9d8312c62224')

build() {
    cd "$srcdir/$pkgname-$pkgver"

    perl Configure.pl --with-parrot=/usr/bin/parrot --prefix=/usr
    make
}
package() {
    cd "$srcdir/$pkgname-$pkgver"
    make DESTDIR="$pkgdir/" install
}

