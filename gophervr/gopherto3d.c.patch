--- gopherto3d.c.old	2013-02-02 10:07:15.107649804 +0100
+++ gopherto3d.c	2013-02-02 10:05:17.382335103 +0100
@@ -125,7 +125,7 @@
 	       rotate( rotation, 'y' );
 	  }
 	  if (SpiralLayout == TRUE)
-	       further += 0.5;
+	       further += 0.05;
 	  
 /* poor man's snprintf() */
 #define ADD_ITYPE(x) ((strlen(text) > 500) ? 0 : sprintf(supertext, "%s %s", x, text))
