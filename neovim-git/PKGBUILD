# Maintainer: Florian Hahn <flo@fhahn.com>

pkgname=neovim-git
pkgdesc="Vim's rebirth for the 21th century"
license=('custom:vim')
pkgver=0.r30.f5c7288
pkgrel=1
depends=('libuv' 'ncurses')
makedepends=('cmake' 'git')
conflicts=('')
provides=('neovim')
arch=('i686' 'x86_64')
url=('https://github.com/neovim/neovim')
source=('git+https://github.com/neovim/neovim.git')
md5sums=('SKIP')
_gitname=neovim

pkgver() {
  cd "${srcdir}/${_gitname}"
  printf "0.r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
}

build() {
  cd "${srcdir}/${_gitname}"
  mkdir build && cd build
  cmake -DCMAKE_INSTALL_PREFIX=/usr ..
  make
}

package() {
  cd "${srcdir}/${_gitname}/build"
  install -Dm755 src/vim "${pkgdir}/usr/bin/neovim"
}

# vim:set sw=2 sts=2 et:
