diff -Naur wirbelscan-0.0.6.orig//scanfilter.c wirbelscan-0.0.6/scanfilter.c
--- wirbelscan-0.0.6.orig//scanfilter.c	2011-02-05 14:00:27.000000000 +0000
+++ wirbelscan-0.0.6/scanfilter.c	2011-02-12 21:59:53.683469513 +0000
@@ -89,13 +89,13 @@
         return (true);
         }
       }
-#endif
     if (IsPvrinput(newChannel)) {
       if ((channel->Source() == newChannel->Source()) && is_nearly_same_frequency(channel, newChannel) &&
           ! strcmp(channel->Parameters(), newChannel->Parameters())) {
         return (true);
         }
       }
+#endif
     if (newChannel->IsSat()) {
       if (!is_different_transponder_deep_scan(newChannel, channel, auto_allowed)) {
         return (true);
