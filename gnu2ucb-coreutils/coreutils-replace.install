pre_install() {
echo "==> last warning! This package may destroy your system"

mkdir /tmp/bbin
for i in $(busybox --list)
 do
   ln -s /bin/busybox /tmp/bbin/$i
 done
export PATH2=$PATH
export PATH=/tmp/bbin:$PATH
}

post_install() {
rm -rf /tmp/bbin
export PATH=$PATH2
}

