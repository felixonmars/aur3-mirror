# Maintainer: Ner0
# Contributor: Damien Churchill <damoxc@gmail.com>
# Contributor: Ionut Biru <ibiru@archlinux.org>
# Contributor: Timm Preetz <timm@preetz.us>
# Contributor: Christer Nissen <christer.nissen@gmail.com>
# Maintainer: Ner0

pkgname=vala-devel
pkgver=0.17.2
pkgrel=1
pkgdesc="Compiler for the GObject type system (development release)"
arch=('i686' 'x86_64')
url="http://live.gnome.org/Vala"
license=('LGPL')
depends=('glib2')
makedepends=('libxslt')
options=('!libtool')
provides=("vala=${pkgver}")
conflicts=('vala')
source=("http://ftp.gnome.org/pub/gnome/sources/${pkgname%-*}/${pkgver%.*}/${pkgname%-*}-${pkgver}.tar.xz")
sha256sums=('03beff415c0de40ce1cd586a67ae13e11582b03dbd8966e32161e6ca1bd02226')

build() {
    cd "${srcdir}/${pkgname%-*}-${pkgver}"
    ./configure --prefix=/usr --enable-vapigen
    make
}

package() {
    cd "${srcdir}/${pkgname%-*}-${pkgver}"
    make DESTDIR="$pkgdir/" install
}
