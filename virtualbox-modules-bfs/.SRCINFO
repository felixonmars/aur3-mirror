pkgbase = virtualbox-modules-bfs
	pkgver = 4.3.20
	pkgrel = 2
	url = http://virtualbox.org
	arch = i686
	arch = x86_64
	license = GPL
	makedepends = linux-bfs>=3.18
	makedepends = linux-bfs<3.19
	makedepends = linux-bfs-headers>=3.18
	makedepends = linux-bfs-headers<3.19
	makedepends = virtualbox-host-dkms>=4.3.20
	makedepends = virtualbox-guest-dkms>=4.3.20

pkgname = virtualbox-host-modules-bfs
	pkgdesc = Host kernel modules for VirtualBox
	install = host.install
	depends = linux-bfs>=3.18
	depends = linux-bfs<3.19
	conflicts = virtualbox-modules-bfs
	replaces = virtualbox-modules-bfs

pkgname = virtualbox-guest-modules-bfs
	pkgdesc = Guest kernel modules for VirtualBox
	install = guest.install
	license = GPL
	depends = linux-bfs>=3.18
	depends = linux-bfs<3.19
	conflicts = virtualbox-modules-bfs
	replaces = virtualbox-modules-bfs

