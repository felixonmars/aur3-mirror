# Maintainer: Christian Hesse <mail@eworm.de>

pkgname=netlink-notify
pkgver=0.6.5
pkgrel=1
pkgdesc="Notify about netlink changes"
arch=('i686' 'x86_64')
url="https://github.com/eworm-de/netlink-notify"
depends=('libnotify')
makedepends=('imagemagick' 'markdown')
license=('GPL')
install=netlink-notify.install
source=("http://www.eworm.de/download/${pkgname}/${pkgname}-${pkgver}.tar.xz"{,.asc})

build() {
	cd ${pkgname}-${pkgver}/

	make
}

package() {
	cd ${pkgname}-${pkgver}/

	make DESTDIR="${pkgdir}" install
}

sha256sums=('f2bcfc3d9fd7f6b688268f008924aa394afb6488552c113195b6352d938a459d'
            'SKIP')
