post_install() {
    if [ -e /home/$USER/.config/camsensor ]
        then mv /home/$USER/.config/camsensor /home/$USER/.config/calise
    fi
    xdg-icon-resource forceupdate
}

post_upgrade() {
    xdg-icon-resource forceupdate
}

post_remove() {
    xdg-icon-resource forceupdate
}