post_install() {
    echo
    echo "Create file that contains word "locked" in /tmp/.apple_sensor.txt"
    echo "to temporarily disable automatic changing of backlight."
    echo "Enable at startup time:"
    echo
    echo -e "\t# systemctl enable lighter.service"
    echo
}
