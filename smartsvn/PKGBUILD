#Maintainer: Rene Peinthor <peinthor@gmail.com>
pkgname=smartsvn
pkgver=6.6.9
_pkgver=6_6_9
pkgrel=1
pkgdesc="A Subversion(SVN) GUI client written in Java"
arch=("any")
url="http://www.syntevo.com/smartsvn/index.html"
license=('custom')
depends=("jre" "desktop-file-utils" "sh")
makedepends=("python")
provides=(smartsvn=$pkgver)
source=(smartsvn.desktop downloader.py)
install="smartsvn.install"
md5sums=('caf0387a86dccae88f7e4b46cf33f36b' 'e3aa1ed1c09226428e652e2501d4bfb5')
smartsvnmd5sum='5ba1ad083dbdf1aba3d85a03fadff5b3'


extracted="$pkgname-$_pkgver"

build() {
	cd "$srcdir"
	python3 downloader.py smartsvn-generic-${_pkgver}.tar.gz

	echo "${smartsvnmd5sum} *smartsvn-generic-${_pkgver}.tar.gz" | md5sum -c || (error "md5sum doesn't match" && return 1)

	tar xzf smartsvn-generic-${_pkgver}.tar.gz
}

package() {
	cd "$srcdir"

	install -D -m644 "${extracted}"/license.html "${pkgdir}/usr/share/licenses/${pkgname}/license.html"
	mkdir -p "${pkgdir}"/opt
	mv "${extracted}" ${pkgdir}/opt/$pkgname || return 1

	install -D -m644 smartsvn.desktop "${pkgdir}"/usr/share/applications/smartsvn.desktop
}
