--- mkv-extractor-gui.sh	2011-11-03 17:22:47.000000000 +0100
+++ mkv-extractor-gui2.sh	2011-11-03 22:51:49.000000000 +0100
@@ -999,7 +999,7 @@
 # Creation des services menus
 if [[ ${menu_service} == True ]]
 then
-	if [[ -e "${HOME}/.kde/share/kde4/services/" ]]
+	if [[ -e "${HOME}/.kde4/share/kde4/services/" ]]
 	then
 		echo '[Desktop Entry]
 Type=Service
@@ -1007,14 +1007,14 @@
 MimeType=video/x-matroska
 Actions=ShowCover
 Encoding=UTF-8
-Icon=/usr/share/mkv-extractor-gui/mkv-extractor-gui.png
+Icon=/opt/mkv-extractor-gui/mkv-extractor-gui.png
 
 [Desktop Action ShowCover]
-Icon=/usr/share/mkv-extractor-gui/mkv-extractor-gui.png
+Icon=/opt/mkv-extractor-gui/mkv-extractor-gui.png
 Name=MKV Extractor Gui
-Exec=mkv-extractor-gui "%U" ' > "${HOME}/.kde/share/kde4/services/mkv_extractor_gui.desktop"
+Exec=mkv-extractor-gui "%U" ' > "${HOME}/.kde4/share/kde4/services/mkv_extractor_gui.desktop"
 
-		chmod +x "${HOME}/.kde/share/kde4/services/mkv_extractor_gui.desktop"
+		chmod +x "${HOME}/.kde4/share/kde4/services/mkv_extractor_gui.desktop"
 	fi
 
 	if [ -e "${HOME}/.gnome2/nautilus-scripts/" ]
@@ -1029,7 +1029,7 @@
 # Suppression des services menus
 elif [[ ${menu_service} == False ]]
 then
-	[[ -e "${HOME}/.kde/share/kde4/services/mkv_extractor_gui.desktop" ]] && rm "${HOME}/.kde/share/kde4/services/mkv_extractor_gui.desktop"
+	[[ -e "${HOME}/.kde4/share/kde4/services/mkv_extractor_gui.desktop" ]] && rm "${HOME}/.kde4/share/kde4/services/mkv_extractor_gui.desktop"
 
 	[[ -e "${HOME}/.gnome2/nautilus-scripts/mkv_extractor_gui" ]] && rm "${HOME}/.gnome2/nautilus-scripts/mkv_extractor_gui"
 fi
