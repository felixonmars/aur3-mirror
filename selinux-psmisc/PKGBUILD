# Maintainer: Nicky726 (Nicky726 <at> gmail <dot> com)
# Contributor: Eric Belanger (eric <at> archlinux <dot> org)

pkgname=selinux-psmisc
_origname=psmisc
pkgver=22.13
pkgrel=1
pkgdesc="SELinux aware miscellaneous procfs tools"
arch=('i686' 'x86_64')
url="http://psmisc.sourceforge.net/index.html"
license=('GPL')
groups=('selinux' 'selinux-system-utilities')
depends=('glibc' 'ncurses' 'selinux-usr-libselinux')
conflicts=('psmisc')
provides=('psmisc=22.13')
source=(http://downloads.sourceforge.net/psmisc/${_origname}-${pkgver}.tar.gz)
md5sums=('e2c339e6b65b730042084023784a729e')

build() {
  cd "${srcdir}/${_origname}-${pkgver}"
  ./configure --prefix=/usr --enable-selinux
  make
}

package() {
  cd "${srcdir}/${_origname}-${pkgver}"
  make DESTDIR="${pkgdir}" install
}
