post_install() {
  echo "--------------------------------------------"
  echo "==> Arch packages do not currently exist for"
  echo "    those marked with an asterisk (*)"
  echo "--------------------------------------------"
}

post_upgrade() {
  post_install $1
}
