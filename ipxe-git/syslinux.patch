diff --git a/src/arch/i386/Makefile b/src/arch/i386/Makefile
index 8314f26..efaf540 100644
--- a/src/arch/i386/Makefile
+++ b/src/arch/i386/Makefile
@@ -73,6 +73,7 @@ CFLAGS			+= -Ui386
 #
 ISOLINUX_BIN_LIST	:= \
 	$(ISOLINUX_BIN) \
+	/usr/lib/syslinux/bios/isolinux.bin \
 	/usr/lib/syslinux/isolinux.bin \
 	/usr/share/syslinux/isolinux.bin \
 	/usr/local/share/syslinux/isolinux.bin
diff --git a/src/util/geniso b/src/util/geniso
index 48ea2f2..243d02f 100755
--- a/src/util/geniso
+++ b/src/util/geniso
@@ -38,6 +38,9 @@ shift
 dir=`mktemp -d bin/iso.dir.XXXXXX`
 cfg=$dir/isolinux.cfg
 cp -p $isolinux_bin $dir
+if [ -s $(dirname ${isolinux_bin})/ldlinux.c32 ]; then
+	cp -v $(dirname ${isolinux_bin})/ldlinux.c32 ${dir}
+fi
 cat > $cfg <<EOF
 # These default options can be changed in the geniso script
 SAY iPXE ISO boot image
