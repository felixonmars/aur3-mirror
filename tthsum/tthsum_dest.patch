--- Makefile.orig	2011-11-09 14:00:33.000000000 +0400
+++ Makefile	2013-05-04 23:25:27.403086506 +0400
@@ -5,10 +5,10 @@
 .PHONY: all clean dists install manual tthsum uninstall test runtest
 
 ifeq ($(BIN),)
-  BIN = $(DESTDIR)/usr/local/bin
+  BIN = $(DESTDIR)/usr/bin
 endif
 ifeq ($(MAN),)
-  MAN = $(DESTDIR)/usr/local/man
+  MAN = $(DESTDIR)/usr/man
 endif
 
 ifeq ($(DEBUG),)
