--- Makefile.orig	2009-05-29 02:05:55.000000000 +0400
+++ Makefile	2009-08-20 21:46:02.000000000 +0400
@@ -8,10 +8,10 @@
 #  ARCH = $(shell uname -m)
 #endif
 ifeq ($(BIN),)
-  BIN = $(DESTDIR)/usr/local/bin
+  BIN = $(DESTDIR)/usr/bin
 endif
 ifeq ($(MAN),)
-  MAN = $(DESTDIR)/usr/local/man
+  MAN = $(DESTDIR)/usr/man
 endif
 
 ifeq ($(DEBUG),)
