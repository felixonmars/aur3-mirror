# Contributor: orumin <dev@orum.in>

pkgname=ttf-koruri
pkgver=20140904
pkgrel=1
pkgdesc="Japanese TrueType font obtained by mixing M+ and Open Sans"
arch=('any')
url="http://sourceforge.jp/projects/koruri"
license=('APACHE')
depends=('fontconfig' 'xorg-font-utils')
install=ttf.install
_mirror="jaist"
source=(http://${_mirror}.dl.sourceforge.jp/koruri/61919/Koruri-${pkgver}.tar.xz)
md5sums=('5c35bf856ebaccb59ba8333350b83eb4')

package() {
    cd "${srcdir}"

    install -d "${pkgdir}/usr/share/fonts/TTF"
    install -m644 "Koruri-${pkgver}"/*.ttf "${pkgdir}/usr/share/fonts/TTF"
}
