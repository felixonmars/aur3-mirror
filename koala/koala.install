post_install() {
  echo "To compile sass files, you need to install ruby-sass, goto setting->sass->Advanced Settings and check the 'Use the System Sass compiler' option."
  echo "And you can try koala-deb building from binary source."
}