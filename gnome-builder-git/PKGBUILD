# Maintainer: Tim Jester-Pfadt <t.jp<at>gmx.de>

pkgname=gnome-builder-git
pkgver=93.0519615
pkgrel=1
pkgdesc="Builder, a new IDE for Gnome"
arch=('i686' 'x86_64')
url="https://github.com/chergert/gnome-builder"
license=(GPL)
depends=(gtksourceview3 gsettings-desktop-schemas dconf devhelp glade gtk-doc clang gjs)
install=gnome-builder.install
source=('git+git://git.gnome.org/gnome-builder')
sha256sums=('SKIP')

build() {
  cd "$srcdir"/gnome-builder
  ./autogen.sh --prefix=/usr --disable-schemas-compile
  make
}

package() {
  cd "$srcdir"/gnome-builder
  make DESTDIR="$pkgdir" install
}

pkgver() {
  cd "$srcdir"/gnome-builder
  echo $(git rev-list --count master).$(git rev-parse --short master)
}
