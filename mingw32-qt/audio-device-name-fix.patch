diff -Naurb ./qt-everywhere-opensource-src-4.8.0/src/multimedia/audio/qaudiodeviceinfo_win32_p.cpp ./qt-everywhere-opensource-src-4.8.0-patched/src/multimedia/audio/qaudiodeviceinfo_win32_p.cpp
--- ./qt-everywhere-opensource-src-4.8.0/src/multimedia/audio/qaudiodeviceinfo_win32_p.cpp	2011-12-08 09:06:03.000000000 +0400
+++ ./qt-everywhere-opensource-src-4.8.0-patched/src/multimedia/audio/qaudiodeviceinfo_win32_p.cpp	2012-02-29 12:36:53.462758638 +0400
@@ -76,7 +76,7 @@
 
 QAudioDeviceInfoInternal::QAudioDeviceInfoInternal(QByteArray dev, QAudio::Mode mode)
 {
-    device = QLatin1String(dev);
+	device = QString::fromUtf8(dev);
     this->mode = mode;
 
     updateLists();
@@ -413,7 +413,7 @@
 	for(i=0;i<iNumDevs;i++) {
 	    if(waveOutGetDevCaps(i, &woc, sizeof(WAVEOUTCAPS))
 	        == MMSYSERR_NOERROR) {
-	        devices.append(QString((const QChar *)woc.szPname).toLocal8Bit().constData());
+			devices.append(QString((const QChar *)woc.szPname).toUtf8().constData());
 	    }
 	}
     } else {
@@ -423,7 +423,7 @@
 	for(i=0;i<iNumDevs;i++) {
 	    if(waveInGetDevCaps(i, &woc, sizeof(WAVEINCAPS))
 	        == MMSYSERR_NOERROR) {
-	        devices.append(QString((const QChar *)woc.szPname).toLocal8Bit().constData());
+			devices.append(QString((const QChar *)woc.szPname).toUtf8().constData());
 	    }
 	}
