--- thinkfan-0.8_alpha2/system.c.orig	2012-04-10 16:35:42.444274536 -0700
+++ thinkfan-0.8_alpha2/system.c	2012-04-10 16:35:55.847607461 -0700
@@ -224,7 +224,7 @@
 			return i;
 		}
 		tmp = strtol(input, &end, 0);
-		if (*end != 0 || tmp < INT_MIN || tmp > INT_MAX) {
+		if ((*end != 0 && *end != 10) || tmp < INT_MIN || tmp > INT_MAX) {
 			report(LOG_ERR, LOG_ERR, MSG_ERR_T_GET);
 			errcnt |= ERR_T_GET;
 			return i;