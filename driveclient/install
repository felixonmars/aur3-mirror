post_install() {

	echo "You will need to run /usr/local/bin/driveclient --configure as root to configure the agent"
	echo ""
	echo "To start the daemon:"
	echo ""
	echo "With systemd: systemctl start driveclient"
	echo "without     : rc.d start driveclient"
}
