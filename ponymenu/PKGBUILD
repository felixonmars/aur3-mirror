# Maintainer: Mattias Andrée <maandree@operamail.com>
pkgname=ponymenu
pkgver=0.9.1
pkgrel=1
pkgdesc="Quick terminal based application menu"
arch=('any')
url="https://github.com/maandree/ponymenu"
license=('GPL3')
depends=('python>=3')
provides=('ponymenu')
conflicts=('ponymenu')
source=(https://github.com/maandree/ponymenu/tarball/0.9.1)
md5sums=(e4580c351e6218b5e0304e73a9302b8b)

build() {
  cd maandree-ponymenu-*
  make -B DESTDIR="$pkgdir/"
}

package() {
  cd maandree-ponymenu-*
  make DESTDIR="$pkgdir/" install
}
