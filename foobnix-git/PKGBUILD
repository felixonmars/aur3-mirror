# Maintainer: archtux <antonio dot arias99999 at gmail dot com>

pkgname=foobnix-git
pkgver=2345.236be1f
pkgrel=1
pkgdesc="Music Player written in Python2, GTK3 and Glade."
arch=('any')
url="http://www.foobnix.com"
license=('GPL3')
depends=('gstreamer0.10-ffmpeg' 'gstreamer0.10-good-plugins' 'gstreamer0.10-python' 'gstreamer0.10-ugly-plugins' 'libkeybinder3' 'libnotify' 'mutagen' 'python2-chardet' 'python2-dbus' 'python2-simplejson' 'webkitgtk')
makedepends=('git')
conflicts=('foobnix')
source=('git+http://github.com/foobnix/foobnix.git')
md5sums=('SKIP')

package() {
  cd $srcdir/foobnix
  python2 setup.py install --root=$pkgdir --optimize=1
}

pkgver() {
  cd $srcdir/foobnix
  echo $(git rev-list --count master).$(git rev-parse --short master)
}