# Maintainer: Zhengyu Xu <xzy3186@gmail.com>

pkgname=insync-thunar
pkgver=1.1.4
pkgrel=1
pkgdesc="Thunar integration for insync"
url="https://support.insynchq.com/forums/discussion/3479/new-insync-version-1-1-4"
license=('custom:insync')
options=(!strip)
arch=('any')

depends=("insync" "thunarx-python")
source=("${pkgname}-${pkgver}.deb::http://s.insynchq.com/builds/${pkgname}_${pkgver}.32046-precise_all.deb")
sha256sums=("080517379d700de7f8571a241ed499bebe75f17985d4e042b3baf386e1ef87a5")
noextract=("${pkgname}-${pkgver}.deb")

package() {
   cd $srcdir
   ar x ${pkgname}-${pkgver}.deb
   tar xvf data.tar.gz
   cp -rp usr $pkgdir
}
