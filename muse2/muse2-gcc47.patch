diff -aur muse-2.0rc2/muse/liste/editevent.cpp muse-2.0rc2.patched/muse/liste/editevent.cpp
--- muse-2.0rc2/muse/liste/editevent.cpp	2012-01-06 20:45:38.000000000 +0100
+++ muse-2.0rc2.patched/muse/liste/editevent.cpp	2012-04-13 03:06:22.139176092 +0200
@@ -642,12 +642,12 @@
                         continue;
                   }
             MusECore::MidiController* c = port->midiController(num);
-            isList i = sList.begin();
-            for (; i != sList.end(); ++i) {
-                  if (*i == c->name())
+            isList j = sList.begin();
+            for (; j != sList.end(); ++j) {
+                  if (*j == c->name())
                         break;
                   }
-            if (i == sList.end())
+            if (j == sList.end())
                   sList.push_back(c->name());
             }
       MusECore::MidiController* mc = port->midiController(num);
