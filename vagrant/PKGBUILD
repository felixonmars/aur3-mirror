# Contributor: Mathieu Clabaut <mathieu.clabaut@gmail.com>
# Last version visible on http://rubygems.org/gems/vagrant
pkgname=vagrant
pkgver=0.8.5
pkgrel=1
pkgdesc="tool for building and distributing virtualized development environments"
arch=(any)
url="http://vagrantup.com/"
license=("MIT")
depends=('ruby' 'virtualbox>=4.1')
source=(http://rubygems.org/downloads/vagrant-${pkgver}.gem)
noextract=(vagrant-$pkgver.gem)
md5sums=('10c2a04995c137d2237098ffd66c8495')

build() {
  cd $srcdir
  local _gemdir="$(ruby -rubygems -e'puts Gem.default_dir')"
  gem install --ignore-dependencies -i "$pkgdir$_gemdir" ${pkgname}-$pkgver.gem
}

