warning() {
	echo ""
	echo "default themes were installed into $HOME/.themes directory, if you want to use the themes on another account then you have to manually copy the themes to the home directory of that account"
	echo ""
}

post_install() {
	warning
}

post_upgrade() {
	warning
}
