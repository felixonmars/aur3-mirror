# Maintainer: Alex Forencich <alex@alexforencich.com>
# Contributor : Alex Forencich <alex@alexforencich.com>
pkgname=python-ivi-git
pkgver=20121026
pkgrel=1
pkgdesc="Python Interchangeable Virtual Instrument Library"
arch=('any')
url="https://github.com/alexforencich/python-ivi"
license=('LGPL')
depends=('python' 'python-numpy')
makedepends=('git')
provides=('python-ivi')
optdepends=('python-vxi11: VXI11 instrument support')

_gitroot='https://github.com/alexforencich/python-ivi.git'
_gitname='python-ivi'

build() {
  cd "$srcdir"
  msg "Connecting to github.com"

  if [ -d "$_gitname" ] ;then
    cd "$_gitname" && git pull origin
    msg "The local files are updated."
  else
    git clone "$_gitroot"
  fi
  msg "GIT checkout done or server timeout"

  cd "$srcdir/$_gitname"

  git checkout master
}

package() {
  cd "$srcdir/$_gitname"
  python setup.py install --prefix=/usr --root="$pkgdir/" --optimize=1
}

