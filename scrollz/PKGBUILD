# Contributor: Nathan Owe <ndowens04 at gmail>
pkgname=scrollz
pkgver=2.0
pkgrel=1
pkgdesc="an advanced IRC client based on ircII"
arch=('i686')
url="http://www.scrollz.com"
license=('custom')
depends=('ncurses')
source=(http://scrollz.lysergide.net/ScrollZ/source/ScrollZ-$pkgver.tar.bz2)
md5sums=('c9fd7581ff7cabbdfcf721b67e146c39') 

build() {
  cd $srcdir/ScrollZ-$pkgver
 mkdir -p $pkgdir/usr/share/$pkgname
  ./configure --prefix=$pkgdir/usr
  make || return 1
  make datadir=$pkgdir/usr/share mandir=$pkgdir/usr/share/man install
install -Dm644 COPYRIGHT $pkgdir/usr/share/licenses/$pkgname/COPYRIGHT
}

# vim:set ts=2 sw=2 et:
