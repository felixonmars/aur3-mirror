#!/bin/sh

post_install() {
  echo ">>>"
  echo ">>> VPCS executable vpcs and config file startup.vpc can be found in /opt/vpcs."
  echo ">>> Run VPCS from that directory, unless you want history logs everywhere."
  echo ">>> Also, that way VPCS will autoload the config file."
  echo ">>> For more info, check /opt/vpcs/readme.txt."
  echo ">>>"
}
