# vim:set ts=2 sw=2 ft=sh et:
post_install () {
	echo "==> Default behavior binds to Alt+F2 and allows the"
    echo "==> window manager to decorate the window. Call the"
    echo "==> binary like 'tudor-do -u -h 'Alt+F1'' to change"
    echo "==> the default behavior."
}

post_upgrade () {
	post_install
}
