diff --git a/ykcore/ykcore_libusb-1.0.c b/ykcore/ykcore_libusb-1.0.c
index a3541c1..0bccdc1 100644
--- a/ykcore/ykcore_libusb-1.0.c
+++ b/ykcore/ykcore_libusb-1.0.c
@@ -83,6 +83,7 @@ int _ykusb_write(void *dev, int report_type, int report_number,
 		if (ykl_errno > 0 && rc2 < 0)
 			ykl_errno = rc2;
 	}
+	usleep(10);
 	if (ykl_errno > 0)
 		return 1;
 	yk_errno = YK_EUSBERR;
