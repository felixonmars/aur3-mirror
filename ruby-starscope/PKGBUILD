# Maintainer: Andy Weidenbaum <archbaum@gmail.com>

pkgname=ruby-starscope
pkgver=1.2.0
pkgrel=1
pkgdesc="A tool like the venerable cscope, but for ruby, golang and other languages"
arch=('any')
url="https://github.com/eapache/starscope"
license=('MIT')
depends=('ruby' 'ruby-oj' 'ruby-parser' 'ruby-ruby-progressbar')
optdepends=('go')
source=(https://rubygems.org/downloads/${pkgname#*-}-${pkgver}.gem)
sha256sums=('e4e177877e4a6568db9ce1cafdca299cc231f2860aa3bf692366559afaa411da')
noextract=("${pkgname#*-}-${pkgver}.gem")
provides=('ruby-starscope' 'starscope')
conflicts=('starscope')

package() {
  cd "$srcdir"

  msg 'Installing...'
  gem install \
    --no-user-install \
    --ignore-dependencies \
    -i "$pkgdir$(ruby -rubygems -e'puts Gem.default_dir')" \
    ${pkgname#*-}-$pkgver.gem
}
