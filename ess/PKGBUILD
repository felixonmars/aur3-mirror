# Maintainer: Stefan Husmann <Stefan-Husmann@t-online.de>
pkgname=ess
pkgver=5.13
pkgrel=1
pkgdesc="Emacs Speaks Statistics: A Universal Interface for Statistical Analysis"
url="http://ess.r-project.org/"
arch=('any')
license=('GPL')
depends=('emacs' 'r')
install=ess.install
source=("http://ess.r-project.org/downloads/$pkgname/$pkgname-$pkgver.tgz")
md5sums=('f97e66a0802773ead5abe4c3dc89feca')

build() {
  cd $srcdir/$pkgname-$pkgver 
  make prefix=/usr
}
package() {
  cd $srcdir/$pkgname-$pkgver
  make DESTDIR=$pkgdir/usr LISPDIR=$pkgdir/usr/share/emacs/site-lisp/ess \
    INFODIR=$pkgdir/usr/share/info/ install 
}
