# Optional settings
# -----------------

# Arguments to send to CouchPotatoServer
CP_ARGS="--quiet --daemon --data_dir /opt/couchpotato/data --config_file /opt/couchpotato/data/settings.conf"

# Run "couchpotato-v2 --help" for more options


# The following below MUST be set
# -------------------------------

# User to run process as
CP_USER="couchpotato"
