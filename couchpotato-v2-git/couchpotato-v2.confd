# Optional settings
# -----------------

# Arguments to send to CouchPotatoServer
CP_ARGS="--quiet --daemon --data_dir /opt/couchpotato/.couchpotato --config_file /opt/couchpotato/.couchpotato/settings.conf"

# Run "couchpotatoserver --help" for more options


# The following below MUST be set
# -------------------------------

# User to run process as
CP_USER="couchpotato"
