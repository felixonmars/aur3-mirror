# Maintainer: attila589 <attila589 at gmail dot com>

pkgname=neonlights
_pkgname=NeonLights
pkgver=1.00
pkgrel=1
pkgdesc="A one-handed autofire arena shooter inspired by Robert Fearon's SYNSO2: Squid Harder"
arch=(i686 x86_64)
url="http://www.niallmoody.com/neonlights.htm"
license=('GPL')
makedepends=('gcc')
depends=('sdl' 'sdl_image' 'sdl_ttf' 'portaudio' 'libvorbis' 'glew')
source=(http://downloads.niallmoody.com.s3.amazonaws.com/${_pkgname}-${pkgver}-src.tar.gz)
md5sums=('208fbedce00ad687368f81dafd6a4da0')
sha256sums=('c76abd36fdb6a5058307e0754c6aac7913ce2984777cc29751d407263e716290')

build() {
  cd "${srcdir}/${_pkgname}-${pkgver}"
  ./configure --prefix=/usr
  make
}

package() {
  cd "${srcdir}/${_pkgname}-${pkgver}"
  make DESTDIR="${pkgdir}" install
}
