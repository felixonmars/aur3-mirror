# Maintainer: Michael Schubert <mschu.dev at gmail>
pkgname=thrust
pkgver=1.6.0
pkgrel=1
pkgdesc="A CUDA library of parallel algorithms similar to the C++ STL"
arch=('any')
url="http://code.google.com/p/thrust/"
license=('Apache 2.0')
depends=('cuda3')
source=("http://thrust.googlecode.com/files/$pkgname-$pkgver.zip")
md5sums=('56721c5341c93aa8f218bcaf93be6767')

package() {
  mkdir -p "$pkgdir/usr/include"
  cp -R "$srcdir/thrust" "$pkgdir/usr/include/"

  mkdir -p "$pkgdir/etc/profile.d"
  echo "export THRUST_PATH=/usr/include" > "$pkgdir/etc/profile.d/thrust.sh"
  chmod a+x "$pkgdir/etc/profile.d/thrust.sh"
}
