
pkgname=pyparticles
pkgver=0.2.0
pkgrel=1
pkgdesc="Particles simulation toolbox for python, with some force model and integrations methods"
arch=(any)
license=('GPL3')
url="http://pyparticles.wordpress.com/"
depends=('python2'  'python2-opengl' 'python2-scipy' 'python2-numpy' 'python2-matplotlib' )


source=("https://github.com/downloads/simon-r/PyParticles//${pkgname}-${pkgver}.tar.gz")
sha1sums=('e776de0a3e802c60e4722c18021fa71f2da5e185')

build() {
  cd "$srcdir/${pkgname}-$pkgver"
  python2 setup.py build
}

package() {
  cd "$srcdir/${pkgname}-$pkgver"
  echo ${pkgdir}
  python2 setup.py install --root=${pkgdir} --prefix=usr --optimize=1
}
