# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: Tom Willemsen <tom.willemsen at archlinux dot us>
pkgname=toudou
pkgver=0.1.1
pkgrel=2
pkgdesc="Just another todo application, yay"
arch=('i686' 'x86_64')
url="http://gitorious.org/toudou/toudou"
license=('GPL3')
depends=('libxml2')
makedepends=('cmake' 'make')
source=(http://lxcoding.alwaysdata.net/files/$pkgname-$pkgver.tar.gz)
md5sums=('e67382f1afca24b4558b17abc3980450')

build() {
  cd "$srcdir/$pkgname-$pkgname"

  cmake -DCMAKE_INSTALL_PREFIX=/usr .
  make
}

package() {
  cd "$srcdir/$pkgname-$pkgname"

  make DESTDIR="$pkgdir/" install
}

# vim:set ts=2 sw=2 et:
