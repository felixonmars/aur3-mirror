#!/bin/bash

build() {
    MODULES="loop"

    add_dir "/tmp"

    add_runscript
}

help () {
cat<<HELPEOF
  This hook mounting of your device from the loader.
HELPEOF
}

# vim: set ft=sh ts=4 sw=4 et:
