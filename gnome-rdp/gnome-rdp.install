# arg 1:  the new package version
post_install() {
  echo "gnome-rdp: Don't forget to delete ~/.gnome-rdp.db if you've used gnome-rdp-0.1.3"
  echo "gnome-rdp: or earlier versions, because the database structure has changed."
}
post_upgrade() {
  post_install "$1"
}
