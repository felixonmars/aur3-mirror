# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: Your Name <youremail@domain.com>
pkgname=xf86-video-qxl
pkgver=0.0.12
pkgrel=2
pkgdesc="Xorg X11 qxl video driver"
arch=(x86_64 i686)
url="http://www.x.org"
license=('MIT')
groups=(xorg-video-drivers)
depends=(xorg-server)
makedepends=(pkgconfig)
install=
changelog=
source=(http://xorg.freedesktop.org/releases/individual/driver/$pkgname-$pkgver.tar.bz2 qxl-remove-assert.patch)

build() {
  cd "$srcdir/$pkgname-$pkgver"

  patch -p1 -i $srcdir/qxl-remove-assert.patch

  ./configure --prefix=/usr --disable-static
  make
}

package() {
  cd "$srcdir/$pkgname-$pkgver"

  make DESTDIR="$pkgdir/" INSTALL='install -p' install
}

# vim:set ts=2 sw=2 et:
md5sums=('542af8575b2870118786846c7d9d48c8')
md5sums=('542af8575b2870118786846c7d9d48c8'
         'c50b3265ef622f63027a24444926e8ab')
