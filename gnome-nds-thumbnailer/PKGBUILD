# Contributor: Marcos Paulino Roriz Junior <marcosrorizinf@gmail.com>

pkgname=gnome-nds-thumbnailer
pkgver=1.2.1
pkgrel=5
pkgdesc="A thumbnailer for GNOME that will make thumbnails for Nintendo DS roms by extracting their preview icon."
arch=('i686' 'x86_64')
url=http://www.hadess.net
depends=("gdk-pixbuf" "glib2" "gconf")
optdepends=('nautilus: required for better use of the schema')
license=("GPL2")
source=(http://ftp.gnome.org/pub/gnome/sources/$pkgname/1.2/$pkgname-$pkgver.tar.gz)
md5sums=("f88ce7c5fd99b275b4472a8ff17acd48")

build() {
	cd $srcdir/${pkgname}-${pkgver}
	./configure --prefix=/usr
	make || return 1
	make DESTDIR=$pkgdir install
}
