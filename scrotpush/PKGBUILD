# Maintainer: Hugo Osvaldo Barrera <hugo@barrera.io>

pkgname=scrotpush
pkgver=1.1
pkgrel=1
pkgdesc="Push screenshot to imgur and copy it's url"
arch=('any')
url="https://github.com/hobarrera/scrotpush"
license=('BSD')
depends=('python2-requests' 'python2-xdg' 'libnotify' 'xclip')
source=("https://github.com/hobarrera/${pkgname}/archive/v${pkgver}.zip")
md5sums=('5f739c110e8d66483b83c4c7636db320')

package() {
  cd "$srcdir/$pkgname-$pkgver"

  make DESTDIR="${pkgdir}" install
}
