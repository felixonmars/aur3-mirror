post_install(){
   echo "You may want to link your libssl.so and libcrypto.so to whatever pacman/your binaries use.
   Try something like  ln -s /usr/lib64/libssl.so /usr/lib64/libssl.so.1.0.0 and 
   ln -s /usr/lib64/libcrypto.so /usr/lib64/libcrypto.so.1.0.0"	
}
