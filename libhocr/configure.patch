--- src/libhocr-0.10.17/configure	2008-08-16 22:38:44.000000000 +0300
+++ configure	2011-01-04 18:27:58.736917784 +0200
@@ -20683,7 +20683,7 @@
 
 
 if test "$have_python" = "yes"; then
-    PY_PREFIX=`python -c 'import sys ; print sys.prefix'`
+    PY_PREFIX=`$PYTHON -c 'import sys; print sys.prefix'`
     { echo "$as_me:$LINENO: checking for $PY_PREFIX/include/python${PYTHON_VERSION}/Python.h" >&5
 echo $ECHO_N "checking for $PY_PREFIX/include/python${PYTHON_VERSION}/Python.h... $ECHO_C" >&6; }
     if test -f $PY_PREFIX/include/python${PYTHON_VERSION}/Python.h; then
