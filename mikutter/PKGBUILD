# Maintainer: 2GMon <t_2gmon@yahoo.co.jp>
pkgname=mikutter
pkgver=0.0.2.12
pkgrel=1
pkgdesc="a moest twitter client"
arch=('i686' 'x86_64')
url="http://mikutter.hachune.net/"
license=('GPL v3')
depends=('ruby-gtk2' 'ruby-hmac')
provides=('mikutter')
install=
source=(http://mikutter.hachune.net/bin/$pkgname.$pkgver.tar.gz)
md5sums=('4cefa110cbc92bb42a3001be0031e688')

build() {
  cd "$srcdir/$pkgname"
  patch -p0 < "$startdir/1.9.2.patch"
}

package() {
  mkdir "$pkgdir/opt"
  cp -r "$srcdir/$pkgname" "$pkgdir/opt"
  
  mkdir -p "$pkgdir/usr/bin"
  cp "$startdir/mikutter" "$pkgdir/usr/bin/mikutter"
  chmod a+x "$pkgdir/usr/bin/mikutter"
}

# vim:set ts=2 sw=2 et:
