# Contributor: zertyz <zertyz@gmail.com>

# This package was made with dependencies from the homonimous package group
# in order to correctly solve package dependencies for automated installations.

pkgname=libreoffice-group

# To keep the package dependencies updated, issue the following command:
# 	echo `pacman -Qg $pkgname | sed 's/.* //'`

# Get the correct version and URL with:
#	pacman -Qi `pacman -Qg $pkgname | sed 's/.* //' | head -n 1`

pkgver=3.4.5
pkgrel=1
pkgdesc="Package created from the homonimous official package group in order to allow 'libreoffice' to be listed as dependencies for other packages"
arch=('i686' 'x86_64')
url="http://www.libreoffice.org/"
license="LGPL3"
depends=(libreoffice-base libreoffice-calc libreoffice-common libreoffice-draw libreoffice-gnome libreoffice-impress libreoffice-kde4 libreoffice-math libreoffice-sdk libreoffice-sdk-doc libreoffice-writer)
makedepends=()
conflicts=()
replaces=()
backup=()
install=
source=()
md5sums=()

build() {
  true
}
