
# Put the desired install prefix HERE
_iprefix=/opt/compiz-09

pkgname=compiz-plugins-extra-09
pkgver=0.9.2.1
pkgrel=1
pkgdesc="Extra plugins for compiz 0.9"
url="http://www.compiz.org/"
license=('GPL' 'LGPL' 'MIT')
arch=('i686' 'x86_64')
depends=('compiz-core-09' 'compiz-plugins-main-09')
makedepends=('intltool' 'cmake')
options=(!libtool !emptydirs)
source=("http://releases.compiz.org/$pkgver/compiz-plugins-extra-${pkgver}.tar.gz")
sha1sums=('6c7dd37e8ecc288f037480972c36f6e611b93068')

# This is a developement version
OPTIONS=(!strip)

build()
{
	cd "compiz-plugins-extra-${pkgver}"
	[[ -d build ]] || mkdir build
	cd build
	env PKG_CONFIG_PATH="${_iprefix}/lib/pkgconfig:$PKG_CONFIG_PATH" \
		cmake .. -DCOMPIZ_PLUGIN_INSTALL_TYPE="compiz" -DCMAKE_INSTALL_PREFIX="${_iprefix}" -DCMAKE_BUILD_TYPE=Debug
	make
	make DESTDIR="${pkgdir}" install
}
