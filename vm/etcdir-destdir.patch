--- Makefile.in.old	2011-05-21 14:39:01.337081675 +0200
+++ Makefile.in	2011-05-21 14:39:38.613546398 +0200
@@ -50,11 +50,11 @@
 
 install-pkg:
 	$(MKDIR) -p "$(DESTDIR)$(docdir)"
-	$(MKDIR) -p "$(etcdir)"
+	$(MKDIR) -p "$(DESTDIR)$(etcdir)"
 	for i in $(SOURCES) ; do                                  \
-	    echo "Installing $$i in '$(DESTDIR)$(docdir)' and '$(etcdir)'" ;  \
+	    echo "Installing $$i in '$(DESTDIR)$(docdir)' and '$(DESTDIR)$(etcdir)'" ;  \
 	    $(INSTALL_DATA) $$i "$(DESTDIR)$(docdir)" ;           \
-	    $(INSTALL_DATA) $$i "$(etcdir)" ;                     \
+	    $(INSTALL_DATA) $$i "$(DESTDIR)$(etcdir)" ;                     \
 	done ;
 
 clean:
