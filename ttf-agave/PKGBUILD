# Maintainer: agaric <binzter  at  g mail>

pkgname=ttf-agave
pkgver=2
pkgrel=1
pkgdesc='A monospaced truetype font for the terminal'
arch=('any')
url=('https://bitbucket.org/agaric/agave')
license=('custom')  # public domain
depends=('fontconfig' 'xorg-font-utils')
install="${pkgname}.install"
source=('https://bitbucket.org/agaric/agave/downloads/agave002.tar.gz')
sha256sums=('f7132e2050990cf05f659df7fc857e38166a661d0e7cc3f5d55ae0bce54eec0c')

package() {
  cd "${srcdir}"
  install -d "${pkgdir}/usr/share/fonts/TTF"
  install -m644 agave-[rbiz].ttf "${pkgdir}/usr/share/fonts/TTF"
  install -Dm644 license.txt "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
