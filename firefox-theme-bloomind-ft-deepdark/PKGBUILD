# Maintainer: Tianjiao Yin <ytj000+aur@gmail.com>

_plugin_name=bloomind-ft-deepdark
_plugin_version=2.4.6.1
_plugin_id=295337
pkgdesc="Smooth dark theme for Firefox."
license=('unknown')

pkgname=firefox-theme-$_plugin_name
pkgver=$_plugin_version
_ffver=`pacman -Q firefox | cut -f2 -d\ | cut -f1 -d-`
pkgrel=1
arch=('any')
url="https://addons.mozilla.org/firefox/addon/$_plugin_id"
depends=("firefox")
makedepends=('unzip')
source=()
md5sums=()

build() {
  depends=("firefox>=${_ffver}" "firefox<=${_ffver/0/99}")
  cd $srcdir
  rm * -rf
  wget https://addons.mozilla.org/firefox/downloads/file/130829/bloomind_ft_deepdark_2-2.4.6.1-fx-linux.jar -O addon-$_plugin_id-latest.jar
  bsdtar -xf addon-$_plugin_id-latest.jar
  emid=$(sed -n '/.*<em:id>\(.*\)<\/em:id>.*/{s//\1/p;q}' install.rdf) || return 1
  local dstdir=$pkgdir/usr/lib/firefox-$_ffver/extensions/${emid}
  install -d $dstdir
  sed -i "s#<em:maxVersion>.*</em:maxVersion>#<em:maxVersion>${_ffver/0/99}</em:maxVersion>#" install.rdf
  rm addon-$_plugin_id-latest.jar
  cp -R * $dstdir
}
