# arg 1:  the new package version
post_install() {
cat << EOF
To turn one or all pins ON
# sudo pport -s 2
		or
# sudo pport -s all

To turn all pins OFF
# sudo pport -s none

To get more help run
# sudo pport --help
EOF
}
