diff --git a/README b/README
index 5af5d3a..1520f1a 100644
--- a/README
+++ b/README
@@ -1,17 +1,18 @@
 remove-accessibility-icon
 -------------------------
 This simple extension does nothing more than to remove the accessibility
-icon in the top right corner of the GNOME panel. The extension was tested
-with Gnome shell 3.0.2 only.
+icon in the top right corner of the GNOME panel. This extension should works
+fine with gnome-shell 3.2.0 or higher, but was tested on gnome-shell 3.2.0
+only.
 
 Installation
 -------------------------
 Dowload the tarball to $HOME/.local/share/gnome-shell/extensions and unpack
 it. A new directory called 'remove-accessibility-icon@martin-weusten.de'
-should be created. 
+should be created.
 
-Restart your Gnome shell (Alt-F2 and 'r') and the accessibility icon should be 
-gone.
+Restart your Gnome shell (Alt-F2 and 'r'). After that, enable this extension
+You can use gnome-tweak-tool.
 
 Website & Source:
 -------------------------
@@ -21,6 +22,13 @@ Author
 -------------------------
 Martin Weusten <martin@martin-weusten.de>
 
+Contributors:
+-------------------------
+Elder Marco <eldermarco@gmail.com>
+    - Support for gnome-shell 3.2.0
+    - Thanks to Felipe Morales:
+        * http://notsnippets.tumblr.com/post/10799581696/notes-on-writing-gnome-shell-extensions
+
 License
 -------------------------
 remove-accessibility-icon is licensed under the "Modified BSD License", for
diff --git a/extension.js b/extension.js
index 7c47967..cc469aa 100644
--- a/extension.js
+++ b/extension.js
@@ -1,9 +1,23 @@
+const Main  = imports.ui.main;
 const Panel = imports.ui.panel;
 
-function main() {
-	let i = Panel.STANDARD_TRAY_ICON_ORDER.indexOf('a11y');
-	if (i >= 0) {
-		Panel.STANDARD_TRAY_ICON_ORDER.splice(i,1);
-	}
-	delete Panel.STANDARD_TRAY_ICON_SHELL_IMPLEMENTATION['a11y'];
+function init() {
+}
+
+function enable(){
+    for (let Index = 0; Index < Main.panel._rightBox.get_children().length; Index++){
+        if(Main.panel._statusArea['a11y'] ==
+                Main.panel._rightBox.get_children()[Index]._delegate){
+            Main.panel._rightBox.get_children()[Index].destroy();
+            break;
+        }
+    }
+
+    Main.panel._statusArea['a11y'] = null;
+}
+
+function disable() {
+    let Indicator = new Panel.STANDARD_STATUS_AREA_SHELL_IMPLEMENTATION['a11y'];
+        Main.panel.addToStatusArea('a11y', Indicator,
+                Panel.STANDARD_STATUS_AREA_ORDER.indexOf('a11y'));
 }
diff --git a/metadata.json b/metadata.json
index f002a0f..6791d02 100644
--- a/metadata.json
+++ b/metadata.json
@@ -4,5 +4,5 @@
 "description": "Removes the accessibility icon from the panel.",
 "url": "http://martin-weusten.de/gnomeshell-extensions/",
 "original-author": "martin@martin-weusten.de",
-"shell-version": [ "3.0.2" ]
+"shell-version": [ "3.2.0", "3.2" ]
 }
diff --git a/stylesheet.css b/stylesheet.css
index a8dc980..0a37cd6 100644
--- a/stylesheet.css
+++ b/stylesheet.css
@@ -1,8 +1 @@
-/* Example stylesheet */
-.helloworld-label {
-    font-size: 36px;
-    font-weight: bold;
-    color: #ffffff;
-    background-color: rgba(10,10,10,0.7);
-    border-radius: 5px;
-}
+/* None used */
