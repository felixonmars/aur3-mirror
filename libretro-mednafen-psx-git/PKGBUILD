# Maintainer: almostalive <almostalive2003 at lavabit dot com>

pkgname=libretro-mednafen-psx-git
_gitname=mednafen-libretro
pkgver=2013.05.24_1b05a4b
pkgrel=1
pkgdesc="libretro implementation of mednafen's PSX. (PlayStation)"
arch=('i686' 'x86_64' 'arm' 'armv6h')
url="https://github.com/libretro/mednafen-libretro"
license=('custom')
makedepends=('git')
source=('git://github.com/libretro/mednafen-libretro.git')
md5sums=('SKIP')

pkgver() {
  cd $_gitname
  echo "$(git log -1 --format="%cd" --date=short | sed 's|-|.|g')_$(git describe --always | sed 's/-/_/g')"
}

build() {
  cd $_gitname
  make core=psx
}

package()
{
  mkdir -p $pkgdir/usr/lib/libretro
  install -v -m644 $srcdir/$_gitname/mednafen_psx_libretro.so $pkgdir/usr/lib/libretro/libretro-mednafen-psx.so
}

