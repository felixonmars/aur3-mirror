post_install() {
  if [ $(grep -c cjdns /etc/passwd) = 0 ]; then
    echo "Adding user cjdns"
    useradd cjdns
  fi
}

post_remove() {
  if [ $(grep -c cjdns /etc/passwd) = 1 ]; then
    echo "Removing user cjdns"
    userdel cjdns
  fi
}
