post_install() {
	echo -e "\033[1;31m==> Have rtorrent open an rpc socket by adding this line to ~/.rtorrent.rc and restarting rtorrent:\033[0m"
	echo "scgi_local = ./.socket/rpc.socket"
	echo "This socket will have to be set rw-enabled for http service."
}
