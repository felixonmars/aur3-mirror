post_install() {
  groupadd lego
  udevadm control --reload-rules

  echo """
For USB support, add your user to the lego group:
    gpasswd -a USERNAME lego
  """
}

post_remove() {
  groupdel lego
}
