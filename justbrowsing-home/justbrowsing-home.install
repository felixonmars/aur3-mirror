_userdir="/usr/share/justbrowsing"
_username="justbrowsing"

pre_install(){
  useradd -U -m -d "${_userdir}" justbrowsing
}

post_install(){
  chown -R ${_username}:${_username} "${_userdir}"
}

post_remove(){
  userdel -rf "${_username}" 2>/dev/null
}
