# Maintainer: Artem Sereda <overmind88@gmail.com>

pkgname=kvpm
pkgver=0.7.8
pkgrel=1
pkgdesc="Frontend for Linux LVM and Gnu parted. It also handles creating and mounting file systems"
arch=('i686' 'x86_64')
url="http://sourceforge.net/projects/kvpm/"
license=('GPL')
depends=('util-linux-ng' 'parted' 'kdelibs' 'lvm2>=2.02.73' 'udev')
makedepends=('cmake' 'automoc4')
source=(http://downloads.sourceforge.net/${pkgname}/${pkgname}-${pkgver}.tar.gz)
md5sums=('ee73c1da06ca5378c0b63a722f9f9a2f')

build () {
 unset LDFLAGS
 cd ${srcdir}/${pkgname}-${pkgver}/kvpm
 cmake -DCMAKE_INSTALL_PREFIX=/usr
 make || return 1
 make DESTDIR=${pkgdir} install
}
