# Maintainer: J. W. Birdsong < at gmail blah blah you know the deal>
pkgname=lib32-gvfs
pkgver=1.10.1
pkgrel=1
pkgrel32=3
pkgdesc="Userspace virtual filesystem implemented as a pluggable module for gio"
url="http://www.gnome.org/"
arch=('x86_64')
license=('LGPL')
depends=('lib32-libxml2' 'lib32-avahi' 'lib32-libgnome-keyring')
source=(http://ftp.archlinux.org/extra/os/i686/gvfs-${pkgver}-${pkgrel32}-i686.pkg.tar.xz)

md5sums=('c70e2fa636a0cf7b915425ce4cc98859')

build() {
  mkdir -p ${pkgdir}/usr/lib32
  cp -rPf ${srcdir}/usr/lib/* ${pkgdir}/usr/lib32
}
