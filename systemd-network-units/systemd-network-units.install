post_install() {
cat << EOF

==> Change static IP address in /etc/conf.d/network.conf

==> To start network use this commands as root :
	systemctl start mysqld.service

==> To enable it on boot :
	systemctl enable mysqld.service

EOF
}
post_upgrade() {
	post_install
}
