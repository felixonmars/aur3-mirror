
post_install() {
  cat <<EOT
Edit /etc/steamroller.conf to point at a directory writable to the 
users able to edit the repo, e.g. \`\$HOME/pkgs'.
EOT
}

post_upgrade() {
  post_install
}

# vim:set ts=2 sw=2 et:
