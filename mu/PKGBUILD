# Maintainer: Uwe <moc liamg skcuneelu <-- look what I did there>
# Contributor: Nicolas Pouillard <nicolas.pouillard@gmail.com>

pkgname=mu
pkgver=0.9.6
pkgrel=1
pkgdesc="collection of utilties for indexing and searching Maildirs"
arch=('i686' 'x86_64')
url="http://www.djcbsoftware.nl/code/mu"
install=${pkgname}.install
license=('GPLv3')
depends=('xapian-core' 'gmime' 'sqlite3')
optdepends=('cronie: updating index regularly')
source=("http://mu0.googlecode.com/files/mu-${pkgver}.tar.gz")

md5sums=('72fdf907f2b7922a54c8d14bc4b06ccf')

build() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  ./configure --prefix=/usr 
  make
}

package() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  make DESTDIR="${pkgdir}" install
  install -Dm644 "${srcdir}/${pkgname}-${pkgver}/COPYING" "${pkgdir}/usr/share/licenses/${pkgname}/COPYING"
}
