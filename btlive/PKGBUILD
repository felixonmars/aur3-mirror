# Contributor: Jonas Heinrich <onny@project-insanity.org>
# Maintainer: Jonas Heinrich <onny@project-insanity.org>

pkgname=btlive
pkgver=0.4.9.122
pkgrel=3
pkgdesc="Bittorrent Live is a technology for p2p live streaming, client version"
arch=('i686' 'x86_64')
url="http://live.bittorrent.com"
license=('nonfree')
depends=('python2.6')
if [ "${CARCH}" = "x86_64" ]; then
  _arch="amd64"
  md5sums=('8cf758e1da62618e938a02a29555be16'
  '7bad98c51617cc85cf291716e9c66cd8')
else
  _arch="i386"
  md5sums=('d52034c64435b6696c8aa563789ff4bc'
  '7bad98c51617cc85cf291716e9c66cd8')
fi 
source=("http://s3.amazonaws.com/live-installer/${pkgname}_${pkgver}_${_arch}.deb"
	"${pkgname}")
options=('!strip')
build() {
  cd $srcdir
  ar x "${pkgname}_${pkgver}_${_arch}.deb"
  tar xvf data.tar.gz
}
package () {
  mkdir -p $pkgdir/usr/lib
  cp -r "$srcdir/usr" "$pkgdir/"
  mkdir -p $pkgdir/etc/rc.d
  cp $srcdir/$pkgname $pkgdir/etc/rc.d/

}
