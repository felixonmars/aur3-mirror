# Maintainer: Balló György <ballogyor+arch at gmail dot com>

pkgname=rhythmbox-ubuntuone
pkgver=4.0.0
pkgrel=1
pkgdesc="A Rhythmbox plugin that integrates Ubuntu One features"
arch=('any')
url="https://launchpad.net/rhythmbox-ubuntuone"
license=('GPL')
depends=('libubuntuone' 'rhythmbox')
makedepends=('python2-distutils-extra')
install=$pkgname.install
source=(http://launchpad.net/rhythmbox-ubuntuone/stable-4-0/$pkgver/+download/$pkgname-$pkgver.tar.gz)
md5sums=('c79d577bd5f3ce8c329a4b691b7e8a69')

build() {
  cd "$srcdir/$pkgname-$pkgver"

  python2 setup.py build
}

package() {
  cd "$srcdir/$pkgname-$pkgver"

  python2 setup.py install --root=$pkgdir/ --optimize=1
}
