# Generated by Xyne::Arch::CPAN 0.07

pkgname=perl-amf-perl
pkgver=0.150.0
pkgrel=1
pkgdesc="CPAN package (no description found)"
arch=('i686 x86_64')
url="http://search.cpan.org/dist/AMF-Perl/"
license=('unknown')
source=('http://search.cpan.org/CPAN/authors/id/S/SI/SIMONF/AMF-Perl-0.15.tar.gz')
md5sums=('653fd3f7c9d67c6ef7d78cea0545be8c')
sha256sums=('054e607dc66f239461ed328464e51cd6ad61598ba1d1868f3639e1739d9be478')
depends=('perl-dbi>=0.10.0' 'perl-encode>=0.10.0' 'perl-exception-class>=0.10.0')
provides=('perl-amf-perl-app-executive' 'perl-amf-perl-io-deserializer' 'perl-amf-perl-io-inputstream' 'perl-amf-perl-io-outputstream' 'perl-amf-perl-io-serializer' 'perl-amf-perl-sql-mysqlrecordset' 'perl-amf-perl-util-object' 'perl-amf-perl-util-remotingservice' 'perl-dataecho' 'perl-parkservice' 'perl-petmarket-api-cartservice' 'perl-petmarket-api-catalogservice' 'perl-petmarket-api-dbconn' 'perl-petmarket-api-orderservice' 'perl-petmarket-api-stringresourcesservice' 'perl-petmarket-api-userservice')
options=(!emptydirs)

build() {
  _dir=$(find $srcdir -maxdepth 2 -type f -name 'Makefile.PL')
  if [ ! -z "$_dir" ]; then
    cd $(dirname "$_dir")
    PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1
    make  || return 1
    make install DESTDIR="${pkgdir}" || return 1

  else
  _dir=$(find $srcdir -maxdepth 2 -type f -name 'Build.PL')
  if [ ! -z "$_dir" ]; then
    cd $(dirname "$_dir")
    PERL_MM_USE_DEFAULT=1 perl Build.PL INSTALLDIRS=vendor || return 1
    ./Build  || return 1
    ./Build install destdir=${pkgdir} || return 1

  else
    echo "error: failed to detect build method for $pkgname"
    echo "you may be able to fix this by editing the PKGBUILD"
    return 1
  fi fi

  # remove perllocal.pod and .packlist
  find ${pkgdir} -name perllocal.pod -delete
  find ${pkgdir} -name .packlist -delete
}

# vim:set ts=2 sw=2 et:

