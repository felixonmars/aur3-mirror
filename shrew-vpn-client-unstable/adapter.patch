--- head/source/iked/ike.socket.cpp	2011-02-01 16:33:25.668745433 +0100
+++ head~/source/iked/ike.socket.cpp	2011-02-01 16:20:19.988758275 +0100
@@ -722,7 +722,7 @@ bool _IKED::client_setup( IDB_TUNNEL * t
 		if( ioctl( sock, SIOCSIFADDR, &ifr ) != 0 )
 		{
 			log.txt( LLOG_ERROR, "!! : failed to configure address for %s ( %s )\n",
-				adapter->name, strerror( errno ) );
+				tunnel->adapter->name, strerror( errno ) );
 
 			close( sock );
 			return false;
@@ -737,7 +737,7 @@ bool _IKED::client_setup( IDB_TUNNEL * t
 		if( ioctl( sock, SIOCSIFNETMASK, &ifr ) != 0 )
 		{
 			log.txt( LLOG_ERROR, "!! : failed to configure netmask for %s ( %s )\n",
-				adapter->name, strerror( errno ) );
+				tunnel->adapter->name, strerror( errno ) );
 
 			close( sock );
 			return false;
@@ -754,7 +754,7 @@ bool _IKED::client_setup( IDB_TUNNEL * t
 		if( ioctl( sock, SIOCSIFBRDADDR, &ifr ) != 0 )
 		{
 			log.txt( LLOG_ERROR, "!! : failed to configure broadcast address for %s ( %s )\n",
-				adapter->name, strerror( errno ) );
+				tunnel->adapter->name, strerror( errno ) );
 
 			close( sock );
 			return false;
