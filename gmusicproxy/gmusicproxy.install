post_install() {
    echo "An example config is provided in /etc/gmusicproxy.cfg.example"
    echo
    echo "For a system-wide config, copy this file to /etc/gmusicproxy.cfg"
    echo "For a per-user config, copy this file to ~/.config/gmusicproxy.cfg"
}

op="$1"
shift
$op $*
