# Maintainer: Patrick McCarty <pnorcks at gmail dot com>
# Contributor: Emmanuel 'guinness' Boudreault

pkgname=guile-devel
pkgver=1.9.14
pkgrel=1
pkgdesc="Guile is a portable, embeddable Scheme implementation written in C"
url="http://www.gnu.org/software/guile/"
arch=('i686' 'x86_64')
license=('GPL')
depends=('gmp>=4.3.1' 'libtool' 'ncurses>=5.7' 'texinfo'
         'libunistring' 'gc' 'libffi')
conflicts=('guile')
provides=("guile=$pkgver")
install=guile.install
source=(ftp://alpha.gnu.org/gnu/guile/guile-${pkgver}.tar.gz)
options=('!libtool')
md5sums=('40667f40527ff0ecadb23f33e9562d2c')
sha256sums=('403dfb80b42f2e6c1cc79448c733293afcaebf248fd52d2fda5232954f4e4398')

build() {
  cd "${srcdir}/guile-${pkgver}"
  ./configure --prefix=/usr
  make LDFLAGS+="-lpthread"
}

package() {
  cd "${srcdir}/guile-${pkgver}"
  make DESTDIR="${pkgdir}" install
}

# vim:set ts=2 sw=2 et:
