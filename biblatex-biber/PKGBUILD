# Maintainer: Andreas dot Wagner at em dot uni-frankfurt dot de

pkgname=biblatex-biber
pkgver=0.8.3
pkgrel=1
pkgdesc="A BibTeX replacement for users of biblatex"
url="http://biblatex-biber.sourceforge.net"
arch=('any')
license=('PerlArtistic')
depends=( \
  'perl-unicode-collate>=0.72' \
  'perl-ipc-run' \
  'perl-file-slurp-unicode' \
  'perl-readonly-xs' \
  'perl-list-allutils' \
  'perl-log-log4perl' \
  'perl-xml-libxml-simple' \
  'perl-text-bibtex>=0.53' \
  'perl-libwww' \
  'texlive-bibtexextra>=2010.20942-1' \
  'perl-config-general' \
  'perl-data-compare' \
  'perl-data-dump' \
  'perl-date-simple' \
  'perl-regexp-common' \
  'perl-xml-libxslt' \
#  'perl-xml-writer' \
  )
optdepends=('perl-sleepycat-dbxml: support for Berkeley DBXML databases' )
conflicts=('biblatex-biber-git')
source=(http://downloads.sf.net/project/$pkgname/$pkgname/current/$pkgname-v$pkgver.tar.gz)

build() {
  cd "${pkgname}-v${pkgver}"
  perl Build.PL installdirs=vendor destdir="$pkgdir/"
  perl Build
#  perl Build test
}


package() {
  cd "${pkgname}-v${pkgver}"
  perl Build install

  mv "$pkgdir/usr/bin/vendor_perl/biber" "$pkgdir/usr/bin/biber"
  rm -rf "$pkgdir/usr/bin/vendor_perl"

  # remove perllocal.pod and .packlist
  find "$pkgdir" -name perllocal.pod -delete
  find "$pkgdir" -name .packlist -delete
}

md5sums=('991af404fd74a4283d96a19eb0bc165d')
