# Contributor: Ciriaco Garcia de Celis <cgarcia at etherpilot.com>
pkgname=fake-jre
pkgver=1.0
pkgrel=2
pkgdesc="Fake JRE runtime dependency (user should provide one manually installed)"
arch=(any)
url="https://bbs.archlinux.org/viewtopic.php?pid=745932#p745932"
license=('GPL')
makedepends=()
provides=('java-runtime')
install=${pkgname}.install
noextract=()
md5sums=()

build() {
    echo "done"
}
