post_install() {
	echo '>>> Add "netatalk" to your DAEMONS list in /etc/rc.conf';
}
post_upgrade() {
	if [ "`grep -E '(cnid|atalkd|afpd)' /etc/rc.conf`" ];then
	echo '>>> Detected old DAEMON entries in /etc/rc.conf';
	echo '>>> Replace these with "netatalk" before restarting.';fi
}