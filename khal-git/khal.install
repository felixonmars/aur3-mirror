post_install() {
  echo 'A sample and self explanatory configuration file has been put in /etc/skel/khal.conf.'
  echo 'Copy it in ~/.config/khal.conf and edit it as needed.'
}

post_upgrade(){
  echo 'You may want to remove your old sqlite database before doing the first khal --sync, as the database internal structure may have changed.'
}