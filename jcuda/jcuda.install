post_install() {
echo "Run \"source /etc/profile\" in a shell or relogin to refresh your" 
echo "PATH variable to include the native CUDA libraries."
}

post_upgrade() {
echo "Run \"source /etc/profile\" in a shell or relogin to refresh your" 
echo "PATH variable to include the native CUDA libraries."
}

# vim:set ts=2 sw=2 et:
