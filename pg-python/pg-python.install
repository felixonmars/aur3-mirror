post_install() {
    echo -e "execute the command: \n\n psql -d <database> -f /usr/share/pg-python/install.sql \n\n\
to install the 'python' language on a db. Don't worry about errors, the script is expected \n\
to fail at some point - just check for the \"Language installed successfully\" message at the end."
}
