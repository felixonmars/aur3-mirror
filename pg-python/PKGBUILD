# Maintainer: Gianni Vialetto <g.vialetto@gmail.com>

pkgname=pg-python
pkgver=1.0.0
pkgrel=1
pkgdesc="Alternative python3 PL implementation for PostgreSQL"
arch=('i686' 'x86_64')
url="http://python.projects.postgresql.org/backend/"
license=('BSD')
depends=('postgresql>=8.3', 'python>=3.1')
install=('pg-python.install')
source=("http://python.projects.postgresql.org/backend/files/$pkgname-$pkgver.tar.bz2")
md5sums=('0346daa0f931d71e90e0c58903ac54af')

build() {
  cd $srcdir/$pkgname-$pkgver
  
  ./configure || return 1
  make || return 1
  make DESTDIR="$pkgdir" install || return 1
  install -D -m644 src/install.sql $pkgdir/usr/share/pg-python/install.sql
}

