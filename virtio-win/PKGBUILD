# Maintainer: Your Name <youremail@domain.com>
pkgname=virtio-win
pkgver=0.1.52
pkgver_=0.1-52
pkgrel=1
pkgdesc="virtio drivers for windows guests"
arch=(i686 x86_64)
url="http://alt.fedoraproject.org/pub/alt/virtio-win/latest/images/bin/"
license=('GPL')
groups=()
depends=(qemu)
makedepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
source=(http://alt.fedoraproject.org/pub/alt/virtio-win/latest/images/bin/${pkgname}-${pkgver_}.iso)
noextract=()

package() {
  install -Dm 644 ${pkgname}-${pkgver_}.iso\
    "$pkgdir/usr/share/virtio-win/${pkgname}.iso"

}
md5sums=('ad651f9710e6be84df27de527428a2af')

