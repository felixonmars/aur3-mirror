# Maintainer: David McIlwraith <archaios at archaios dot net>
# Contributor: Qusai Al Shidi <aquafox90 at gmail dot com>
pkgname=crossfire-client
pkgver=1.60.0
pkgrel=1
pkgdesc="A free and open source cross platform MMORPG."
url="http://crossfire.real-time.com/"
arch=('i686 x86_64')
license=('GPL2')
depends=('libx11' 'sdl' 'alsa-lib' 'gtk2' 'lua' 'curl' 'libglade' 'libgl')
source=(http://downloads.sourceforge.net/project/crossfire/crossfire-client/${pkgver}/${pkgname}-${pkgver}.tar.gz
	crossfire-curl-rm-types-h-dep.patch)
md5sums=('7b22bf93ebb581a5bfd5682df107af76'
         'a2b9bad739fa0f65001be2931fe494fb')

build(){
  cd $startdir/src/$pkgname-$pkgver
  patch -Np1 < ../crossfire-curl-rm-types-h-dep.patch
  ./configure --prefix=/usr || return 1
  make || return 1
  make DESTDIR=$startdir/pkg install || return 1
}
