# Maintainer: archtux <antonio dot arias99999 at gmail dot com>

pkgname=ttx
pkgver=0.0.2
pkgrel=1
pkgdesc="Teletext-browser with support for Dutch, Belgian, Austrian, German, Croatian, Portuguese, Finnish and Italian teletext services"
arch=('i686' 'x86_64')
url="http://code.google.com/p/ttx-teletext/"
license=('GPL3')
depends=('gtk3' 'hicolor-icon-theme' 'libsoup')
source=(http://ttx-teletext.googlecode.com/files/$pkgname-$pkgver.tar.gz)
md5sums=('81ea7e212a97b9eca1e2300830f37cd3')

build() {
  cd $srcdir/$pkgname-$pkgver

  ./configure --prefix=/usr
  make
}

package() {
  cd $srcdir/$pkgname-$pkgver

  make DESTDIR=$pkgdir install
}