#
# Parameters to be passed to purple-tmpfs
#

# directory where ~/.purple directories are moved
# TMPFS=/dev/shm
# TMPFS=/tmp

# list of users separated by spaces, default is all
# USERS="`ls -d -c1 /home/*/.purple | cut -d/ -f3`"
# USERS=""
