# Maintainer: Pascal Houliston <pascal@whitewallweb.com>
# Contributor: Douglas Soares de Andrade <dsa@archlinux-br.org>
# Contributor: Rob Gabaree <rob@rawb.net>
# Contributor: William Rea <sillywilly@gmail.com>
# Contributor: Benjamin Andresen <klapmuetz@gmail.com>

pkgname=rubygems
pkgver=1.3.7
pkgrel=1
pkgdesc="A package management framework for the Ruby programming language"
arch=('i686' 'x86_64')
url="http://docs.rubygems.org"
license=("GPL")
depends=('ruby>=1.8.6')
source=(http://production.cf.rubygems.org/rubygems/rubygems-${pkgver}.tgz)
md5sums=('e85cfadd025ff6ab689375adbf344bbe')

build() {
    cd $srcdir/$pkgname-$pkgver
    export GEM_HOME=${pkgdir}/usr/lib/ruby/gems/1.8
    cd $srcdir/$pkgname-$pkgver
    ruby setup.rb --prefix=$pkgdir/usr || return 1
    mkdir -p $pkgdir/usr/lib/ruby/site_ruby/1.8
    cd $pkgdir/usr/lib
    mv rbconfig rubygems rubygems.rb ubygems.rb $pkgdir/usr/lib/ruby/site_ruby/1.8
}
