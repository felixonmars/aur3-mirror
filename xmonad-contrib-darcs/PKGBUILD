# Maintainer: Daniel Wallace <danielwallace at gtmanfred dot com>
# Contributor: adam vogt <vogt. adam at gmail.com>
# Package generated by cabal2arch 0.6
pkgname=xmonad-contrib-darcs
pkgrel=1
pkgver=20130311
pkgdesc="Third party extensions for xmonad"
url="http://hackage.haskell.org/package/xmonad-contrib"
license=('custom:BSD3')
arch=('i686' 'x86_64')
conflicts=('xmonad-contrib')
depends=('haskell-random' 'haskell-x11-xft' 'xmonad-darcs' 'haskell-x11>=1.6.1')
options=('strip')
install=xmonad-contrib.install

_darcsmod="XMonadContrib"
_darcstrunk="http://code.haskell.org"

build() {
  cd "$srcdir"
  msg "Checking for previous build...."

  if [[ -d "$_darcsmod/_darcs" ]]; then
    msg "Retrieving missing patches"
    cd "$_darcsmod"
    darcs pull -a "$_darcstrunk/$_darcsmod"
  else
    msg "Retrieving complete sources"
    darcs get --set-scripts-executable "$_darcstrunk/$_darcsmod"
    cd "$_darcsmod"
  fi

  rm -rf "$srcdir/$_darcsmod-build"
  cp -r "$srcdir/$_darcsmod" "$srcdir/$_darcsmod-build"
  cd "$srcdir/$_darcsmod-build"

  msg "Starting build..."

  runhaskell Setup configure --prefix=/usr --flags="use_xft,use_utf8" --docdir=/usr/share/doc/${pkgname}
  runhaskell Setup build
  runhaskell Setup haddock
  runhaskell Setup register   --gen-script
  runhaskell Setup unregister --gen-script
}
package() {
	cd "$srcdir/$_darcsmod-build"
    install -D -m744 register.sh   ${pkgdir}/usr/share/haskell/$pkgname/register.sh
    install    -m744 unregister.sh ${pkgdir}/usr/share/haskell/$pkgname/unregister.sh
    install -d -m755 $pkgdir/usr/share/doc/ghc/libraries
    runhaskell Setup copy --destdir=${pkgdir}
    mkdir -p ${pkgdir}/usr/share/doc/ghc/html/libraries/
    ln -s /usr/share/doc/${pkgname}/html ${pkgdir}/usr/share/doc/ghc/html/libraries/xmonad-contrib
    install -D -m644 LICENSE ${pkgdir}/usr/share/licenses/$pkgname/LICENSE
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
