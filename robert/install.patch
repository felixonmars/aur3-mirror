--- install	2009-04-08 14:39:51.000000000 +0200
+++ install	2009-04-22 13:14:36.867726875 +0200
@@ -15,7 +15,7 @@
 # This works for everything else.
 #
 [ -z "$BASH" ] && { 
-	echo "Not bash, executing bash explicitly." 
+	echo "Not bash, executing bash explicitly."
 	bash install || exit 1
 	echo OK
 	exit 0 
@@ -30,11 +30,16 @@
 EXEXBSH=`which bash`
 NAME=Robert
 
-PREFIX=/usr/local
+PREFIX=/usr
 SHARE=$PREFIX/share
 BINDIR=$PREFIX/bin
 DESTDIR=$SHARE/$NAME
 
+PKGPREFIX=$pkgdir/$PREFIX
+PKGSHARE=$pkgdir/$SHARE
+PKGBINDIR=$pkgdir/$BINDIR
+PKGDESTDIR=$pkgdir/$DESTDIR
+
 ./clean
 
 echo "#! ${EXEXBSH}" > $NAME
@@ -69,36 +74,37 @@
 #echo "python -OO ${DESTDIR}/${NAME}.py" >> $NAME
 echo "python ${DESTDIR}/${NAME}.py" >> $NAME
 
-install -m 755 $NAME $BINDIR/$NAME
+
+install -D -m 755 $NAME $PKGBINDIR/$NAME
 
 
-if [ -e $DESTDIR ] ; then
-	if [ -e $DESTDIR.old ] ; then
-		( cd $SHARE ; rm -R $NAME.old )
+if [ -e $PKGDESTDIR ] ; then
+	if [ -e $PKGDESTDIR.old ] ; then
+		( cd $PKGSHARE ; rm -R $NAME.old )
 	fi
-	mv $DESTDIR $DESTDIR.old
+	mv $PKGDESTDIR $PKGDESTDIR.old
 fi
 
-install -d $DESTDIR
-install -m 644 *.py $DESTDIR
+install -d $PKGDESTDIR
+install -m 644 *.py $PKGDESTDIR
 
-install -d $DESTDIR/BitTorrent
-install -m 644 BitTorrent/*.py $DESTDIR/BitTorrent
+install -d $PKGDESTDIR/BitTorrent
+install -m 644 BitTorrent/*.py $PKGDESTDIR/BitTorrent
 
-install -d $DESTDIR/images
-install -m 644 images/*.ico $DESTDIR/images
-install -m 644 images/*.bmp $DESTDIR/images
-install -m 644 images/*.png $DESTDIR/images
+install -d $PKGDESTDIR/images
+install -m 644 images/*.ico $PKGDESTDIR/images
+install -m 644 images/*.bmp $PKGDESTDIR/images
+install -m 644 images/*.png $PKGDESTDIR/images
 
-install -d $DESTDIR/images/flags
-install -m 644 images/flags/*.ico $DESTDIR/images/flags
+install -d $PKGDESTDIR/images/flags
+install -m 644 images/flags/*.ico $PKGDESTDIR/images/flags
 
 for i in locale/*/LC_MESSAGES ; {
-	install -d $DESTDIR/$i
-	install -m 644 $i/*.mo $DESTDIR/$i
+	install -d $PKGDESTDIR/$i
+	install -m 644 $i/*.mo $PKGDESTDIR/$i
 }
 
-python -OO -c "import compileall; compileall.compile_dir('${DESTDIR}')" > /dev/null || { echo Something went wrong. ; exit 1 ; }
+python -OO -c "import compileall; compileall.compile_dir('${PKGDESTDIR}')" > /dev/null || { echo Something went wrong. ; exit 1 ; }
 echo Install completed.
 exit 0
 
