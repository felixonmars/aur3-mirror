post_install() {
  if [ -z "$(getent passwd git)" ]; then
    echo "Creating 'git' user"
    /usr/sbin/useradd -r -m -d /srv/gitolite git
  fi
}

post_remove() {
  if [ -n "$(getent passwd git)" ]; then
    echo "Removing 'git' user"
    /usr/sbin/userdel git
  fi
}

# vim:set ts=2 sw=2 et:
