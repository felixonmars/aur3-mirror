post_install() {
  echo "==> Make sure you have installed java in wine before running the application"
  echo "  -> Download the offline installer (Java 1.6 recommended) from www.java.com" 
  echo "==> Add user to 'wheel' group"
  echo "  -> gpasswd -a <USER> wheel"
}
