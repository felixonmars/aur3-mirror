# Contributor: Andrey <gornet[at]gmail[dot]com>

pkgname=gtkevemon-git
pkgver=a5dda8e
pkgrel=1
pkgdesc="GTK clone of EveMon skill monitor for EVE Online - github by ecraven"
arch=('i686' 'x86_64')
url="http://gtkevemon.battleclinic.com/index.html"
license=('GPL3')
depends=('gtk2' 'gtkmm' 'libxml2' 'openssl')
makedepends=('git')
provides=('gtkevemon' 'gtkevemon-svn')
conflicts=('gtkevemon' 'gtkevemon-svn' 'gtkevemon-hg')
options=()
install=
source=('git+https://github.com/ecraven/gtkevemon.git')
md5sums=('SKIP') #generate with 'makepkg -g'

pkgver() {
    cd gtkevemon
    echo "$(git rev-parse --short HEAD)"
}

build() {
    cd "${srcdir}/gtkevemon/"
    make
}

package() {
    cd "${srcdir}/gtkevemon/"
    make INSTALL_BIN=$pkgdir/usr/bin \
        INSTALL_DIR_ICON=$pkgdir/usr/share/pixmaps \
        INSTALL_DIR_DESK=$pkgdir/usr/share/applications \
        install
}
