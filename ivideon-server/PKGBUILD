# Maintainer: Elovsky Valentin (evvsoft@gmail.com)
# Contributor: Shalygin Konstantin (kostya@opentech.ru)

pkgname='ivideon-server'
pkgver='3.5.1'
pkgrel='169'
pkgdesc='Ivideon Server (with QT5 GUI)'
arch=('i686' 'x86_64')
url=('http://ivideon.ru/')
license=('freeware')
depends=('ivideon-server-headless' 'qt5-base' 'qt5-svg' 'qt5-script')
conflicts=('ivideon-video-server-nogui' 'ivideon-video-server')

if [[ "$CARCH" == "i686" ]]; then
  source+=("http://packages.ivideon.com/ubuntu/pool/non-free/i/ivideon-video-server/ivideon-video-server_${pkgver}-${pkgrel}~b720a50c8717_i386.deb")
  sha256sums+=('3d231a7d91a0fe0e7625f48c893c17b83486f5e7189488e38badc479657262fd')
fi

if [[ "$CARCH" == "x86_64" ]]; then
  source+=("http://packages.ivideon.com/ubuntu/pool/non-free/i/ivideon-video-server/ivideon-video-server_${pkgver}-${pkgrel}~b720a50c8717_amd64.deb")
  sha256sums+=('091092d8331315ff62ff26ca3c188d1ce86257ed99b57d606216097cd5b38c2e')
fi

build() {
  cd "$srcdir"
  bsdtar xf "$srcdir/data.tar.gz"
}

package() {
  cp -dpr --no-preserve=ownership "$srcdir/opt" "$pkgdir"
  cp -dpr --no-preserve=ownership "$srcdir/usr" "$pkgdir"
}