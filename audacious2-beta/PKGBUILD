# Contributor: Kemka Andrey <dr.andru@gmail.com>
# Contributor: Anton Lobov <ahmad200512@yandex.ru>

pkgname=audacious2-beta
pkgver=2.5
pkgrel=1
realname=audacious
subver=beta1
pkgdesc="Audacious is an advanced audio player"
arch=('i686' 'x86_64')
url="http://audacious-media-player.org/"
license=('GPL3')
depends=('dbus-glib' 'libmowgli-new' 'libmcs' 'libsamplerate' 'unzip' 'libguess')
makedepends=('pkgconfig')
replaces=('audacious' 'audacious-player' 'audacious-player-hg' 'audacious-alpha', 'audacious-beta', 'audacious2', 'audacious2-alpha')
conflicts=('audacious' 'audacious-player' 'audacious-player-hg' 'audacious-alpha', 'audacious-beta', 'audacious2', 'audacious2-aplha')
provides=("audacious=${pkgver}" "audacious-player=${pkgver}" "audacious-player-hg=${pkgver}" "audacious-alpha=${pkgver}" "audacious-beta=${pkgver}")
install=audacious2.install
source=(http://distfiles.atheme.org/${realname}-${pkgver}-${subver}.tgz)
md5sums=('70a08e48c7e77343f5c57f62e4561d0c')


build() {
  cd "${srcdir}/${realname}-${pkgver}-${subver}"

  ./configure --prefix=/usr --enable-chardet
  make || return 1
  make DESTDIR="${pkgdir}" install || return 1
}

