# Maintainer: SpepS <dreamspepser at yahoo dot it>

pkgname=rho
pkgver=1.0.2
pkgrel=1
pkgdesc="A small, very configurable console-based text editor."
arch=(i686 x86_64)
url="http://rho.tuxfamily.org/"
license=('GPL')
depends=('ncurses')
install="$pkgname.install"
source=($url$pkgname-$pkgver.tar.bz2)
md5sums=('852feaf698b11950f8db06a6101799b4')

build() {
  cd "$srcdir/current"

  # force DESTDIR in Makefile
  sed -i "s| \(/usr\)| $pkgdir\1|g" Makefile

  make
}

package() {
  cd "$srcdir/current"

  # prepare dirs
  install -d "$pkgdir"/usr/{bin,share/$pkgname}

  make install

  # readme
  install -Dm644 README "$pkgdir/usr/share/$pkgname"
}

# vim:set ts=2 sw=2 et:
