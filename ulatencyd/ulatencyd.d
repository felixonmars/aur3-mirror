# ULATENCYD init daemon configuration file
# sourced by /etc/rc.d/ulatencyd

# Additional options passing to the daemon.
# Type "ulatencyd -h" to get all possible options.

# Example:
# ULATENCYD_OPTS="-c /etc/ulatencyd/ulatencyd.conf -r /etc/ulatencyd/rules/ -v 3"

ULATENCYD_OPTS="-d"