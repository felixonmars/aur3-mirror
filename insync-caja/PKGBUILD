# Maintainer: Zhengyu Xu <xzy3186@gmail.com>

pkgname=insync-caja
pkgver=1.0.32
pkgrel=1
pkgdesc="Caja integration for insync"
url="https://support.insynchq.com/forums/discussion/3270/new-insync-version-1-0-32"
license=('custom:insync')
options=(!strip)
arch=('any')

depends=("insync" "python2-caja")
source=("${pkgname}-${pkgver}.deb::http://s.insynchq.com/builds/${pkgname}_${pkgver}.31767-precise_all.deb")
sha256sums=("ad6f0fa916ec00a071cd12a27d129960ddb19c41475fdbede0dc967f41afb603")
noextract=("${pkgname}-${pkgver}.deb")

package() {
   cd $srcdir
   ar x ${pkgname}-${pkgver}.deb
   tar xvf data.tar.gz
   cp -rp usr $pkgdir
}
