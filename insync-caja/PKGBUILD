# Maintainer: Zhengyu Xu <xzy3186@gmail.com>

pkgname=insync-caja
pkgver=1.0.29
pkgrel=1
pkgdesc="Caja integration for insync"
url="https://support.insynchq.com/forums/discussion/2789/new-insync-version-1-0-29/p1"
license=('custom:insync')
options=(!strip)
arch=('any')

depends=("insync" "python2-caja")
source=("${pkgname}-${pkgver}.deb::http://s.insynchq.com/builds/${pkgname}_${pkgver}.31750_all.deb")
sha256sums=("7b308194deb6f329e441374f71996a27d07ce20d67b39a78c7225213ff2aa1df")
noextract=("${pkgname}-${pkgver}.deb")

package() {
   cd $srcdir
   ar x ${pkgname}-${pkgver}.deb
   tar xvf data.tar.gz
   cp -rp usr $pkgdir
}
