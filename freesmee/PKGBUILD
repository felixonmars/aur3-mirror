pkgname=freesmee
pkgver=0.7
_ticppver=2.5.3
pkgrel=1
pkgdesc="Freesmee is a software that sends SMS and MMS through internet"
url="http://www.freesmee.com/"
license=('other')
arch=('i686' 'x86_64')
depends=('qt' 'curl' 'p7zip' 'gtk2')
conflicts=('jacksms' 'jacksms-desktop')
_arch=i386
[ "$CARCH" = "x86_64" ] && _arch=amd64

source=(http://download.opensuse.org/repositories/home:/${pkgname}/xUbuntu_11.04/${_arch}/${pkgname}_${pkgver}_${_arch}.deb
http://download.opensuse.org/repositories/home:/${pkgname}/xUbuntu_11.04/${_arch}/libticpp-dev_${_ticppver}_${_arch}.deb)

md5sums=('90d0cbf5df3d4128fa9e0a3d6d37e352'
         'efbd1f23617d5b38b4051e7ddfc83f57')


[ "$CARCH" = "x86_64" ] && md5sums=('82299b3683646876e09e16eabbf523cf' 'bf4d3f5b384aa95276b843671bd4caa3')

build() {
    ar vx  ${pkgname}_${pkgver}_${_arch}.deb
    tar xvzf data.tar.gz
    rm data.tar.gz 
    rm control.tar.gz

    ar vx  libticpp-dev_${_ticppver}_${_arch}.deb
    tar xvzf data.tar.gz
    rm data.tar.gz 
    rm control.tar.gz

    cp -R usr $pkgdir
   
} 
