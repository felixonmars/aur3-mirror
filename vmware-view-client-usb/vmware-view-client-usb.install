post_install() {
	echo " > To enable USB redirection you have to launch"
	echo " > /usr/lib/systemd/scripts/vmware-view-usbd or start/enable"
	echo " > systemd service vmware-view-usbd (systemctl start vmware-view-usbd)."
}

post_upgrade(){
	post_install
}
