--- ./source/scanner.c.~1~	2008-10-03 01:14:02.000000000 +0200
+++ ./source/scanner.c	2008-11-14 05:03:12.000000000 +0100
@@ -202,11 +202,11 @@
   v++;
   while (u != NULL) {
     while (v[0] != NULL_CHAR) {
-      if (whether_bold (u, v, "COMMENT") && delim == BOLD_COMMENT_SYMBOL) {
+      if (whether_bold (u, v, "COMMENT") || whether_bold (u, v, "TNEMMOC") && delim == BOLD_COMMENT_SYMBOL) {
         *top = u;
         *ch = &v[1];
         return (A68_TRUE);
-      } else if (whether_bold (u, v, "CO") && delim == STYLE_I_COMMENT_SYMBOL) {
+      } else if (whether_bold (u, v, "CO") || whether_bold (u, v, "OC") && delim == STYLE_I_COMMENT_SYMBOL) {
         *top = u;
         *ch = &v[1];
         return (A68_TRUE);
