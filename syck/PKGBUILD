pkgname=syck
pkgver=0.55
pkgrel=1
pkgdesc="Syck is an extension for reading and writing YAML swiftly in popular scripting languages."
url="http://whytheluckystiff.net/syck/"
license=""
depends=()
makedepends=()
conflicts=()
replaces=()
backup=()
install=
source=(http://rubyforge.org/frs/download.php/4492/$pkgname-$pkgver.tar.gz)
md5sums=('a57b7c46d81170b9318e2f384f77910c')

build() {
  cd $startdir/src/$pkgname-$pkgver
  ./configure --prefix=/usr
  make || return 1
  make check || return 1
  make DESTDIR=$startdir/pkg install
#  cd $startdir/src/$pkgname-$pkgver/ext/python/
#  python setup.py build
#  python setup.py bdist_dumb -k || return 1
#  cd $startdir/pkg/
#  tar zxf $startdir/src/$pkgname-$pkgver/ext/python/dist/Syck-1.0.linux-i686.tar.gz
}
