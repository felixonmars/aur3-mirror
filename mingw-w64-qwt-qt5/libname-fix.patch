--- a/branches/qwt-6.1/qwtfunctions.pri
+++ b/branches/qwt-6.1/qwtfunctions.pri
@@ -41,26 +41,26 @@
 
     mac:contains(QWT_CONFIG, QwtFramework) {
 
-        LINKAGE = -framework $${LIB_NAME}$${QT_LIBINFIX}
+        LINKAGE = -framework $${LIB_NAME}
     }
 
     isEmpty(LINKAGE) {
 
         if(!debug_and_release|build_pass):CONFIG(debug, debug|release) {
 
-            mac:LINKAGE = -l$${LIB_NAME}$${QT_LIBINFIX}_debug
-            win32:LINKAGE = -l$${LIB_NAME}$${QT_LIBINFIX}d
+            mac:LINKAGE = -l$${LIB_NAME}_debug
+            win32:LINKAGE = -l$${LIB_NAME}d
         }
     }
 
     isEmpty(LINKAGE) {
 
-        LINKAGE = -l$${LIB_NAME}$${QT_LIBINFIX}
+        LINKAGE = -l$${LIB_NAME}
     }
 
     !isEmpty(QMAKE_LSB) {
 
-        QMAKE_LFLAGS *= --lsb-shared-libs=$${LIB_NAME}$${QT_LIBINFIX}
+        QMAKE_LFLAGS *= --lsb-shared-libs=$${LIB_NAME}
     }
 
     LIBS += $$LINKAGE
