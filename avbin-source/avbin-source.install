post_install() {
  /bin/cat << EOF

===> avbin-source post-install message:

===> avbin-source is built against ffmpeg sources
===> matching the revision currently installed.
===> Please reinstall avbin-source when ffmpeg is
===> upgraded to avoid any compatibility issues.

EOF
}
