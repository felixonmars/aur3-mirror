pkgname=fortune-mod-kylexy
pkgver=20130515
pkgrel=2
_ver=3
pkgdesc="Fortune quotes from Kyle XY"
arch=('any')
license=('custom:cc-sa-3.0')
url=("http://en.wikiquote.org/wiki/Kyle_XY")
depends=('fortune-mod')
makedepends=('fortune-mod' 'make' 'coreutils' 'sed' 'grep' 'wikiquote-fortune>=1.3')
source=("https://github.com/maandree/wikiquote-fortune-collection/archive/${_ver}.tar.gz")
sha256sums=(f5a89d3e0c642a932a5755d59896a1d0a8039c717759f156eb2c049ff8fb4596)


build()
{
    cd "${srcdir}/wikiquote-fortune-collection-${_ver}"
    make SHOWS=kylexy SCRIPTS=
}

package()
{
    cd "${srcdir}/wikiquote-fortune-collection-${_ver}"
    cd quotes/kylexy
    install -dm755 -- "${pkgdir}/usr/share/fortune"
    install -m644 -- kylexy kylexy.dat "${pkgdir}/usr/share/fortune"
}

