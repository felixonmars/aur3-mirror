post_install() {
	echo "Enable the required modules in /etc/php/php.ini"
	echo "Don't forget to set the required string in .htaccess"
	echo "       DirectoryIndex index.html index.php /srv/http/h5ai/server/php/index.php"
}

post_remove() {
	echo "Disable the php modules in /etc/php/php.ini"
}
