#!/bin/bash

LIBDIR="@LIBDIR@"

if [[ -e "$LIBDIR/libv4l/v4l2convert.so" ]]; then
	export LD_PRELOAD="${LD_PRELOAD:+$LD_PRELOAD:}$LIBDIR/libv4l/v4l2convert.so"
fi

xhost +local:
exec gksudo -u _skype "$LIBDIR/skype/skype" "$@"
