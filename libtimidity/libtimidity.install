post_install() {

cat << _EOF
    You need to install and configure compatible sound fonds for libtimidity to works.
      - Install your sound found (timidity-freepats)
      - Copy the configuration file to /etc/timidity.cfg (cp /etc/timidity++/timidity-freepats.cfg /etc/timidity.cfg)
      - Try to run make check in libtimidity sources if you want to validate that it works
_EOF

}
