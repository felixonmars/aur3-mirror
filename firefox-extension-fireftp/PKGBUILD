# Maintainer: Tianjiao Yin <ytj000+aur@gmail.com>

_plugin_name=fireftp
_plugin_version=2.0
_plugin_id=684
pkgdesc="Plugin for firefox. A free, secure, cross-platform FTP/SFTP client for Mozilla Firefox."
license=('MPL')

pkgname=firefox-extension-$_plugin_name
pkgver=$_plugin_version
pkgrel=1
arch=('any')
url="https://addons.mozilla.org/firefox/addon/$_plugin_id"
depends=("firefox>=8.0" "firefox<11.0")
source=("https://addons.mozilla.org/firefox/downloads/file/137964/fireftp-2.0-fx.xpi")

build() {
  cd $srcdir
  emid=$(sed -n '/.*<em:id>\(.*\)<\/em:id>.*/{s//\1/p;q}' install.rdf) || return 1
  local dstdir=$pkgdir/usr/lib/firefox/extensions/${emid}
  install -d $dstdir
  sed -i 's#<em:maxVersion>.*</em:maxVersion>#<em:maxVersion>10.*</em:maxVersion>#' install.rdf
  rm *.xpi
  mv * $dstdir
}
md5sums=('d58a0670b4ee18f82e94b082bdfb79ff')
