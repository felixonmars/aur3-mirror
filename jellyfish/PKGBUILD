# Maintainer:  Eric Biggers <ebiggers3 at gmail dot com>

pkgname=jellyfish
pkgver=2.1.1
pkgrel=1
pkgdesc="A tool for fast, memory-efficient counting of k-mers in DNA"

# Note: according to the README, "Support for 32-bits platform has not been
# tested."
arch=("x86_64")
url="http://www.genome.umd.edu/jellyfish.html"

# Note: the source tree includes another license file ("HalfLicense") but the
# code to which it supposedly applies no longer exists in the source tree.
license=("GPL3")
source=("ftp://ftp.genome.umd.edu/pub/jellyfish/jellyfish-${pkgver}.tar.gz")

build() {
    cd "${pkgname}-${pkgver}"
    ./configure --prefix=/usr
    make
}

package() {
    cd "${pkgname}-${pkgver}"
    make DESTDIR=${pkgdir} install
}
sha1sums=('d283b73ffd82b962529512ad81d3c301ae5593ef')
