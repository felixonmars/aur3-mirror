# Maintainer: Hugo Doria <ly50247@gmail.com>

pkgname=nmap-nogui
pkgver=5.21
pkgrel=1
pkgdesc="A network exploration tool and security/port scanner, without zenmap"
_realname=nmap
arch=('i686' 'x86_64')
url="http://nmap.org"
license=('GPL')
depends=('pcre' 'openssl' 'libpcap>=1.0.0' 'lua')
options=('!makeflags')
provides=('nmap')
conflicts=('nmap')
source=(http://nmap.org/dist/${_realname}-${pkgver}.tar.bz2)

build() {
  cd ${srcdir}/${_realname}-${pkgver}

  ./configure --without-zenmap --prefix=/usr --mandir=/usr/share/man \
    --libexecdir=/usr/lib --with-openssl=/usr/include/openssl --with-libpcap=/usr/include/pcap
  make || return 1
  make DESTDIR="${pkgdir}" install

  # install GPL2 license
  install -D -m644 COPYING ${pkgdir}/usr/share/licenses/${_realname}/LICENSE
}
md5sums=('f77fa51d89ab27d35e5cd87bb086b858')
