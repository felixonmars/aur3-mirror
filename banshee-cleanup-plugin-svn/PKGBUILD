# Maintainer: cdude <cdude996@hotmail.com>
pkgname=banshee-cleanup-plugin-svn
pkgver=5
pkgrel=1
pkgdesc="Cleanup the song library in Banshee"
url="http://banshee-project.org/Plugins"
license="MIT"
depends=('banshee-svn')
makedepends=('subversion')
conflicts=('banshee-cleanup-plugin')
provides=('banshee-cleanup-plugin')
arch=('i686')

_svntrunk=http://banshee-cleanup-plugin.googlecode.com/svn/trunk
_svnmod=banshee-cleanup-plugin
source=()
md5sums=()


build() {
  export MONO_SHARED_DIR=$startdir/src/.wabi
  mkdir -p $MONO_SHARED_DIR $startdir/pkg/usr/bin $startdir/pkg/usr/lib $startdir/pkg/usr/share

  cd $startdir/src

  svn co $_svntrunk --config-dir ./ -r $pkgver $_svnmod
  cd $_svnmod
  ./autogen.sh --enable-user-plugins --prefix=/usr


  msg "SVN checkout done or server timeout"
  msg "Starting make..."

  cp -r ../$_svnmod ../$_svnmod-build
  cd ../$_svnmod-build

  make || return 1
  make DESTDIR=$startdir/pkg/ install

  rm -rf $MONO_SHARED_DIR
  rm -rf $startdir/src/$_svnmod-build
}
