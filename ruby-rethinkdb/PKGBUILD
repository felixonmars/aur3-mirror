# Generated by gem2arch (https://github.com/anatol/gem2arch)
# Maintainer: Anatol Pomozov <anatol dot pomozov at gmail dot com>

_gemname=rethinkdb
pkgname=ruby-$_gemname
pkgver=1.11.0.1
pkgrel=1
pkgdesc='This package provides the Ruby driver library for the RethinkDB database server.'
arch=(any)
url='http://rethinkdb.com'
license=(Apache-2)
depends=(ruby ruby-json ruby-ruby-protocol-buffers)
options=(!emptydirs)
source=(https://rubygems.org/downloads/$_gemname-$pkgver.gem)
noextract=($_gemname-$pkgver.gem)
sha1sums=(2cd18321d55779d33d815d096ffb975917617852)

package() {
  local _gemdir="$(ruby -e'puts Gem.default_dir')"
  gem install --ignore-dependencies --no-user-install -i "$pkgdir/$_gemdir" -n "$pkgdir/usr/bin" $_gemname-$pkgver.gem
  rm "$pkgdir/$_gemdir/cache/$_gemname-$pkgver.gem"
}
