post_install() {
  cat << MSG

===> Cronwhip Usage

  To run cronwhip at startup, add "cronwhipd" to your DAEMONS array in
  /etc/rc.conf. If you prefer to have a 5 minute delay, then add the following
  line to /etc/rc.local instead:

    (sleep 300 && /usr/bin/cronwhip) &

MSG
}

# vim:set ts=2 sw=2 et:
