# Contributor: Andreas B. Wagner <AndreasBWagner@gmail.com>
# Contributor: N30N <archlinux@alunamation.com>

pkgname="srw"
pkgver=0.6
pkgrel=2
pkgdesc="A simple read wrapper."
url="http://deepcube.net/code/srw/"
license=("MIT")
arch=("i686" "x86_64")
depends=()
makedepends=()
source=("http://deepcube.net/download/${pkgname}-${pkgver}.tar.gz")
md5sums=("6abe525794e7d849eadf638f3cda5bd9")

build() {
  cd "${pkgname}-${pkgver}"
  make
}

package() {
  cd "${pkgname}-${pkgver}"
  make PREFIX="/usr" DESTDIR="${pkgdir}" install
  install -D -m644 LICENSE \
    "${pkgdir}/usr/share/licenses/${pkgname}/license.txt"
}

# vim:set ts=2 sw=2 et:
