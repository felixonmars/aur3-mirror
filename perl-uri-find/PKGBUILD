# CPAN Name  : URI-Find
# Contributor: Caleb Cushing <xenoterracide@gmail.com>
# Generator  : CPANPLUS::Dist::Arch 1.04
# Template @ http://github.com/xenoterracide/AURpan/blob/master/perl-uri-find/PKGBUILD.tt
# File bugs @ http://github.com/xenoterracide/AURpan/issues

pkgname='perl-uri-find'
pkgver='20100505'
pkgrel='1'
pkgdesc="Find URIs in plain text"
arch=('any')
url='http://search.cpan.org/dist/URI-Find'
license=('PerlArtistic' 'GPL')
depends=('perl' 'perl-uri>=1.00')


options=('!emptydirs')

source=('http://search.cpan.org/CPAN/authors/id/M/MS/MSCHWERN/URI-Find-20100505.tar.gz')
md5sums=('d470e33f31986839cd49a77a08133520')

build() {
  DIST_DIR="${srcdir}/URI-Find-20100505"
  export PERL_MM_USE_DEFAULT=1 PERL5LIB=""                 \
    PERL_AUTOINSTALL=--skipdeps                            \
    PERL_MM_OPT="INSTALLDIRS=vendor DESTDIR='$pkgdir'"     \
    PERL_MB_OPT="--installdirs vendor --destdir '$pkgdir'" \
    MODULEBUILDRC=/dev/null
  {
	cd "$DIST_DIR" &&
    perl Build.PL &&
    perl Build &&
    perl Build test &&
    perl Build install;
  } || return 1;

  find "$pkgdir" -name .packlist -o -name perllocal.pod -delete

}

