--- tests/test_spelling.py.old
+++ tests/test_spelling.py
@@ -174,6 +174,8 @@
     fname = "english-words.10.gz"
     if testdir in files:
         path = os.path.join(testdir, fname)
+        if not os.path.exists(path):
+            return
     elif fname in files:
         path = fname
     else:
