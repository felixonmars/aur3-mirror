# Generated by Xyne::Arch::CPAN 0.07

pkgname=perl-marc-record
pkgver=2.0.2
pkgrel=1
pkgdesc="Perl extension for handling MARC records"
arch=('i686' 'x86_64')
url="http://search.cpan.org/dist/MARC-Record/"
license=('unknown')
source=('http://search.cpan.org/CPAN/authors/id/G/GM/GMCHARLT/MARC-Record-2.0.2.tar.gz')
md5sums=('4fdd863c6aa3ea2fc2c9c7329aaba734')
sha256sums=('add49bda73fc5e0d0e5d84e2c477f1fec87f806b452b8f832b03ca8b6f148e94')
makedepends=('perl-extutils-makemaker')
provides=('perl-marc-batch' 'perl-marc-field' 'perl-marc-file' 'perl-marc-file-encode' 'perl-marc-file-microlif' 'perl-marc-file-usmarc')
options=(!emptydirs)

build() {
  _dir=$(find $srcdir -maxdepth 2 -type f -name 'Makefile.PL')
  if [ ! -z "$_dir" ]; then
    cd $(dirname "$_dir")
    PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1
    make  || return 1
    make install DESTDIR="${pkgdir}" || return 1

  else
  _dir=$(find $srcdir -maxdepth 2 -type f -name 'Build.PL')
  if [ ! -z "$_dir" ]; then
    cd $(dirname "$_dir")
    PERL_MM_USE_DEFAULT=1 perl Build.PL INSTALLDIRS=vendor || return 1
    ./Build  || return 1
    ./Build install destdir=${pkgdir} || return 1

  else
    echo "error: failed to detect build method for $pkgname"
    echo "you may be able to fix this by editing the PKGBUILD"
    return 1
  fi fi

  # remove perllocal.pod and .packlist
  find ${pkgdir} -name perllocal.pod -delete
  find ${pkgdir} -name .packlist -delete
}

# vim:set ts=2 sw=2 et:

