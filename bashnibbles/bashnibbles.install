post_install() {
  echo "==> BashNibbles control keys:"
  echo "  -> Up:    k"
  echo "  -> Down:  j"
  echo "  -> Left:  h"
  echo "  -> Right: l"
  echo "  -> Quit:  q"
  echo "==> Note for non-vi users:"
  echo "  -> These movement keys may feel awkward for you."
  echo "  -> You can easily change the keys (e.g. 'wasd') by editing the script."
}
