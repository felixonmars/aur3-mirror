post_install() {
echo	Edit /etc/conf.d/sasc-ng and cardclient.conf before using!
echo	Don't forget to install sascng-kernel26-patch from aur also!
echo	Otherwise it's unusable
    depmod -a
}
post_upgrade() {
    depmod -a
}
post_remove() {
    depmod -a
}
