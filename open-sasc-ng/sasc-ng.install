post_install() {

echo	You MUST also do the following to allow this package to work:
echo	Edit /etc/conf.d/sasc-ng and /etc/cardclient.conf
echo	Install sascng-linux3-patch from AUR

    depmod -a
}
post_upgrade() {
    depmod -a
}
post_remove() {
    depmod -a
}
