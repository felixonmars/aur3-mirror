# Maintainer hashstat <hashstat .AT. yahoo .DOT. com>

pkgname=firefox-gnome-keyring-git
pkgver=20141015
pkgrel=1
pkgdesc="Firefox GNOME Keyring integration"
arch=(any)
url='https://github.com/swick/moz-gnome-keyring-integration'
license=(GPL)
depends=(firefox libgnome-keyring)
makedepends=(git)
provides=(firefox-gnome-keyring)
conflicts=(firefox-gnome-keyring)
source=('git+https://github.com/swick/moz-gnome-keyring-integration')
md5sums=(SKIP)

package() {
  cd "$srcdir/moz-gnome-keyring-integration"

	extdir="$pkgdir/usr/lib/firefox/browser/extensions/gnome-keyring-integration@sebastianwick.net"
	install -d "$extdir"
	for file in install.rdf chrome.manifest {components,content,defaults/preferences}/*.js; do
	  install -m 0644 -D "$file" "$extdir/$file"
  done
}

# vim:set ts=2 sw=2 et:
