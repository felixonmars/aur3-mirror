--- systemd-35/src/fsck.c.orig	2011-09-13 01:02:45.023644673 +0300
+++ systemd-35/src/fsck.c	2011-09-13 01:03:03.103645282 +0300
@@ -124,12 +124,10 @@
                         arg_skip = true;
                 else if (startswith(w, "fsck.mode"))
                         log_warning("Invalid fsck.mode= parameter. Ignoring.");
-#if defined(TARGET_FEDORA) || defined(TARGET_MANDRIVA)
                 else if (strneq(w, "fastboot", l))
                         arg_skip = true;
                 else if (strneq(w, "forcefsck", l))
                         arg_force = true;
-#endif
         }
 
         free(line);
