# Maintainer: Alexander Fehr <pizzapunk gmail com>

pkgname=firefox-adblock-plus
pkgver=1.3.3
_firefoxver=3.6
pkgrel=1
pkgdesc="Firefox add-on which blocks ads and banners"
arch=('any')
url="http://adblockplus.org/"
license=('MPL')
depends=('firefox')
source=(http://releases.mozilla.org/pub/mozilla.org/addons/1865/adblock_plus-$pkgver-fn+fx+sm+tb.xpi)
md5sums=('63e2145d117848e44376787fd3ab6c3d')

build() {
  cd $srcdir

  emid=$(sed -n '/.*<em:id>\(.*\)<\/em:id>.*/{s//\1/p;q}' install.rdf)

  install -d $pkgdir/usr/lib/firefox-$_firefoxver/extensions/$emid
  cp -a * $pkgdir/usr/lib/firefox-$_firefoxver/extensions/$emid

  rm -f $pkgdir/usr/lib/firefox-$_firefoxver/extensions/$emid/*.xpi
  chmod -R -x+X $pkgdir/usr/lib/firefox-$_firefoxver/extensions/$emid
}
