# Maintainer: Martin Hundebøll <martin@hundeboll.net>
pkgname=gotty
pkgver=1.0
pkgrel=1
pkgdesc="Go TTY - The Really Simple TTY Terminal Application"
url="http://gotty.io"
arch=('x86_64' 'i686')
license=('GPLv2')
conflicts=('gotty-git')
source=("https://github.com/gotty/gotty/releases/download/v$pkgver/$pkgname-$pkgver.tar.xz")
sha256sums=("4336af6bfcdd940a2eefcee743c5dcc9f664cd06553f00b9f5b0f591e19834f2")

build() {
  cd "$srcdir/$pkgname-$pkgver"
  ./configure --prefix=/usr
  make
}

package() {
  cd "$srcdir/$pkgname-$pkgver"
  make DESTDIR="$pkgdir" install
}

# vim:set ts=2 sw=2 et:
