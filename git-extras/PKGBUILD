# Maintainer: Thomas Jost <schnouki+aur@schnouki.net>
pkgname=git-extras
pkgver=0.7.0
_pkgtag=035668c
pkgrel=1
pkgdesc="GIT utilities -- repo summary, commit counting, repl, changelog population and more"
arch=(any)
url="http://github.com/visionmedia/git-extras"
license=('unknown')
depends=('git' 'bash')
makedepends=('ronn')
source=(https://github.com/visionmedia/${pkgname}/tarball/${pkgver})
md5sums=('0bfc661b27dac3c024f8782850323dc5')
sha1sums=('03fd10ad4b1b3786912a77bdb32c30973507dc6a')

build() {
  cd "$srcdir/visionmedia-$pkgname-$_pkgtag"
  make docs
}

package() {
  cd "$srcdir/visionmedia-$pkgname-$_pkgtag"

  install -dm 755 "$pkgdir/usr/bin"
  make PREFIX="$pkgdir/usr" install
}

# vim:set ts=2 sw=2 et:
