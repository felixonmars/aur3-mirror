diff -aur ll-scope-0.2.1/Makefile ll-scope-0.2.1-modified/Makefile
--- ll-scope-0.2.1/Makefile	2005-05-31 12:31:25.000000000 +0200
+++ ll-scope-0.2.1-modified/Makefile	2009-04-22 16:40:29.000000000 +0200
@@ -1,4 +1,4 @@
-INSTALL_DIR=/usr/local/lib/dssi
+INSTALL_DIR=/usr/lib/dssi
 
 all:	ll-scope.so ll-scope_gtk
 
@@ -16,7 +16,7 @@
 	rm -f ll-scope.so ll-scope_gtk *.o
 
 install:	ll-scope.so ll-scope_gtk
-	mkdir -p $(INSTALL_DIR)
-	cp -f ll-scope.so $(INSTALL_DIR)
-	mkdir -p $(INSTALL_DIR)/ll-scope
-	cp -f ll-scope_gtk ll-scope.glade pixmaps/icon.png $(INSTALL_DIR)/ll-scope/
+	mkdir -p $(PREFIX)$(INSTALL_DIR)
+	install -m 755 ll-scope.so $(PREFIX)$(INSTALL_DIR)
+	mkdir -p $(PREFIX)$(INSTALL_DIR)/ll-scope
+	install -m 755 ll-scope_gtk ll-scope.glade pixmaps/icon.png $(PREFIX)$(INSTALL_DIR)/ll-scope/
diff -aur ll-scope-0.2.1/dssiuiclient.hpp ll-scope-0.2.1-modified/dssiuiclient.hpp
--- ll-scope-0.2.1/dssiuiclient.hpp	2005-05-31 12:31:25.000000000 +0200
+++ ll-scope-0.2.1-modified/dssiuiclient.hpp	2009-04-22 15:53:55.000000000 +0200
@@ -33,7 +33,6 @@
 
 
 using namespace Glib;
-using namespace sigc;
 using namespace std;
 
 
@@ -89,13 +88,13 @@
   // Host to UI
   /** This signal is emitted when the host sends a new control value.
       The parameters are the control port number and the new control value. */
-  signal<void, int, float> control_received;
+  sigc::signal<void, int, float> control_received;
   /** Emitted when the host sends a program change. The parameters are the 
       bank and program numbers. */
-  signal<void, int, int> program_received;
+  sigc::signal<void, int, int> program_received;
   /** Emitted when the host sends a configuration value. The parameters are
       the configuration key and the configuration value. */
-  signal<void, const string, const string> configure_received;
+  sigc::signal<void, const string, const string> configure_received;
   /** Emitted when the host wants the UI to be visible. A DSSI GUI should not
       show any windows until this signal is emitted. */
   Dispatcher show_received;
@@ -124,7 +123,7 @@
   void* allocate_shared_memory(int bytes);
   
   /** Emitted when the plugin has attached to the shared memory segment. */
-  signal<void> plugin_attached;
+  sigc::signal<void> plugin_attached;
   
   /** Returns true if the plugin has attached to the shared memory segment. */
   bool plugin_has_attached();
diff -aur ll-scope-0.2.1/ringbuffer.c ll-scope-0.2.1-modified/ringbuffer.c
--- ll-scope-0.2.1/ringbuffer.c	2005-05-31 12:31:25.000000000 +0200
+++ ll-scope-0.2.1-modified/ringbuffer.c	2009-04-22 16:05:58.000000000 +0200
@@ -23,7 +23,7 @@
 /* #include <stdio.h> */
 
 #include "ringbuffer.h"
-
+#include <string.h>
 
 void ringbuf_init(ringbuf_t* rb, int atom_size, int size) {
   rb->read_pos = 0;
diff -aur ll-scope-0.2.1/ringbuffer.h ll-scope-0.2.1-modified/ringbuffer.h
--- ll-scope-0.2.1/ringbuffer.h	2005-05-31 12:31:25.000000000 +0200
+++ ll-scope-0.2.1-modified/ringbuffer.h	2009-04-22 16:04:40.000000000 +0200
@@ -20,6 +20,8 @@
 
 ****************************************************************************/
 
+#include <stdlib.h>
+
 #ifndef RINGBUFFER_H
 #define RINGBUFFER_H
 
diff -aur ll-scope-0.2.1/scopewidget.hpp ll-scope-0.2.1-modified/scopewidget.hpp
--- ll-scope-0.2.1/scopewidget.hpp	2005-05-31 12:31:25.000000000 +0200
+++ ll-scope-0.2.1-modified/scopewidget.hpp	2009-04-22 15:54:04.000000000 +0200
@@ -59,7 +59,7 @@
   void clear();
   
   // signals
-  signal<void> middle_button_pressed;
+  sigc::signal<void> middle_button_pressed;
   
 private:
   
