--- common/libBuffer/include/CommonCharacterBuffer.h  2011-08-14 19:44:25.366995898 +0200
+++ common/libBuffer/include/CommonCharacterBuffer.h	2011-08-14 19:44:33.493618539 +0200
@@ -94,7 +94,7 @@
 		}
 
 		// Check if the buffer size is large enough
-		COLLADABU_ASSERT(getBytesAvailable() >= maxIntLength);
+//		COLLADABU_ASSERT(getBytesAvailable() >= maxIntLength);
 		if ( getBytesAvailable() < maxIntLength )
 		{
 			//No chance to convert the double with this buffer
