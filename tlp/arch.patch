diff -rupN orig//Makefile arch//Makefile
--- orig//Makefile	2011-03-16 20:52:58.000000000 +0100
+++ arch//Makefile	2011-04-24 12:38:00.676102617 +0200
@@ -26,12 +26,14 @@ install:
 	install -m 755 tlp-rf-func $(TLIB)/
 	install -m 755 tlp-nop $(TLIB)/
 	[ -f $(DESTDIR)/etc/default/tlp ] || install -m 644 default $(DESTDIR)/etc/default/tlp
-	install -m 755 tlp.init $(DESTDIR)/etc/init.d/tlp
-	install -m 755 tlp-ifup $(DESTDIR)/etc/network/if-up.d/tlp-ifup
+	install -m 755 tlp.init $(DESTDIR)/etc/rc.d/tlp
+	#install -m 755 tlp-ifup $(DESTDIR)/etc/network/if-up.d/tlp-ifup
 	install -m 755 zztlp $(PLIB)/power.d/zztlp
 	install -m 755 49wwan $(PLIB)/sleep.d/49wwan
 	install -m 755 49bay $(PLIB)/sleep.d/49bay
 	install -m 644 tlp.desktop $(DESTDIR)/etc/xdg/autostart/tlp.desktop
+	install -m 755 post_install $(TLIB)/
+	install -m 755 pre_remove $(TLIB)/
 
 uninstall: 
 	rm $(SBIN)/tlp
@@ -45,9 +47,11 @@ uninstall:
 	rm $(TLIB)/tlp-rf-func
 	rmdir $(TLIB)
 #	rm $(DESTDIR)/etc/default/tlp
-	rm $(DESTDIR)/etc/init.d/tlp
-	rm $(DESTDIR)/etc/network/if-up.d/tlp-ifup
+	rm $(DESTDIR)/etc/rc.d/tlp
+#	rm $(DESTDIR)/etc/network/if-up.d/tlp-ifup
 	rm $(PLIB)/power.d/zztlp
 	rm $(PLIB)/sleep.d/49wwan
 	rm $(PLIB)/sleep.d/49bay
 	rm $(DESTDIR)/etc/xdg/autostart/tlp.desktop
+	rm $(TLIB)/post_install
+	rm $(TLIB)/pre_remove
diff -rupN orig//post_install arch//post_install
--- orig//post_install	1970-01-01 01:00:00.000000000 +0100
+++ arch//post_install	2011-04-24 10:38:48.216464806 +0200
@@ -0,0 +1,9 @@
+#!/bin/sh
+# Disable conflicting pm-utils hooks
+for i in 95hdparm-apm disable_wol hal-cd-polling intel-audio-powersave laptop-mode \
+         journal-commit pcie_aspm sata_alpm sched-powersave wireless \
+         xfs_buffer; do
+    if [ -x /usr/lib/pm-utils/power.d/$i ]; then
+        ln -sf /usr/lib/tlp-pm/tlp-nop /etc/pm/power.d/$i
+    fi
+done
diff -rupN orig//pre_remove arch//pre_remove
--- orig//pre_remove	1970-01-01 01:00:00.000000000 +0100
+++ arch//pre_remove	2011-04-24 10:47:30.376438309 +0200
@@ -0,0 +1,9 @@
+#!/bin/sh
+# Remove pm-utils hook disablers
+for i in 95hdparm-apm disable_wol hal-cd-polling intel-audio-powersave laptop-mode \
+         journal-commit pcie_aspm sata_alpm sched-powersave wireless \
+         xfs_buffer; do
+    if [ "$(readlink /etc/pm/power.d/$i)" = "/usr/lib/tlp-pm/tlp-nop" ]; then
+        rm /etc/pm/power.d/$i
+    fi
+done
diff -rupN orig//tlp-functions arch//tlp-functions
--- orig//tlp-functions	2011-03-13 19:31:28.000000000 +0100
+++ arch//tlp-functions	2011-04-23 22:08:17.000000000 +0200
@@ -8,7 +8,7 @@ DEFAULT_FILE=/etc/default/tlp
 ACPWR=/usr/bin/on_ac_power
 ETHTOOL=/usr/sbin/ethtool
 HDPARM=/sbin/hdparm
-IWC=/sbin/iwconfig
+IWC=/usr/sbin/iwconfig
 MODPRO=/sbin/modprobe
 TPMODULES="thinkpad_acpi tp_smapi"
 UDEVADM=/sbin/udevadm
diff -rupN orig//tlp-ifup arch//tlp-ifup
--- orig//tlp-ifup	2011-02-01 16:09:01.000000000 +0100
+++ arch//tlp-ifup	2011-04-23 22:09:06.000000000 +0200
@@ -1,6 +1,6 @@
 #!/bin/sh
 # tlp - ifup hook
-IWC=/sbin/iwconfig
+IWC=/usr/sbin/iwconfig
 TLP=/usr/sbin/tlp
 
 [ -x $IWC ] || exit 0
diff -rupN orig//tlp-rf-func arch//tlp-rf-func
--- orig//tlp-rf-func	2011-03-03 22:42:49.000000000 +0100
+++ arch//tlp-rf-func	2011-04-23 22:12:33.000000000 +0200
@@ -3,7 +3,7 @@
 
 # ----------------------------------------------------------------------------
 # Definitions
-RFKILL="/sbin/rfkill"
+RFKILL="/usr/sbin/rfkill"
 
 # ----------------------------------------------------------------------------
 # Functions
