diff --git a/scripts/xdg-open b/scripts/xdg-open
index d48afb2..8b14c5e 100644
--- a/scripts/xdg-open
+++ b/scripts/xdg-open
@@ -111,6 +111,14 @@ open_generic_xdg_mime()
 
 open_generic()
 {
+    # mimeo can handle files and any type of uri (if configured)
+    if mimeo -h 2>/dev/null 1>&2; then
+        mimeo "$1"
+        if [ $? -eq 0 ]; then
+            exit_success
+        fi
+    fi
+
     # Paths or file:// URLs
     if (echo "$1" | grep -q '^file://' ||
         ! echo "$1" | egrep -q '^[a-zA-Z+\.\-]+:'); then
