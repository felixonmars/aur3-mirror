diff -urN dosbox.orig/src/hardware/gameblaster.cpp dosbox/src/hardware/gameblaster.cpp 
--- dosbox.orig/src/hardware/gameblaster.cpp 	2014-02-20 20:13:16.465532013 -0500
+++ dosbox/src/hardware/gameblaster.cpp 	2014-02-20 20:38:10.422747196 -0500
@@ -205,9 +205,9 @@
     {
 		switch (saa->noise_params[ch])
 		{
-		case 0: saa->noise[ch].freq = 31250.0 * 2; break;
-		case 1: saa->noise[ch].freq = 15625.0 * 2; break;
-		case 2: saa->noise[ch].freq =  7812.5 * 2; break;
+		case 0: saa->noise[ch].freq = 27966.0 * 2; break;
+		case 1: saa->noise[ch].freq = 13983.0 * 2; break;
+		case 2: saa->noise[ch].freq =  6991.5 * 2; break;
 		case 3: saa->noise[ch].freq = saa->channels[ch * 3].freq; break;
 		}
 	}
@@ -221,7 +221,7 @@
 		for (ch = 0; ch < 6; ch++)
 		{
             if (saa->channels[ch].freq == 0.0)
-                saa->channels[ch].freq = (double)((2 * 15625) << saa->channels[ch].octave) /
+                saa->channels[ch].freq = (double)((2 * 13983) << saa->channels[ch].octave) /
                     (511.0 - (double)saa->channels[ch].frequency);
 
             /* check the actual position in the square wave */
@@ -229,7 +229,7 @@
 			while (saa->channels[ch].counter < 0)
 			{
 				/* calculate new frequency now after the half wave is updated */
-				saa->channels[ch].freq = (double)((2 * 15625) << saa->channels[ch].octave) /
+				saa->channels[ch].freq = (double)((2 * 13983) << saa->channels[ch].octave) /
 					(511.0 - (double)saa->channels[ch].frequency);
 
 				saa->channels[ch].counter += sample_rate;
