# Maintainer: Moritz Maxeiner <moritz@ucworks.org>

pkgname=dub
pkgver=0.9.17
pkgrel=1
pkgdesc="Package manager for D packages"
arch=('i686' 'x86_64')
url="https://github.com/rejectedsoftware/dub"
license=('MIT')
depends=('dmd' 'curl')
source=(${url}/archive/v${pkgver}.tar.gz)
conflicts=('dub-git')
md5sums=('c92be31375f9095caa7af9f7328669a0')
sha256sums=('cb745996be1bc8a35a2ea3cda9e32ed4c23a096e694900df48f8e783bada8daf')


build()
{
  cd "${srcdir}/${pkgname}-${pkgver}"
  ./build.sh
}

package()
{
  cd "${srcdir}/${pkgname}-${pkgver}"
  install -D -m755 bin/dub "${pkgdir}/usr/bin/dub"
  install -D -m644 LICENSE.txt "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE.txt"
}
