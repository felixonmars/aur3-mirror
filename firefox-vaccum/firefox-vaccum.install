post_install() {

	echo
	echo "	Databases optimization is performed every 10 shutdowns"
	echo "	You can execute it manually using /usr/bin/firefox-vaccum as root"
	echo "	Manual call does not reset the shutdown count"
	echo
	echo "	Because you just installed firefox-vaccum, it will perform databases optimization at your next shutdown"
	echo

}
