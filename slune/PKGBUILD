# Maintainer: Eric Belanger <belanger@astro.umontreal.ca>
# Contributor: Patrik Ilg <p.ilg@arcor.de>

pkgname=slune
pkgver=1.0.15
pkgrel=1
pkgdesc="A 3D racing and car-crashing game written in Python"
url="http://home.gna.org/oomadness/en/slune"
license="GPL"
arch=('i686')
depends=('python>=2.2' 'python-opengl' 'glew' 'sdl' 'cal3d' 'pyogg')
source=(http://download.gna.org/$pkgname/Slune-$pkgver.tar.bz2)

build() {
  cd $startdir/src/Slune-$pkgver
  python setup.py build
  python setup.py install --root=$startdir/pkg
}
md5sums=('ce45fe4ef3d024f77e66c6b1cf6704a8')
