diff -daur t-engine4-src-1.0.0beta43.orig/src/core_lua.c t-engine4-src-1.0.0beta43/src/core_lua.c
--- t-engine4-src-1.0.0beta43.orig/src/core_lua.c	2012-11-27 11:40:25.001771163 +1100
+++ t-engine4-src-1.0.0beta43/src/core_lua.c	2012-11-27 11:41:47.546391434 +1100
@@ -35,6 +35,7 @@
 #include "main.h"
 #include "utf8proc.h"
 #include "useshader.h"
+#include <libgen.h>
 #include <math.h>
 #include <time.h>
 #include <png.h>
@@ -509,10 +510,15 @@
 	SDL_RWops *src = PHYSFSRWOPS_openRead(name);
 	if (!src)
 	{
-		return luaL_error(L, "could not load font: %s (%d)", name, size);
+		char *sysfont = NULL;
+		asprintf(&sysfont, "/usr/share/fonts/TTF/%s", basename(name));
+		*f = TTF_OpenFont(sysfont, size);
+		free(sysfont);
+	}
+	else
+	{
+		*f = TTF_OpenFontRW(src, TRUE, size);
 	}
-
-	*f = TTF_OpenFontRW(src, TRUE, size);
 
 	if (!*f)
 	{
