diff -aur -r orig/build/te4core.lua patched/build/te4core.lua
--- orig/build/te4core.lua	2013-04-21 16:16:58.000000000 +0200
+++ patched/build/te4core.lua	2013-08-04 22:10:50.810349518 +0200
@@ -96,7 +96,8 @@
 
 
 	configuration "linux"
-		libdirs {"/opt/SDL-2.0/lib/"}
+		includedirs {"/usr/include/SDL2"}
+		libdirs {""}
 		links { "dl", "SDL2", "SDL2_ttf", "SDL2_image", "png", "openal", "vorbisfile", "GL", "GLU", "m", "pthread" }
 		defines { [[TENGINE_HOME_PATH='".t-engine"']], 'SELFEXE_LINUX' }
 
diff -aur -r orig/premake4.lua patched/premake4.lua
--- orig/premake4.lua	2013-04-21 16:17:08.000000000 +0200
+++ patched/premake4.lua	2013-08-04 22:25:16.627001972 +0200
@@ -17,7 +17,7 @@
 		"src/physfs",
 		"src/zlib",
 		"src/bzip2",
-		"/opt/SDL-2.0/include/SDL2",
+		"/usr/include/SDL2",
 		"/usr/include/GL",
 	}
 	if _OPTIONS.lua == "default" then includedirs{"src/lua"}
@@ -40,7 +40,7 @@
 configuration "Debug"
 	defines { }
 	flags { "Symbols" }
-	buildoptions { "-ggdb" }
+	buildoptions { "-g" }
 	targetdir "bin/Debug"
 	if _OPTIONS.luaassert then defines {"LUA_USE_APICHECK"} end
 	if _OPTIONS.pedantic then buildoptions { "-Wall" } end
diff -aur -r orig/src/core_lua.c patched/src/core_lua.c
--- orig/src/core_lua.c	2013-04-21 16:17:03.000000000 +0200
+++ patched/src/core_lua.c	2013-08-04 21:21:17.587064000 +0200
@@ -34,6 +34,7 @@
 #include "zlib.h"
 #include "main.h"
 #include "useshader.h"
+#include <libgen.h>
 #include <math.h>
 #include <time.h>
 
@@ -513,10 +514,15 @@
 	SDL_RWops *src = PHYSFSRWOPS_openRead(name);
 	if (!src)
 	{
-		return luaL_error(L, "could not load font: %s (%d)", name, size);
+        char *sysfont = NULL;
+        asprintf(&sysfont, "/usr/share/fonts/TTF/%s", basename(name));
+        *f = TTF_OpenFont(sysfont, size);
+        free(sysfont);
 	}
-
-	*f = TTF_OpenFontRW(src, TRUE, size);
+    else
+    {
+        *f = TTF_OpenFontRW(src, TRUE, size);
+    }
 
 	if (!*f)
 	{
diff -aur -r orig/src/fov/fov.c patched/src/fov/fov.c
--- orig/src/fov/fov.c	2013-04-21 16:16:59.000000000 +0200
+++ patched/src/fov/fov.c	2013-08-04 21:26:33.867059000 +0200
@@ -578,6 +578,9 @@
 #define GET_BUFFER(target, buffer_data, len)                                                      \
     /* hurray, no branching, modulus, or malloc! */                                               \
     buffer_data->index = (buffer_data->index + buffer_data->prev_len) & (FOV_BUFFER_SIZE - 1);    \
+    if (buffer_data->index+len >= FOV_BUFFER_SIZE) {                                              \
+        buffer_data->index = 0;                                                                   \
+    }                                                                                             \
     buffer_data->prev_len = len;                                                                  \
     target = buffer_data->buffer + buffer_data->index;
 
