diff -rupN old/premake4.lua new/premake4.lua
--- old/premake4.lua	2014-05-19 10:42:14.000000000 +0200
+++ new/premake4.lua	2014-06-04 16:23:21.170727402 +0200
@@ -30,7 +30,7 @@ solution "TEngine"
 		}
 	else
 		includedirs {
-			"/opt/SDL-2.0/include/SDL2",
+			"/usr/include/SDL2",
 			"/usr/include/GL",
 		}
 	end
diff -rupN old/src/core_lua.c new/src/core_lua.c
--- old/src/core_lua.c	2014-05-19 10:42:14.000000000 +0200
+++ new/src/core_lua.c	2014-06-04 16:26:44.308920370 +0200
@@ -614,10 +614,15 @@ static int sdl_new_font(lua_State *L)
 	SDL_RWops *src = PHYSFSRWOPS_openRead(name);
 	if (!src)
 	{
-		return luaL_error(L, "could not load font: %s (%d)", name, size);
+		char *sysfont = NULL;
+		asprintf(&sysfont, "/usr/share/fonts/TTF/%s", basename(name));
+		*f = TTF_OpenFont(sysfont, size);
+		free(sysfont);
+	}
+	else 
+	{
+		*f = TTF_OpenFontRW(src, TRUE, size);
 	}
-
-	*f = TTF_OpenFontRW(src, TRUE, size);
 
 	if (!*f)
 	{
