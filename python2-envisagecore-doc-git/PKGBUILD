# Maintainer: Andrzej Giniewicz <gginiu@gmail.com>
pkgname=python2-envisagecore-doc-git
pkgver=20110205
pkgrel=1
pkgdesc="Documentation for EnvisageCore"
arch=('any')
url="https://github.com/enthought/envisagecore"
license=('BSD')
makedepends=('git' 'python2-distribute' 'python2-setupdocs')
provides=('python2-envisagecore-doc')
conflicts=('python2-envisagecore-doc' 'python-ets-envisagecore-doc-svn')
replaces=('python-ets-envisagecore-doc-svn')
options=(!emptydirs)

_gitroot="https://github.com/enthought/envisagecore.git"
_gitname="envisagecore"

build() {
  cd "$srcdir"
  msg "Connecting to GIT server...."

  if [ -d $_gitname ] ; then
    cd $_gitname && git pull origin
    msg "The local files are updated."
  else
    git clone $_gitroot $_gitname
  fi

  msg "GIT checkout done or server timeout"
  msg "Starting make..."

  rm -rf "$srcdir/$_gitname-build"
  git clone "$srcdir/$_gitname" "$srcdir/$_gitname-build"
  cd "$srcdir/$_gitname-build"

  python2 setup.py build_docs || return 1
}

package() {
  cd "$srcdir/$_gitname-build"

  install -d "${pkgdir}"/usr/share/doc/enthought/${_gitname}
  cp -r build/docs/html/* "${pkgdir}"/usr/share/doc/enthought/${_gitname}
}

