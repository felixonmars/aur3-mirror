# Maintainer: anekos <anekos@snca.net>
pkgname=growl-for-linux
pkgver=0.6.3
pkgrel=1
pkgdesc="Growl for Linux"
url="http://mattn.github.com/growl-for-linux/"
arch=('i686' 'x86_64')
license=('BSD')
depends=('git' 'openssl' 'curl' 'gtk2' 'sqlite3' 'dbus-glib' 'desktop-file-utils')
optdepends=('libnotify')
makedepends=('tar')
conflicts=('growl-for-linux-git')
replaces=()
backup=()

source=("https://nodeload.github.com/mattn/growl-for-linux/tarball/${pkgver}")
md5sums=('e6472316cf96847694251c3169690462')
dirname="mattn-growl-for-linux-427b6b8"

build () {
  cd "$srcdir"
  tar -xf "../$pkgver"
  cd "$dirname"

  ./autogen.sh
  ./configure --prefix=/usr && make
}

package () {
  cd "$srcdir/$dirname"
  echo $PWD
  make install DESTDIR="$pkgdir" prefix=/usr
}

# vim:set ts=2 sw=2 et:
