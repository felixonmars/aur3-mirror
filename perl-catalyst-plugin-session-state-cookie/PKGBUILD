# PKGBUILD generated by pacpan
pkgname=perl-catalyst-plugin-session-state-cookie
_realname=Catalyst-Plugin-Session-State-Cookie
pkgver=0.17
pkgrel=1
pkgdesc="'Maintain session IDs using cookies.'"
arch=(i686 x86_64)
license=('perl')
url="http://search.cpan.org/~mstrout/Catalyst-Plugin-Session-State-Cookie"
options=(!emptydirs)

depends=('perl' 'perl-namespace-autoclean' 'perl-catalyst-runtime' 'perl-mro-compat' 'perl-moose' 'perl-catalyst-plugin-session')
makedepends=('perl' 'perl-moose')

#provides=('Catalyst-Plugin-Session-State-Cookie')
provides=('catalyst-plugin-session-state-cookie=0.17' 'Catalyst::Plugin::Session::State::Cookie=0.17' 'perl-catalyst-plugin-session-state-cookie=0.17')

source=(http://search.cpan.org/CPAN/authors/id/M/MS/MSTROUT/Catalyst-Plugin-Session-State-Cookie-0.17.tar.gz)
md5sums=('f2a2d43d80777ceb2687b9e682cf5c76')

build() {
  _expected_dir="${srcdir}/${_realname}-${pkgver}"
  if [ -d "$_expected_dir" ]; then
    cd "$_expected_dir"
  else
    _expected_dir="${srcdir}/$(bsdtar -t -f $(basename $source) | head -n1)"
    if [ -d "$_expected_dir" ]; then
      cd "$_expected_dir"
    else
      _makefile=$(find $srcdir -iname Makefile.PL)
      if [ ! -z "$_makefile" ]; then
        _expected_dir=$(dirname $_makefile)
        if [ -d "$_expected_dir" ]; then
          cd "$_expected_dir"
        else
          echo "[1;31mERROR[0m unable to detect source directory"
          echo "[1;34m-->[0m this is often due to CPAN's lack of standard naming conventions"
          echo "[1;34m-->[0m it may be possible to fix this by adjusting the build function in the PKGBUILD"
        fi
      fi
    fi
  fi
  # install module in vendor directories.
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1
  make  || return 1
  make install DESTDIR=${pkgdir} || return 1

  # remove perllocal.pod and .packlist
  find ${pkgdir} -name perllocal.pod -delete
  find ${pkgdir} -name .packlist -delete
}

# vim:set ts=2 sw=2 et:
# END OF PACPAN PKGBUILD

