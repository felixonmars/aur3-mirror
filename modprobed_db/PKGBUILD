# Contributor: graysky <graysky AT archlinux dot us>
pkgname=modprobed_db
pkgver=1.8
pkgrel=1
pkgdesc='Keeps track of EVERY kernel module that has ever been probed. Useful for those of us who make localmodconfig :)'
arch=('any')
license=('GPL')
depends=('module-init-tools')
optdepends=('sudo')
url=https://wiki.archlinux.org/index.php/Modprobed_db
source=("https://github.com/graysky2/modprobed_db/raw/master/$pkgname-source.tar.xz")
sha256sums=('bfa020050f72d76daae1fd45bd195b41d1711d7a064738230cd4994bd0696da7')
install=readme.install
backup=(etc/modprobed_db.conf)

package() {
	cd "$srcdir/$pkgname-source"
	install -Dm755 $pkgname "$pkgdir/usr/bin/$pkgname"
  install -Dm755 reload_database "$pkgdir/usr/bin/reload_database"
  install -Dm644 $pkgname.conf "$pkgdir/etc/$pkgname.conf"
}
