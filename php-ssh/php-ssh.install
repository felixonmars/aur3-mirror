# arg 1:  the new package version
post_install() {
  echo "
PHP-SSH2 Instructions:

After installing PHP-SSH2 you need to add the following to you php.ini:

extension=ssh2.so

Somewhere close to the bottom where all the other extensions are, then restart the web service
"
  /bin/true
}

op=$1
shift
$op $*
