# Maintainer: Glennie Vignarajah <glennie@glennie.fr>

pkgname=python-robobrowser
pkgver=0.3.1
pkgrel=2
pkgdesc="RoboBrowser is a simple, Pythonic library for browsing the web without a standalone web browser."
arch=('any')
url="https://github.com/jmcarp/robobrowser"
license=('MIT')
depends=('python' 'python-requests' 'python-beautifulsoup4')
makedepends=('python-setuptools')
checkdepends=('python-nose')
source=("http://pypi.python.org/packages/source/r/robobrowser/robobrowser-$pkgver.tar.gz" "setup-tools.patch")
md5sums=('e5fa952bb9b347ee4b0dd1bb4aba2cc4' '3aec485cad271afc107ee2b77faf5f17')


prepare() {
  cd "$srcdir/robobrowser-$pkgver"
  patch -p2 -i ${srcdir}/setup-tools.patch
  python setup.py build
}

check() {
  cd "$srcdir/robobrowser-$pkgver"
  python setup.py test -q
}

package() {
  cd "$srcdir/robobrowser-$pkgver"
  python setup.py install --root="$pkgdir" --optimize=1
  install -Dm644 README.rst "$pkgdir/usr/share/doc/$pkgname/README.rst"
}

# vim:set ts=2 sw=2 et:
