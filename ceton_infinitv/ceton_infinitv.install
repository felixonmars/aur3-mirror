post_install() {
	/sbin/depmod -a
    echo ""
    echo "[+] modprobe ctn91xx"
    echo ">>> add ctn91xx to the MODULES line in /etc/rc.conf if module loads correctly"
}

post_upgrade() {
    post_install
}
