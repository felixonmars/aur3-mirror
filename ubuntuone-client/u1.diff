=== modified file 'libsyncdaemon/syncdaemon-config-interface.c'
--- ubuntuone-client-1.7.0/libsyncdaemon/syncdaemon-config-interface.c	2010-10-21 11:41:33 +0000
+++ ubuntuone-client-1.7.0/libsyncdaemon/syncdaemon-config-interface.c	2011-06-25 14:49:04 +0000
@@ -99,15 +99,14 @@
 		       const gchar *disable_method,
 		       gboolean enabled)
 {
-	gboolean result;
 	GError *error = NULL;
 
 	if (enabled) {
-		result = dbus_g_proxy_call (DBUS_G_PROXY (interface->priv->proxy), enable_method, &error,
+		dbus_g_proxy_call (DBUS_G_PROXY (interface->priv->proxy), enable_method, &error,
 					    G_TYPE_INVALID,
 					    G_TYPE_INVALID);
 	} else {
-		result = dbus_g_proxy_call (DBUS_G_PROXY (interface->priv->proxy), disable_method, &error,
+		dbus_g_proxy_call (DBUS_G_PROXY (interface->priv->proxy), disable_method, &error,
 					    G_TYPE_INVALID,
 					    G_TYPE_INVALID);
 	}

