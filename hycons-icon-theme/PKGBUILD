# Mantainer: William Díaz <wdiaz@archlinux.us>

pkgname=hycons-icon-theme
_pkgname=Hycons
pkgver=210810
pkgrel=1
pkgdesc="Icon theme created especially for KDE SC 4"
arch=('any')
depends=('kdebase-workspace')
url="http://hycons.fbnetwork.org"
license=('CC')
source=("http://kde-look.org/CONTENT/content-files/101767-${_pkgname}.tar.bz2")
md5sums=('e1381f2cdd896b34a20692dafa43383f')

build() {
  cd "${srcdir}/${_pkgname}"
  find -type f -print0 | xargs -0 chmod 644
  find -type d -print0 | xargs -0 chmod 755
}

package() {
  install -d "${pkgdir}/usr/share/icons/"
  cp -r "${srcdir}/${_pkgname}" "${pkgdir}/usr/share/icons/"
}
