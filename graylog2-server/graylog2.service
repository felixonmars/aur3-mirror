[Unit]
Description=Graylog2 management server
After=network.target

[Service]
PIDFile=/run/graylog2.pid
ExecStart=/usr/bin/java -jar /usr/lib/graylog2-server.jar -f /etc/graylog2.conf -p /run/graylog2.pid >> /var/log/graylog2-server.log

[Install]
WantedBy=multi-user.target
