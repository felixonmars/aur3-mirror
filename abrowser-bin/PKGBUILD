# Contributor: Figue <ffigue at gmail dot com>

pkgname=abrowser-bin
s_pkgname=abrowser
pkgver=11.0
pkgrel=1
pkgdesc="Binary version of Abrowser, safe and easy web browser from Mozilla"
arch=('i686' 'x86_64')
license=('GPL')
url="https://trisquel.info/en/wiki/abrowser-help"
depends=( 'dbus-glib' 'desktop-file-utils' 'libxt' 'mime-types' 'nss' 'shared-mime-info' )

if [ "${CARCH}" = 'x86_64' ]; then
  ARCH='amd64'
  md5sums=('fba5e06c68ffd7bdfb34018901410237')
elif [ "${CARCH}" = 'i686' ]; then
  ARCH='i386'
  md5sums=('ac12578b56b20fa6fe61ef4b7dfd36d2')
fi

source=("http://us.archive.trisquel.info/trisquel/pool/main/f/firefox/${s_pkgname}_${pkgver}+build1-0ubuntu0.11.10.1+5.5trisquel5_${ARCH}.deb")

build(){
  tar xzvf ${srcdir}/data.tar.gz -C ${pkgdir}/
}

package(){
  rm -rv "${pkgdir}"/{etc/apport,etc/apparmor.d,usr/share/apport,usr/share/lintian,usr/share/menu}
}
