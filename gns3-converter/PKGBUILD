# Maintainer: korjjj <korjjj+aur[at]gmail[dot]com>

pkgname=gns3-converter
pkgver=1.1.0
pkgrel=1
pkgdesc='GNS3 topology converter.'
arch=('any')
url='https://github.com/dlintott/gns3-converter'
license=('GPL3')
makedepends=('python-setuptools')
depends=('python-configobj' 'python-cx_freeze')
source=("https://github.com/dlintott/${pkgname}/archive/v${pkgver}.tar.gz")
md5sums=('c98d8da871e750062fca27d370343053')

package() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  python setup.py install --root="${pkgdir}" --optimize=1
  install -Dm644 COPYING "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}

# vim:set ts=2 sw=2 et:
