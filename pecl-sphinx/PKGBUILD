# Maintainer: Piotr Maliński <riklaunim@gmail.com>
# Contributor: Piotr Maliński <riklaunim@gmail.com>

pkgname=pecl-sphinx
pkgver=0.2.0
pkgrel=1
pkgdesc="Binary alternative PHP API for Sphinx SQL full-text search engine"
arch=(i686 x86_64)
url="http://pecl.php.net/package/sphinx/"
license=('GPL')
depends=('php sphinx')

source="http://pecl.php.net/get/sphinx-${pkgver}.tgz"
md5sums=('202847fda20ebbe89cfdcbbb0fcf48e0')

build(){
  cd $startdir/src/sphinx-$pkgver
  phpize
  ./configure --prefix=/usr --with-sphinx
  make || return 1
  make INSTALL_ROOT=$startdir/pkg install
}
