# PKGBUILD generated by pacpan
pkgname=perl-catalyst-plugin-session
_realname=Catalyst-Plugin-Session
pkgver=0.29
pkgrel=1
pkgdesc="'Generic Session plugin - ties together server side storage and client side state required to maintain session data.'"
arch=(i686 x86_64)
license=('perl')
url="http://search.cpan.org/~bobtfish/Catalyst-Plugin-Session"
options=(!emptydirs)

depends=('perl>=5.8.0' 'perl-moosex-emulate-class-accessor-fast' 'perl-object-signature' 'perl-namespace-clean' 'perl-catalyst-runtime' 'perl-mro-compat' 'perl-moose')
makedepends=('perl-test-exception' 'perl' 'perl-test-deep')

#provides=('Catalyst-Plugin-Session')
provides=('catalyst-plugin-session=0.29' 'Catalyst::Plugin::Session::Store=undef' 'perl-catalyst-plugin-session-store=undef' 'Catalyst::Plugin::Session::Test::Store=123' 'perl-catalyst-plugin-session-test-store=123' 'Catalyst::Plugin::Session::Store::Dummy=undef' 'perl-catalyst-plugin-session-store-dummy=undef' 'Catalyst::Plugin::Session::State=undef' 'perl-catalyst-plugin-session-state=undef' 'Catalyst::Plugin::Session=0.29' 'perl-catalyst-plugin-session=0.29')

source=(http://search.cpan.org/CPAN/authors/id/B/BO/BOBTFISH/Catalyst-Plugin-Session-0.29.tar.gz)
md5sums=('b580af6ccd996e4a86f3fbd9dd56b255')

build() {
  _expected_dir="${srcdir}/${_realname}-${pkgver}"
  if [ -d "$_expected_dir" ]; then
    cd "$_expected_dir"
  else
    _expected_dir="${srcdir}/$(bsdtar -t -f $(basename $source) | head -n1)"
    if [ -d "$_expected_dir" ]; then
      cd "$_expected_dir"
    else
      _makefile=$(find $srcdir -iname Makefile.PL)
      if [ ! -z "$_makefile" ]; then
        _expected_dir=$(dirname $_makefile)
        if [ -d "$_expected_dir" ]; then
          cd "$_expected_dir"
        else
          echo "[1;31mERROR[0m unable to detect source directory"
          echo "[1;34m-->[0m this is often due to CPAN's lack of standard naming conventions"
          echo "[1;34m-->[0m it may be possible to fix this by adjusting the build function in the PKGBUILD"
        fi
      fi
    fi
  fi
  # install module in vendor directories.
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1
  make  || return 1
  make install DESTDIR=${pkgdir} || return 1

  # remove perllocal.pod and .packlist
  find ${pkgdir} -name perllocal.pod -delete
  find ${pkgdir} -name .packlist -delete
}

# vim:set ts=2 sw=2 et:
# END OF PACPAN PKGBUILD

