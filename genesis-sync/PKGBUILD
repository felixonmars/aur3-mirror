# Maintainer: Ng Oon-Ee <ngoonee.talk@gmail.com>
# Contributor: M Rawash <mrawash@gmail.com>
pkgname=genesis-sync
pkgver=0.6.3
pkgrel=1
pkgdesc="A graphical frontend for SyncEvolution written in PyGTK."
arch=('any')
url="https://launchpad.net/genesis-sync"
license=('GPL')
depends=("syncevolution>=1.0" 'hicolor-icon-theme' 'dbus-python' 'python-notify' 'python-wnck')
makedepends=('python-distutils-extra')
source=(http://code.launchpad.net/$pkgname/0.6/$pkgver/+download/${pkgname}_$pkgver.tar.gz)
md5sums=('29cce0cb65f3a4b895c6ca8700db315e')

build() {
  cd "${srcdir}/${pkgname}"
  python2 setup.py install --root=$pkgdir
}
