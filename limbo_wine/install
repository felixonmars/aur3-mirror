post_install() {

echo "Important informations :
        - delete your $HOME/.limbo folder if you had a previous installation of Limbo from another PKGBUILD.
        - it seems that Limbo doesn't work with nouveau.
        - if you are using x86_64 architecture, don't forget to install 32-bit graphics driver."

}
