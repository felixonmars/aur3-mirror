# Contributor: markuman <markuman@gmail.com>
# This is not a useless package! It's the easiest way to remove ca-certificates without breaking tons of dependencies.
# For all who manage the trust of certificates for themselves

pkgname=no-certificates
pkgver=1
pkgrel=1
pkgdesc="Dummy package which provides core/ca-certificates to perform dependencies on it"
arch=(any)
license=('WTFPL')
url=('http://127.0.0.1')
conflicts=('ca-certificates')
provides=('ca-certificates')

build() {
echo "This is not a useless package! It's the easiest way to remove ca-certificates without breaking tons of dependencies."
}

package() {
echo "For all who manage the trust of certificates for themselves"
}

