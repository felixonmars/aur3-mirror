--- CMakeLists.txt
+++ CMakeLists.txt
@@ -179,6 +179,8 @@
 # Check for MPI, ParMETIS and SCOTCH
 if (DOLFIN_ENABLE_MPI)
   find_package(MPI)
+  # remove whitespace at start of flags that leads to cmake error
+  string(REGEX REPLACE "^ " "" MPI_LINK_FLAGS "${MPI_LINK_FLAGS}")

   # FIXME: Should be set CMake to use the MPI compiler wrappers?
 
   # Check for ParMETIS
