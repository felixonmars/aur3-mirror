--- caw/caw.py
+++ caw/caw.py
@@ -372,6 +372,8 @@ class Caw:
                 print "Bad Match:", (e.args[0].bad_value), e.args[0].major_opcode
             except IOError:
                 break
+            except: # Catch and see what happens if the thing is being a NoneType
+                break
 
             #self.connection.flush()
 
