DESTDIR=./

pure : $(DESTDIR)
	rm -rf $(DESTDIR)/usr/share/man
	rm -rf $(DESTDIR)/usr/share/info
	rm -rf $(DESTDIR)/usr/share/doc
	rm -rf $(DESTDIR)/usr/bin/automake
	rm -rf $(DESTDIR)/usr/bin/aclocal
