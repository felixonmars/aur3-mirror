post_install() {
  echo "==> Edit your /etc/slim.conf and change the current_theme to sleek-openbox:"
  echo "==> current_theme       sleek-openbox"
}

post_remove() {
  echo "==> Edit your /etc/slim.conf and change the current_theme for"
  echo "==> a another theme than sleek-openbox"
}

op=$1
shift
$op $*
