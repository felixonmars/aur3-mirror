# Maintainer: korjjj <korjjj+aur[at]gmail[dot]com>

pkgname=gns3-server
pkgver=1.2.3
pkgrel=1
pkgdesc='GNS3 network simulator. Server package.'
arch=('any')
url='https://github.com/GNS3/gns3-server'
license=('GPL3')
makedepends=('python-setuptools')
depends=('python-tornado' 'python-pyzmq' 'python-jsonschema' 'python-apache-libcloud' 'python-requests')
optdepends=('gns3-gui: graphical user interface for GNS3 server.'
'vboxwrapper: VirtualBox wrapper for GNS3.'
'iouyap: Bridge IOU to UDP, TAP and Ethernet.'
'qemu: Used by GNS3 to run Cisco ASA, PIX and IDS.'
'vpcs: Simple PC emulation for basic network operations.'
'python-netifaces: Host network interface information.'
)
install="${pkgname}.install"
md5sums=('3d7bd5f2523c40de3247ab4c4929937c'
         'f602390385890dab14f68e5e0a8cac2d')

source=("https://github.com/GNS3/${pkgname}/archive/v${pkgver}.tar.gz"
        "${pkgname}@.service")

package() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  python setup.py install --root="${pkgdir}" --optimize=1
  install -Dm644 LICENSE "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
  install -Dm644 "${srcdir}/${pkgname}@.service" "${pkgdir}/usr/lib/systemd/system/${pkgname}@.service"
}

# vim:set ts=2 sw=2 et:
