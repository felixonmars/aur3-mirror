--- hacks/analogtv.c.bak	2014-02-19 01:02:25.169862983 +0300
+++ hacks/analogtv.c	2014-02-19 01:02:52.006840023 +0300
@@ -331,7 +331,7 @@
     }
   else if (ratio > max_ratio)
     {
-      wlim = hlim*max_ratio;
+      /*wlim = hlim*max_ratio;*/
 # ifdef DEBUG
       fprintf (stderr,
                "size: center H: %dx%d in %dx%d (%.3f < %.3f < %.3f)\n",
@@ -341,7 +341,7 @@
     }
   else /* ratio < min_ratio */
     {
-      hlim = wlim/min_ratio;
+      /*hlim = wlim/min_ratio;*/
 # ifdef DEBUG
       fprintf (stderr,
                "size: center V: %dx%d in %dx%d (%.3f < %.3f < %.3f)\n",
