post_install() {
    echo "WARNING: this is a pre-beta release of the Organizer component"
    echo "for Organism, you should consider it as unstable!"
    echo "All the included plugins are enabled by default, but you will have"
    echo "to customize their menu entries in your configuration file"
    echo "(default is ~/.config/organism/organism.conf)."
}
