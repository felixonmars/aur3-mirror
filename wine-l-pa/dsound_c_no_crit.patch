--- wine-1.6-rc4/dlls/dsound/dsound.c.orig	2013-07-15 17:35:51.491085860 -0400
+++ wine-1.6-rc4/dlls/dsound/dsound.c	2013-07-15 17:36:44.594088599 -0400
@@ -40,6 +40,8 @@
 #include "dsound.h"
 #include "dsound_private.h"
 
+#define L_RT_THREADS 1
+
 WINE_DEFAULT_DEBUG_CHANNEL(dsound);
 
 typedef struct IDirectSoundImpl {
@@ -859,7 +861,7 @@
     ZeroMemory(&device->volpan, sizeof(device->volpan));
 
     device->thread = CreateThread(0, 0, DSOUND_mixthread, device, 0, 0);
-    SetThreadPriority(device->thread, THREAD_PRIORITY_TIME_CRITICAL); 
+    SetThreadPriority(GetCurrentThread(), L_RT_THREADS);  
 
     *ppDevice = device;
     list_add_tail(&DSOUND_renderers, &device->entry);
