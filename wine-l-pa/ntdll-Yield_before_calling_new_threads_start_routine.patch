--- wine-l-pa-1.7.16/dlls/ntdll/thread.c.orig	2014-06-24 19:02:32.879944525 -0400
+++ wine-l-pa-1.7.16/dlls/ntdll/thread.c	2014-06-24 19:01:47.441942181 -0400
@@ -1504,6 +1504,8 @@
         {
             LIST_ENTRY *entry;
             DWORD index;
+            HANDLE* h_thread;
+            h_thread = GetCurrentThread();
 
             if (length != sizeof(DWORD)) return STATUS_INVALID_PARAMETER;
             index = *(const DWORD *)data;
@@ -1516,6 +1518,10 @@
                     teb->TlsSlots[index] = 0;
                 }
                 RtlReleasePebLock();
+#if defined (HAVE_SCHED_H) && !defined (L_RT_THREADS)
+                if (h_thread != THREAD_PRIORITY_TIME_CRITICAL)
+                sched_yield();              
+#endif
             }
             else
             {
@@ -1529,6 +1535,10 @@
                     if (teb->TlsExpansionSlots) teb->TlsExpansionSlots[index] = 0;
                 }
                 RtlReleasePebLock();
+#if defined (HAVE_SCHED_H) && !defined (L_RT_THREADS)
+                if (h_thread != THREAD_PRIORITY_TIME_CRITICAL)
+                sched_yield();              
+#endif
             }
             return STATUS_SUCCESS;
         }
