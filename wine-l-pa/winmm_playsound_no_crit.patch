--- wine-1.6-rc4/dlls/winmm/playsound.c.orig	2013-07-12 14:37:04.000000000 -0400
+++ wine-1.6-rc4/dlls/winmm/playsound.c	2013-07-15 14:54:29.020586349 -0400
@@ -33,6 +33,8 @@
 #include "winemm.h"
 #include "winternl.h"
 
+#define L_RT_THREADS 1
+
 #include "wine/debug.h"
 
 WINE_DEFAULT_DEBUG_CHANNEL(winmm);
@@ -527,7 +529,7 @@
         HANDLE      handle;
         wps->bLoop = (fdwSound & SND_LOOP) != 0;
         if ((handle = CreateThread(NULL, 0, proc_PlaySound, wps, 0, NULL)) != 0) {
-            SetThreadPriority(handle, THREAD_PRIORITY_TIME_CRITICAL);
+            SetThreadPriority(handle, THREAD_PRIORITY_TIME_CRITICAL);
             CloseHandle(handle);
             return TRUE;
         }
