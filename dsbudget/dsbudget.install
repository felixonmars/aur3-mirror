
post_install() {

	echo
    echo "Before running, configure the conf file at /opt/dsbudget/dsbudget.conf. "
	echo "You won't be able to write to the main BudgetDocument.xml file."
	echo
	echo "It seems to have some difficulty launching certain browsers.  You can manually"
	echo "access the page at http://localhost:16091/dsbudget/main once the server has"
	echo "started."
	echo
}