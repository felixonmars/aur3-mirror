pkgname=firejail
pkgver=0.9.12.1
pkgrel=1
pkgdesc="Linux namespaces sandbox program"
arch=(i686 x86_64)
license=(GPL2)
url=https://l3net.wordpress.com/projects/firejail/
source=(http://sourceforge.net/projects/$pkgname/files/$pkgname/$pkgname-$pkgver.tar.bz2)
sha256sums=('190bf7417f711e2fc5eadfb11da1dbf701db3013261b8d4d9c14cbc8df43aeec')

build() {
	cd $pkgname-$pkgver
	# fix build
	export CFLAGS=${CFLAGS/-fsanitize=undefined/}
	./configure --prefix=/usr
	make
}

package() {
	cd $pkgname-$pkgver
	sed -i 's| $1| "${DESTDIR}"/$1|g;s| /etc| "${DESTDIR}"/etc|g' -i install.sh
	make DESTDIR="${pkgdir}" install
}
