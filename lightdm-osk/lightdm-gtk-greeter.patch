diff -aur lightdm-1.0.6/greeters/gtk/greeter.ui lightdm-1.0.6.patched/greeters/gtk/greeter.ui
--- lightdm-1.0.6/greeters/gtk/greeter.ui	2011-10-26 02:05:16.000000000 +1100
+++ lightdm-1.0.6.patched/greeters/gtk/greeter.ui	2011-12-18 07:55:13.226189172 +1100
@@ -290,6 +290,15 @@
                     <signal name="toggled" handler="a11y_contrast_cb" swapped="no"/>
                   </object>
                 </child>
+                <child>
+                  <object class="GtkCheckMenuItem" id="keyboard_menuitem">
+                    <property name="visible">True</property>
+                    <property name="can_focus">False</property>
+                    <property name="use_action_appearance">False</property>
+                    <property name="label" translatable="yes">On Screen Keyboard</property>
+                    <signal name="toggled" handler="a11y_keyboard_cb" swapped="no"/>
+                  </object>
+                </child>
               </object>
             </child>
           </object>
diff -aur lightdm-1.0.6/greeters/gtk/lightdm-gtk-greeter.c lightdm-1.0.6.patched/greeters/gtk/lightdm-gtk-greeter.c
--- lightdm-1.0.6/greeters/gtk/lightdm-gtk-greeter.c	2011-10-25 06:56:40.000000000 +1100
+++ lightdm-1.0.6.patched/greeters/gtk/lightdm-gtk-greeter.c	2011-12-18 08:07:41.987317390 +1100
@@ -29,7 +29,9 @@
 static GtkComboBox *session_combo;
 static GtkComboBox *language_combo;
 static gchar *default_font_name, *default_theme_name;
+static gchar *a11y_keyboard_launch, *a11y_keyboard_terminate;
 static gboolean cancelling = FALSE, prompted = FALSE;
+static gboolean a11y_keyboard = FALSE;
 
 static gchar *
 get_session ()
@@ -331,8 +333,15 @@
     if (lightdm_greeter_get_is_authenticated (greeter))
     {
         if (prompted)
-            start_session ();
-    }
+		{
+			if (a11y_keyboard)
+			{
+				system(a11y_keyboard_terminate);
+				a11y_keyboard = FALSE;
+			}
+             start_session ();
+		}
+	}
     else
     {
         if (prompted)
@@ -554,6 +563,29 @@
         g_object_set (gtk_settings_get_default (), "gtk-theme-name", default_theme_name, NULL);
 }
 
+void a11y_keyboard_cb (GtkWidget *widget);
+G_MODULE_EXPORT
+void
+a11y_keyboard_cb (GtkWidget *widget)
+{
+	if (gtk_check_menu_item_get_active (GTK_CHECK_MENU_ITEM (widget)))
+	{
+		if (a11y_keyboard_launch)
+		{
+			system(a11y_keyboard_launch);
+			a11y_keyboard = TRUE;
+		}
+	}
+	else
+	{
+		if (a11y_keyboard_terminate)
+		{
+			system(a11y_keyboard_terminate);
+			a11y_keyboard = FALSE;
+		}
+	}
+}
+
 static void
 sigterm_cb (int signum)
 {
@@ -847,6 +879,11 @@
         g_object_set (gtk_settings_get_default (), "gtk-xft-rgba", value, NULL);
     g_free (value);
 
+	/* Set a11y on screen keyboard commands*/
+	a11y_keyboard_launch = g_key_file_get_value (config, "a11y", "keyboard-launch", NULL);
+	a11y_keyboard_terminate = g_key_file_get_value (config, "a11y", "keyboard-terminate", NULL);
+	g_debug ("a11y keyboard commands are '%s' : '%s'", a11y_keyboard_launch, a11y_keyboard_terminate);
+
     /* Load out installed icons */
     gtk_icon_theme_append_search_path (gtk_icon_theme_get_default (), GREETER_DATA_DIR);
     gchar **path;
diff -aur lightdm-1.0.6/greeters/gtk/lightdm-gtk-greeter.conf lightdm-1.0.6.patched/greeters/gtk/lightdm-gtk-greeter.conf
--- lightdm-1.0.6/greeters/gtk/lightdm-gtk-greeter.conf	2011-09-28 17:11:35.000000000 +1000
+++ lightdm-1.0.6.patched/greeters/gtk/lightdm-gtk-greeter.conf	2011-12-18 07:58:37.861413951 +1100
@@ -1,4 +1,5 @@
 #
+# [greeter]
 # background = Background file to use, either an image path or a color (e.g. #772953)
 # theme-name = GTK+ theme to use
 # font-name = Font to use
@@ -16,3 +17,11 @@
 #xft-dpi=
 #xft-hintstyle=
 #xft-rgba=
+#
+# [a11y]
+# keyboard-launch = Command to launch on screen keyboard (must be sent to background with '&')
+# keyboard-terminate = Command to terminate on screen keyboard
+#
+[a11y]
+#keyboard-launch=
+#keyboard-terminate=
