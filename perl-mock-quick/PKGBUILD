# Contributor: AUR Perl <aurperl@juster.info>
# Generator  : CPANPLUS::Dist::Arch 1.12

pkgname='perl-mock-quick'
pkgver='1.101'
pkgrel='1'
pkgdesc="Quickly mock objects and classes, even temporarily replace them,
side-effect free."
arch=('any')
license=('PerlArtistic' 'GPL')
options=('!emptydirs')
depends=('perl-exporter-declare>=0.103')
makedepends=('perl-fennec-lite>=0.004' 'perl-test-exception>=0.29')
url='http://search.cpan.org/dist/Mock-Quick'
source=('http://search.cpan.org/CPAN/authors/id/E/EX/EXODIST/Mock-Quick-1.101.tar.gz')
md5sums=('571c819916b549d755d207805057631a')
_distdir="${srcdir}/Mock-Quick-1.101"

build() {
  ( export PERL_MM_USE_DEFAULT=1 PERL5LIB=""                 \
      PERL_AUTOINSTALL=--skipdeps                            \
      PERL_MM_OPT="INSTALLDIRS=vendor DESTDIR='$pkgdir'"     \
      PERL_MB_OPT="--installdirs vendor --destdir '$pkgdir'" \
      MODULEBUILDRC=/dev/null

    cd "$_distdir"
    /usr/bin/perl Build.PL
    /usr/bin/perl Build
  )
}

check() {
  cd "$_distdir"
  ( export PERL_MM_USE_DEFAULT=1 PERL5LIB=""
    /usr/bin/perl Build test
  )
}

package() {
  cd "$_distdir"
  /usr/bin/perl Build install
  find "$pkgdir" -name .packlist -o -name perllocal.pod -delete
}

# Local Variables:
# mode: shell-script
# sh-basic-offset: 2
# End:
# vim:set ts=2 sw=2 et:
