# Contributor: mosra <mosra@centrum.cz>

pkgname=kdevelop-clang-git
pkgver=20140221
pkgrel=1
pkgdesc="Clang integration plugin for KDevelop - Git build"
arch=('i686' 'x86_64')
url="http://www.kdevelop.org/"
license=('GPL')
groups=('kde' 'kdevelop-plugins')
depends=('kdevplatform-git')
optdepends=('kdevelop-git')
makedepends=('cmake' 'automoc4' 'git' 'clang')
source=('desktop-file.patch')
md5sums=('50588cda52e5229260ae99f0a4c291b8')

_gitroot="git://anongit.kde.org/kdev-clang"
_gitname="clang"

pkgver() {
    date +%Y%m%d
}

build() {
    cd "$srcdir"
    msg "Connecting to Git server..."

    if [ -d $_gitname ] ; then
        cd $_gitname

        # Revert desktop-file.patch
        git checkout .

        git pull origin
        msg "The local files are updated."
    else
        git clone $_gitroot $_gitname
    fi

    msg "Git checkout done."
    msg "Starting make..."

    # Fix mistyped desktop file name
    cd "$srcdir/$_gitname"
    git apply "$srcdir/desktop-file.patch"

    mkdir -p "$srcdir/build"
    cd "$srcdir/build"

    cmake ../$_gitname \
        -DCMAKE_BUILD_TYPE=Release \
        -DCMAKE_INSTALL_PREFIX=/usr

    make
}

package() {
    cd "$srcdir/build"

    make DESTDIR="$pkgdir" install
}
