post_upgrade() {
  echo "Important: Update 389-ds-base instances by stopping the appropriate systemd"
  echo "services and running:"
  echo
  echo "  /usr/sbin/setup-ds.pl -u -s General.UpdateMode=offline"
}
