post_install() {
  echo SUMO_HOME=/usr/lib/sumo | tee -a /etc/environment
  echo ">>>>> SUMO_HOME=/usr/lib/sumo has been written to /etc/environment"
  echo ">>>>> Log out and back in to use it"
}

post_remove() {
  sed -i '/SUMO_HOME=\/usr\/lib\/sumo/d' /etc/environment
}
