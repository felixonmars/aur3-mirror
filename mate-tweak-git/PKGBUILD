# Maintainer: Tom < reztho at archlinux dot us >
pkgname=mate-tweak-git 
pkgver=3.1.4.r50.280cecd
pkgrel=1
pkgdesc="Mate desktop configuration tool"
arch=('any')
url="https://bitbucket.org/flexiondotorg/mate-tweak"
license=('GPL')
depends=('python2')
makedepends=('git') 
provides=("${pkgname%-git}")
conflicts=("${pkgname%-git}")
source=('mate-tweak::git+https://bitbucket.org/flexiondotorg/mate-tweak.git')
md5sums=('SKIP')

pkgver() {
    cd "$srcdir/${pkgname%-git}"
	printf "%s" "$(git describe --long | sed 's/\([^-]*-\)g/r\1/;s/-/./g')"
}

package() {
    cd "$srcdir/${pkgname%-git}"
    /usr/bin/cp -R "./usr" "$pkgdir/"
    /usr/bin/chmod -R u=rwX,go=rX "$pkgdir/usr" 
}
