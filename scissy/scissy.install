#! /bin/bash

post_install() {
    systemd-tmpfiles --create scissy.conf
    chown -R git:git /var/lib/scissy
    chown -R git:git /etc/scissy
}

post_upgrade() {
    chown -R git:git /var/lib/scissy
    chown -R git:git /etc/scissy
}
