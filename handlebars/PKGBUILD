# Maintainer: Gavin Costello <gavcos at gmail dot com>
# Contributor:
pkgname=handlebars
pkgver=1.0.0
_release=rc.4
pkgrel=6
pkgdesc="Handlebars.js is an extension to the Mustache templating language created by Chris Wanstrath. Handlebars.js and Mustache are both logicless templating languages that keep the view and the code separated like we all know they should be."
arch=(any)
url="http://handlebarsjs.com"
license=('MIT')
depends=('jquery')
optdepends=('nodejs' 'nodejs-optimist')
source=("https://raw.github.com/wycats/handlebars.js/${pkgver}-${_release}/dist/handlebars.js" "https://raw.github.com/wycats/handlebars.js/master/bin/handlebars")
md5sums=('996a2066f9d8ceba9a6a890c7fc9dd57' '672ac7b32e1aeb6726a50dda7a8b12f0')

package() {
  cd "${srcdir}"
  mkdir -p "${pkgdir}/usr/share/handlebars"
  mkdir -p "${pkgdir}/usr/bin"
  install -m644 "handlebars.js" "${pkgdir}/usr/share/handlebars"
  install -m755 "handlebars" "${pkgdir}/usr/bin/handlebars"
}

# vim:set ts=2 sw=2 et:
