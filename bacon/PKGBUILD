pkgname=bacon
pkgver=0.14
pkgrel=1
pkgdesc="Tool for downloading CyanogenMod ROMs for Android devices"
arch=('i686' 'x86_64')
url="http://github.com/natediddy/${pkgname}"
license=('GPL3')
depends=('curl')
optdepends=()
makedepends=()
conflicts=()
replaces=()
backup=()
#install=
source=(https://github.com/downloads/natediddy/${pkgname}/${pkgname}-${pkgver}.tar.gz)
md5sums=('0fd059f36145a62e5cc8ce3b66e60910')

build() {
  echo "cd ${srcdir}/${pkgname}-${pkgver}"
  cd "${srcdir}/${pkgname}-${pkgver}"
  ./configure --prefix=/usr
  make
}

package() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  make DESTDIR="${pkgdir}" install
  install -Dm644 COPYING "${pkgdir}/usr/share/licenses/${pkgname}/COPYING"
}
