diff -Nur gloobus-preview-build.orig/configure.ac gloobus-preview-build/configure.ac
--- gloobus-preview-build.orig/configure.ac	2010-12-16 15:38:29.000000000 +0100
+++ gloobus-preview-build/configure.ac	2010-12-16 15:40:39.365682680 +0100
@@ -50,12 +50,6 @@
 PKG_CHECK_MODULES(SWFDEC, swfdec-0.8)
 PKG_CHECK_MODULES(SWFDECGTK, swfdec-gtk-0.8)
 
-PKG_CHECK_MODULES(GMODULE, gmodule-2.0)											# To compile XCF loader
-PKG_CHECK_MODULES(GDKPIXBUF, gdk-pixbuf-2.0)									# To compile XCF loader
-	
-AC_CHECK_HEADER(bzlib.h,,AC_MSG_ERROR(Can not find bzlib header))				# To compile XCF loader
-AC_CHECK_LIB(bz2,BZ2_bzDecompressInit,,AC_MSG_ERROR(Can not find libbz2))		# To compile XCF loader ==> sudo apt-get install libtool libbz2-dev
-
 # Checks for typedefs, structures, and compiler characteristics.
 AC_HEADER_STDBOOL
 
@@ -87,8 +81,6 @@
 	src/plugin-text/Makefile
 	src/plugin-ttf/Makefile
 	src/plugin-swfdec/Makefile
-	src/loaders/PSD/Makefile
-	src/loaders/XCF/Makefile
    	data/Makefile
    	data/images/Makefile
    	data/gtkrc/Makefile
diff -Nur gloobus-preview-build.orig/src/Makefile.am gloobus-preview-build/src/Makefile.am
--- gloobus-preview-build.orig/src/Makefile.am	2010-12-16 15:38:29.000000000 +0100
+++ gloobus-preview-build/src/Makefile.am	2010-12-16 15:41:04.605679747 +0100
@@ -1,4 +1,4 @@
-SUBDIRS = loaders/PSD loaders/XCF plugin-comic plugin-folder plugin-compressed plugin-djvu plugin-icns plugin-gstreamer-audio plugin-gstreamer-video plugin-openoffice plugin-pdf plugin-pixbuf plugin-ps plugin-text plugin-ttf plugin-swfdec .
+SUBDIRS = plugin-comic plugin-folder plugin-compressed plugin-djvu plugin-icns plugin-gstreamer-audio plugin-gstreamer-video plugin-openoffice plugin-pdf plugin-pixbuf plugin-ps plugin-text plugin-ttf plugin-swfdec .
 
 #CXXFLAGS = -Werror -g				#If -O2  iDocument threads don't work
 AM_CXXFLAGS = -Wall -Werror #-g -O0
