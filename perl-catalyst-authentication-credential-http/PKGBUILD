# PKGBUILD generated by pacpan
pkgname=perl-catalyst-authentication-credential-http
_realname=Catalyst-Authentication-Credential-HTTP
pkgver=1.011
pkgrel=1
pkgdesc="'HTTP Basic and Digest authentication'"
arch=(i686 x86_64)
license=('perl')
url="http://search.cpan.org/~dhoss/Catalyst-Authentication-Credential-HTTP"
options=(!emptydirs)

depends=('perl-test-mockobject' 'perl-uri' 'perl-test-exception' 'perl-class-accessor' 'perl-data-uuid' 'perl-catalyst-plugin-authentication' 'perl-string-escape' 'perl-catalyst-runtime')
makedepends=('perl')

#provides=('Catalyst-Authentication-Credential-HTTP')
provides=('catalyst-authentication-credential-http=1.011' 'Catalyst::Authentication::Credential::HTTP=1.011' 'perl-catalyst-authentication-credential-http=1.011')

source=(http://search.cpan.org/CPAN/authors/id/D/DH/DHOSS/Catalyst-Authentication-Credential-HTTP-1.011.tar.gz)
md5sums=('b35090dfca3e8c4253921f711f943695')

build() {
  _expected_dir="${srcdir}/${_realname}-${pkgver}"
  if [ -d "$_expected_dir" ]; then
    cd "$_expected_dir"
  else
    _expected_dir="${srcdir}/$(bsdtar -t -f $(basename $source) | head -n1)"
    if [ -d "$_expected_dir" ]; then
      cd "$_expected_dir"
    else
      _makefile=$(find $srcdir -iname Makefile.PL)
      if [ ! -z "$_makefile" ]; then
        _expected_dir=$(dirname $_makefile)
        if [ -d "$_expected_dir" ]; then
          cd "$_expected_dir"
        else
          echo "[1;31mERROR[0m unable to detect source directory"
          echo "[1;34m-->[0m this is often due to CPAN's lack of standard naming conventions"
          echo "[1;34m-->[0m it may be possible to fix this by adjusting the build function in the PKGBUILD"
        fi
      fi
    fi
  fi
  # install module in vendor directories.
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1
  make  || return 1
  make install DESTDIR=${pkgdir} || return 1

  # remove perllocal.pod and .packlist
  find ${pkgdir} -name perllocal.pod -delete
  find ${pkgdir} -name .packlist -delete
}

# vim:set ts=2 sw=2 et:

