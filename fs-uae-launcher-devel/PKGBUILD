# Maintainer: grimi <grimi at poczta dot fm>

_pkgname=fs-uae
pkgname=fs-uae-launcher-devel
pkgver=1.3.15wip2
pkgrel=1
pkgdesc="FS-UAE Launcher is a graphical user interface for setting up FS-UAE (development version)."
arch=("any")
url="http://fengestad.no/fs-uae"
license=("GPL2")
depends=("fs-uae-devel" "xdg-utils" "wxpython" "python-pygame" "python2-distribute")
install="${pkgname}.install"
source=("launcher.zip::http://eab.abime.net/attachment.php?attachmentid=32017&d=1341054972")
#source=("http://fengestad.no/${_pkgname}/files/${pkgver}/${_pkgname}-${pkgver}.tar.gz")
provides=("fs-uae-launcher")
conflicts=("fs-uae-launcher")
md5sums=('654588af3949110bfef924ae8fae0fe5')




build() {
   cd launcher
   make -f Makefile.mk
}



package() {
   cd launcher
   python2 setup.py install --prefix=/usr --root="${pkgdir}" --optimize=1
   make install -f Makefile.mk DESTDIR="${pkgdir}"
}


