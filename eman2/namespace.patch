Index: src/EMAN2/src/eman2/libEM/marchingcubes.cpp
===================================================================
--- src.orig/EMAN2/src/eman2/libEM/marchingcubes.cpp
+++ src/EMAN2/src/eman2/libEM/marchingcubes.cpp
@@ -50,6 +50,7 @@ using namespace EMAN;
 #define min(a,b)(((a) < (b)) ? (a) : (b))
 #define max(a,b)(((a) > (b)) ? (a) : (b))
 
+namespace EMAN {
 //a2fVertexOffset lists the positions, relative to vertex0, of each of the 8 vertices of a cube
 static const int a2fVertexOffset[8][3] =
 {
@@ -1159,3 +1160,4 @@ BOOST_PYTHON_MODULE(gorgon)
     ;
 }
 */
+}
Index: src/EMAN2/src/eman2/libEM/sparx/util_sparx.cpp
===================================================================
--- src.orig/EMAN2/src/eman2/libEM/sparx/util_sparx.cpp
+++ src/EMAN2/src/eman2/libEM/sparx/util_sparx.cpp
@@ -59,6 +59,8 @@ using namespace EMAN;
 using namespace std;
 using std::complex;
 
+namespace EMAN {
+
 vector<float> Util::infomask(EMData* Vol, EMData* mask, bool flip = false)
 //  flip true:  find statistics under the mask (mask >0.5)
 //  flip false: find statistics ourside the mask (mask <0.5)
@@ -21578,3 +21580,5 @@ int Util::branch_factor_4(int* costlist,
 	
 
 }
+
+}
