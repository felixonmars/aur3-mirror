# CPAN Name  : Pod-Spell
# Contributor: Caleb Cushing <xenoterracide@gmail.com>
# Generator  : CPANPLUS::Dist::Arch 0.20
# Template @ http://github.com/xenoterracide/AURpan/blob/master/perl-pod-spell/PKGBUILD.tt
# File bugs @ http://github.com/xenoterracide/AURpan/issues

pkgname='perl-pod-spell'
pkgver='1.01'
pkgrel='1'
pkgdesc="a formatter for spellchecking Pod"
arch=('any')
license=('PerlArtistic' 'GPL')
options=('!emptydirs')
depends=('perl')

url='http://search.cpan.org/dist/Pod-Spell'

source=('http://search.cpan.org/CPAN/authors/id/S/SB/SBURKE/Pod-Spell-1.01.tar.gz')
md5sums=('aa4964844da2586562aae5208e2dbe61')

build() {
  DIST_DIR="${srcdir}/Pod-Spell-1.01"
  export PERL_MM_USE_DEFAULT=1
  {

	cd "$DIST_DIR" &&

    perl Makefile.PL INSTALLDIRS=vendor &&
    make &&
    make test &&
    make DESTDIR="$pkgdir" install;
  } || return 1;

  find "$pkgdir" -name .packlist -o -name perllocal.pod -delete
}

