## arg 1:  the new package version
post_install() {
	echo -e "Note! At the moment, i3-tree is not able to read"
        echo -e "a standard i3 configuration file. Please copy the"
	echo -e "sample config from /etc/i3/config and customize it"
	echo -e "as you like. Make sure to backup your old i3 config"
	echo -e "file (if you have one)."
}
