# vim:set ts=2 sw=2 et ft=sh tw=100: expandtab
# Contributor: Piotr Rogoża <rogoza dot piotr at gmail dot com>
# Created: 13/12/2011

pkgname=vim-misc-xolox
_pkgname=xolox-vim-misc
pkgver=20130312
pkgrel=1
pkgdesc='Miscellaneous auto-load Vim scripts'
arch=('any')
url='http://peterodding.com/code/vim/misc'
license=('MIT')
groups=()
depends=()
makedepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
_gitroot='https://github.com/xolox/vim-misc.git'
_gitname='vim-misc-xolox'
noextract=()

build(){
  cd "$srcdir"
  msg2 "Connecting to GIT server...."

  if [ -d ${_gitname}/.git ] ; then
    cd $_gitname
    git pull origin
    msg2 "The local files are updated."
  else
    git clone $_gitroot $_gitname
    cd $_gitname
  fi
  msg2 "GIT checkout done or server timeout"
}
package(){
  cd "$srcdir"/$_gitname
  _vim_dir='/usr/share/vim/vimfiles/autoload/xolox/misc'
  install -dm755 "$pkgdir"$_vim_dir
  cp *.vim "$pkgdir"/$_vim_dir/
}
