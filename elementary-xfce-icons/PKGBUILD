# Maintainer: Limao Luo <luolimao+AUR@gmail.com>
# Contributor: flan_suse

# This icon theme was created by the Shimmer Project
# to better integrate into Xfce and match their
# Xfce themes. It is based on the Elementary icons,
# and uses upstream GNOME icons, as well.

_pkgname=elementary-xfce
pkgname=$_pkgname-icons
pkgver=0.2
pkgrel=4
pkgdesc='Elementary icons with better Xfce support'
arch=(any)
url=https://github.com/shimmerproject/elementary-xfce
license=(GPL2)
depends=(gnome-icon-theme)
options=(!strip)
conflicts=($pkgname-git)
optdepends=('xfce-theme-albatross: matching Shimmer Project Xfce theme'
    'xfce-theme-bluebird: matching Shimmer Project Xfce theme'
    'xfce-theme-greybird: matching Shimmer Project Xfce theme')
source=($pkgname-$pkgver.tar.gz::https://github.com/shimmerproject/$_pkgname/archive/v$pkgver.tar.gz)
sha256sums=('4d03c11a103a193fa0056f5ea2e184d15b5452ec1081cd3b2fa3c069e1da8bd4')
sha512sums=('8eae932336e83e043a92b53e74043d7bc6e9271c1608773256c75ecd319adf90c4cb0923f7b96e642a00bfc9de9d9748b3bbf0be32687cd7bbe6599d8c215aca')

package() {
    install -d "$pkgdir"/usr/share/icons/
    cp -rf $_pkgname-$pkgver/$_pkgname{,-dark} "$pkgdir"/usr/share/icons/
    install -Dm644 $_pkgname-$pkgver/README "$pkgdir"/usr/share/doc/$pkgname/README
}
