# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: flub <flub123@gmx.de>
pkgname=maui
pkgver=3.3.1
pkgrel=1
pkgdesc="Cluster job scheduler for use with a batch system e.g. torque."
arch=('i686' 'x86_64')
url="http://www.clusterresources.com/products/maui-cluster-scheduler.php"
license=('unknown')
groups=()
depends=('torque')
makedepends=('gcc' 'make')
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=maui.install
changelog=
source=(maui.cfg
        maui)
noextract=()
md5sums=('923dd25f842afa4c634eb4f5cab1fd9e'
         'e457940abc9c45e17cc352051d915c99')

build() {
echo "sign in and download on your own"
#  cd "$srcdir/$pkgname-$pkgver"
#  ./configure --with-pbs \
#              --prefix=/usr \
#              --mandir=/usr/share/man || return 1
#  make
}

package() {
echo "sign in and download on your own"
#  cd "$srcdir/$pkgname-$pkgver"
#  make BUILDROOT="${pkgdir}/" install || return 1
#  cp $srcdir/maui.cfg ${pkgdir}/usr/local/maui/maui.cfg
#  mkdir -p ${pkgdir}/etc/rc.d/
#  cp ${srcdir}/maui ${pkgdir}/etc/rc.d/
}

# vim:set ts=2 sw=2 et:
