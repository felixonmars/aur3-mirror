pkgname=libmatekeyring
pkgver=2011.10.29
pkgrel=1
pkgdesc="MATE keyring client library"
arch=('i686' 'x86_64')
license=('GPL' 'LGPL')
depends=('libgcrypt') # 'eggdbus' 'mate-keyring'
makedepends=('intltool' 'pkgconfig' 'python')
groups=('mate')
options=('!emptydirs' '!libtool')
url="http://matsusoft.com.ar/projects"
source=(http://sourceforge.net/projects/matede/files/${pkgver}/${pkgname}.tar.gz/download)
sha256sums=('bf37d199b9bfa0ea6deb2e24b37343ee4c9c3174f798bbc23440bfc07c867df2')

build() {
	cd "${srcdir}/${pkgname}"
	./configure --prefix=/usr --sysconfdir=/etc \
		--localstatedir=/var --disable-static \
		--libexecdir=/usr/lib/mate-keyring || return 1

	make || return 1
}

package() {
    cd "${srcdir}/${pkgname}"

    make DESTDIR="${pkgdir}" install || return 1
}
