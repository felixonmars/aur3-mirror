# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: nullren <chopflop@gmail.com>
pkgname=bitlbee-skype
#pkgver=0.9.0
pkgver=0.8.4
pkgrel=1
pkgdesc="Skype plugin for BitlBee"
arch=(i686 x86_64)
url="http://vmiklos.hu/project/bitlbee-skype/"
license=('GPL')
groups=()
depends=(bitlbee skype4py)
makedepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
source=(http://vmiklos.hu/project/bitlbee-skype/$pkgname-$pkgver.tar.gz)
noextract=()

build() {
  cd "$srcdir/$pkgname-$pkgver"
  ./configure --prefix=/usr --sysconfdir=/etc --mandir=/usr/share/man
  make
}

package() {
  cd "$srcdir/$pkgname-$pkgver"
  make DESTDIR="$pkgdir/" install
}

md5sums=('c1e8983760513f76cdfdcf9921b4fbf4')
