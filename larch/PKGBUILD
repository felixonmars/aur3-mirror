# Contributor:
# Maintainer: Ethan Hall <ethankhall@gmail.com>
pkgname=larch
pkgver=7
pkgrel=1
pkgdesc="a live CD/DVD/USB-stick construction kit"
arch=(any)
url="http://larch.berlios.de/"
license=('custom')
source=(ftp://ftp.berlios.de/pub/$pkgname/$pkgname$pkgver/i686/larch-setup)
md5sums=('eb552b5b2400211b9e7f41723fc92f17')
depends=('pyqt' 'python' 'python-pexpect' )

build() {
  cd $srcdir/
	rm $pkgname-setup
	cp ../$pkgname-setup ./
  chmod +rwx $pkgname-setup
  ./$pkgname-setup
	mkdir $pkgdir/opt
	mkdir $pkgdir/opt/larch
	cp -r larch0/* $pkgdir/opt/larch/
	
}

