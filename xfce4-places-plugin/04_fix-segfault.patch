diff -Naur xfce4-places-plugin-1.2.0.orig/panel-plugin/cfg.c xfce4-places-plugin-1.2.0/panel-plugin/cfg.c
--- xfce4-places-plugin-1.2.0.orig/panel-plugin/cfg.c	2011-12-18 18:08:43.676513372 +0100
+++ xfce4-places-plugin-1.2.0/panel-plugin/cfg.c	2011-12-18 18:11:38.026508986 +0100
@@ -505,7 +505,7 @@
     gtk_widget_show(tmp_label);
     gtk_box_pack_start(GTK_BOX(tmp_box), tmp_label, FALSE, FALSE, 0);
 
-    GtkObject *adj = gtk_adjustment_new(cfg->show_recent_number, 1, 25, 1, 5, 5);
+    GtkObject *adj = gtk_adjustment_new(cfg->show_recent_number, 1, 25, 1, 5, 0);
 
     tmp_widget = gtk_spin_button_new(GTK_ADJUSTMENT(adj), 1, 0);
     gtk_label_set_mnemonic_widget(GTK_LABEL(tmp_label), tmp_widget);
diff -Naur xfce4-places-plugin-1.2.0.orig/panel-plugin/model.c xfce4-places-plugin-1.2.0/panel-plugin/model.c
--- xfce4-places-plugin-1.2.0.orig/panel-plugin/model.c	2008-01-09 02:20:42.000000000 +0100
+++ xfce4-places-plugin-1.2.0/panel-plugin/model.c	2011-12-18 18:12:35.526507483 +0100
@@ -113,9 +113,6 @@
         bookmark->actions = NULL;
     }
 
-    if(bookmark->finalize != NULL)
-        bookmark->finalize(bookmark);
-    
     g_free(bookmark);
 }
 
