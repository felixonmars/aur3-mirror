## arg 1:  the new package version
post_install() {
  echo "Check out /etc/conf.d/emane.conf for arguments passed to daemons."
  echo "By default, it looks for config files in /etc/emane."
  echo "Create a config and put it there before you start the daemons."
}

# vim:set ts=2 sw=2 et:
