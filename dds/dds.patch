--- dds.cpp	2013-04-19 20:24:34.000000000 +0200
+++ dds_p.cpp	2013-07-11 18:16:13.030510315 +0200
@@ -6390,9 +6390,11 @@
   presp->parScore[1][2]=' ';
   presp->parScore[1][3]='\0';
 
-  itoa(par_score[0], temp, 10);
+  i = sprintf(temp, "%d", par_score[0]);
+  /* itoa(par_score[0], temp, 10); */
   strcat(presp->parScore[0], temp);
-  itoa(par_score[1], temp, 10);
+  i = sprintf(temp, "%d", par_score[1]);
+  /* itoa(par_score[1], temp, 10); */
   strcat(presp->parScore[1], temp);
 
   for (i=0; i<=1; i++) {
@@ -6450,7 +6452,8 @@
 
 	strcat(presp->parContractsString[i], buff);
 
-	itoa(par_tricks[i]-6, temp, 10);
+       i = sprintf(temp, "%d", par_tricks[i]-6);
+	/* itoa(par_tricks[i]-6, temp, 10); */
 	buff[0]=cardSuit[denom_conv[j]];
 	buff[1]='x';
 	buff[2]='\0';
@@ -6529,7 +6532,8 @@
 
 	n = CalcMultiContracts(max_lower, par_tricks[i]);
 
-	itoa(n, temp, 10);
+       i = sprintf(temp, "%d", n);
+	/* itoa(n, temp, 10); */
 	buff[0]=cardSuit[denom_conv[j]];
 	buff[1]='\0';
 	strcat(temp, buff);
@@ -6556,7 +6560,8 @@
 
 	  strcat(presp->parContractsString[i], buff);
 
-	  itoa(5, temp, 10);
+         i = sprintf(temp, "%d", 5);
+	  /* itoa(5, temp, 10); */
 	  buff[0]=cardSuit[denom_conv[j]];
 	  buff[1]='\0';
 	  strcat(temp, buff);
@@ -6633,7 +6638,8 @@
 
 	  n = CalcMultiContracts(max_lower, 9);
 
-	  itoa(n, temp, 10);
+         i = sprintf(temp, "%d", n);
+	  /* itoa(n, temp, 10); */
 	  buff[0]=cardSuit[denom_conv[j]];
 	  buff[1]='\0';
 	  strcat(temp, buff);
@@ -6707,7 +6713,8 @@
 
 	  n = CalcMultiContracts(max_lower, 8);
 
-	  itoa(n, temp, 10);
+         i = sprintf(temp, "%d", n);
+	  /* itoa(n, temp, 10); */
 	  buff[0]=cardSuit[denom_conv[j]];
 	  buff[1]='\0';
 	  strcat(temp, buff);
