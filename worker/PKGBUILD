# Maintainer: J. W. Birdsong  <jwbirdsong AT gmail DOT com>
# Contributor: Kevin Piche <kevin@archlinux.org>

pkgname=worker
pkgver=2.19.1
pkgrel=1
pkgdesc="A file manager for the X Window System   XFT enabled"
arch=('i686' 'x86_64')
license=('GPL')
url="http://www.boomerangsworld.de/worker"
depends=('avfs' 'gcc-libs' 'libx11' 'file')
makedepends=('libxt')
source=(http://www.boomerangsworld.de/${pkgname}/downloads/${pkgname}-${pkgver}.tar.bz2)

md5sums=('48c5cb85ca6ff1744e690634459130d2')
sha1sums=('51cd31fd7fa7e2808cad2eed5792b667cb76c53b')
build() {
  cd "${srcdir}/${pkgname}-${pkgver}"

  ./configure --enable-xft --prefix=/usr
  make
}

package() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  make DESTDIR="${pkgdir}" install
}

# vim:set ts=8 sw=2 et:

