Only in src/: fbsplash-no-pre-freeze-progress.patch
Only in src/: fbsplash-progress-return-to-zero.patch
diff -rub src.orig//suspend-0.8/suspend.c src//suspend-0.8/suspend.c
--- src.orig//suspend-0.8/suspend.c	2007-12-31 19:50:12.000000000 +0100
+++ src//suspend-0.8/suspend.c	2011-01-04 16:23:42.818275318 +0100
@@ -839,7 +839,7 @@
 	 * drop to 'verbose mode' after the freeze() call.
 	 */
 	splash.switch_to();
-	splash.progress(15);
+	// splash.progress(15);
 
 	if (error)
 		goto Unfreeze;
@@ -1560,7 +1560,7 @@
 		goto Restore_console;
 	}
 
-	splash.progress(5);
+	// splash.progress(5);
 
 #ifdef CONFIG_BOTH
 	/* If s2ram_hacks returns != 0, better not try to suspend to RAM */
@@ -1584,7 +1584,7 @@
 
 	sync();
 
-	splash.progress(10);
+	// splash.progress(10);
 
 	rlim.rlim_cur = 0;
 	rlim.rlim_max = 0;
