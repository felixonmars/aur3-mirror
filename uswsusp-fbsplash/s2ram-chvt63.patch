diff -ru src.orig//suspend-0.8/s2ram-main.c src//suspend-0.8/s2ram-main.c
--- src.orig//suspend-0.8/s2ram-main.c	2007-12-31 19:50:12.000000000 +0100
+++ src//suspend-0.8/s2ram-main.c	2010-03-17 11:15:11.292613739 +0100
@@ -84,10 +84,11 @@
 	if (ret)
 		goto out;
 
-	/* switch to console 1 first, since we might be in X */
+	/* switch to console 63 first, since we might be in X,
+	 * but don't want to see the console first */
 	active_console = fgconsole();
-	printf("Switching from vt%d to vt1\n", active_console);
-	chvt(1);
+	printf("Switching from vt%d to vt63\n", active_console);
+	chvt(63);
 
 	ret = s2ram_hacks();
 	if (ret)
