post_upgrade() {
	echo "!!!"; echo It is now a bash function. Usage has changed a lot.
	echo "!!!"; echo Normally you source the file now. The bind -x command is in the file.
	echo "!!!"; echo kbdmenu is added to replace the ability of the python script to perform as a console dmenu.
}
