## arg 1:  the new package version
## arg 2:  the old package version
pre_upgrade() {
  if (( $(vercmp $2 0.7) < 0 )); then
    cat << EOM
The configuration in /etc/ashd/ will be removed. Make sure to make a backup copy of your files.
The new configuration location is /etc/.
Also the /etc/conf.d/ashd.conf syntax has changed. 
EOM
  fi
}

