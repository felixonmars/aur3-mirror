# Contributor: Andreas Schrafl <andy@jetnet.ch>
pkgname=colinux-modules
#_pkgdate=20100901
_kernelver=2.6.33.5
pkgver=0.7.8
pkgrel=1
pkgdesc="coLinux Kernel ${_kernelver} Modules"
arch=('i686')
url="http://www.henrynestler.com/colinux/releases/${pkgver}/packages/"
license=('GPL')
source=(${url}/modules-${_kernelver}-co-${pkgver}.tgz)
md5sums=('41d4b4de4c43c45f382be640871d8aa7')

build() {
 tar -xzf $srcdir/modules-${_kernelver}-co-${pkgver}.tgz -C $pkgdir
}
