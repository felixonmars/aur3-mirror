# Maintainer: Andrea Scarpino <andrea@archlinux.org>

pkgname=kde-workspace-git
pkgver=r57811.76d9995
pkgrel=1
pkgdesc='Provides the interface and basic tools for the KDE workspace'
arch=('i686' 'x86_64')
url='https://projects.kde.org/projects/kde/kde-workspace'
license=('LGPL')
depends=('frameworkintegration-git' 'kcmutils-git' 'knotifyconfig-git' 'kross-git' 'kded-git'
         'kdesu-git' 'knewstuff-git' 'qimageblitz' 'kidletime-git' 'kitemmodels-git'
         'libxkbfile' 'libxcursor' 'lm_sensors' 'libraw1394' 'libusb-compat' 'khtml-git' 'kinit-git'
         'pciutils' 'libbsd' 'kf5-akonadi' 'kdoctools-git' 'kde4support-git' 'kjsembed-git' 'kemoticons-git' 'krunner-git')
makedepends=('extra-cmake-modules' 'git' 'qt5-tools' 'kf5umbrella-git')
conflicts=('kde-workspace')
provides=('kde-workspace')
source=('git://anongit.kde.org/kde-workspace.git')
md5sums=('SKIP')

pkgver() {
  cd kde-workspace
  printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
}

prepare() {
  mkdir -p build
}

build() {
  export XDG_DATA_DIRS="/opt/kf5/share:$XDG_DATA_DIRS"
  export CMAKE_PREFIX_PATH="/opt/kf5"

  cd build
  cmake ../kde-workspace \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=/opt/kf5 \
    -DLIB_INSTALL_DIR=lib \
    -DBUILD_TESTING=OFF
#    -DSYSCONF_INSTALL_DIR=/etc
  make
}

package() {
  cd build
  make DESTDIR="${pkgdir}" install
}
