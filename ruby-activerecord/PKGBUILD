# Contributor: Alexsandr Pavlov <kidoz at mail dot ru>
pkgname=ruby-activerecord
pkgver=3.0.8
pkgrel=1
pkgdesc="Implements the ActiveRecord pattern (Fowler, PoEAA) for ORM."
arch=(any)
url="http://www.rubyonrails.org/"
license=('MIT')
depends=('ruby' 'ruby-tzinfo' 'ruby-arel' 'ruby-activemodel' 'ruby-activesupport')
source=(http://gems.rubyforge.org/gems/activerecord-$pkgver.gem)
noextract=(activerecord-$pkgver.gem)
md5sums=('377d885c0157eab13ef674dd6cdaacac')

build() {
  cd $srcdir
  # _gemdir is defined inside build() because if ruby[gems] is not installed on the system
  # makepkg will exit with an error when sourcing the PKGBUILD
  local _gemdir="$(ruby -rubygems -e'puts Gem.default_dir')"

  gem install --ignore-dependencies -i "$pkgdir$_gemdir" activerecord-$pkgver.gem
}
