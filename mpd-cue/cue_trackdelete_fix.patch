--- orig.src/directory.c	2008-08-22 22:54:17.000000000 +0400
+++ src/directory.c	2008-08-22 23:09:15.000000000 +0400
@@ -293,9 +293,9 @@
 
 static void removeSongFromDirectory(Directory * directory, char *shortname)
 {
-	void *song;
+	void *song, *tmpsong;
 
-	if (findInList(directory->songs, shortname, &song)) {
+	if (findInList(directory->songs, shortname, &song) && !findInList(directory->songs, url_filename(shortname), &tmpsong)) {
 		LOG("removing: %s\n", getSongUrl((Song *) song));
 		deleteFromList(directory->songs, shortname);
 	}
