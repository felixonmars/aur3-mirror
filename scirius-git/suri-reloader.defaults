# The following parameters are the most commonly needed to configure
# suricata. A full list can be seen by running /usr/bin/suricata --help
# -p Directory to monitor for scirius.reload file
# -r  If set reload Suricata instead of restarting (rule-reload must be set to true)
# -l  File to log output to (default to stdout)

# Add options to be passed to the daemon
OPTIONS="-p /etc/suricata/rules/"
