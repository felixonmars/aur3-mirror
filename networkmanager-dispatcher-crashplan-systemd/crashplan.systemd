#!/bin/bash

if [ "$(systemctl is-enabled crashplan.service)" == "enabled" ]; then
  case "${2}" in
    up|vpn-up)
	  # Restart the engine to avoid the error Waiting for connection
      systemctl restart crashplan.service
      ;;
    down|vpn-down)
      ;;
  esac
fi
