# Maintainer: talavis <talavis gmail com>
pkgname=ugene
pkgver=1.10.0
pkgrel=1
pkgdesc="A free cross-platform genome analysis suite."
arch=('i686' 'x86_64')
url="http://ugene.unipro.ru/"
license=('GPL')
depends=('qt' 'mesa')
source=(${url}downloads/$pkgname-$pkgver.tar.gz)
sha1sums=('9c593d971af07954873bb8bb774c4bd8df7898e0')


build() {
  cd "$srcdir/$pkgname-$pkgver"
  [ "$CARCH" = "i686" ] && qmake PREFIX=/usr -r
  [ "$CARCH" = "x86_64" ] && qmake CONFIG+=x64 PREFIX=/usr -r
  make || return 1
  make INSTALL_ROOT="$pkgdir" install || return 1
}
