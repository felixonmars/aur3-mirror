# PKGBUILD generated by pacpan
pkgname=perl-spreadsheet-writeexcel
_pkgname='Spreadsheet-WriteExcel'
pkgver=2.39
pkgrel=1
pkgdesc="Spreadsheet::WriteExcel is a Perl module which can be used to create native Excel binary files"
arch=(any)
license=(GPL)
url="http://search.cpan.org/dist/Spreadsheet-WriteExcel/"
depends=('perl-parse-recdescent' 'perl-file-temp' 'perl-ole-storage-lite')
options=(!emptydirs)

source=(http://search.cpan.org/CPAN/authors/id/J/JM/JMCNAMARA/${_pkgname}-${pkgver}.tar.gz)
md5sums=('12ea4e43252619eaa89209aec20b061c')
sha512sums=('c11a67c497ae79b952b8b554570795d053d3784eebbb279a419978f8b8a1f6cef7c1b8e9376e692e5240b916dccd3f980551df822f38f6453d823284067c9dbc')

build() {
  ( export PERL_MM_USE_DEFAULT=1 PERL5LIB=""                 \
      PERL_AUTOINSTALL=--skipdeps                            \
      PERL_MM_OPT="INSTALLDIRS=vendor DESTDIR='$pkgdir'"     \
      PERL_MB_OPT="--installdirs vendor --destdir '$pkgdir'" \
      _distdir="${srcdir}/${_pkgname}-${pkgver}" \
      MODULEBUILDRC=/dev/null

    cd "$_distdir"
    /usr/bin/perl Makefile.PL
    make
    make test
    make install
    find "$pkgdir" -name .packlist -o -name perllocal.pod -delete
  )
}
