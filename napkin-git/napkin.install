post_upgrade() {
  echo "Your user must be in the 'uucp' group."
  echo " # usermod -aG uucp USERNAME"
}

post_install() {
  echo "Your user must be in the 'uucp' group."
  echo " # usermod -aG uucp USERNAME"
}
# vim:set ts=2 sw=2 et:
