post_install() {
cat << EOM

==> There are two configuration files samples: /etc/pommed.conf.{pmac,mactel},
==> use them as base for your /etc/pommmed.conf

==> Run 'systemctl enable pommed.service' as root to start it at boot,
==> to start it now, run 'systemctl start pommed.service' as root

EOM
}


# vim:set ts=2 sw=2 et:
