pkgname=gtkd
pkgver=2.3.2
pkgrel=2
pkgdesc="D binding and OO wrapper of GTK+."
arch=('i686' 'x86_64')
url="http://gtkd.org"
options=('staticlibs')
license=('LGPL')
depends=('gtk3')
makedepends=('dmd')
optdepends=('pango' 'atk' 'gtk-pixbuf2' 'gtksourceview3' 'gstreamer')
source=("http://master.dl.sourceforge.net/project/gtkd-packages/sources/GtkD-${pkgver}.zip")
build() {
    cd ${srcdir}
    make gtkd
}
package() {
    cd ${srcdir}
    make prefix="/usr" DESTDIR="${pkgdir}/" install
}
md5sums=('10549792aad61950efce13ee91415a79')
