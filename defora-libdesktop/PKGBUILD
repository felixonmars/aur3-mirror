# Contributor: Calimero <calimeroteknik@free.fr>
pkgname=defora-libdesktop
pkgver=0.0.2
pkgrel=1
pkgdesc="The common lib to all Defora desktop apps"
arch=('i686' 'x86_64')
url="http://defora.org/os"
license=('GPL3')
depends=('defora-libsystem')
source=(http://www.defora.org/os/download/download/3357/libDesktop-${pkgver}.tar.gz)
md5sums=('daf27f0ab44bdc806303d37d0a43429e')

build() {
  cd "$srcdir"/libDesktop-$pkgver
  make || return 1
  make DESTDIR="$pkgdir" PREFIX="/usr" install
}
