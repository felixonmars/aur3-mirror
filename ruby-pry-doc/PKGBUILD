# Generated by gem2arch (https://github.com/anatol/gem2arch)
# Maintainer: Joakim Reinert <mail@jreinert.com>

_gemname=pry-doc
pkgname=ruby-$_gemname
pkgver=0.6.0
pkgrel=1
pkgdesc='Provides YARD and extended documentation support for Pry'
arch=(any)
url='https://github.com/pry/pry-doc'
license=(MIT)
depends=(ruby ruby-yard ruby-pry)
options=(!emptydirs)
source=(https://rubygems.org/downloads/$_gemname-$pkgver.gem)
noextract=($_gemname-$pkgver.gem)
sha1sums=('a4b3d946eaf2dc984ce254de966ee1c421734d92')

package() {
  local _gemdir="$(ruby -e'puts Gem.default_dir')"
  gem install --ignore-dependencies --no-user-install -i "$pkgdir/$_gemdir" -n "$pkgdir/usr/bin" $_gemname-$pkgver.gem
  rm "$pkgdir/$_gemdir/cache/$_gemname-$pkgver.gem"
  install -D -m644 "$pkgdir/$_gemdir/gems/$_gemname-$pkgver/LICENSE" "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
}
