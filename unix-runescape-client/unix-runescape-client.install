TEXT="==> for compatibility mode to work, you need to install 'wine' and to install any windows jre inside it.\n==> to have desktop icons, run:\n/opt/runescape/install-desktop-icons
"

post_install() {
    echo -e $TEXT
}

post_upgrade() {
    echo -e $TEXT
}
