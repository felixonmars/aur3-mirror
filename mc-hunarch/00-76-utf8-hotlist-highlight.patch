Highlight the lines of hotlist in full width.

diff -Naurdp mc-4.6.1.orig/src/widget.c mc-4.6.1/src/widget.c
--- mc-4.6.1.orig/src/widget.c	2005-09-05 18:31:51.000000000 +0200
+++ mc-4.6.1/src/widget.c	2005-09-05 18:32:00.000000000 +0200
@@ -1937,6 +1937,7 @@ listbox_draw (WListbox *l, int focused)
 {
     WLEntry *e;
     int i;
+    int j;
     int sel_line;
     Dlg_head *h = l->widget.parent;
     int normalc = NORMALC; 
@@ -1967,7 +1968,8 @@ listbox_draw (WListbox *l, int focused)
 	    text = e->text;
 	    e = e->next;
 	}
-	printw (" %-*s ", l->width-2, name_trunc (text, l->width-2));
+	j = columns_to_bytes (name_trunc (text, l->width-2), l->width-2);
+	printw (" %-*s ", j, name_trunc (text, l->width-2));
     }
     l->cursor_y = sel_line;
     if (!l->scrollbar)
