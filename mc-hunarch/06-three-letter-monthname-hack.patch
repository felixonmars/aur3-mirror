diff -Naurd mc-4.6.1.orig/src/util.c mc-4.6.1/src/util.c
--- mc-4.6.1.orig/src/util.c	2005-09-05 17:54:32.000000000 +0200
+++ mc-4.6.1/src/util.c	2005-09-05 17:58:40.000000000 +0200
@@ -989,7 +989,20 @@
     else
 	fmt = fmttime;
     
-    strftime (timebuf, sizeof (timebuf) - 1, fmt, localtime(&when));
+    if (fmt[0] == '%' && fmt[1] == 'b') {
+	wchar_t wcsbuf[8];
+	strftime (timebuf, 8, "%b", localtime(&when));
+	timebuf[8] = 0;
+	while (1) {
+	    int i;
+	    i = mbstowcs(wcsbuf, timebuf, 8);
+	    if (i >= 0 && i <= 3) break;
+	    timebuf[strlen(timebuf)-1] = 0;
+	}
+	strftime (timebuf + strlen(timebuf), sizeof (timebuf) - 1 - strlen(timebuf), fmt + 2, localtime(&when));
+    } else {
+	strftime (timebuf, sizeof (timebuf) - 1, fmt, localtime(&when));
+    }
     return timebuf;
 }
 
diff -Naurd mc-4.6.1.orig/src/util.h mc-4.6.1/src/util.h
--- mc-4.6.1.orig/src/util.h	2005-09-05 17:54:32.000000000 +0200
+++ mc-4.6.1/src/util.h	2005-09-05 17:57:47.000000000 +0200
@@ -100,7 +100,7 @@
 char *utf8_to_local(char *str);
 
 
-#define MAX_I18NTIMELENGTH 14
+#define MAX_I18NTIMELENGTH 12
 #define MIN_I18NTIMELENGTH 10
 #define STD_I18NTIMELENGTH 12
 
