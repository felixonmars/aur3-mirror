diff -Naur mc-4.6.1.orig/vfs/extfs/deb.in mc-4.6.1/vfs/extfs/deb.in
--- mc-4.6.1.orig/vfs/extfs/deb.in	2004-08-24 21:55:01.000000000 +0200
+++ mc-4.6.1/vfs/extfs/deb.in	2006-06-29 19:17:06.000000000 +0200
@@ -34,7 +34,7 @@
 	local($archivename)=@_;
 	local $qarchivename = quote($archivename);
 	chop($date=`LC_ALL=C date "+%b %d %Y %H:%M"`);
-	chop($info_size=`dpkg -I $qarchivename | wc -c`);
+	chop($info_size=`LC_ALL=C dpkg -I $qarchivename | wc -c`);
 	$install_size=length($pressinstall);
 
 	print "dr-xr-xr-x   1 root     root     0 $date CONTENTS\n";
@@ -71,8 +71,7 @@
 			$link=$_[$pathindex++];
 			$link2=$_[$pathindex++];
 
-			$owgr=~s!/! !;
-			next if $path=~m!/$!;
+			$owgr=~s!/! ! unless $owgr eq "/";
 			if($arrow eq 'link')
 			{
 # report hard links as soft links
@@ -88,7 +87,7 @@
 		}
 	}
         # begin from Patrik Rak
-        if ( open(PIPEIN, "dpkg-deb -I $qarchivename |") )
+        if ( open(PIPEIN, "LC_ALL=C dpkg-deb -I $qarchivename |") )
         {
                while(<PIPEIN>)
                {
