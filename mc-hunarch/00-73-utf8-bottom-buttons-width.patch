Use six character width cyan rectangles for bottom row's buttons.

diff -Naurdp mc-4.6.1.orig/src/widget.c mc-4.6.1/src/widget.c
--- mc-4.6.1.orig/src/widget.c	2005-09-05 17:08:08.000000000 +0200
+++ mc-4.6.1/src/widget.c	2005-09-05 17:15:30.000000000 +0200
@@ -2483,11 +2483,14 @@ buttonbar_callback (WButtonBar *bb, int 
 	attrset (DEFAULT_COLOR);
 	printw ("%-*s", bb->widget.cols, "");
 	for (i = 0; i < COLS / 8 && i < 10; i++) {
+	    int j;
 	    widget_move (&bb->widget, 0, i * 8);
 	    attrset (DEFAULT_COLOR);
 	    printw ("%d", i + 1);
 	    attrset (SELECTED_COLOR);
-	    printw ("%-*s", ((i + 1) * 8 == COLS ? 5 : 6),
+	    j = columns_to_bytes(bb->labels[i].text ? bb->labels[i].text : "",
+	      ((i + 1) * 8 == COLS ? 5 : 6));
+	    printw ("%-*s", j,
 		    bb->labels[i].text ? bb->labels[i].text : "");
 	    attrset (DEFAULT_COLOR);
 	}
