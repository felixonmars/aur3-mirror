Fix formatting the output of "mc --help".

diff -Naurdp mc-4.6.1.orig/src/main.c mc-4.6.1/src/main.c
--- mc-4.6.1.orig/src/main.c	2005-09-05 18:16:26.000000000 +0200
+++ mc-4.6.1/src/main.c	2005-09-05 18:18:18.000000000 +0200
@@ -1859,7 +1859,7 @@ print_mc_usage (poptContext ctx, FILE *s
 
     /* print help for options */
     leftColWidth = poptPrintHelp (ctx, stream, 0);
-    fprintf (stream, "  %-*s   %s\n", leftColWidth, _("+number"),
+    fprintf (stream, "  %-*s   %s\n", leftColWidth + strlen(_("+number")) - mbstrlen(_("+number")), _("+number"),
 	     _("Set initial line number for the internal editor"));
     fputs (_
 	   ("\n"
diff -Naurdp mc-4.6.1.orig/src/popthelp.c mc-4.6.1/src/popthelp.c
--- mc-4.6.1.orig/src/popthelp.c	2004-11-11 18:37:20.000000000 +0100
+++ mc-4.6.1/src/popthelp.c	2005-09-05 18:18:18.000000000 +0200
@@ -93,7 +93,7 @@ static void singleOptionHelp(FILE * f, i
 	goto out;
     }
 
-    helpLength = strlen(help);
+    helpLength = mbstrlen(help);
     while (helpLength > lineLength) {
 	ch = help + lineLength - 1;
 	while (ch > help && !isspace(*ch)) ch--;
@@ -104,7 +104,7 @@ static void singleOptionHelp(FILE * f, i
 	fprintf(f, "%.*s\n%*s", (int) (ch - help), help, indentLength, " ");
 	help = ch;
 	while (isspace(*help) && *help) help++;
-	helpLength = strlen(help);
+	helpLength = mbstrlen(help);
     }
 
     if (helpLength) fprintf(f, "%s\n", help);
@@ -223,7 +223,7 @@ static int singleOptionUsage(FILE * f, i
     if (len == 3) return cursor;
 
     if (argDescrip) 
-	len += strlen(argDescrip) + 1;
+	len += mbstrlen(argDescrip) + 1;
 
     if ((cursor + len) > 79) {
 	fprintf(f, "\n       ");
@@ -292,7 +292,7 @@ void poptPrintUsage(poptContext con, FIL
     singleTableUsage(f, cursor, con->options, NULL);
 
     if (con->otherHelp) {
-	cursor += strlen(con->otherHelp) + 1;
+	cursor += mbstrlen(con->otherHelp) + 1;
 	if (cursor > 79) fprintf(f, "\n       ");
 	fprintf(f, " %s", con->otherHelp);
     }
diff -Naurdp mc-4.6.1.orig/src/util.c mc-4.6.1/src/util.c
--- mc-4.6.1.orig/src/util.c	2005-09-05 18:16:26.000000000 +0200
+++ mc-4.6.1/src/util.c	2005-09-05 18:18:18.000000000 +0200
@@ -89,7 +89,7 @@ size_t
 mbstrlen (const char *str)
 {
 #ifdef UTF8
-    if (SLsmg_Is_Unicode) {
+    if (1) {
         size_t width = 0;
 
         for (; *str; str++) {
