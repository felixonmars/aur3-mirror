diff -Naur mc-4.6.1.orig/lib/mc.ext.in mc-4.6.1/lib/mc.ext.in
--- mc-4.6.1.orig/lib/mc.ext.in	2005-09-05 19:04:31.000000000 +0200
+++ mc-4.6.1/lib/mc.ext.in	2005-09-05 19:24:00.000000000 +0200
@@ -107,20 +107,20 @@
 ### Archives ###
 
 # .tgz, .tpz, .tar.gz, .tar.z, .tar.Z
-regex/\.t([gp]?z|ar\.g?[zZ])$
+regex/\.[tT]([gGpP]?[zZ]|[aA][rR]\.[gG]?[zZ])$
 	Open=%cd %p#utar
 	View=%view{ascii} gzip -dc %f 2>/dev/null | tar tvvf -
 
-regex/\.tar\.bz$
+regex/\.[tT][aA][rR]\.[bB][zZ]$
 	# Open=%cd %p#utar
 	View=%view{ascii} bzip -dc %f 2>/dev/null | tar tvvf -
 
-regex/\.t(ar\.bz2|bz|b2)$
+regex/\.[tT]([aA][rR]\.[bB][zZ]2|[bB][zZ]|[bB]2)$
 	Open=%cd %p#utar
 	View=%view{ascii} bzip2 -dc %f 2>/dev/null | tar tvvf -
 
 # .tar.F - used in QNX
-regex/\.tar\.F$
+regex/\.[tT][aA][rR]\.F$
 	# Open=%cd %p#utar
 	View=%view{ascii} freeze -dc %f 2>/dev/null | tar tvvf -
 
@@ -130,7 +130,7 @@
 	View=%view{ascii} gzip -dc %f 2>/dev/null | tar tvvf -
 
 # tar
-regex/\.(tar|TAR)$
+regex/\.[tT][aA][rR]$
 	Open=%cd %p#utar
 	View=%view{ascii} tar tvvf %f
 
@@ -159,7 +159,7 @@
 # rar
 regex/\.[rR]([aA][rR]|[0-9][0-9])$
 	Open=%cd %p#urar
-	View=%view{ascii} rar v -c- %f
+	View=%view{ascii} unrar v -c- %f
 
 # cpio
 shell/.cpio.Z
@@ -175,11 +175,13 @@
 	View=%view{ascii} cpio -itv <'%f' 2>/dev/null
 
 # ls-lR
-regex/(^|\.)ls-?lR(\.g?z|Z|bz2)?$
+regex/(^|\.)ls-?lR$
+	Open=%cd %p#lslR
+regex/(^|\.)ls-?lR(\.g?z|Z|bz2)$
 	Open=%cd %p#lslR
 
 # patch
-regex/\.(diff|patch)(\.(bz2|gz|Z))?$
+regex/\.(diff?|patch)(\.(bz2|gz|Z))?$
         Open=%cd %p#patchfs
 
 # ar library
@@ -231,7 +230,7 @@
 	Open=%var{EDITOR:vi} %f
 
 # Object
-shell/.o
+regex/\.k?o$
 	#Open=%var{PAGER:more} %f
 	View=%view{ascii} file %f && nm %f
 
@@ -265,6 +264,10 @@
 	Open=case %d/%f in */log/*|*/logs/*) cat %f ;; *) nroff @MAN_FLAGS@ @MANDOC@ %f ;; esac | %var{PAGER:more}
 	View=%view{ascii,nroff} case %d/%f in */log/*|*/logs/*) cat %f ;; *) nroff @MAN_FLAGS@ @MANDOC@ %f ;; esac
 
+regex/\.pod$
+	Open=pod2man %f | nroff @MAN_FLAGS@ @MANDOC@ | %var{PAGER:more}
+	View=%view{ascii,nroff} pod2man %f | nroff @MAN_FLAGS@ @MANDOC@
+
 # Troff with me macros.
 # Exception - "read.me" is not a nroff file.
 shell/read.me
@@ -334,7 +337,7 @@
 	View=sxpm %f
 
 include/image
-	Open=if [ "$DISPLAY" = "" ]; then zgv %f; else (gqview %f &); fi
+	Open=if [ "$DISPLAY" = "" ]; then zgv %f; else (gthumb %f &); fi
 	View=%view{ascii} identify %f
 	#View=%view{ascii} asciiview %f
 
@@ -403,7 +406,7 @@
 	Open=(realplay %f >/dev/null 2>&1 &)
 
 include/video
-	Open=(mplayer %f >/dev/null 2>&1 &)
+	Open=(totem %f >/dev/null 2>&1 &)
 	#Open=(gtv %f >/dev/null 2>&1 &)
 	#Open=(xanim %f >/dev/null 2>&1 &)
 
@@ -412,13 +415,13 @@
 
 # Postscript
 type/^PostScript
-	Open=(gv %f &)
+	Open=(evince %f &)
 	View=%view{ascii} ps2ascii %f
 
 # PDF
 type/^PDF
-	Open=(xpdf %f &)
-	#Open=(acroread %f &)
+	#Open=(xpdf %f &)
+	Open=(evince %f 2>/dev/null &)
 	#Open=(ghostview %f &)
 	View=%view{ascii} pdftotext %f -
 
@@ -427,16 +430,16 @@
 
 # html
 regex/\.([hH][tT][mM][lL]?)$
-	Open=(if test -n "@X11_WWW@" && test -n "$DISPLAY"; then (@X11_WWW@ file://%d/%p &) 1>&2; else links %f || lynx -force_html %f || ${PAGER:-more} %f; fi) 2>/dev/null
-	View=%view{ascii} lynx -dump -force_html %f
+	Open=(if test -n "" && test -n "$DISPLAY"; then ( file://%d/%p &) 1>&2; else links %f || w3m %f || lynx -force_html %f || ${PAGER:-more} %f; fi) 2>/dev/null
+	View=%view{ascii} w3m -dump %f
 
 # StarOffice 5.2
 shell/.sdw
-	Open=(ooffice %f &)
+	Open=(openoffice %f &)
 
 # StarOffice 6 and OpenOffice.org formats
 regex/\.(odt|ott|sxw|stw|ods|ots|sxc|stc|odp|otp|sxi|sti|odg|otg|sxd|std|odb|odf|sxm|odm|sxg)$
-	Open=(ooffice %f &)
+	Open=(openoffice %f &)
 	View=%view{ascii} unzip -p %f content.xml | o3totxt
 
 # AbiWord
@@ -445,27 +448,27 @@
 
 # Microsoft Word Document
 regex/\.([Dd][oO][cCtT]|[Ww][rR][iI])$
-	Open=(abiword %f >/dev/null 2>&1 &)
-	View=%view{ascii} catdoc -w %f || word2x -f text %f - || strings %f
+	Open=(openoffice %f >/dev/null 2>&1 &)
+	View=%view{ascii} antiword %f || catdoc -w %f || word2x -f text %f - || strings %f
 type/^Microsoft\ Word
-	Open=(abiword %f >/dev/null 2>&1 &)
-	View=%view{ascii} catdoc -w %f || word2x -f text %f - || strings %f
+	Open=(openoffice %f >/dev/null 2>&1 &)
+	View=%view{ascii} antiword %f || catdoc -w %f || word2x -f text %f - || strings %f
 
 # RTF document
 regex/\.([rR][tT][fF])$
-	Open=(abiword %f >/dev/null 2>&1 &)
+	Open=(openoffice %f >/dev/null 2>&1 &)
 
 # Microsoft Excel Worksheet
 regex/\.([xX][lL][sSwW])$
-	Open=(gnumeric %f >/dev/null 2>&1 &)
+	Open=(openoffice %f >/dev/null 2>&1 &)
 	View=%view{ascii} xls2csv %f || strings %f
 type/^Microsoft\ Excel
-	Open=(gnumeric %f >/dev/null 2>&1 &)
+	Open=(openoffice %f >/dev/null 2>&1 &)
 	View=%view{ascii} xls2csv %f || strings %f
 
 # Use OpenOffice.org to open any MS Office documents
 type/^Microsoft\ Office\ Document
-	Open=(ooffice %f &)
+	Open=(openoffice %f &)
 
 # Framemaker
 type/^FrameMaker
@@ -538,6 +541,22 @@
 	View=%view{ascii} gzip -dc %f 2>/dev/null
 
 
+# checksum files
+regex/\.([sS][fF][vV])$
+	Open=cksfv -f %f
+
+regex/\.([mM][dD]5)$
+	Open=%view{ascii}md5sum -c %f
+
+
+regex/\.([dD][jJ][vV][uU])$
+	Open=(evince %f &)
+
+
+regex/\.([iI][cC][oO])$
+	Open=(gthumb %f &)
+
+
 ### Default ###
 
 # Default target for anything not described above
