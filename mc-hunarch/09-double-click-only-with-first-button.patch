diff -Naur mc-4.6.1.orig/src/key.c mc-4.6.1/src/key.c
--- mc-4.6.1.orig/src/key.c	2005-06-08 14:27:19.000000000 +0200
+++ mc-4.6.1/src/key.c	2006-04-19 16:00:00.000000000 +0200
@@ -511,7 +511,7 @@
     } else {
         ev->type = GPM_DOWN;
 	GET_TIME (tv2);
-	if (tv1.tv_sec && (DIF_TIME (tv1,tv2) < double_click_speed)){
+	if (tv1.tv_sec && (DIF_TIME (tv1,tv2) < double_click_speed) && (btn == 0)){
 	    clicks++;
 	    clicks %= 3;
 	} else
@@ -1083,6 +1083,10 @@
 	    && FD_ISSET (gpm_fd, &select_set)) {
 	    Gpm_GetEvent (&ev);
 	    Gpm_FitEvent (&ev);
+	    if (ev.buttons != GPM_B_NONE && ev.buttons != GPM_B_LEFT) {
+		ev.type &= ~(GPM_DOUBLE|GPM_TRIPLE);
+		ev.type |= GPM_SINGLE;
+	    }
 	    *event = ev;
 	    return EV_MOUSE;
 	}
