# Contributor: Borislav Gerassimov (borislav_ba (at) hotmail.com>
# Maintainer: corvolino <corvolino@archlinux.com.br>

pkgname=idzebra
pkgver=2.0.44
pkgrel=2
pkgdesc="High-performance, general-purpose structured text indexing and retrieval engine."
arch=("i686" "x86_64")
url=("http://indexdata.dk/zebra")
license=('GPL')
depends=("yaz" "expat")
makedepends=("make" "gcc")
source=("http://ftp.indexdata.dk/pub/zebra/${pkgname}-${pkgver}.tar.gz")
md5sums=("aa7562e0a1bff05d79893c128bfde35e")

build() {
  cd "$srcdir/$pkgname-$pkgver"

  ./configure --prefix=/usr
  make
}

package() {
  cd "$srcdir/$pkgname-$pkgver"

  make DESTDIR="$pkgdir/" install
}



