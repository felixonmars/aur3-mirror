build() {
  MODULES="$(all_modules "\(btrfs\|\(lib\)\?crc32c\?\|zlib_\(de\|in\)flate\).ko")"
  BINARIES="$(type -p btrfs) $(type -p btrfsctl) $(type -p btrfsck)"
  FILES=""
  SCRIPT="btrfs_advanced"
}
