# Maintainer: Christian Hesse <mail@eworm.de>

pkgname=nullshell
pkgver=0.0.1
pkgrel=1
pkgdesc="do nothing but print asterisks, can be used for login shell"
arch=('i686' 'x86_64')
url="https://github.com/eworm-de/nullshell"
conflicts=('nullshell-git')
license=('GPL')
install=nullshell.install
source=("http://www.eworm.de/download/linux/${pkgname}-${pkgver}.tar.xz"{,.asc})
sha256sums=('SKIP'
            'SKIP')

build() {
	cd ${pkgname}-${pkgver}/

	make
}

package() {
	cd ${pkgname}-${pkgver}/

	make DESTDIR="${pkgdir}" install
}

