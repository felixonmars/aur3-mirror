# Contributor: giniu <gginiu@gmail.com>
pkgname=cloud
pkgver=2.2.0
pkgrel=1
pkgdesc="Cloud is the client-side module to connect to the PiCloud servers, simple cloud computing solution with Python."
arch=(any)
url="http://www.picloud.com/"
license=('LGPL')
depends=('python2')
makedepends=('setuptools')
optdepends=('pyopenssl')
options=(!emptydirs)
source=(http://media.picloud.com/downloads/${pkgname}/${pkgver}/${pkgname}-${pkgver}.tar.gz)
md5sums=('3140e5087dff080de52ce7811a232eb3')

build() {
  cd "$srcdir"/${pkgname}-$pkgver
  python2 setup.py install --root="$pkgdir"/ --optimize=1 || return 1
}

