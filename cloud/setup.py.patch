--- setup.py.orig	2012-04-05 14:14:44.000000000 +0200
+++ setup.py	2012-04-05 14:15:27.000000000 +0200
@@ -103,8 +103,8 @@
         'Topic :: System :: Networking',
         
         ],
-      #data_files=[('/etc/bash_completion.d', ['bash_completion.d/picloud']),
-      #            ('/usr/share/man/man1', ['doc/picloud.1'])],
+      data_files=[('/etc/bash_completion.d', ['bash_completion.d/picloud']),
+                  ('/usr/share/man/man1', ['doc/picloud.1'])],
       entry_points={
           'console_scripts': [
               'picloud = cloud.cli.main:main',]
@@ -112,21 +112,6 @@
      )
 
 if 'install' in sys.argv:
-    if os.name != 'nt':
-        
-        # Try to install manpage and bash scripts:
-        try:
-            shutil.copyfile('bash_completion.d/picloud', '/etc/bash_completion.d/picloud')
-        except (OSError, IOError), e:
-            sys.stderr.write('Warning: Could not install bash completion scripts due to %s\n' % str(e))
-        
-        try:
-            shutil.copyfile('doc/picloud.1', '/usr/share/man/man1/picloud.1')
-        except (OSError, IOError), e:
-            sys.stderr.write('Warning: Could not install bash completion scripts due to %s\n' % str(e))
-
-    
-    
     print ''
     print '********************************************************'
     print '********************************************************'
