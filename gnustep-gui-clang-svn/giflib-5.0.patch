--- a/Source/NSBitmapImageRep+GIF.m	2013-08-14 14:47:10.278573901 +0200
+++ b/Source/NSBitmapImageRep+GIF.m	2013-08-14 15:10:05.449777559 +0200
@@ -39,7 +39,7 @@
 #import "NSBitmapImageRep+GIF.h"
 #import "GSGuiPrivate.h"
 
-#if HAVE_LIBUNGIF || HAVE_LIBGIF
+// #if HAVE_LIBUNGIF || HAVE_LIBGIF
 
 /*
 gif_lib.h (4.1.0b1, possibly other versions) uses Object as the name of an
@@ -144,9 +144,6 @@ static int gs_gif_output(GifFileType *fi
   gs_gif_init_input_source(&src, imageData);
 #if GIFLIB_MAJOR >= 5
   file = DGifOpen(&src, gs_gif_input, NULL);
-#else
-  file = DGifOpen(&src, gs_gif_input);
-#endif
   if (file == NULL)
     {
       /* we do not use giferror here because it doesn't
@@ -220,7 +217,7 @@ static int gs_gif_output(GifFileType *fi
 
   /* open the image */
   gs_gif_init_input_source(&src, imageData);
-  file = DGifOpen(&src, gs_gif_input);
+  file = DGifOpen(&src, gs_gif_input, NULL);
   if (file == NULL)
     {
       /* we do not use giferror here because it doesn't