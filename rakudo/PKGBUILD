# Maintainer: spider-mario <spidermario@free.fr>
# Contributor: Marcin Karpezo <sirmacik at gmail dot com>
# Contributor: Daenyth <Daenyth+Arch [at] gmail [dot] com>
pkgname=rakudo
pkgver=2013.01
pkgrel=1
pkgdesc="Perl6 compiler for the Parrot virtual machine"
arch=('i686' 'x86_64')
url='http://rakudo.org/'
license=(PerlArtistic)
depends=('nqp>=2013.01')
makedepends=('perl>=5.8')
options=('!makeflags')
source=(http://${pkgname}.org/downloads/star/$pkgname-star-${pkgver}.tar.gz)
md5sums=('b4521370638bbf5dc489cf61d3ce4a5e')

build() {
    cd "$srcdir/$pkgname-star-$pkgver"
    perl Configure.pl --prefix=/usr \
                      --with-nqp=/usr/bin/nqp
    make
}
package() {
    cd "$srcdir/$pkgname-star-$pkgver"

    make DESTDIR="$pkgdir" install
    mv "$pkgdir"/usr/man "$pkgdir"/usr/share/man
}

