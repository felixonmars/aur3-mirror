# Generated by Xyne::Arch::CPAN 0.07

pkgname=perl-marc-crosswalk-dublincore
pkgver=0.02
pkgrel=1
pkgdesc="Convert data between MARC and Dublin Core"
arch=('i686' 'x86_64')
url="http://search.cpan.org/dist/MARC-Crosswalk-DublinCore/"
license=('PerlArtistic' 'GPL')
source=('http://search.cpan.org/CPAN/authors/id/B/BR/BRICAS/MARC-Crosswalk-DublinCore-0.02.tar.gz')
md5sums=('9927560489c7e87aafe57c3a5e7a69ba')
sha256sums=('0e759b4c87350ce484f0c135b3d1d343e26aa0429d60e8cef4accb3d3a2fea29')
depends=('perl-dublincore-record' 'perl-marc-record' )
options=(!emptydirs)

build() {
  _dir=$(find $srcdir -maxdepth 2 -type f -name 'Makefile.PL')
  if [ ! -z "$_dir" ]; then
    cd $(dirname "$_dir")
    PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1
    make  || return 1
    make install DESTDIR="${pkgdir}" || return 1

  else
  _dir=$(find $srcdir -maxdepth 2 -type f -name 'Build.PL')
  if [ ! -z "$_dir" ]; then
    cd $(dirname "$_dir")
    PERL_MM_USE_DEFAULT=1 perl Build.PL INSTALLDIRS=vendor || return 1
    ./Build  || return 1
    ./Build install destdir=${pkgdir} || return 1

  else
    echo "error: failed to detect build method for $pkgname"
    echo "you may be able to fix this by editing the PKGBUILD"
    return 1
  fi fi

  # remove perllocal.pod and .packlist
  find ${pkgdir} -name perllocal.pod -delete
  find ${pkgdir} -name .packlist -delete
}

# vim:set ts=2 sw=2 et:

