pkgname=(git-ftp)
pkgver=20121014
pkgrel=1
pkgdesc="Git powered FTP client written as shell script."
arch=(any)
url="https://github.com/resmo/git-ftp.git"
license=(GPL3)
depends=('git')
makedepends=('git')
provides=('git-ftp')
source=()
noextract=()

_gitroot="git://github.com/resmo/git-ftp.git"
_gitname="master"

build() {
  cd "$srcdir"
  msg "Connecting to GIT server...."

  if [[ -d "$_gitname" ]]; then
    cd "$_gitname" && git pull origin
    msg "The local files are updated."
  else
    git clone "$_gitroot" "$_gitname"
  fi

  msg "Starting build..."

  rm -rf "$srcdir/$_gitname-build"
  git clone "$srcdir/$_gitname" "$srcdir/$_gitname-build"
  cd "$srcdir/$_gitname-build"

  #
  # BUILD HERE
  #
  # Nothing to be done.
}

package() {
  cd "$srcdir/$_gitname-build"
  make DESTDIR="$pkgdir/usr/" install
}
