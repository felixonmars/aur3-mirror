# Maintainer: Josip Ponjavic <josipponjavic at gmail dot com>

pkgname=deepin-icon-theme
pkgver=git20130618142313~840c343521
pkgrel=2
pkgdesc="This package is DeepinIconTheme which inherits the faenza-icon-theme."
arch=('any')
options=(!strip)
url="http://www.linuxdeepin.com"
license=('LGPL-3+')
depends=('faenza-icon-theme')
source=("http://packages.linuxdeepin.com/deepin/pool/main/d/$pkgname/${pkgname}_13.06.12+${pkgver}.tar.gz")
md5sums=('b07cf2de59bee8c1450b1c70a6562fc3')

package() {
  cd "$srcdir/$pkgname-13.06.12+$pkgver"
  mkdir -p "$pkgdir/usr/share/icons/"
  
  cp -rf Deepin "$pkgdir/usr/share/icons/Deepin"
}
