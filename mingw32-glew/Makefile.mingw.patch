2,4c2,6
< CC = gcc
< # use gcc for linking, with ld it does not work
< LD = gcc
---
> CC = i486-mingw32-gcc
> AR = i486-mingw32-ar
> STRIP = i486-mingw32-strip
> # use i486-mingw32-gcc for linking, with ld it does not work
> LD = i486-mingw32-gcc
