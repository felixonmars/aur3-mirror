diff -Naur core-orig/python/core/services/nrl.py core/python/core/services/nrl.py
--- core-orig/python/core/services/nrl.py	2012-02-03 13:33:51.000000000 -0500
+++ core/python/core/services/nrl.py	2012-02-08 14:08:23.579392656 -0500
@@ -45,8 +45,9 @@
         '''
         cmd = cls._startup[0]
         cmd += " instance %s" % node.name
-        netifs = list(node.netifs())
-        # TODO: remove control interfaces here
+        # Filter out control interfaces
+        netifs = filter(lambda x: not x.name.startswith("ctrl"),
+                        node.netifs())
         if len(netifs) > 0:
             cmd += " cf "
             interfacenames = map(lambda x: x.name,  netifs)
