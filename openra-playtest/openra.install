post_install() {
    echo "The latest OpenRA playtest was installed in /opt/openra."
    echo
    echo "Run /opt/openra/bin/openra to start it."
}

post_remove() {
    echo "OpenRA may have downloaded data files and saved replays and"
    echo "settings to ~/.openra."
    echo "You might want to delete those."
}
