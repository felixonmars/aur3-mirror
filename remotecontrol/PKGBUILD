# Maintainer: MacCyber <jonas.enge at gmail dot com>

pkgname=remotecontrol
pkgver=1.0
pkgrel=1
pkgdesc="GTK remotecontrol for dreambox web"
arch=('i686' 'x86_64')
url="http://jonasenge.no/tv-remote-control/"
license=('GPL')
depends=('gtk3')
source=("http://w166890-www.php5.dittdomene.no/div/remotecontrol-${pkgver}.tar.gz")
md5sums=('e3adcf1728ebc543591e4c8ab3acc361')

build() {
  cd "remotecontrol-${pkgver}"
  ./configure
  make
}

package() {
  install -Dm755 "remotecontrol-${pkgver}/src/remotecontrol" "${pkgdir}/usr/bin/remotecontrol"
  install -Dm644 "remotecontrol-${pkgver}/src/remotecontrol.ui" "${pkgdir}/usr/share/remotecontrol/ui/remotecontrol.ui"
  install -Dm644 "remotecontrol-${pkgver}/src/remote.png" "${pkgdir}/usr/share/pixmaps/remotecontrol/remote.png"
}
