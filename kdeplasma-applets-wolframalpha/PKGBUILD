# Maintainer: Andrea Scarpino <andrea@archlinux.org>

pkgname=kdeplasma-applets-wolframalpha
pkgver=1.0
pkgrel=1
pkgdesc="A plasmoid frontend for the Wolfram Alpha service"
arch=('any')
url="https://github.com/ndr/wolframalpha-plasmoid"
license=('GPL')
depends=('kdebase-workspace')
makedepends=('cmake' 'automoc4')
source=(${pkgname}-${pkgver}.tar.gz::"https://github.com/ndr/wolframalpha-plasmoid/tarball/${pkgver}")
md5sums=('4cbd2ae4aada27a87c403ad1dda98db7')

build() {
  mkdir build
  cd build
  cmake ../ndr-wolframalpha-plasmoid-025ce2d \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=$(kde4-config --prefix)
  make
}

package() {
  cd build
  make DESTDIR="$pkgdir/" install
}
