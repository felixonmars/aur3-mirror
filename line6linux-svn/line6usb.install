## arg 1:  the new package version
post_install() {
  echo "==> run 'depmod -a' as root to load the new module"
}

## arg 1:  the new package version
## arg 2:  the old package version
post_upgrade() {
  echo "==> run 'depmod -a' as root to load the new module"
}

# vim:set ts=2 sw=2 et:
