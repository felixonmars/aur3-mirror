--- a/jetty.sh	2014-02-18 00:36:56.645556282 +0200
+++ b/jetty.sh	2014-02-18 00:44:55.413817496 +0200
@@ -282,10 +282,8 @@
 #####################################################
 # Find a location for the pid file
 #####################################################
-if [ -z "$JETTY_RUN" ] 
-then
-  JETTY_RUN=$(findDirectory -w /var/run /usr/var/run $JETTY_BASE /tmp)
-fi
+mkdir -p "$JETTY_RUN"
+chown -R "$JETTY_USER":"$JETTY_USER" "$JETTY_RUN"
 
 #####################################################
 # Find a pid and state file
@@ -297,7 +295,7 @@
 
 if [ -z "$JETTY_STATE" ] 
 then
-  JETTY_STATE=$JETTY_BASE/${NAME}.state
+  JETTY_STATE=$JETTY_RUN/${NAME}.state
 fi
 JAVA_OPTIONS+=("-Djetty.state=$JETTY_STATE")
 rm -f $JETTY_STATE
