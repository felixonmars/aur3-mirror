# Contributor: Guilherme "nirev" Nogueira <guilherme@nirev.org>
# Contributor: Chris Darnell <cedeel@gmail.com>
# vim:set ts=2 sw=2 et:
_gemname=less
pkgname=ruby-${_gemname}
pkgver=2.3.2
pkgrel=1
pkgdesc="ruby gem; Leaner CSS."
arch=(any)
url="http://www.lesscss.org/"
license=('MIT')
depends=('ruby' 'ruby-commonjs')
makedepends=('rubygems')
source=("http://rubygems.org/downloads/${_gemname}-${pkgver}.gem")
noextract=("${_gemname}-$pkgver.gem")
sha256sums=('c7b801a343c59457c3a218d8fa8071a9475ce3b49ad39a031ce8aa104b9e6bdc')

build() {
  cd $srcdir
  export HOME=/tmp
  local _gemdir="$(ruby -rubygems -e'puts Gem.default_dir')"
  gem install  --no-user-install --ignore-dependencies -i "${pkgdir}${_gemdir}" -n "${pkgdir}/usr/bin" ${_gemname}-$pkgver.gem
}

