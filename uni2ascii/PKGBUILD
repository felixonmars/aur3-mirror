# Maintainer: Andrew Antle <andrew@antlechrist.org>
# Contributor: Anton Bazhenov <anton.bazhenov at gmail>
# Contributor: Ashok `ScriptDevil` Gautham <ScriptDevil@gmail.com>

pkgname=uni2ascii
pkgver=4.15
pkgrel=1
pkgdesc="Provides conversion in both directions between UTF-8 and many 7-bit ASCII equivalents"
arch=('i686' 'x86_64')
url="http://billposer.org/Software/uni2ascii.html"
license=('GPL3')
source=(http://billposer.org/Software/Downloads/$pkgname-$pkgver.tar.bz2)
md5sums=('030c50d6c788eee9ff38e064dc56bf1b')

build() {
  cd "$srcdir/$pkgname-$pkgver"

  ./configure \
    --prefix=/usr \
    --mandir=/usr/share/man
  make
}

package() {
  cd "$srcdir/$pkgname-$pkgver"

  make DESTDIR="$pkgdir" install
}
