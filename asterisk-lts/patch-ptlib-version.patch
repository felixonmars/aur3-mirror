diff -aur asterisk-11.0.1-orig/configure asterisk-11.0.1/configure
--- asterisk-11.0.1-orig/configure	2012-09-20 20:18:47.000000000 +0200
+++ asterisk-11.0.1/configure	2012-11-11 16:31:18.788892696 +0100
@@ -25544,7 +25544,11 @@
 	fi
 
 	if test "${HAS_PWLIB:-unset}" != "unset"; then
-		PWLIB_VERSION=`grep "$VNAME" ${PWLIB_INCDIR}/ptbuildopts.h | sed -e 's/[[:space:]]\{1,\}/ /g' | cut -f3 -d ' ' | sed -e 's/"//g'`
+		if test "x${PTLIB_CONFIG}" != "x"; then
+			PWLIB_VERSION=`${PTLIB_CONFIG} --version`
+		else
+			PWLIB_VERSION=`grep "$VNAME" ${PWLIB_INCDIR}/ptbuildopts.h | sed -e 's/[[:space:]]\{1,\}/ /g' | cut -f3 -d ' ' | sed -e 's/"//g'`
+		fi
 		PWLIB_MAJOR_VERSION=`echo ${PWLIB_VERSION} | cut -f1 -d.`
 		PWLIB_MINOR_VERSION=`echo ${PWLIB_VERSION} | cut -f2 -d.`
 		PWLIB_BUILD_NUMBER=`echo ${PWLIB_VERSION} | cut -f3 -d.`
