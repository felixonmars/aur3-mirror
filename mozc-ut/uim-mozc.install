post_install() {
  cat << _EOF
  ==> sudo uim-module-manager --register mozc
  ==> to register mozc on uim.
_EOF
}

post_remove() {
  cat << _EOF
  ==> sudo uim-module-manager --unregister mozc
  ==> to unregister mozc from uim.
_EOF
}