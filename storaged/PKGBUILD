# Maintainer: Iwan Timmer <irtimmer@gmail.com>

pkgname=storaged
pkgver=0.3.0
pkgrel=1
pkgdesc="LVM DBus add-on for udisks"
url="https://github.com/cockpit-project/storaged"
license=('LGPL')
arch=('i686' 'x86_64')
depends=('udisks2' 'lvm2')
makedepends=()
source=(https://github.com/cockpit-project/${pkgname}/archive/${pkgver}.tar.gz)
md5sums=('086193394e8759b6c416c3d1488da5d7')

build() {
  cd ${pkgname}-${pkgver}
  ./autogen.sh
  ./configure --prefix=/usr --sbindir=/usr/bin --sysconfdir=/etc
  make
}

package() {
  cd ${pkgname}-${pkgver}
  make DESTDIR="$pkgdir" install
}
