# Maintainer: Stefan Husmann <stefan-husmann@t-online.de>
pkgname=hop-devel  
pkgver=2.5.0_pre6
_pkgver=${pkgver//_/-}
pkgrel=1
pkgdesc="a Software Development Kit for the Web"
url='http://hop.inria.fr'
arch=('i686' 'x86_64')
license=('GPL' 'LGPL')
depends=('bigloo>=4.1a' 'sqlite')
conflicts=('hop')
provides=('hop')
install=hop.install
options=('!makeflags')
source=(ftp://ftp-sop.inria.fr/indes/fp/Hop/hop-${_pkgver}.tar.gz)
md5sums=('ae0c5b50dc52236f722db06340f1a0c7')

build() {
  cd ${srcdir}/hop-${_pkgver}
  ./configure --prefix=/usr --etcdir=/etc/hop --mandir=/usr/share/man
  make clean
  make
}

package() {
  cd ${srcdir}/hop-${_pkgver}
  make DESTDIR=${pkgdir} install
  cd ${pkgdir}/usr/bin; rm hop; ln -s hop-2.5.0 hop
}

