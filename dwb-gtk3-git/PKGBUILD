# Contributor: portix <portix@gmx.net>

pkgname=dwb-gtk3-git
_gitname=dwb
pkgver=f873771_2013.04.08
pkgrel=1
pkgdesc="A webkit web browser with vi-like keyboard shortcuts, latest git-pull" 
url="http://portix.bitbucket.org/dwb-git"
arch=('i686' 'x86_64')
license=('GPL')
depends=('webkitgtk2' 'desktop-file-utils')
provides=(dwb)
source=("git+https://git@bitbucket.org/portix/dwb.git")
install=dwb.install
conflicts=('dwb' 'dwb-git' 'dwb-gtk3' 'dwb-gtk3-hg' 'dwb-hg')
makedepends=('mercurial' 'json-c')
md5sums=('SKIP')

pkgver() {
    cd "$srcdir"/$pkgname
    git log -1 --format="%h_%cd" --date=short | sed 's/-/./g'
}
build() {
  cd "$srcdir/$_gitname"
  make GTK=3
}
package() {
  cd "${srcdir}/${_gitname}"
  make DESTDIR="${pkgdir}" install \
      BASHCOMPLETION=/usr/share/bash-completion/completions
}
