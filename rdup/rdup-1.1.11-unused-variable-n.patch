diff -up rdup-1.1.11/rdup.c rdup-1.1.11.new/rdup.c
--- rdup-1.1.11/rdup.c	2011-02-19 23:30:07.000000000 +0100
+++ rdup-1.1.11.new/rdup.c	2011-05-06 12:29:33.000000000 +0200
@@ -59,7 +59,7 @@ g_tree_subtract(GTree *a, GTree *b)
 static GTree *
 g_tree_read_file(FILE *fp)
 {
-	gchar 	      *buf, *n, *p, *q;
+	gchar 	      *buf, *p, *q;
 	gchar 	      delim, linktype;
 	mode_t        modus;
 	GTree         *tree;
@@ -94,8 +94,6 @@ g_tree_read_file(FILE *fp)
 		if (s < LIST_MINSIZE)
 			CORRUPT("Corrupt entry at line: %zd, line to short");
 
-		n = strrchr(buf, '\n');
-
 		/* get modus */
 		if (buf[LIST_SPACEPOS] != ' ')
 			CORRUPT("Corrupt entry at line: %zd, no space found");
