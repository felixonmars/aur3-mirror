# Maintainer: aratmos OR argos-void <void.at.null AT gmail.com>. Usually at #archlinux IRC channel :)

#This process was simpler before the adobe flash 10.2 update, moving the /tmp file to somewhere"
# It was impossible to download videos this way. Now, is again possible."

# The script contained in this package was originally written by:
# Enrique Molina <kique165@gmail.com>
# Carlos Alberto <elcazador_rca@yahoo.es>
# Esteban RM <exio4.com@gmail.com>

pkgname=flash
pkgver=0.5
_pkgopt=EN-beta

pkgrel=1
pkgdesc="Isnt a video downloader; it only moves the file created by your web browser's flash-plugin to somewhere you want"
url="http://kike.xtreemhost.com/flash/index.html"
arch=('x86_64' 'i686')
license=('GPLv3')
depends=('bash' 'bzip2' 'tar')
optdepends=()
makedepends=()
conflicts=()
replaces=()
backup=()
#install=${pkgname}.install
source=("http://kike.xtreemhost.com/files/pub/flash/${pkgname}-${pkgver}-${_pkgopt}.tar.bz2")

# I did them in Sat 06/19/11
md5sums=('efb0ac92ed165b1de184c42f221d3eed')

build() {
  cd ${srcdir}
  tar xvf ${pkgname}-${pkgver}-${_pkgopt}.tar.bz2
  cp -rf usr ${pkgdir}
  cp -rf opt ${pkgdir}
  clear;
  echo "***READ THIS!!!"
  echo "***PROCESS FOR DOWNLOADING ANY VIDEO FROM THE INTERNET:***"
  echo "-You must have running your web browser, with the video you want to download playing."
  echo "-You do flash -dd (WAIT,its slow), follow the script steps, and you have the video!"
  sleep 1;
}
