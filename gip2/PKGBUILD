#gip lazy build

pkgname=gip2
pkgver=2
pkgrel=3
pkgdesc="get_iplayer gui"
arch=('x86_64' 'i686')
url="http://www.lumbium.co.uk"
license=('GPL')
groups=()
depends=('get_iplayer' 'gtk2')
opdepends=('vlc' 'mplayer')
makedepends=('lazarus')
provides=('gip2')
backup=()
install=
source=('http://www.lumbium.co.uk/gip/gip2-3.tar.gz')
md5sums=('bb143e0cb28d495cfc00ef713b2d9aea')

build() {

mkdir -p $pkgdir/usr/bin
mkdir -p $pkgdir/usr/share/applications
mkdir -p $pkgdir/usr/share/pixmaps

if test "$CARCH" == i686; then
cd $srcdir/$pkgname-$pkgrel/gip32
else
cd $srcdir/$pkgname-$pkgrel/gip
fi

lazbuild project1.lpr

cp gip $pkgdir/usr/bin/
cp gips $pkgdir/usr/bin/
cp gip.desktop $pkgdir/usr/share/applications/
cp iplayer.png $pkgdir/usr/share/pixmaps/
chmod +x $pkgdir/usr/bin/gip
chmod +x $pkgdir/usr/bin/gips

}