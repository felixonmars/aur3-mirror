# Maintainer: Gordian Edenhofer <gordian[dot]edenhofer[at]yahoo[dot]de
# Contributor: Gustavo Castro <gustawho[at]gmail[dot]com>
# Submitter: 	acrox999 <acrox999[at]gmail[dot]com>

pkgname=gtk-theme-iris-dark-git
pkgver=76.492869a
pkgrel=1
pkgdesc="A flat theme that uses varying shades and tones to create distinction and a modern experience. It supports Gtk 3.10, Gtk 3.12 and Gtk 2 (using the Murrine engine)."
arch=('any')
url="http://thevirtualdragon.deviantart.com/art/Iris-Dark-Gtk-Theme-v1-9-429628194"
license=('GPL3')
depends=('gtk3' 'gtk-engine-murrine')
makedepends=('git')
provides=('gtk-theme-iris-dark-git')
conflicts=('gtk-theme-iris-dark-git')
changelog=('CHANGELOG')
source=("iris-dark"::'git+https://github.com/xyl0n/iris.git')
sha256sums=('SKIP')
_realname=iris-dark

pkgver() {
  cd "${_realname}"
  echo $(git rev-list --count HEAD).$(git rev-parse --short HEAD)
}

package() {
  install -dm755 "${pkgdir}/usr/share/themes/"
  cp -dpr --no-preserve=ownership "${srcdir}/${_realname}" "${pkgdir}/usr/share/themes/"
  find "${pkgdir}" -type d -exec chmod 755 "{}" \;
  find "${pkgdir}" -type f -exec chmod 644 "{}" \;
  find "${pkgdir}" -type d -name ".git" -exec rm -r '{}' +
}
