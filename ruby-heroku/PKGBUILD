# Contributor: Emiliano Vavassori <syntaxerrormmm(at)gmail.com>
# Maintainer: Emiliano Vavassori <syntaxerrormmm(at)gmail.com>
pkgname=ruby-heroku
_gemname=heroku
pkgver=2.31.2
pkgrel=1
pkgdesc="Deploy apps to Heroku from the command line"
arch=(any)
url="http://github.com/heroku/heroku"
license=('MIT')
depends=('ruby' 'ruby-heroku-api>=0.3.4' 'ruby-netrc>=0.7.7' 'ruby-launchy>=0.3.2' 'ruby-rest-client>=1.6.1' 'ruby-rubyzip')
makedepends=('ruby')
source=(http://rubygems.org/downloads/${_gemname}-${pkgver}.gem)
noextract=(${_gemname}-${pkgver}.gem)
md5sums=('cac4d327db42b279dc567e915f288c16')

package() {
  cd "$srcdir"
  local _gemdir="$(ruby -rubygems -e'puts Gem.default_dir')"

  install -d "${pkgdir}/usr/bin"
  gem install --ignore-dependencies --no-user-install -i "${pkgdir}${_gemdir}" -n "${pkgdir}/usr/bin" "${_gemname}-${pkgver}.gem"
}
