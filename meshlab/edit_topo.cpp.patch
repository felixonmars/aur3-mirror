diff -Nur meshlab.orig/src/fgt/edit_topo/edit_topo.cpp meshlab/src/fgt/edit_topo/edit_topo.cpp
--- meshlab.orig/src/fgt/edit_topo/edit_topo.cpp	2011-01-25 18:49:27.000000000 +1100
+++ meshlab/src/fgt/edit_topo/edit_topo.cpp	2011-02-19 11:09:36.330009396 +1100
@@ -1210,16 +1210,16 @@
 	out.clear();
 
 	if(first_model_generated)
-		parentGla->meshDoc->meshList.pop_back();
+		parentGla->md()->meshList.pop_back();
 	
 
-        MeshModel *mm= parentGla->meshDoc->addNewMesh("");
-        //parentGla->meshDoc->meshList.push_back(mm);
+        MeshModel *mm= parentGla->md()->addNewMesh("", NULL);
+        //parentGla->md()->meshList.push_back(mm);
 	first_model_generated = true;
 
 
-	MeshModel *m = parentGla->meshDoc->meshList.back();	// destination = new mesh
-	MeshModel *currentMesh  = parentGla->meshDoc->mm();	// source = current	mesh
+	MeshModel *m = parentGla->md()->meshList.back();	// destination = new mesh
+	MeshModel *currentMesh  = parentGla->md()->mm();	// source = current	mesh
 
 	// if debug value is true, the algorithm will respond with all the
 	// auto generated vertices.
