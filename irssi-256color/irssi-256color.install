post_install() {
	echo '-- Use alias.diff to add two aliases to your config which test color output: /cubes and /bluebar'
	echo '-- The /cubes alias is already hard coded into any *new* config irssi may generate'
}

post_remove() {
	echo '-- You may want to remove leftover aliases (cubes, bluebar) from your config"
}
