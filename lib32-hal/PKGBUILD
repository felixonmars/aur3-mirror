# Maintainer: Nick Campbell <s0ma AT theangryfist DOT net>
# Contributer: Biru Ionut <ionut@archlinux.ro>
# Contributor: Andreas Sch̦nfelder <passtschu at freenet dot org>

_pkgsourcename=hal
pkgname=lib32-hal
pkgver=0.5.14
pkgrel=4
pkgdesc="Hardware Abstraction Layer"
url="http://www.freedesktop.org/wiki/Software/hal"
license=('GPL' 'custom')
arch=('x86_64')
provides=('lib32-hal')
replaces=('lib32-hal')
depends=('lib32-dbus-core' 'lib32-dbus-glib' 'lib32-glibc' 'lib32-glib2')
source=(ftp://ftp.archlinux.org/extra/os/i686/${_pkgsourcename}-${pkgver}-${pkgrel}-i686.pkg.tar.xz)

md5sums=('4c1f32ded18165344c9ebd205b69eb78')

build() {
        cd $srcdir
        mkdir -p $pkgdir/opt/lib32/usr/lib
        cp -dp usr/lib/*.so* $pkgdir/opt/lib32/usr/lib
} 

