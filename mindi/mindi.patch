--- mindi-2.1.1/mindi	2012-02-24 01:36:32.000000000 +0000
+++ mindi-2.1.1/mindi.new	2012-02-28 12:52:52.000000000 +0000
@@ -2655,7 +2655,7 @@
 	done
 
 	# Also copy modules.dep in case of udev so that normal modprobe works
-	tar cf - -C / /$needed_modules_path/modules.dep 2>> $MINDI_TMP/$$.log | (cd "$mountpoint" ; tar xf -) || LogIt "Unable to copy modules.dep to $mountpoint" $MINDI_TMP/$$.log
+	tar cf - -C / /$needed_modules_path/modules.* 2>> $MINDI_TMP/$$.log | (cd "$mountpoint" ; tar xf -) || LogIt "Unable to copy modules.* to $mountpoint" $MINDI_TMP/$$.log
 
 	# Copy FW in case some drivers needs it
 	if [ -d "/lib/firmware" ]; then
