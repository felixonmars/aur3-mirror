diff -Naur mindi-2.0.7.6/deplist.d/base.conf mindi-2.0.7.6-new/deplist.d/base.conf
--- mindi-2.0.7.6/deplist.d/base.conf	2010-04-07 13:17:08.000000000 +0100
+++ mindi-2.0.7.6-new/deplist.d/base.conf	2011-03-21 19:36:42.423333337 +0000
@@ -4,6 +4,9 @@
 #
 /etc/fstab
 /etc/modules.conf
+# On ArchLinux
+/etc/modprobe.d/
+
 /etc/hosts
 /etc/host.conf
 /etc/resolv.conf
@@ -27,6 +30,9 @@
 /usr/bin/file
 /usr/share/misc/file/magic
 /usr/share/file/magic
+# On ArchLinux
+/usr/share/file/magic.mgc
+/usr/share/misc/magic.mgc
 
 /usr/bin/getfattr
 /usr/bin/setfattr
@@ -35,12 +41,12 @@
 
 # On RHEL
 /usr/bin/bzip2
-# On Debian
+# On Debian and ArchLinux
 /bin/bzip2
 # is a link => No additional place
 # On RHEL
 /usr/bin/bunzip2
-# On Debian
+# On Debian and ArchLinux
 /bin/bunzip2
 /usr/bin/lzop
 /usr/bin/lzma
@@ -53,6 +59,8 @@
 /usr/bin/wc
 /usr/bin/xargs
 /usr/bin/less
+# On ArchLinux
+/bin/less
 /sbin/ldconfig
 /usr/bin/tee
 
diff -Naur mindi-2.0.7.6/deplist.d/hw.conf mindi-2.0.7.6-new/deplist.d/hw.conf
--- mindi-2.0.7.6/deplist.d/hw.conf	2009-09-18 14:25:35.000000000 +0100
+++ mindi-2.0.7.6-new/deplist.d/hw.conf	2011-03-21 19:36:39.400000003 +0000
@@ -6,6 +6,8 @@
 /usr/sbin/dmidecode
 /usr/sbin/lshw
 /usr/bin/lspci
+# On ArchLinux
+/usr/sbin/lspci
 /usr/bin/lsusb
 
 # For Itanium platforms
diff -Naur mindi-2.0.7.6/deplist.d/minimal.conf mindi-2.0.7.6-new/deplist.d/minimal.conf
--- mindi-2.0.7.6/deplist.d/minimal.conf	2011-02-26 16:37:13.000000000 +0000
+++ mindi-2.0.7.6-new/deplist.d/minimal.conf	2011-03-21 19:36:28.916666671 +0000
@@ -19,8 +19,15 @@
 /usr/bin/loadkeys
 /bin/loadkeys
 /sbin/pidof
+# On ArchLinux
+/bin/pidof
 /usr/bin/reboot
+#On Archlinux
+/sbin/reboot
+/sbin/shutdown
 /etc/init.d/functions
+# On ArchLinux
+/etc/rc.d/functions
 
 # Compression
 /bin/gzip
@@ -28,6 +35,8 @@
 # Logs
 /sbin/klogd
 /sbin/syslogd
+# On ArchLinux
+/usr/sbin/syslog-ng
 
 # HW related
 /sbin/fdisk
@@ -40,6 +49,8 @@
 /usr/bin/sshfs 
 /sbin/mount.fuse
 /usr/bin/fusermount
+# On ArchLinux
+/bin/fusermount
 /usr/bin/ssh
 # Required by ssh as dlopened
 /usr/lib/libnss_compat.so
@@ -48,6 +59,8 @@
 /usr/lib64/libnss_files.so
 /etc/ssh
 /usr/bin/ulockmgr_server
+#On ArchLinux
+/bin/ulockmgr_server
 /sbin/mount.cifs
 /sbin/mount.cifs3
 /sbin/mount.smb
@@ -66,6 +79,8 @@
 /sbin/udev.static
 /sbin/udevsend
 /sbin/udevd
+# On ArchLinux
+/sbin/udevadm
 /sbin/udevstart.static
 /sbin/udev_volume_id
 /sbin/start_udev
@@ -77,6 +92,8 @@
 # Modules
 /sbin/insmod
 /sbin/lsmod
+# On ArchLinux
+/bin/lsmod
 /sbin/modprobe
 /etc/modprobe.d
 /etc/modprobe.conf
@@ -115,6 +132,9 @@
 /sbin/route
 /sbin/dhclient
 /var/lib/dhcp/dhclient.leases
+# On ArchLinnux
+/sbin/dhcpcd
+/var/lib/dhcpd
 
 # Busybox
 /usr/lib/mindi/rootfs/bin/busybox
diff -Naur mindi-2.0.7.6/mindi mindi-2.0.7.6-new/mindi
--- mindi-2.0.7.6/mindi	2011-02-28 04:50:38.000000000 +0000
+++ mindi-2.0.7.6-new/mindi	2011-03-21 19:43:48.843333336 +0000
@@ -377,6 +377,9 @@
 	elif [ -e "/etc/conf.d/keymaps" ] ; then
 		echo "Gentoo-style config detected." >> $LOGFILE
 		keyfile=/etc/conf.d/keymaps
+	elif [ -e "/etc/rc.conf" ] ; then
+		echo "ArchLinux config detected." >> $LOGFILE
+		keyfile=/etc/rc.conf
 	else
 		echo -en "Searching for rc.config ..."
 		keyfile=`find /etc -name rc.config | head -n1`
@@ -394,7 +397,7 @@
 	echo "keyfile=$keyfile" >> $LOGFILE
 	locale=`grep -F KEYTABLE "$keyfile" | tr -d '"' |cut -d'=' -f2`
 	[ ! "$locale" ] && locale=`grep '.map$' "$keyfile" | sed 's/^.* //'`		# Slackware
-	[ ! "$locale" ] && locale=`grep -E '^KEYMAP=' "$keyfile" | tr -d '"' |cut -d'=' -f2`		# Gentoo
+	[ ! "$locale" ] && locale=`grep -E '^KEYMAP=' "$keyfile" | tr -d '"' |cut -d'=' -f2`		# Gentoo & ArchLinux
 	echo "locale=$locale" >> $LOGFILE
 	#
 	# Process the keymaps dir once for all
