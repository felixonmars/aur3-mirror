--- mindi-2.0.7.7/rootfs/sbin/init	2011-04-12 00:16:04.000000000 +0100
+++ mindi-2.0.7.7/rootfs/sbin/init-new	2011-04-14 11:16:16.646666674 +0100
@@ -902,12 +902,12 @@
 ide-opt
 
 #ctrlaltdel soft
-for path in /usr.bin /usr/bin ; do
-	fname=$path/nano
-	[ -e "$fname" ] && ln -sf $fname /usr/bin/pico
-done
-res=`which nano 2> /dev/null`
-[ "$res" ] && ln -sf /usr/bin/
+#for path in /usr.bin /usr/bin ; do
+#	fname=$path/nano
+#	[ -e "$fname" ] && ln -sf $fname /usr/bin/pico
+#done
+#res=`which nano 2> /dev/null`
+#[ "$res" ] && ln -sf /usr/bin/
 for i in null stdout stdin stderr ; do
 	cp -af /dev/$i /tmp
 done
