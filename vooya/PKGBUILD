# Maintainer: archtux <antonio dot arias99999 at gmail dot com>

pkgname=vooya
pkgver=0.5.1
pkgrel=1
pkgdesc="Vooya is a player for raw (uncompressed and containerless) YUV and RGB video data, most commonly used in scientific environments."
arch=('i686' 'x86_64')
url="http://www.offminor.de/"
license=('GPL')
depends=('qt4')
options=('!docs')

if [ "${CARCH}" = 'x86_64' ]; then
  source=(http://www.offminor.de/?file=tl_files/vooya/vooya-0.5-amd64.deb)
  md5sums=('6b1b43094d27a42255d0374b41978366')
elif [ "${CARCH}" = 'i686' ]; then 
  source=(http://www.offminor.de/?file=tl_files/vooya/vooya-0.5-i686.deb)
  md5sums=('933f14a7b1ba530a394ba4a741f93414')
fi

package() {

  # Extract .deb
  bsdtar xf $pkgname*
  bsdtar xf data.tar.gz -C $pkgdir
}