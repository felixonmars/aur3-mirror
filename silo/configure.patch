diff -aur silo-4.9.old/configure silo-4.9.new/configure
--- silo-4.9.old/configure	2013-01-23 19:45:04.000000000 -0800
+++ silo-4.9.new/configure	2013-03-04 08:46:55.536522164 -0800
@@ -23289,14 +23289,14 @@
 
 
 
-SILEX=$DEFAULT_SILEX
+#SILEX=$DEFAULT_SILEX
 # Check whether --enable-silex was given.
-if test "${enable_silex+set}" = set; then
-  enableval=$enable_silex; if test "$enable_silex" = "no"; then
-        SILEX=""
-    fi
-fi
-
+#if test "${enable_silex+set}" = set; then
+#  enableval=$enable_silex; if test "$enable_silex" = "no"; then
+#        SILEX=""
+#    fi
+#fi
+SILEX=""
 
 
 
