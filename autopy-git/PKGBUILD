# Maintainer: David Phillips <david@daphsoft.com> 
pkgname=autopy-git
pkgver=20110107
pkgrel=1
pkgdesc="A simple, cross-platform GUI automation toolkit for Python."
arch=('x86' 'x86_64')
url="https://github.com/msanders/autopy"
license=('MIT')
depends=('libpng' 'zlib' 'libxtst' 'xorg-xmessage' 'python2')
source=('fix_bitmap.patch')
md5sums=('aa2021ba2cb2575420adee317bc81173')

_gitroot="git://github.com/msanders/autopy.git"
_gitname="autopy"

build() {
  cd "$srcdir"
  msg "Connecting to GIT server...."

  if [ -d $_gitname ] ; then
    cd $_gitname && git pull origin
    msg "The local files are updated."
  else
    git clone $_gitroot 
  fi

  msg "GIT checkout done or server timeout"
  msg "Starting build..."
  
  cd $srcdir
  patch -p0 < fix_bitmap.patch

  cd $_gitname
  python2 setup.py build

}

package() {
  cd "$srcdir/$_gitname"
  python2 setup.py install --prefix=/usr --root="$pkgdir"
} 
