# Maintainer: Sergei Lebedev <superbobry@gmail.com>
# Contributor: Kostas Andreadis <kmandreadis@gmail.com>

pkgname=lacaml
pkgver=5.5.2
pkgrel=1
pkgdesc="BLAS/LAPACK-interface for OCaml"
arch=('i686' 'x86_64')
url="http://ocaml.info/home/ocaml_sources.html"
license=('LGPL')
depends=('ocaml' 'lapack' 'blas')
makedepends=('ocaml-findlib')
source=(http://hg.ocaml.info/release/lacaml/archive/release-${pkgver}.tar.gz)
md5sums=('9fbe831791b7d8a636856d33314b19bf')
options=(!strip)

build() {
  cd "$srcdir/${pkgname}-release-$pkgver"
  make
}

package() {
  cd "$srcdir/${pkgname}-release-$pkgver"

  export OCAMLFIND_DESTDIR=$pkgdir$(ocamlfind printconf destdir)
  mkdir -p $OCAMLFIND_DESTDIR/stublibs
  make install
}
