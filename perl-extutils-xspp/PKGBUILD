# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# See http://wiki.archlinux.org/index.php/Perl_Package_Guidelines for more
# information on Perl packaging.

# Contributor: DarkHeart luke<at>archlinux.us
pkgname=perl-extutils-xspp
pkgver=0.09
pkgrel=1
pkgdesc="ExtUtils::XSpp - XS for C++"
arch=('any')
url="http://search.cpan.org/dist/ExtUtils-XSpp/lib/ExtUtils/XSpp.pod"
license=('GPL' 'PerlArtistic')
depends=('perl>=5.10.0')
makedepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=(!emptydirs)
install=
source=(http://search.cpan.org/CPAN/authors/id/S/SM/SMUELLER/ExtUtils-XSpp-0.09.tar.gz)
md5sums=(6432cf0a06726172c40aae6c80e15f3b)

build() {
     cd "$srcdir/ExtUtils-XSpp-$pkgver"

       perl Build.PL installdirs=vendor destdir="$pkgdir/"
       perl Build
       perl Build install
}

# vim:set ts=2 sw=2 et:

