# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: Troels Mæhl Folke <troelsmf@gmail.com>
# Contributor: Troels Mæhl Folke <troelsmf@gmail.com>
pkgname=ordbogsprogrammet2
pkgver=12024
pkgrel=1
pkgdesc="A danish online based dictionary service with multiple languages"
arch=('i686')
url="http://www.ordbogen.com"
license=('custom')
depends=('qt>=4.5.0', 'openssl')
source=(http://www.ordbogen.com/downloads/program/version2/${pkgname}_${pkgver}-1_i386.deb)
noextract=('$pkgname_$pkgver_i386.deb')
md5sums=('3fde17f1eb5af22b0e1ff71cf64cf095')

build() {
  cd $srcdir
  ar x ${pkgname}_${pkgver}-1_i386.deb
  tar -xzf data.tar.gz
  find usr -type f -exec install -m555 -D {} $pkgdir/{} \; || return 1
  mkdir $pkgdir/usr/lib
  ln -s /usr/lib/libcrypto.so ${pkgdir}/usr/lib/libcrypto.so.0.9.8 || return 1
}
