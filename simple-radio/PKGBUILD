pkgname=simple-radio
pkgver=hg_latest
pkgrel=1
pkgdesc="Play radio streams with mplayer"
arch=('i686' 'x86_64')
url="http://alphaos.tuxfamily.org"
license=('GPLv3')
depends=('mplayer' 'libnotify')
makedepends=('vala')
source=(https://bitbucket.org/simargl/alphaos/get/tip.tar.gz)
md5sums=('SKIP')
options=(!strip)

package() {
  cd "$srcdir/"simargl-alphaos-*/def-scripts/03_extra/emendo/emendo
  make DESTDIR="$pkgdir/" install
}
