# Maintainer: Thomas Krug <phragment@htu.tugraz.at>
# Contributor: Thomas Krug <phragment@htu.tugraz.at>

pkgname=pulseview
pkgver=0.1.0
pkgrel=1
pkgdesc="A Qt based logic analyzer GUI for sigrok."
arch=('i686' 'x86_64')
url="http://sigrok.org/wiki/Main_Page"
license=('GPL3')
depends=('libsigrok' 'qt4' 'boost-libs')
makedepends=('cmake' 'boost')
source=("http://sigrok.org/download/source/${pkgname}/${pkgname}-${pkgver}.tar.gz"
        'pulseview.desktop')
md5sums=('0b462664854f4186c67ce1aae78b6d06'
         'f816b4aff1ddf5683d5df94e69576ec7')
install=${pkgname}.install

build() {
  cd "$srcdir/${pkgname}-${pkgver}"

  cmake -DCMAKE_INSTALL_PREFIX:PATH=/usr .
  make
}

package() {
  cd "$srcdir/${pkgname}-${pkgver}"

  make DESTDIR="$pkgdir" PREFIX=/usr install
  install -Dm644 ../pulseview.desktop "$pkgdir/usr/share/applications/pulseview.desktop"
}

# vim:set ts=2 sw=2 et:
