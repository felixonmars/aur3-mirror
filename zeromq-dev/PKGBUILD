# Maintainer: Nicolas Martyanoff <khaelin@gmail.com>
pkgname=zeromq-dev
pkgver=2.1.0
pkgrel=1
pkgdesc="The development version of ØMQ, a fast messaging library."
url="http://zeromq.org"
license=("LGPL")
source=("http://download.zeromq.org/zeromq-${pkgver}.tar.gz")
md5sums=("8b05003a5c65ca934d9d909834a04bee")
arch=("i686" "x86_64")
depends=()
makedepends=("autoconf" "make" "gcc")
provides=("zeromq")
conflicts=("zeromq")

build() {
    cd "$srcdir"/zeromq-$pkgver
    ./configure prefix="$startdir/pkg/usr"
    make
    make install
}
