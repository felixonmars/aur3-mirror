# Maintainer: Nicolas Martyanoff <khaelin@gmail.com>
pkgname=zeromq-dev
pkgver=3.1.0
pkgrel=1
pkgdesc="The development version of ØMQ, a fast messaging library."
url="http://zeromq.org"
license=("LGPL")
source=("http://download.zeromq.org/zeromq-${pkgver}-beta.tar.gz")
md5sums=("c9615e4442b325bd0c23a8205e380c40")
arch=("i686" "x86_64")
depends=()
makedepends=("autoconf" "make" "gcc")
provides=("zeromq")
conflicts=("zeromq")

build() {
    cd "$srcdir"/zeromq-$pkgver
    ./configure prefix="$startdir/pkg/usr"
    make
    make install
}
