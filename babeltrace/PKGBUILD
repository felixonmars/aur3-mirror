# Maintainer: Christian Babeux <christian.babeux@0x80.ca>
# Contributor: Manuel Mendez <mmendez534 at gmail dot com>

pkgname=babeltrace
pkgver=1.0.3
pkgrel=1
pkgdesc="command-line trace reader for LTTng"
arch=('i686' 'x86_64')
url="http://lttng.org/"
license=('GPL' 'MIT')
depends=('popt' 'util-linux' 'glib2>=2.22.0')
source=(http://lttng.org/files/$pkgname/$pkgname-$pkgver.tar.bz2)
install='babeltrace.install'
sha1sums=('d03894022757298f5de3678c6cd51a26a8931010')

build()
{
	cd "$srcdir/$pkgname-$pkgver"
	./configure --prefix=/usr
	make
}

package()
{
	cd "$srcdir/$pkgname-$pkgver"
	make DESTDIR="$pkgdir" install
	mkdir -p "$pkgdir"/usr/share/licenses/$pkgname
	cp mit-license.txt "$pkgdir"/usr/share/licenses/$pkgname
}
