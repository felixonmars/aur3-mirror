# Maintainer: Manuel Mendez <mmendez534 at gmail dot com>

pkgname=babeltrace
pkgver=1.0
pkgrel=4
extraver=0-rc2
pkgdesc="command-line trace reader for LTTng"
arch=('i686' 'x86_64')
url="http://lttng.org/"
license=('GPL' 'MIT')
depends=('liburcu')
source=(http://lttng.org/files/$pkgname/$pkgname-$pkgver.$extraver.tar.bz2)
sha1sums=('fe35c67cfa9c731584a91670aaac2a8277d114c8')
install='babeltrace.install'

build()
{
	cd "$srcdir/$pkgname-$pkgver.$extraver"
	./configure
	make
}

package()
{
	cd "$srcdir/$pkgname-$pkgver.$extraver"
	make DESTDIR="$pkgdir" install
	mkdir -p "$pkgdir"/usr/share/licenses/$pkgname
	cp mit-license.txt "$pkgdir"/usr/share/licenses/$pkgname
}
