# Maintainer: Philip Babu <sasha.1594@gmail.com>
pkgname=smoothx-xfce
pkgver=0.1.6
pkgrel=1
pkgdesc="Complete icon set's for XFCE desktop environment"
url="https://github.com/sasha1594/smoothX-Xfce"
arch=('any')
license=('GPLv3')
depends=('gtk-update-icon-cache' 'gnome-icon-theme' 'librsvg')
source=("https://github.com/sasha1594/smoothX-Xfce/archive/master.zip")
optdepends=()
makedepends=()
conflicts=()
options=(!strip)
install='smoothx.install'
md5sums=('93f7a973ab8cee4fc32e7420693325ad')


package() {
  cd "${srcdir}"
  mkdir -p "${pkgdir}/usr/share/icons"
  cp -r  smoothX-Xfce-master/smoothX* "${pkgdir}/usr/share/icons/"
  find "${pkgdir}/usr/share/icons" -type d -exec chmod 755 '{}' \;
  find "${pkgdir}/usr/share/icons" -type f -exec chmod 644 '{}' \;

}
