# Maintainer: boenki <boenki at gmx dot de>

pkgname=otter-browser
pkgver=0.1.01
pkgrel=1
pkgdesc="Browser aiming to recreate classic Opera (12.x) UI using Qt5."
arch=('i686' 'x86_64')
url="http://otter-browser.org/"
license=('GPL3')
depends=('qt5-webkit')
source=(${pkgname}-${pkgver}.tar.gz::https://github.com/Emdek/otter/archive/v$pkgver.tar.gz)
md5sums=('19ff81e517df3966f5d0601be8c4c430')

build() {
  cd otter-$pkgver
  qmake PREFIX=/usr otter.pro
  make
}

package() {
  # 'make install' currently does not work for me, so just...
  install -Dm755 otter-$pkgver/$pkgname $pkgdir/usr/bin/$pkgname
}