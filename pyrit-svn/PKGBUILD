# Maintainer: Hoàng Đức Hiếu <hdh@lazny.tang.la>
# Contributor: Gilfran Ribeiro <contato [at] gilfran [dot] net>
# Contributor: William Díaz <wdiaz [at] archlinux [dot] us>
# Modifed: Gustavo Alvarez <sl1pkn07 [at] gmail [dot] com>

pkgname=pyrit-svn
pkgver=308
pkgrel=2
pkgdesc="GPU-accelerated attack against WPA-PSK authentication, development version"
url="http://code.google.com/p/pyrit/"
license=('GPL3')
arch=('i686' 'x86_64')
depends=('python2' 'openssl' 'zlib' 'libpcap')
makedepends=('gcc')
optdepends=('cpyrit-calpp-svn: CAL++ backend'
            'cpyrit-cuda-svn: CUDA backend'
            'cpyrit-opencl-svn: OpenCL backend'
            'cpyrit-opencl-nvidia-svn: OpenCL backend with Nvidia support')

_svntrunk=http://pyrit.googlecode.com/svn/trunk
_svnmod=pyrit

build() {
  svn co ${_svntrunk}/${_svnmod}

  cd ${_svnmod}
  LDFLAGS="" python2 setup.py build
}

package() {
  cd ${srcdir}/${_svnmod}
  python2 setup.py install --root=${pkgdir}
}
