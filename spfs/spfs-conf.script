# this is default port "reserved" by 9p FS,
# but non-root user needs to use non-priviliged port >1024
SPFS_ARGS="-s -p 564"
# it's not a good idea to run spfs as root... since it exposes rootfs
SPFS_USER=
