# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: Argyros Argyridis <arargyridis@gmail.com>
pkgname=monteverdi
pkgver=1.12
pkgrel=1
pkgdesc="A remote sensing application based on Orfeo Toolbox"
arch=(x86_64)
url="http://www.orfeo-toolbox.org/otb/monteverdi.html"
license=('CeCILL')
groups=()
depends=('orfeo-toolbox-hg')
makedepends=()
optdepends=()
provides=(monteverdi)
conflicts=(monteverdi)
replaces=(monteverdi)
backup=()
options=()
install=
changelog=
source=(http://switch.dl.sourceforge.net/project/orfeo-toolbox/Monteverdi/Monteverdi-$pkgver.$pkgrel/Monteverdi-$pkgver.$pkgrel.tgz)
noextract=()
md5sums=(8022a95fea9e7db7643f28283c8499b6) #generate with 'makepkg -g'

build() {



  #msg "checking done or server timeout occurerror while loading shared libraries: libfltk.so.9: cannot open shared object file: No such file or directory    ed"
  
  msg "starting make..."
  rm -rf build/
  mkdir build/
  cd build
  ls -l

  export LD_LIBRARY_PATH=/usr/local/lib/otb
  cmake ../Monteverdi-$pkgver.$pkgrel

  make
}

package() {

  cd "$srcdir/"build
  make DESTDIR="$pkgdir" install
}
