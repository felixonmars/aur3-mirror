#!/bin/bash

. /etc/wpa_auto.conf

if [ "$2" = "CONNECTED" ]; then
    [ -f /var/run/dhcpcd-$1.pid ] && /sbin/dhcpcd -qx $1
    /sbin/dhcpcd -q $DHCPCD_OPTIONS $1
elif [ "$2" = "DISCONNECTED" ]; then
    /sbin/dhcpcd -qx $1
fi

exit 0

