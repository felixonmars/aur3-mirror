#!/bin/bash
# Update our pacman database

PACMAN=`whereis pacman`

if [[ $PACMAN ]]; then
    $PACMAN --sync --refresh
fi

pkgs=$(pacman -Qqu | wc -l)

if [[ ${pkgs} -gt 0 ]]; then
      kdialog --passivepopup "There are ${pkgs} updated packages" 10
fi
