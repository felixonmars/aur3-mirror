[Unit]
Description=Video Disk Recorder
After=syslog.target network.target lircd.service

[Service]
Type=simple
ExecStart=/usr/bin/runvdr
ExecStop=/usr/bin/killall -q -TERM runvdr ; /usr/bin/killall -q -TERM vdr
KillMode=none

[Install]
WantedBy=multi-user.target
