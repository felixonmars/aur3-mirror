# Contributor: Michael Kanis <mkanis@gmx.de>
# Contributor: Andrea Cimitan <andrea.cimitan@gmail.com>
pkgname=clutk
pkgver=0.2.0
pkgrel=1
pkgdesc="clutk"
arch=('i686' 'x86_64')
url="http://launchpad.net"
license=('LGPL')
depends=('gtk2')
options=('!libtool')
source=(http://launchpad.net/${pkgname}/0.2/${pkgver}/+download/${pkgname}-${pkgver}.tar.gz)
md5sums=('edd0d10d93f79d86eaf4b147449f20fd')

build() {
  cd ${startdir}/src/${pkgname}-${pkgver}
  ./configure --prefix=/usr --disable-introspection
  make || return 1
  make DESTDIR=${startdir}/pkg install || return 1
}

