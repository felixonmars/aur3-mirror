# arg 1:  the new package version
post_install() {
  echo '
  ==> PHP extension

  Add pam.servicename="php"; to your php.ini and create a
  file called /etc/pam.d/php similar to /etc/pam.d/login.
  '
}

op=$1
shift
[ "$(type -t "$op")" = "function" ] && $op "$@"

