[Unit]
Description=Jenkins CI
After=network.target

[Service]
EnvironmentFile=/etc/conf.d/jenkins
Type=forking
User=jenkins
StandardInput=null
ExecStart=/bin/bash -l -c "${JAVA} ${JAVA_ARGS} -jar ${JENKINS_WAR} ${JENKINS_ARGS} >> ${JENKINS_LOG} 2>&1 &"

[Install]
WantedBy=multi-user.target

