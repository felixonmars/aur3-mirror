post_install() {
  getent passwd bonanza >/dev/null || useradd -M -s /usr/bin/false bonanza
  chown bonanza -R /opt/bonanza/ /usr/bin/bonanza
}

pre_remove() {
  getent passwd bonanza && userdel bonanza
}