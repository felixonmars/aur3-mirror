# Maintainer: Rob McCathie <archaur at rmcc dot com dot au>

pkgname=qconnman-ui-git
pkgver=20130706
pkgrel=2
pkgdesc="A Qt ConnMan management interface"
arch=('i686' 'x86_64')
url="https://github.com/OSSystems/qconnman-ui"
license=('LGPL')
depends=('qconnman-git')
makedepends=('git')

_gitroot=https://github.com/OSSystems/qconnman-ui.git
_gitname=qconnman-ui

build() {
  cd "$srcdir"
  msg "Connecting to GIT server...."
  
  if [[ -d "$_gitname" ]]; then
    cd "$_gitname" && git pull origin
    msg "The local files are updated."
  else
    git clone "$_gitroot" "$_gitname"
    cd "$_gitname"
  fi
  
  qmake-qt4 PREFIX=/usr
  make
}

package() {
  cd "$srcdir"/"$_gitname"
  make INSTALL_ROOT="$pkgdir" install
}
