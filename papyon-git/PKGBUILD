# Contributor: Alessio Biancalana <dottorblaster@gmail.com>

pkgname=papyon-git
pkgver=20101209
pkgrel=1
pkgdesc="A msn python library, rewrite of the Ivy library"
url="http://git.collabora.co.uk/?p=papyon.git;a=summary"
arch=('i686' 'x86_64')
license=('GPL2')
depends=('adns-python' 'pycrypto' 'pygobject' 'pyopenssl' 'farsight2')
makedepends=('git')
provides=('pymsn' 'papyon')
conflicts=('pymsn' 'papyon')
source=()
md5sums=()

_gitroot="git://github.com/Kjir/papyon.git"
_gitname="papyon"

build() {
  cd $startdir/src

  cd $srcdir
  msg "Connecting to the GIT server...."
  
  if [[ -d $srcdir/$_gitname ]] ; then
    cd $_gitname
    git pull origin
    msg "The local files are updated."
  else
    git clone $_gitroot $_gitname
  fi
  
  msg "GIT checkout done"
  msg "Starting make..."

  cd $srcdir/$_gitname
  python2 setup.py install --root=$startdir/pkg || return 1
}
