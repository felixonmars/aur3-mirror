# arg 1:  the new package version
post_install() {
cat << EOF
Note:
Before running hrktorrent make sure you have created a config file at ~/.hrktorrent/
An example is located at /usr/share/examples/hrktorrent/hrktorrent.rc

For IP-Filtering, make sure it is enabled and copy the ipfilter.dat file to ~/.hrktorrent/ipfilter.dat
EOF
}