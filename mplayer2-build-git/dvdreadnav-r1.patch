--- mplayer2-build/mplayer/configure	2013-01-25 02:00:30.814661960 +0100
+++ mplayer2-build-build/mplayer/configure	2013-02-26 11:48:29.334402924 +0100
@@ -4263,8 +4263,8 @@
 elif test "$_dvdread" = auto ; then
   _dvdread=no
   if test "$_dl" = yes; then
-    _dvdreadcflags=$($_dvdreadconfig --cflags 2> /dev/null)
-    _dvdreadlibs=$($_dvdreadconfig --libs 2> /dev/null)
+    _dvdreadcflags=$($_dvdreadconfig --cflags dvdread 2> /dev/null)
+    _dvdreadlibs=$($_dvdreadconfig --libs dvdread 2> /dev/null)
     if header_check dvdread/dvd_reader.h $_dvdreadcflags $_dvdreadlibs $_ld_dl ; then
       _dvdread=yes
       extra_cflags="$extra_cflags $_dvdreadcflags"
@@ -5210,13 +5210,13 @@
     dvdnav_internal=yes
     res_comment="internal"
   else
-    $_dvdnavconfig --version --minilibs >> $TMPLOG 2>&1 || _dvdnav=no
+    $_dvdnavconfig --modversion dvdnavmini >> $TMPLOG 2>&1 || _dvdnav=no
   fi
 fi
 if test "$_dvdnav" = auto ; then
   _dvdnav=no
-  _dvdnavdir=$($_dvdnavconfig --cflags)
-  _dvdnavlibs=$($_dvdnavconfig --libs)
+  _dvdnavdir=-I$($_dvdnavconfig --variable=includedir dvdnav)
+  _dvdnavlibs=$($_dvdnavconfig --libs dvdnav)
   statement_check_broken stdint.h dvdnav/dvdnav.h 'dvdnav_t *dvd = 0' $_dvdnavdir $_dvdnavlibs $_ld_dl $_ld_pthread && _dvdnav=yes
 fi
 if test "$_dvdnav" = yes ; then
@@ -5225,8 +5225,8 @@
     cflags_libdvdnav="-Ilibdvdnav"
     inputmodules="dvdnav(internal) $inputmodules"
   else
-    extra_cflags="$extra_cflags $($_dvdnavconfig --cflags)"
-    extra_ldflags="$extra_ldflags $($_dvdnavconfig --minilibs)"
+    extra_cflags="$extra_cflags $($_dvdnavconfig --cflags dvdnav)"
+    extra_ldflags="$extra_ldflags $($_dvdnavconfig --libs dvdnavmini)"
     inputmodules="dvdnav $inputmodules"
   fi
 else
