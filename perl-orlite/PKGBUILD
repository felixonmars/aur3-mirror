# PKGBUILD generated by pacpan
#Modified by Jonathas Rodrigues
pkgname=perl-orlite
_realname=ORLite
pkgver=1.51
pkgrel=1
pkgdesc="Extremely light weight SQLite-specific ORM"
arch=(i686 x86_64)
license=('perl')
url="http://search.cpan.org/~adamk/ORLite"
options=(!emptydirs)

depends=('perl-file-remove' 'perl>=5.10.1' 'perl-params-util' 'perl-dbd-sqlite' 'perl-dbi')
makedepends=('perl' 'perl-test-script')

#provides=('ORLite')
provides=('orlite=1.51' 'ORLite=1.51' 'perl-orlite=1.51')

source=(http://search.cpan.org/CPAN/authors/id/A/AD/ADAMK/ORLite-1.51.tar.gz)
md5sums=('8be9e304f0a214017cba4f6273ec6cb1')

build() {
  _expected_dir="${srcdir}/${_realname}-${pkgver}"
  if [ -d "$_expected_dir" ]; then
    cd "$_expected_dir"
  else
    _expected_dir="${srcdir}/$(bsdtar -t -f $(basename $source) | head -n1)"
    if [ -d "$_expected_dir" ]; then
      cd "$_expected_dir"
    else
      _makefile=$(find $srcdir -iname Makefile.PL)
      if [ ! -z "$_makefile" ]; then
        _expected_dir=$(dirname $_makefile)
        if [ -d "$_expected_dir" ]; then
          cd "$_expected_dir"
        else
          echo "[1;31mERROR[0m unable to detect source directory"
          echo "[1;34m-->[0m this is often due to CPAN's lack of standard naming conventions"
          echo "[1;34m-->[0m it may be possible to fix this by adjusting the build function in the PKGBUILD"
        fi
      fi
    fi
  fi
  # install module in vendor directories.
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1
  make  || return 1
  make install DESTDIR=${pkgdir} || return 1

  # remove perllocal.pod and .packlist
  find ${pkgdir} -name perllocal.pod -delete
  find ${pkgdir} -name .packlist -delete
}

# vim:set ts=2 sw=2 et:

