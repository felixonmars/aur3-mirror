# Contributor: Lucas Hermann Negri <lucashnegri@gmail.com>

pkgname=neuralview
pkgver=1.3.0
pkgrel=1
pkgdesc="Neural Network simulator"
arch=('i686' 'x86_64')
url="http://oproj.tuxfamily.org" 
depends=('lua>=5.1' 'lgob>=13.02' 'lcl>=1.0.0' 'lfann>=1.0.1')
optdepends=('gnuplot: error plotting')
license=(LGPL)
source=("http://downloads.tuxfamily.org/oproj/$pkgname/$pkgname-$pkgver.tar.bz2")
md5sums=("6f7d462559cd134ede89405ee81d7873")

build() { 
  cd "$startdir/src/$pkgname-$pkgver"
  make install DESTDIR=$startdir/pkg/opt/neuralview || 1
  
  APPDIR=$startdir/pkg/usr/share/
  mkdir -p $APPDIR
  cp neuralview.desktop $APPDIR
}
