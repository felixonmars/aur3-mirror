post_install() {
  echo
  echo "Please start ramlog *before* anything else!"
  echo "For example in rc.conf: DAEMONS=(ramlog syslog-ng ..."
  echo "You can set ramdisk size with ramdisk_size kernel option:"
  echo "ramdisk_size=80000    <- for a 80 MB ramdisk"
  echo
}
