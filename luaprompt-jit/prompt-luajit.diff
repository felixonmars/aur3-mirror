diff -Naur prompt.old/Makefile prompt/Makefile
--- prompt.old/Makefile	2013-08-14 07:38:16.000000000 -0300
+++ prompt/Makefile	2013-12-03 13:44:33.798709857 -0200
@@ -1,11 +1,12 @@
-PREFIX=/usr/local
-BINDIR=${PREFIX}/bin
-LIBDIR=$(PREFIX)/lib/lua/5.2
+PREFIX=/usr
+DESTDIR=/
+BINDIR=${DESTDIR}/${PREFIX}/bin
+LIBDIR=${DESTDIR}/$(PREFIX)/lib/lua/5.1
 
-LUA_CFLAGS=`pkg-config --cflags lua5.2`
-LUA_LDFLAGS=`pkg-config --libs lua5.2`
+LUA_CFLAGS=`pkg-config --cflags luajit`
+LUA_LDFLAGS=`pkg-config --libs luajit`
 
-CFLAGS=-g -Wall -D_GNU_SOURCE -DHAVE_LIBREADLINE -DHAVE_READLINE_READLINE_H -DHAVE_READLINE_HISTORY -DHAVE_READLINE_HISTORY_H -DHAVE_ASPRINTF -I..
+CFLAGS=-g -Wall -DHAVE_WORDEXP -D_GNU_SOURCE -DHAVE_LIBREADLINE -DHAVE_READLINE_READLINE_H -DHAVE_READLINE_HISTORY -DHAVE_READLINE_HISTORY_H -DHAVE_ASPRINTF -I..
 LDFLAGS=-lreadline -lhistory
 
 INSTALL=/usr/bin/install
diff -Naur prompt.old/luap.c prompt/luap.c
--- prompt.old/luap.c	2013-08-14 07:38:16.000000000 -0300
+++ prompt/luap.c	2013-12-03 13:45:17.409450930 -0200
@@ -28,6 +28,7 @@
 #include <lauxlib.h>
 #include <getopt.h>
 #include <unistd.h>
+#include <luajit.h>
 
 #include "prompt.h"
 
@@ -41,9 +42,9 @@
 #endif
 
 #if LUA_VERSION_NUM == 501
-#define COPYRIGHT LUA_VERSION "  " LUA_COPYRIGHT
+#define COPYRIGHT LUAJIT_VERSION "  " LUAJIT_COPYRIGHT
 #else
-#define COPYRIGHT LUA_COPYRIGHT
+#define COPYRIGHT LUAJIT_COPYRIGHT
 #endif
 
 #define LUAP_VERSION "0.3"
