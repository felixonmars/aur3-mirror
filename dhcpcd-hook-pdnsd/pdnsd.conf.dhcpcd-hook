server {
	label =		"dynamic";
	# no ip address definition here, this is just a placeholder
	proxy_only =	off;
	lean_query =	on;
	timeout =	4;
	uptest =	none;
	root_server =	off;
	purge_cache =	off;
}
