# Maintainer: TDY <tdy@gmx.com>

pkgname=ruby-tzinfo
pkgver=0.3.28
pkgrel=1
pkgdesc="A Ruby library for time zone transformations"
arch=('any')
url="http://tzinfo.rubyforge.org/"
license=('MIT')
depends=('ruby')
source=(http://rubyforge.org/frs/download.php/74711/${pkgname#*-}-$pkgver.gem)
md5sums=('d41d8cd98f00b204e9800998ecf8427e')

build() {
  cd "$srcdir"
  local _gemdir="$(ruby -rubygems -e'puts Gem.default_dir')"
  gem install --ignore-dependencies -i "$pkgdir$_gemdir" ${pkgname#*-}-$pkgver.gem
}
