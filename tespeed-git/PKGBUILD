# Maintainer: janhouse

pkgname=tespeed-git
pkgver=20121211
pkgrel=2
pkgdesc="Terminal network speed test that uses servers from Speedtest.net. It uses nearest test server but can also use one manually specified by the user."
arch=('any')

url="https://github.com/Janhouse/tespeed/blob/master/tespeed.py"
license=('MIT')
depends=('python2' 'python2-lxml')
source=(${url/blob/raw})
md5sums=('b79b84e7b257a891c36a8660ef62b5e3')

package() {
  install -Dm755 tespeed.py "$pkgdir/usr/bin/tespeed"
}
