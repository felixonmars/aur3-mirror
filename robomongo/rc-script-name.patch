diff --git a/src/robomongo/core/engine/ScriptEngine.cpp b/src/robomongo/core/engine/ScriptEngine.cpp
index a4c380b..625ddf7 100644
--- a/src/robomongo/core/engine/ScriptEngine.cpp
+++ b/src/robomongo/core/engine/ScriptEngine.cpp
@@ -91,10 +91,10 @@ namespace Robomongo
             _scope = scope;
             _engine = mongo::globalScriptEngine;
 
-            // Load '.mongorc.js' from user's home directory
+            // Load '.robomongorc.js' from user's home directory
             // We are not checking whether file exists, because it will be
             // checked by 'Scope::execFile'.
-            std::string mongorcPath = QString("%1/.mongorc.js").arg(QDir::homePath()).toStdString();
+            std::string mongorcPath = QString("%1/.robomongorc.js").arg(QDir::homePath()).toStdString();
             scope->execFile(mongorcPath, false, false);
         }
 
