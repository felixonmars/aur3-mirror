# Contributor: Artem Grunichev <gluk@umbrosia.ru>

pkgname=plasma-active-git
pkgver=20140222
pkgrel=1
pkgdesc="Metapackage for full set of KDE Plasma Active related packages"
arch=(i686 x86_64)
url='http://plasma-active.org/'
license=('LGPL')
depends=('bodega-client-git' 'contour-git' 'declarative-plasmoids-git' 'share-like-connect-git')
makedepends=()
groups=('plasma-active-git')
source=()
md5sums=()

build() {
  echo 'nothing to do'
}

package() {
  echo 'nothing to do'
}

