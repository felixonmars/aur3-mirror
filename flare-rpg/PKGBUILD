# Maintainer: Mikael Eriksson <mikael_eriksson@miffe.org>

pkgname=flare-rpg
pkgver=012
pkgrel=1
pkgdesc="open source action roleplaying game"
arch=('i686' 'x86_64')
url="http://clintbellanger.net/rpg/"
license=('GPL')
depends=('sdl' 'sdl_image' 'sdl_mixer')
replaces=('osare')
source=(http://flare-engine.googlecode.com/files/flare_src_v$pkgver.zip)
md5sums=('ab90e788518c46064a347cc3e17c7cab')

build() {
  cd $srcdir/flare_v$pkgver/src

  g++ *.cpp -o flare.bin -I/usr/include/SDL -lSDL -lSDL_image -lSDL_mixer
  install -Dm755 flare.bin $pkgdir/usr/bin/flare.bin

  install -d -m755 $pkgdir/usr/share/flare
  for dir in enemies fonts images items maps music npcs powers saves soundfx src tilesetdefs; do
    cp -r $srcdir/flare_v$pkgver/$dir $pkgdir/usr/share/flare
  done
  chmod -R a-x+X $pkgdir/usr/share/flare
  chmod 775 $pkgdir/usr/share/flare/saves
  chown root:games $pkgdir/usr/share/flare/saves
  find $pkgdir/usr/share/flare -name .DS_Store -exec rm -rf {} \;

cat > $pkgdir/usr/bin/flare << EOF
#!/bin/sh
cd /usr/share/flare
exec flare.bin
EOF
  chmod 755 $pkgdir/usr/bin/flare
}

# vim:set ts=2 sw=2 et:
