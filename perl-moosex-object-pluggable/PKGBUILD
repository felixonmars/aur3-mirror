# CPAN Name  : MooseX-Object-Pluggable
# Contributor: Caleb Cushing <xenoterracide@gmail.com>
# Generator  : CPANPLUS::Dist::Arch 0.20
# Template @ http://github.com/xenoterracide/AURpan/blob/master/perl-moosex-object-pluggable/PKGBUILD.tt
# File bugs @ http://github.com/xenoterracide/AURpan/issues

pkgname='perl-moosex-object-pluggable'
pkgver='0.0011'
pkgrel='1'
pkgdesc="Add plugin support to your Moose classes via roles."
arch=('any')
url='http://search.cpan.org/dist/MooseX-Object-Pluggable'
license=('PerlArtistic' 'GPL')
depends=('perl' 'perl-extutils-makemaker>=6.56' 'perl-moose>=0.35')

options=('!emptydirs')

source=('http://search.cpan.org/CPAN/authors/id/G/GR/GRODITI/MooseX-Object-Pluggable-0.0011.tar.gz')
md5sums=('ce3b5ffc6d05bdd77102488a85e401d1')

build() {
  DIST_DIR="${srcdir}/MooseX-Object-Pluggable-0.0011"
  export PERL_AUTOINSTALL=--skipdeps PERL_MM_USE_DEFAULT=1
  {
	cd "$DIST_DIR" &&
    perl Makefile.PL INSTALLDIRS=vendor &&
    make &&
    make test &&
    make DESTDIR="$pkgdir" install;
  } || return 1;

  find "$pkgdir" -name .packlist -o -name perllocal.pod -delete
}

