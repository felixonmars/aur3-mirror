# arg 1:  the new package version
post_install() {
  echo
  echo "You need stitchers so you should install panorama-tools to get PTStitcher"
  echo "(non-free!) or hugin to get nona (free!)"
  echo
}

op=$1
shift
$op $*
