# Maintainer: Carl George <carl@carlgeorge.us>
# Contributor:  Ryan Peters <sloshy45@sbcglobal.net>
# Special thanks to the Linux Mint project, without them this would not exist.

pkgname=mint-x-icons
pkgver=1.1.1
pkgrel=2
pkgdesc="Mint-X Icon themes"
arch=('any')
url="http://packages.linuxmint.com/pool/main/m/${pkgname}"
license=('GPL')
depends=()
optdepends=("mint-x-theme")
source=("${url}/${pkgname}_${pkgver}.tar.gz")
md5sums=('cd73664ca2d176b0e2cebfd11f57c4e0')

package() {
  cp -dr ${srcdir}/${pkgname}/usr ${pkgdir}/
}

# vim:syntax=sh
