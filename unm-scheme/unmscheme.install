post_install() {
	echo #################################
	echo 
	echo set UNMSCHEME_HOME in .bashrc
	echo if the library path of unmscheme
	echo change to location other than
	echo /usr/share/unmscheme
	echo 
	echo #################################
}

post_upgrade() {
	post_install
}
