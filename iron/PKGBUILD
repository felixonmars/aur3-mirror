pkgname=iron
pkgver=6.0.475.1
pkgrel=2
pkgdesc="A fast web browser based on chromium without Google's tracking 'features' -- switch to 'iron-bin' package"
url='http://www.srware.net/en/software_srware_iron.php'
depends=()
arch=('i686' 'x86_64')
license=('BSD')
source=()

build() {
	cd ${srcdir}
	error "Use package iron-bin instead."
	exit 1
}

