Only in pcb/.git: FETCH_HEAD
Only in pcb/.git: ORIG_HEAD
Only in pcb.new: action.c
diff -aur pcb/configure.ac pcb.new/configure.ac
--- pcb/configure.ac	2013-04-28 23:42:27.768213888 +0200
+++ pcb.new/configure.ac	2013-04-28 23:46:03.400073487 +0200
@@ -5,7 +5,7 @@
 AC_PREREQ([2.60])
 AM_INIT_AUTOMAKE([1.9])
 AC_GNU_SOURCE
-AM_CONFIG_HEADER([config.h])
+AC_CONFIG_HEADERS([config.h])
 
 ##########################################################################
 #
diff -aur pcb/doc/extract-docs pcb.new/doc/extract-docs
--- pcb/doc/extract-docs	2013-04-28 23:42:27.771547147 +0200
+++ pcb.new/doc/extract-docs	2013-04-28 23:57:53.024069516 +0200
@@ -128,7 +128,7 @@
 		if ($synt{$key}) {
 		    $new .= "\@cartouche\n\@format\n";
 		    $new .= $synt{$key};
-		    $new .= "\@end format\n\@end cartouche\n\n";
+		    $new .= "\n\@end format\n\@end cartouche\n\n";
 		}
 		if ($desc{$key}) {
 		    $new .= $desc{$key} . "\n";
diff -aur pcb/doc/pcb.texi pcb.new/doc/pcb.texi
--- pcb/doc/pcb.texi	2013-04-28 23:42:27.781546923 +0200
+++ pcb.new/doc/pcb.texi	2013-04-28 23:51:18.226400824 +0200
@@ -3366,7 +3366,7 @@
 @cindex octagonal flag, changing
 @cindex square flag, changing
 @cindex thermal flag, changing
-@itemx ChangeFlag(Object|SelectElements|SelectedPins|SelectedVias|Selected,thermal|octagon|square,0|1)
+@item ChangeFlag(Object|SelectElements|SelectedPins|SelectedVias|Selected,thermal|octagon|square,0|1)
 Sets/clears the indicated flag.  This adds/removes thermals, adds/removes the flag
 which indicates a pin/pad should be square, or adds/removes the flag which
 indicates a pin/pad should be octagonal.
@@ -3407,7 +3407,7 @@
 @cindex pins, changing shape of
 @cindex vias, changing shape of
 @cindex octagonal pins and vias
-@itemx ChangeOctagon(Object|SelectElements|SelectedPins|SelectedVias|Selected)
+@item ChangeOctagon(Object|SelectElements|SelectedPins|SelectedVias|Selected)
 Toggles what shape the affected pin(s) or via(s) will be drawn when they
 are not square. The shape will either be round or octagonal.
 Default:
@@ -3471,7 +3471,7 @@
 @cindex octagonal flag, clearing
 @cindex square flag, clearing
 @cindex thermal flag, clearing
-@itemx ClrFlag(Object|SelectElements|SelectedPins|SelectedVias|Selected,thermal|octagon|square)
+@item ClrFlag(Object|SelectElements|SelectedPins|SelectedVias|Selected,thermal|octagon|square)
 Clears the indicated flag.  This removes thermals, removes the flag
 which indicates a pin/pad should be square, or removes the flag which
 indicates a pin/pad should be octagonal.
@@ -3614,7 +3614,7 @@
 @findex ExecuteFile()
 @cindex actions file, executing
 @cindex script file, executing
-@itemx ExecuteFile(filename)
+@item ExecuteFile(filename)
 Executes the PCB actions contained in the specified file.
 This can be used to automate a complex sequence of operations.
 @example
@@ -3954,7 +3954,7 @@
 @cindex octagonal flag, setting
 @cindex square flag, setting
 @cindex thermal flag, setting
-@itemx SetFlag(Object|SelectElements|SelectedPins|SelectedVias|Selected,thermal|octagon|square)
+@item SetFlag(Object|SelectElements|SelectedPins|SelectedVias|Selected,thermal|octagon|square)
 Sets the indicated flag.  This adds thermals, sets the flag
 which indicates a pin/pad should be square, or sets the flag which
 indicates a pin/pad should be octagonal.
diff -aur pcb/gts/Makefile.am pcb.new/gts/Makefile.am
--- pcb/gts/Makefile.am	2013-04-28 23:42:27.784880183 +0200
+++ pcb.new/gts/Makefile.am	2013-04-29 00:05:03.767535519 +0200
@@ -1,6 +1,6 @@
 ## Process this file with automake to produce Makefile.in
 
-INCLUDES = -I$(srcdir) -DG_LOG_DOMAIN=\"Gts\"
+AM_CPPFLAGS = -I$(srcdir) -DG_LOG_DOMAIN=\"Gts\"
 
 noinst_LIBRARIES = libgts.a
 
diff -aur pcb/src/Makefile.am pcb.new/src/Makefile.am
--- pcb/src/Makefile.am	2013-04-28 23:42:27.794879959 +0200
+++ pcb.new/src/Makefile.am	2013-04-29 00:05:28.296974649 +0200
@@ -213,7 +213,7 @@
 	mv $@.tmp $@
 
 # for globalconst.h
-INCLUDES=	-I$(top_srcdir) -I$(srcdir)/icons -I$(srcdir)/../gts
+AM_CPPFLAGS=	-I$(top_srcdir) -I$(srcdir)/icons -I$(srcdir)/../gts
 
 DEFS= 	-DLOCALEDIR=\"$(localedir)\" @DEFS@
 
diff -aur pcb/src/hid/lesstif/main.c pcb.new/src/hid/lesstif/main.c
--- pcb/src/hid/lesstif/main.c	2013-04-28 23:42:27.804879737 +0200
+++ pcb.new/src/hid/lesstif/main.c	2013-04-29 00:56:42.457600398 +0200
@@ -747,7 +747,7 @@
 The command window allows the user to manually enter actions to be
 executed.  Action syntax can be done one of two ways:
 
-@table @code
+@itemize @bullet
 
 @item
 Follow the action name by an open parenthesis, arguments separated by
@@ -757,7 +757,7 @@
 Separate the action name and arguments by spaces.  Example: @code{Abc
 1 2 3}.
 
-@end table
+@end itemize
 
 The first option allows you to have arguments with spaces in them,
 but the second is more ``natural'' to type for most people.
