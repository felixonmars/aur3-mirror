# Maintainer: joaolvcm <joaolvcm at gmail dot com>
# Maintainer: peace4all <markspost at rocketmail dot com>
pkgname=unified-remote-server
pkgver=3.0.10.559
pkgrel=1
pkgdesc="Unified Remote Server"
arch=('i686' 'x86_64')
url="http://www.unifiedremote.com/"
depends=('libxext' 'libx11')
license=('freeware')
install=$pkgname.install
source=("urserver-${pkgver}.deb::http://www.unifiedremote.com/download/linux-x86-deb")
md5sums=('64c8709fb614729f2d455f3641b86d9f')

if test "$CARCH" == x86_64; then
	source=("urserver-${pkgver}.deb::http://www.unifiedremote.com/download/linux-x64-deb")
	md5sums=('6657e0e6b4a969a3892bf6d4e32f0756')
fi

package() {
	cd ${srcdir}

	# decompress data
	tar zxf data.tar.gz

	# fix and revert desktop file to old behaviour
	sed -i -e '9,24d;26d' $(find . -name 'urserver.desktop')

	# install folders
	mkdir -p ${pkgdir}/{opt,usr}
	cp -r {opt,usr} "${pkgdir}/"

	# clean up permissions
	find "${pkgdir}" -type d | xargs -I {} chmod -R 755 "{}"
	find "${pkgdir}" -type f | xargs -I {} chmod -R 644 "{}"
	chmod 755 "${pkgdir}/opt/urserver/urserver"
}

