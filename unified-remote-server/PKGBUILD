# Maintainer: peace4all <markspost at rocketmail dot com>

pkgname=unified-remote-server
pkgver=3.0.3.404
pkgrel=1
pkgdesc="Unified Remote Server"
arch=('i686' 'x86_64')
url="http://www.unifiedremote.com/"
depends=('libxext' 'libx11')
license=('freeware')
install=$pkgname.install
source=("https://unifiedservicesstorage.blob.core.windows.net/builds/server/linux-x86/404/Server.deb"
	"urserver.service")
md5sums=('6c512527bd1adbc6b1e783e596c4d554'
         '065062b989e25eb3f8555b05132ab59f')

 if test "$CARCH" == x86_64; then

source=("https://unifiedservicesstorage.blob.core.windows.net/builds/server/linux-x64/404/Server.deb"
	"urserver.service")
md5sums=('92dad4bd95508914395e4c47d4387e08'
         '065062b989e25eb3f8555b05132ab59f')
 fi

package() {
  cd ${srcdir}

  # decompress data
  tar zxf data.tar.gz

  # install folders
  mkdir -p ${pkgdir}/{opt,usr/lib/systemd/system}
  cp -r {opt,usr} "${pkgdir}/"

  # install service file
  cp urserver.service "${pkgdir}/usr/lib/systemd/system/urserver.service"

  # clean up permissions
  find "${pkgdir}" -type d | xargs -I {} chmod -R 755 "{}"
  find "${pkgdir}" -type f | xargs -I {} chmod -R 644 "{}"
  chmod 755 "${pkgdir}/opt/urserver/urserver"
}

