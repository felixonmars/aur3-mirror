From c95add234d87eaf1a9ebb3b8285e1c2ee52baf8c Mon Sep 17 00:00:00 2001
From: Paul Gideon Dann <pdgiddie@gmail.com>
Date: Wed, 20 May 2009 11:42:28 +0100
Subject: [PATCH 1/4] Cairo backend added to Qt4 wrapper

---
 qt4/src/CMakeLists.txt      |   14 ++++++++++++
 qt4/src/poppler-document.cc |    1 +
 qt4/src/poppler-page.cc     |   47 +++++++++++++++++++++++++++++++++++++++++++
 qt4/src/poppler-private.h   |   12 +++++++++++
 qt4/src/poppler-qt4.h       |    3 +-
 5 files changed, 76 insertions(+), 1 deletions(-)

diff --git a/qt4/src/CMakeLists.txt b/qt4/src/CMakeLists.txt
index 8ad3fbc..7736a63 100644
--- a/qt4/src/CMakeLists.txt
+++ b/qt4/src/CMakeLists.txt
@@ -5,6 +5,10 @@ include_directories(
   ${QT4_INCLUDE_DIR}
   ${CMAKE_CURRENT_BINARY_DIR}
 )
+if (HAVE_CAIRO)
+  include_directories(${CAIRO_INCLUDE_DIRS})
+  add_definitions(${CAIRO_CFLAGS})
+endif (HAVE_CAIRO)
 
 set(poppler_qt4_SRCS
   poppler-annotation.cc
@@ -27,10 +31,20 @@ set(poppler_qt4_SRCS
   poppler-page-transition.cc
   ${CMAKE_SOURCE_DIR}/poppler/ArthurOutputDev.cc
 )
+if (HAVE_CAIRO)
+  set(poppler_qt4_SRCS ${poppler_qt4_SRCS}
+    ${CMAKE_SOURCE_DIR}/poppler/CairoOutputDev.cc
+    ${CMAKE_SOURCE_DIR}/poppler/CairoRescaleBox.cc
+    ${CMAKE_SOURCE_DIR}/poppler/CairoFontEngine.cc
+  )
+endif(HAVE_CAIRO)
 qt4_automoc(${poppler_qt4_SRCS})
 add_library(poppler-qt4 SHARED ${poppler_qt4_SRCS})
 set_target_properties(poppler-qt4 PROPERTIES VERSION 3.3.0 SOVERSION 3)
 target_link_libraries(poppler-qt4 poppler ${QT4_QTCORE_LIBRARY} ${QT4_QTGUI_LIBRARY} ${QT4_QTXML_LIBRARY})
+if (HAVE_CAIRO)
+  target_link_libraries(poppler-qt4 ${CAIRO_LIBRARIES})
+endif (HAVE_CAIRO)
 if(MSVC)
 target_link_libraries(poppler-qt4 poppler ${poppler_LIBS})
 endif(MSVC)
diff --git a/qt4/src/poppler-document.cc b/qt4/src/poppler-document.cc
index 41d35b6..922a9a6 100644
--- a/qt4/src/poppler-document.cc
+++ b/qt4/src/poppler-document.cc
@@ -501,6 +501,7 @@ namespace Poppler {
         ret << Document::SplashBackend;
 #endif
         ret << Document::ArthurBackend;
+        ret << Document::CairoBackend;
         return ret;
     }
 
diff --git a/qt4/src/poppler-page.cc b/qt4/src/poppler-page.cc
index 6dbf50f..69c1e0f 100644
--- a/qt4/src/poppler-page.cc
+++ b/qt4/src/poppler-page.cc
@@ -31,6 +31,7 @@
 #include <QtGui/QPainter>
 
 #include <config.h>
+#include <math.h>
 #include <PDFDoc.h>
 #include <Catalog.h>
 #include <Form.h>
@@ -269,6 +270,52 @@ QImage Page::renderToImage(double xres, double yres, int x, int y, int w, int h,
       img = tmpimg;
       break;
     }
+    case Poppler::Document::CairoBackend:
+    {
+#if defined(HAVE_CAIRO)
+      CairoOutputDev *output_dev =
+        static_cast<CairoOutputDev *>(m_page->parentDoc->getOutputDev());
+      double width, height;
+      int buffer_width, buffer_height, rotate;
+      cairo_surface_t *surface;
+      cairo_t *cairo;
+
+      rotate = rotation + m_page->page->getRotate ();
+      if (rotate == 90 || rotate == 270) {
+        height = m_page->page->getCropWidth ();
+        width = m_page->page->getCropHeight ();
+      } else {
+        width = m_page->page->getCropWidth ();
+        height = m_page->page->getCropHeight ();
+      }
+
+      const double xscale = xres / 72.0;
+      const double yscale = yres / 72.0;
+      buffer_width = (int) ceil(width * xscale);
+      buffer_height = (int) ceil(height * yscale);
+
+      img = QImage(buffer_width, buffer_height, QImage::Format_ARGB32);
+      img.fill(Qt::white);  // Never transparent
+
+      surface = cairo_image_surface_create_for_data(img.bits(),
+                CAIRO_FORMAT_ARGB32,
+                buffer_width, buffer_height,
+                img.bytesPerLine());
+
+      cairo = cairo_create (surface);
+      output_dev->setCairo (cairo);
+
+      m_page->parentDoc->doc->displayPageSlice(
+        output_dev, m_page->index + 1, xres, yres, rotation, false, true,
+        false, x, y, w, h);
+
+      // Clean up
+      output_dev->setCairo(NULL);
+      cairo_destroy (cairo);
+      cairo_surface_destroy (surface);
+#endif
+      break;
+    }
   }
 
   return img;
diff --git a/qt4/src/poppler-private.h b/qt4/src/poppler-private.h
index f148dbc..da0440b 100644
--- a/qt4/src/poppler-private.h
+++ b/qt4/src/poppler-private.h
@@ -38,6 +38,9 @@
 #if defined(HAVE_SPLASH)
 #include <SplashOutputDev.h>
 #endif
+#if defined(HAVE_CAIRO)
+#include <CairoOutputDev.h>
+#endif
 
 #include "poppler-qt4.h"
 
@@ -145,6 +148,15 @@ namespace Poppler {
 #endif
 			break;
 			}
+			case Document::CairoBackend:
+			{
+#if defined(HAVE_CAIRO)
+			CairoOutputDev *cairoOutputDev = new CairoOutputDev();
+			cairoOutputDev->startDoc(doc->getXRef (), doc->getCatalog ());
+			m_outputDev = cairoOutputDev;
+#endif
+			break;
+			}
 			}
 		}
 		return m_outputDev;
diff --git a/qt4/src/poppler-qt4.h b/qt4/src/poppler-qt4.h
index 117dc43..fa1afd1 100644
--- a/qt4/src/poppler-qt4.h
+++ b/qt4/src/poppler-qt4.h
@@ -681,7 +681,8 @@ delete it;
 	*/
 	enum RenderBackend {
 	    SplashBackend,   ///< Splash backend
-	    ArthurBackend   ///< Arthur (Qt4) backend
+	    ArthurBackend,   ///< Arthur (Qt4) backend
+	    CairoBackend     ///< Cairo backend
 	};
 
 	/**
-- 
1.7.3.1

