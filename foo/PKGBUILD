pkgbase=foo
pkgname=(foo bar)
url=http://www.google.com
arch=(any)
pkgver=1
pkgrel=1
depends=(quite some depends)

package_foo() {
  pkgdesc="all the depends"
}

package_bar() {
  pkgdesc="none of the depends"
  depends=()
}
