# Maintainer: Austin Adams <screamingmoron AT gmail DOT com>
pkgname="pybf"
pkgver=0.2
pkgrel=1
pkgdesc="A brainfuck iterpreter written in Python"
arch=("any")
url="http://code.google.com/p/pybf"
license=("GPL3")
depends=("python2")
makedepends=("mercurial")
_hgrevision="5e0df628cfa1"

build() {
	cd "$srcdir/"

	hg clone http://pybf.googlecode.com/hg/ "$pkgname-$pkgver" -r $_hgrevision
	
	cd "$pkgname-$pkgver/"
}

package() {
  cd "$srcdir/$pkgname-$pkgver/"

  make DESTDIR="$pkgdir/" install
}
