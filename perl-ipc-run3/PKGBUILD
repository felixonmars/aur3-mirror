# Contributor: Troels Liebe Bentsen(tlbdk) <tlb@rapanden.dk>
# Contributor: giniu <gginiu@gmail.com>
pkgname=perl-ipc-run3
pkgver=0.044
pkgrel=1
pkgdesc="Run a subprocess with input/ouput redirection"
arch=('any')
url="http://search.cpan.org/dist/IPC-Run3/"
license=('GPL' 'PerlArtistic')
depends=('perl>=5.10.0')
options=(!emptydirs)
source=(http://search.cpan.org/CPAN/authors/id/R/RJ/RJBS/IPC-Run3-$pkgver.tar.gz)
md5sums=('7df73a65d9efc7b9e7eb04075ff1fd8f')

build() {
  cd "$srcdir/IPC-Run3-$pkgver"

  # install module in vendor directories.
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1
  make || return 1
  make install DESTDIR="$pkgdir/" || return 1

  # remove perllocal.pod and .packlist
  find "$pkgdir" -name perllocal.pod -delete
  find "$pkgdir" -name .packlist -delete
}

# vim:set ts=2 sw=2 et:
