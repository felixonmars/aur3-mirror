post_install() {
  echo '
-------------------------------------------------------------------------
A default configuration file for traydevice is provided in :

/usr/share/udisksvm/default.xml

Before using the udisksvm script, this file must be copied as :

$HOME/.config/traydevice/default.xml

which is the default configuration file traydevice looks for.

Other infos for using the script are given in /usr/share/udisksvm/README
-------------------------------------------------------------------------
'
}

post_upgrade() {
  echo '
-------------------------------------------------------------------------
Make sure changes in : 

/usr/share/udisksvm/default.xml

are copied in :

$HOME/.config/traydevice/default.xml

for traydevice to use them.

Other infos for using the script are given in /usr/share/udisksvm/README
-------------------------------------------------------------------------
'
}

# vim:set ts=2 sw=2 et:
