--- Makefile	2010-01-15 17:01:03.765034074 +0300
+++ Makefile	2010-01-15 17:01:51.998782031 +0300
@@ -1,6 +1,6 @@
 #!/usr/bin/make
 # Configure prefix here:
-prefix="/usr/local"
-aircrack_prefix="/usr/local" # for packaged aircrack-ng change me to /usr
+#prefix="${pkgdir}/usr"
+aircrack_prefix="/usr" # for packaged aircrack-ng change me to /usr
 OSTYPE:=$(shell uname -s|cut -d_ -f1)
 include Makefile-$(OSTYPE)
