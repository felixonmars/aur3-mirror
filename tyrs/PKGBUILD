# Maintener: Nic0 <nicolas.caen@gmail.com>

pkgname=tyrs
pkgver=0.3.2
pkgrel=1
pkgdesc="A lightweight console Twitter and Identica client using ncurses and Python."
arch=('any')
url="http://tyrs.nicosphere.net"
license=('GPL')
depends=('python2-distribute' 'python-distutils-extra')
source=('http://src.nicosphere.net/tyrs/tyrs-0.3.2.tar.gz')
md5sums=('784f2e8ba8e761b7f2de6ba7f0285b91')

build() {
  cd "$srcdir"

  tar xvf $pkgname-$pkgver.tar.gz && cd $pkgname

  python2 setup.py build
}

package() {
  cd "$srcdir/$pkgname"

  python2 setup.py install --prefix=/usr --root="$pkgdir"
}
