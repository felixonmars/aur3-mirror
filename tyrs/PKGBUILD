# Maintener: Nic0 <nicolas.caen@gmail.com>

pkgname=tyrs
pkgver=0.2.1.1
pkgrel=1
pkgdesc="A lightweight console Twitter Client using ncurses and Python."
arch=('any')
url="http://tyrs.nicosphere.net"
license=('GPL')
depends=('python-twitter')
source=('http://src.nicosphere.net/tyrs/tyrs-0.2.1.1.tar.gz')
md5sums=('3d31c54747a48e8b26e2af772fdfc87a')

build() {
  cd "$srcdir"

  tar xvf $pkgname-$pkgver.tar.gz && cd $pkgname

  python2 setup.py build
}

package() {
  cd "$srcdir/$pkgname"

  python2 setup.py install --prefix=/usr --root="$pkgdir"
}
