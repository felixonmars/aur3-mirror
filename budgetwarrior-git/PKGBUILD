# Maintainer: Fabien Dubosson <fabien.dubosson@gmail.com>
pkgname=budgetwarrior-git
_pkgname=budgetwarrior
pkgver=0.2.2.r36.g9212ff1
pkgrel=1
pkgdesc="Simple command line utility to helps keeping track of your expenses and the state of your budget."
arch=('i686' 'x86_64')
url="https://github.com/wichtounet/budgetwarrior"
license=('MIT')
depends=('boost' 'util-linux')
makedepends=('cmake')
provides=("${_pkgname}")
conflicts=("${_pkgname}")
changelog="ChangeLog"
source=("${_pkgname}::git+https://github.com/wichtounet/${_pkgname}.git")
md5sums=('SKIP')

pkgver() {
  cd "${srcdir}/${_pkgname}"
  git describe --long --tags | sed -E 's/([^-]*-g)/r\1/;s/-/./g'
}

build() {
    cd "${srcdir}/${_pkgname}"
    cmake .
    make
}

package() {
    cd "${srcdir}/${_pkgname}"
    make DESTDIR="${pkgdir}/" install
}
