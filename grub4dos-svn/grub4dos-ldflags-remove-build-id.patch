diff --git a/configure b/configure
index 4d5dc19..46a8aa9 100755
--- a/configure
+++ b/configure
@@ -4361,15 +4361,16 @@ fi
 
 fi
 
-LDFLAGS_SAVED="${LDFLAGS}"
+LDFLAGS_SAVED="${LDFLAGS} -Wl,--build-id=none"
 case "$host_os" in
   mingw32 | cygwin)
-    BIN_LDFLAGS="-Wl,-T"'$(top_srcdir)'"/ldscript"
-    LDFLAGS="${LDFLAGS} -Wl,-T${srcdir}/ldscript"
+    BIN_LDFLAGS="-Wl,-T"'$(top_srcdir)'"/ldscript -Wl,--build-id=none"
+    LDFLAGS="${LDFLAGS} -Wl,-T${srcdir}/ldscript -Wl,--build-id=none"
     BUILD_DIRS="netboot stage2 stage1"
     ;;
   *)
-    BIN_LDFLAGS="-Wl,-N"
+    BIN_LDFLAGS="-Wl,-N -Wl,--build-id=none"
+    LDFLAGS="${LDFLAGS} -Wl,--build-id=none"
     BUILD_DIRS="netboot stage2 stage1 lib grub util"
     ;;
 esac
@@ -5087,7 +5088,7 @@ echo "$as_me: error: Neither end nor _end is defined" >&2;}
    { (exit 1); exit 1; }; }
 fi
 
-LDFLAGS=${LDFLAGS_SAVED}
+LDFLAGS="${LDFLAGS_SAVED} -Wl,--build-id=none"
 
 # Check for curses libraries.
 
diff --git a/configure.ac b/configure.ac
index 7e9a5b0..cb9409d 100644
--- a/configure.ac
+++ b/configure.ac
@@ -172,15 +172,16 @@ else
   AC_CHECK_TOOL(OBJCOPY, objcopy)
 fi
 
-LDFLAGS_SAVED="${LDFLAGS}"
+LDFLAGS_SAVED="${LDFLAGS} -Wl,--build-id=none"
 case "$host_os" in
   mingw32 | cygwin)
-    BIN_LDFLAGS="-Wl,-T"'$(top_srcdir)'"/ldscript"
-    LDFLAGS="${LDFLAGS} -Wl,-T${srcdir}/ldscript"
+    BIN_LDFLAGS="-Wl,-T"'$(top_srcdir)'"/ldscript -Wl,--build-id=none"
+    LDFLAGS="${LDFLAGS} -Wl,-T${srcdir}/ldscript -Wl,--build-id=none"
     BUILD_DIRS="netboot stage2 stage1"
     ;;
   *)
-    BIN_LDFLAGS="-Wl,-N"
+    BIN_LDFLAGS="-Wl,-N -Wl,--build-id=none"
+    LDFLAGS="${LDFLAGS} -Wl,--build-id=none"
     BUILD_DIRS="netboot stage2 stage1 lib grub util"
     ;;
 esac
@@ -226,7 +227,7 @@ if test "x$grub_cv_check_end_symbol" != "xyes" \
   AC_MSG_ERROR([Neither end nor _end is defined])
 fi
 
-LDFLAGS=${LDFLAGS_SAVED}
+LDFLAGS="${LDFLAGS_SAVED} -Wl,--build-id=none"
 
 # Check for curses libraries.
 AC_ARG_WITH(curses,