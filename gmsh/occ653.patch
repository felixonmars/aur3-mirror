diff -Nur Geo.orig/OCC_Connect.cpp Geo/OCC_Connect.cpp
--- Geo.orig/OCC_Connect.cpp	2012-04-25 14:26:11.785828616 +0200
+++ Geo/OCC_Connect.cpp	2012-04-25 14:26:23.869106529 +0200
@@ -597,7 +597,7 @@
             double tolerance=std::max(BRep_Tool::Tolerance(edge),
                                       BRep_Tool::Tolerance(vertex));
             for(int i=1;i<=distance.NbExt();i++) {
-                if(distance.Value(i)<tolerance) {
+                if(distance.SquareDistance(i)<tolerance) {
                     try {
                         // No idea why this can fail
                         splitter1.Add(vertex,distance.Parameter(i),edge);
@@ -616,7 +616,7 @@
                             double d2=BRep_Tool::Pnt(v2).Distance(
                                 BRep_Tool::Pnt(vertex));
                             cout << "Adding " << i << " to edge " << e
-                                << " distance=" << distance.Value(i)
+                                << " distance=" << distance.SquareDistance(i)
                                 << " parameter=" << distance.Parameter(i)
                                 << " point=" << distance.Point(i)
                                 << " dv1=" << d1
@@ -649,7 +649,7 @@
             double tolerance=std::max(BRep_Tool::Tolerance(edge),
                                       BRep_Tool::Tolerance(vertex));
             for(int i=1;i<=distance.NbExt();i++) {
-                if(distance.Value(i)<tolerance) {
+                if(distance.SquareDistance(i)<tolerance) {
                     try {
                         splitter2.Add(vertex,distance.Parameter(i),edge);
                     }
@@ -667,7 +667,7 @@
                             double d2=BRep_Tool::Pnt(v2).Distance(
                                 BRep_Tool::Pnt(vertex));
                             cout << "Adding " << i << " to edge " << e
-                                << " distance=" << distance.Value(i)
+                                << " distance=" << distance.SquareDistance(i)
                                 << " parameter=" << distance.Parameter(i)
                                 << " point=" << distance.Point(i)
                                 << " dv1=" << d1
diff -Nur Geo.orig/OCCFace.cpp Geo/OCCFace.cpp
--- Geo.orig/OCCFace.cpp	2012-04-25 14:26:11.785828616 +0200
+++ Geo/OCCFace.cpp	2012-04-25 14:26:44.945676528 +0200
@@ -349,7 +349,7 @@
   Standard_Real aDiscret = aCoeff * dMax;
   BRepMesh_FastDiscret aMesher(aDiscret, 0.5, aBox, Standard_False, Standard_True,
                                Standard_False, Standard_True);
-  aMesher.Add(s);
+  //aMesher.Add(s);
   TopLoc_Location loc;
   Handle(Poly_Triangulation) triangulation = BRep_Tool::Triangulation(s, loc);
   if(triangulation.IsNull()){
@@ -425,7 +425,7 @@
 
 void OCCFace::replaceEdgesInternal(std::list<GEdge*> &new_edges)
 {
-  IntTools_Context myContext;
+  Handle_IntTools_Context myContext;
   // we simply replace old edges by new edges in the structure
   
   // make a copy of s
