# Maintainer: Joel Teichroeb <joel@teichroeb.net>
# Contributor: Sébastien Luttringer
# Contributor: Emmanuel Gil Peyrot <linkmauve@linkmauve.fr>

pkgname=weston-git
pkgver=20130406
pkgrel=1
pkgdesc='Reference implementation of a Wayland compositor'
arch=(i686 x86_64)
url='http://wayland.freedesktop.org'
license=('MIT')
options=(!libtool)
makedepends=('git')
depends=('libxkbcommon' 'wayland-git' 'mesa-full' 'cairo' 'poppler-glib' 'mtdev' 'libxcursor' 'pixman' 'glu' 'libunwind')
conflicts=('weston')

source=('git://anongit.freedesktop.org/wayland/weston')
sha1sums=('SKIP')

build() {
    cd "weston"
	./autogen.sh --prefix=/usr --libexecdir=/usr/lib/weston
	make
}

package() {
    cd "weston"
	make DESTDIR="${pkgdir}" install

	for _c in clickdot cliptest dnd editor eventdemo flower gears image \
	 resizor simple-egl simple-shm smoke view; do
		install -Dm755 "clients/$_c" "$pkgdir/usr/bin/weston-$_c"
	done
}

