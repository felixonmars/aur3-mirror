# Maintainer: David McMackins <mcmackinsd@gmail.com>
pkgname=calccmd
pkgver=0.2.2
pkgrel=1
epoch=
pkgdesc="A free command line calculator"
arch=(any)
url="https://github.com/2mac/calcCMD/"
license=('GPL3')
groups=()
depends=()
makedepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
source=(https://github.com/2mac/$pkgname/archive/$pkgver.tar.gz)
noextract=()
md5sums=('1239c3d6cdbafa7d99e6e07e68007a00')

build() {
    make -C "$pkgname-$pkgver"
}

package() {
    make -C "$pkgname-$pkgver" PREFIX=/usr DESTDIR="$pkgdir/" install
}
