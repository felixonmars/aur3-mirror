# Maintainer: Markus Unterwaditzer <markus[AT]unterwaditzer.net>
pkgname=tox-ntox
pkgdesc='DEPRECATED: Use tox-ntox-git'

license=('GLPv3')
pkgrel=2
pkgver=49a61ef
depends=()
makedepends=()
arch=('i686' 'x86_64')
url=('http://tox.im')

source=()
md5sums=()


_show_error() {
    echo "tox-ntox is a git package and should have -git in its name."
    echo "This package will not be maintained anymore. Please use tox-ntox-git."
}

build() {
    _show_error
    false
}

package() {
    _show_error
    false
}
