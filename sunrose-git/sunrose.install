post_install() {
   cat << EOF
Install the example configs:

    install -d "\${XDG_CONFIG_HOME:-\$HOME/.config}/sunrose"
    install -Dm755  /usr/share/doc/sunrose/example/config/* "\${XDG_CONFIG_HOME:-\$HOME/.config}/sunrose"
EOF
}
