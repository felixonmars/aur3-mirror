# Contributor: Tocer Deng <tocer.deng@gmail.com>
pkgname=openresty-dev
_pkgname=ngx_openresty
pkgver=1.2.4.9
pkgrel=1
pkgdesc="a powerful web app server by extending nginx. development release."
arch=('i686' 'x86_64')
url="http://openresty.org/"
license=('BSD')
groups=()
depends=('perl>=5.6.1' 'readline' 'pcre' 'openssl')
makedepends=()
optdepends=()
provides=('openresty=1.2.4.9')
conflicts=('openresty')
replaces=('openresty')
backup=()
options=()
install=
source=(http://agentzh.org/misc/nginx/$_pkgname-$pkgver.tar.gz)
noextract=()
md5sums=('01cf83429834f3c6ef5e2816640aa340')

build() {
  cd "$srcdir/$_pkgname-$pkgver"

  ./configure --prefix=/opt/openresty --with-luajit --with-http_postgres_module --with-http_realip_module
  make ${MAKEFLAGS} || return 1
  make DESTDIR="$pkgdir/" install
}

# vim:set ts=2 sw=2 et:
