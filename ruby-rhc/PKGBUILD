# Maintainer: OmeGa <omg@vmail.me>
# Contributor: Guilherme "nirev" Nogueira <guilherme@nirev.org>

pkgname=ruby-rhc
_gemname=rhc
pkgver=1.0.4
pkgrel=1
pkgdesc="The client tools for the OpenShift platform that allow for application management."
arch=('any')
url="https://openshift.redhat.com/app/express"
license=('APACHE-2.0')
depends=('ruby' 'ruby-rest-client' 'ruby-parseconfig' 'ruby-json' 'ruby-net-ssh' 'ruby-archive-tar-minitar' 'ruby-commander' 'ruby-open4' 'ruby-test-unit')
makedepends=('rubygems')
source=("http://rubygems.org/downloads/${_gemname}-${pkgver}.gem")
sha1sums=('9d0b3cc46f341f30f75d8a81214ae944cc8264ed')
noextract=("${_gemname}-$pkgver.gem")

build() {
  cd $srcdir
  export HOME=/tmp
  export RHC_RPMBUILD=1
  local _gemdir="$(ruby -rubygems -e'puts Gem.default_dir')"
  gem install --no-user-install --ignore-dependencies -i "${pkgdir}${_gemdir}" -n "${pkgdir}/usr/bin" ${_gemname}-$pkgver.gem
}

# vim:set ts=2 sw=2 et:

