# Contributor: Doug Newgard <scimmia22 at outlook dot com>
# Contributor: Robert Orzanna <orschiro at gmail dot com>
# Maintainer: Jay Garcia <morbidj at gmail dot com>


pkgname=timeshift
pkgver=1.5.1
pkgrel=1
pkgdesc="A system restore utility for Linux"
arch=('i686' 'x86_64')
url="https://launchpad.net/~teejee2008/+archive/ubuntu/ppa/"
license=('GPL')
depends=('gtk3' 'rsync' 'libgee06' 'libsoup' 'json-glib' 'desktop-file-utils')
optdepends=('gksu: run timeshift from a menu')
makedepends=('vala')
install=$pkgname.install
options=('!emptydirs')
source=("https://launchpad.net/~teejee2008/+archive/ubuntu/ppa/+files/timeshift_1.5.1~117~ubuntu14.04.1.tar.gz")
md5sums=('6959c1a694a15c9377bdf893d84f67f6')

build() {
  cd "$srcdir/recipe-{debupstream}~{revno}"

  make
}

package() {
  cd "$srcdir/recipe-{debupstream}~{revno}"

  make DESTDIR="$pkgdir" install
}


