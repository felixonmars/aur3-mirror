diff -aur src.old/hanvon/hanvon.c ./hanvon.c
--- src.old/hanvon/hanvon.c	2013-08-09 11:43:08.457194643 -0400
+++ ./hanvon.c	2013-08-09 11:44:24.550525940 -0400
@@ -85,10 +85,10 @@
 		case -ENOENT:
 		case -ESHUTDOWN:
 			/* this urb is terminated, clean up */
-			dbg("%s - urb shutting down with status: %d", __func__, urb->status);
+			//dbg("%s - urb shutting down with status: %d", __func__, urb->status);
 			return;
 		default:
-			dbg("%s - nonzero urb status received: %d", __func__, urb->status);
+			//dbg("%s - nonzero urb status received: %d", __func__, urb->status);
 			goto exit;
 	}
 
@@ -121,7 +121,8 @@
 exit:
 	retval = usb_submit_urb (urb, GFP_ATOMIC);
 	if (retval)
-		dbg("%s - usb_submit_urb failed with result %d", __func__, retval);
+		//dbg("%s - usb_submit_urb failed with result %d", __func__, retval);
+	;
 }
 
 static struct usb_device_id hanvon_ids[] = {
