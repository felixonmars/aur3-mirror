# Maintainer: Blaž Tomažič <blaz.tomazic@gmail.com>
# Contributor: William Rea <sillywilly@gmail.com>
# Contributor: Bjorn Lindeijer <bjorn lindeijer nl>
# Contributor: Geoffroy Carrier <geoffroy.carrier@koon.fr>
# Contributor: Alexsandr Pavlov <kidoz at mail dot ru>

pkgname=ruby1.8-sqlite3
gem_name=sqlite3
pkgver=1.3.3
pkgrel=1
pkgdesc="A Ruby interface for the SQLite3 database engine."
arch=('i686' 'x86_64')
url="http://rubygems.org/gems/sqlite3"
license=('BSD')
depends=('ruby1.8' 'sqlite3')
makedepends=('rubygems1.8')
source=(http://rubygems.org/downloads/$gem_name-$pkgver.gem)
noextract=($gem_name-$pkgver.gem)
md5sums=('25f9cf0744157dc7ecf1308e5e619967')

build() {
  cd $srcdir
  local _gemdir="$(ruby-1.8 -rubygems -e'puts Gem.default_dir')"
  gem-1.8 install --ignore-dependencies -i "$pkgdir$_gemdir" $gem_name-$pkgver.gem
 }
