# Maintainer: eagletmt <eagletmt@gmail.com>
pkgname=ibus-skk
pkgver=1.3.6
pkgrel=1
pkgdesc='Japanese input method SKK engine for IBus'
arch=('i686' 'x86_64')
url='https://github.com/ueno/ibus-skk'
license=('GPL')
depends=('ibus')
makedepends=()
source=(https://github.com/downloads/ueno/$pkgname/$pkgname-$pkgver.tar.gz)

build() {
  cd "$srcdir/$pkgname-$pkgver"

  PYTHON=python2 ./configure --prefix=/usr --libexecdir=/usr/lib/ibus
  make
}

package() {
  cd "$srcdir/$pkgname-$pkgver"
  make DESTDIR="$pkgdir" install
}

md5sums=('22670ec98da404739ac5c08d2ab9a5d7')
