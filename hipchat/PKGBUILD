# Maintainer: Douglas Meyer <Douglas.Meyer@mail.com>
# Contributor: Tom Vincent <http://tlvince.com/contact>

pkgname=hipchat
pkgver=1.96.524
pkgrel=1
epoch=1
pkgdesc="Persistent group chat using XMPP"
arch=('i686' 'x86_64')
url="https://www.hipchat.com/linux"
license=('unknown')
depends=('libxft' 'libltdl')

host="http://downloads.hipchat.com"
#NOTE: the Location header has "/./" in it, and curl doesn't seem to agree with that.
location=$(curl --silent --head ${host}/linux/arch/hipchat-$CARCH.tar.xz | grep Location | cut -c 11- | tr --delete "\r" | sed "s/\/\.\//\//")
source=(${location})

if [ "$CARCH" == "x86_64" ]; then
  md5sums=('3d4c5af59d24f1105c3ac4b99c265fba')
else
  md5sums=('ff3d994aea5db2040d997ede795f6cdf')
fi

package() {
  cp -R $srcdir/{opt,usr} "$pkgdir"
}
