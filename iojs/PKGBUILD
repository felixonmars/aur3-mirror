# Maintainer: Josip Janzic <josip.janzic@gmail.com>
pkgname=iojs
pkgver=1.0.3
pkgrel=1
pkgdesc="Bringing ES6 to the Node Community!"
arch=('i686' 'x86_64')
url="https://iojs.org/"
license=('MIT')
depends=('openssl')
optdepends=('python2: for node-gyp')
provides=('nodejs')
conflicts=('nodejs')
options=('!emptydirs')

_arch=x64
[[ $CARCH = i686 ]] && _arch=x86

source=("https://iojs.org/dist/v${pkgver}/iojs-v${pkgver}-linux-${_arch}.tar.xz")

sha256sums=('5e444266a6b5d842ab81527734be68e217393a86db024836b6f2d9f516120f6a')
[[ $CARCH = i686 ]] && sha256sums[0]='8b3f1cd8d421e5015405948b4cb90da4a9262d97383603eaf9560a40c7320709'

_extracted_arch=x64
[[ $CARCH = i686 ]] && _extracted_arch=ia32

package() {
	cd "iojs-v${pkgver}-linux-${_extracted_arch}"

	mkdir "$pkgdir/usr/"

	rm -rf ./share/systemtap
	mv ./{bin,include,lib,share} "$pkgdir/usr/"

	install -D -m644 LICENSE "$pkgdir"/usr/share/licenses/iojs/LICENSE
}
