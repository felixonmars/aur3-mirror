# Maintainer: Josip Janzic <josip.janzic@gmail.com>
pkgname=iojs
pkgver=1.0.1
pkgrel=2
pkgdesc="Bringing ES6 to the Node Community!"
arch=('i686' 'x86_64')
url="https://iojs.org/"
license=('MIT')
depends=('openssl')
optdepends=('python2: for node-gyp')
provides=('nodejs')
conflicts=('nodejs')
options=('!emptydirs')

_arch=x64
[[ $CARCH = i686 ]] && _arch=x86

source=("https://iojs.org/dist/v${pkgver}/iojs-v${pkgver}-linux-${_arch}.tar.xz")

sha256sums=('1415cbe8011baaba1ec538e774323317f56e9d594de6fe1cea6a9ddc7aa21de1')
[[ $CARCH = i686 ]] && sha256sums[0]='4b097d1c41f98dac2096d288486916d5a4f6cb03c67bc2b715527085050b4cbf'

package() {
	cd "iojs-v${pkgver}-linux-${_arch}"

	mkdir "$pkgdir/usr/"

	rm -rf ./share/systemtap
	mv ./{bin,include,lib,share} "$pkgdir/usr/"

	install -D -m644 LICENSE "$pkgdir"/usr/share/licenses/iojs/LICENSE
}
