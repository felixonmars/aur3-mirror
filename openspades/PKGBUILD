# Maintainer: kusakata <shohei atmark kusakata period com>

pkgname=openspades
pkgver=0.0.9
pkgrel=2
pkgdesc="A clone of Voxlap Ace of Spades 0.75"
arch=('i686' 'x86_64')
url="https://sites.google.com/a/yvt.jp/openspades/"
license=('GPL3')
depends=('curl' 'fltk' 'glew' 'mesa' 'openspades-data' 'sdl')
makedepends=('cmake')
source=("https://codeload.github.com/yvt/openspades/tar.gz/v${pkgver}" "openspades.desktop")

build() {
	cd "${srcdir}/openspades-${pkgver}"
	cmake -D CMAKE_INSTALL_PREFIX=/usr -D OPENSPADES_RESDIR=/usr/share/openspades-data/Resources .
	make
}

package() {
	cd "${srcdir}/openspades-${pkgver}/bin"
	install -Dm755 OpenSpades "${pkgdir}/usr/bin/${pkgname}"
	cd "${srcdir}"
	install -Dm644 "${pkgname}.desktop" "${pkgdir}/usr/share/applications/${pkgname}.desktop"
}

md5sums=('87b3fa8f11f707d7ad860a8746c06ef4'
         '2cdc6049dc12fd48b2edfaa363bdd11d')
