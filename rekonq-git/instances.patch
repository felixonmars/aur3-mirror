diff -Naur rekonq//src/main.cpp rekonq-new//src/main.cpp
--- rekonq//src/main.cpp	2011-03-23 00:22:15.866180740 -0500
+++ rekonq-new//src/main.cpp	2011-03-22 23:13:32.000000000 -0500
@@ -199,11 +199,11 @@
     // Add options from Application class
     Application::addCmdLineOptions();
 
-    if (!Application::start())
-    {
-        kWarning() << "rekonq is already running!";
-        return 0;
-    }
+//     if (!Application::start())
+//     {
+//         kWarning() << "rekonq is already running!";
+//         return 0;
+//     }
 
 #if defined(Q_WS_X11)
     // On X11, the raster engine gives better performance than native.
