# Autors Jazahum'Allah Khayran:
#   --  Ahmed Raghdi (أحمد رغدي) <asmaaarab@gmail.com> (main programmer)
#   --  Fayssal Chamekh (فيصل شامخ) <chamfay@gmail.com>
#
# Licence: Waqf General Public Licence http://ojuba.org/wiki/waqf/license

pkgname=asmaa
pkgver=2.1.1
pkgrel=1
pkgdesc=' Linux alternative of the Islamic Arabic Shamela library. '
url='http://sourceforge.net/projects/asmaalibrary/files/'
arch=('any')
license=('Waqf GPL')
depends=('python2' 'python-gobject' 'gtk3' 'webkitgtk' 'mdbtools')
makedepends=('patch')
source=("http://downloads.sourceforge.net/sourceforge/asmaalibrary/${pkgname}-${pkgver}.${pkgrel}.tar.gz" 'fix_import_shamela.patch')
md5sums=('378e05041f9cdda6cde856991c9779f3' 'e8e5d7d8b9851cc8e0614de3227e80ef')

build(){
  cd ${srcdir}/${pkgname}-${pkgver}.${pkgrel}
  patch -Np0 -i ${srcdir}/fix_import_shamela.patch
}

package() {
  cd ${srcdir}/${pkgname}-${pkgver}.${pkgrel}
  python2 setup.py install --root=${pkgdir}/
}
