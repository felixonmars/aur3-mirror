post_install() {
    echo
    echo "-- .::REMINDER::. ---------------------------------------------------"
    echo " If you set custom options in the pkgbuild, do the same for the rtorrent-pyro-git package"
    echo "---------------------------------------------------------------------"
    echo
}
post_upgrade() {
    post_install
}
