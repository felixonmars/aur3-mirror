post_install() {
	if [ ! -e /etc/boot.conf ] && [ -e /usr/share/doc/coreboot/boot.conf.example ]
	then
		cp -R /usr/share/doc/coreboot/boot.conf.example /etc/boot.conf
	fi
}

post_upgrade() {
        post_install
}

