# Maintainer: Jochen Schalanda <jochen+aur@schalanda.name>
_gemname=r10k
pkgname=ruby-$_gemname
pkgver=1.0.0
pkgrel=1
pkgdesc='Smarter Puppet deployment, powered by killer robots'
arch=(any)
url='http://github.com/adrienthebo/r10k'
license=('Apache')
depends=('ruby' 'ruby-colored>=1.2' 'ruby-cri>=2.3.0' 'ruby-systemu>=2.5.2'
         'ruby-middleware>=0.1.0' 'ruby-json>=1.7.5' 'ruby-log4r>=1.1.10')
makedepends=('rubygems')
source=(http://gems.rubyforge.org/gems/$_gemname-$pkgver.gem)
noextract=($_gemname-$pkgver.gem)

package() {
  cd "$srcdir"
  # _gemdir is defined inside package() because if ruby[gems] is not installed on
  # the system, makepkg will exit with an error when sourcing the PKGBUILD.
  local _gemdir="$(ruby -rubygems -e'puts Gem.default_dir')"

  gem install --no-user-install --ignore-dependencies -i "$pkgdir$_gemdir" \
    -n "$pkgdir/usr/bin" "$_gemname-$pkgver.gem"
}
md5sums=('86cda448fe5ddbe1cfdf1ee978ee3d77')
