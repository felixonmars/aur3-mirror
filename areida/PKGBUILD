pkgname=areida
pkgver=0.0.28
pkgrel=1
pkgdesc="Areida Open Source is an application intended for web browsing. His condition is currently beta, so you may encounter errors and inconsistencies."
arch=('i686' 'amd64')
url="http://ayortan.uphero.com/"
license=('GPL')
depends=('qt ')
makedepends=()
conflicts=()
replaces=()
backup=()
install=
source=($pkgname-$pkgver.tar.gz)
noextract=()
md5sums=('08b3d76699850d9ee35fff9ad48074ad') #generate with 'makepkg -g'

build() {
  cd "$srcdir/$pkgname-$pkgver"
  mv * /usr/src
}

