post_install() {
    echo "* put something like this in your .zshrc (if using zsh):"
    echo "  function precmd () {"
    echo "    z --add \"\$(pwd -P)\""
    echo "  }"
    echo "if using bash, you shouldn't have to do anything"
}
