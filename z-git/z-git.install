post_install() {
    echo "* put something like this in your .zshrc (if you're using zsh):"
    echo "  function precmd () {"
    echo "    z --add \"\$(pwd -P)\""
    echo "  }"
    echo "if you're using bash and you get a syntax errors, try removing"
    echo "the semicolon from the PROMPT_COMMAND env var in the script"
}
