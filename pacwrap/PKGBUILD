# Contributor: Xavion <Xavion (dot) 0 (at) Gmail (dot) com>
# Contributor: Bruenig <Matthew (dot) Bruenig (at) Gmail (dot) com>

pkgname=pacwrap
pkgver=0.2.0
pkgrel=1
pkgdesc="Wraps the 'pacman' and 'packer' tools"
arch=("any")
url="http://github.com/bruenig/${pkgname}"
license=("GPL3")
depends=("bash" "packer")
options=(!emptydirs)
source=(${pkgname}.sh)

package() {
	cd "${srcdir}"

	install -D -m755 ${pkgname}.sh "${pkgdir}"/usr/bin/${pkgname}.sh
}

sha1sums=('e62a302005672bebec21785e3d93cd11deaf243d')
