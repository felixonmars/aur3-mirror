# Contributor: Dmitriy Morozov <archlinux@foxcub.org>
pkgname=visit-bin
_pkgname=visit
pkgver=2.2.1
_pkgver=2_2_1
pkgrel=1
pkgdesc="Interactive parallel visualization and graphical analysis tool"
url="https://wci.llnl.gov/codes/visit/"
arch=('x86_64')
license="BSD"
depends=('libpng12')
source=(https://wci.llnl.gov/codes/${_pkgname}/$pkgver/${_pkgname}${_pkgver}.linux-x86_64.tar.gz
        https://wci.llnl.gov/codes/${_pkgname}/$pkgver/${_pkgname}-install${_pkgver}
        visit-bin.sh)
md5sums=('6eaa5cad798f0e9ef2c9ca6571eb5f4e'
         'a22caec439a3bd0da095e752ea1c1713'
         'eda322f9745ff8ace1cfbec7536929df')

build()
{
    cd $srcdir
    mkdir -p $pkgdir/opt/$_pkgname
    sh $_pkgname-install$_pkgver -c none $pkgver linux-x86_64 $pkgdir/opt/$_pkgname
    mkdir -p $pkgdir/etc/profile.d/
    install -m 755 visit-bin.sh $pkgdir/etc/profile.d/
}
