# Maintainer: Phillip Smith <fukawi2@NO-SPAM.gmail.com>
# http://github.com/fukawi2/aur-packages

### I AM ONLY THE PACKAGER, NOT THE DEVELOPER
### Please ask support questions about this software in one of:
###   1) The AUR comments; OR
###   2) Upstream forums/maillist etc; OR
###   3) The ArchLinux forums
### I do not always know enough about the software itself, or don't have the
### time to promptly respond to direct emails.
### If you have found a problem with the package/PKGBUILD (as opposed to
### the software) then please do email me or post an AUR comment.

pkgname=mod_macro
pkgver=1.1.11
pkgrel=1
pkgdesc="DSO module for Apache that allows the definition and use of macros within apache runtime configuration files. "
url="http://coelho.net/mod_macro/"
arch=('i686' 'x86_64')
license=('apache')
depends=('apache')
makedepends=('apache')
source=("http://coelho.net/mod_macro/$pkgname-$pkgver.tar.gz")
md5sums=('dd372f31d788728cfead785eec420d22')

_MODDIR='usr/lib/httpd/modules'
_DOCDIR="usr/share/doc/$pkgname"

build() {
  cd $srcdir/$pkgname-$pkgver

  # make the module
  /usr/sbin/apxs -c -o $pkgname.so $pkgname.c
}

package() {
  cd $srcdir/$pkgname-$pkgver

  install -Dm644 .libs/$pkgname.so $pkgdir/$_MODDIR/$pkgname.so

  # install docs
  install -Dm644 $pkgname.html $pkgdir/$_DOCDIR/$pkgname.html
}

# vim:set ts=2 sw=2 et:
