# Maintainer: Gilbert Kennen <gilbert firewatcher org>
pkgname=elixir
pkgver=1.0.2
pkgrel=1
pkgdesc="a functional meta-programming aware language built on top of the Erlang VM"
url="http://elixir-lang.org"
arch=('any')
license=('Apache' 'custom:EPL')
depends=('erlang-nox')
conflicts=()
replaces=()
backup=()
source=("https://github.com/elixir-lang/elixir/tarball/v${pkgver}")
sha512sums=('fea1a62abf3274dcc62d568a1f6a39f76f602bda393dc4eb0b7bb875bc12fc949c70737c90101970d28fd21dfcfe8985659e1729678b66804cb925d4a3afffdd')

prepare() {
  ELIXIR_SRC_DIR=${srcdir}/$(tar "-tzf${srcdir}/v${pkgver}" | grep -o '^[^/]\+' | sort -u)
}

build() {
  cd $ELIXIR_SRC_DIR
  make
}

check() {
  cd $ELIXIR_SRC_DIR
  make test_erlang
} 

package() {
  mkdir -p "${pkgdir}/usr/share/licenses/${pkgname}"

  local ELIXIR_SRC_DIR=${srcdir}/$(tar "-tzf${srcdir}/v${pkgver}" | grep -o '^[^/]\+' | sort -u)
  cd $ELIXIR_SRC_DIR

  install -Dm644 {LEGAL,LICENSE} "${pkgdir}/usr/share/licenses/${pkgname}"

  make DESTDIR=$pkgdir PREFIX=/usr install
}
