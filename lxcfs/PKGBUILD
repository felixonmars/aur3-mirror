# $Id: PKGBUILD 126810 2015-01-30 14:27:46Z spupykin $
# Maintainer: Sergej Pupykin <pupykin.s+arch@gmail.com>

pkgname=lxcfs
pkgver=0.5
pkgrel=1
pkgdesc="FUSE filesystem for LXC"
arch=(i686 x86_64)
url="https://linuxcontainers.org/"
license=('GPL')
depends=('lxc' 'cgmanager>=0.35-1' 'libnih')
makedepends=('help2man')
source=("https://linuxcontainers.org/downloads/lxcfs/lxcfs-$pkgver.tar.gz")
md5sums=('586d7184a7a85dab1220478872f68e77')

build() {
	cd "$srcdir/$pkgname-$pkgver"
#	./bootstrap.sh
    ./configure --prefix=/usr --sbindir=/usr/bin --localstatedir=/var
	make
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	make DESTDIR="$pkgdir/" install
}
