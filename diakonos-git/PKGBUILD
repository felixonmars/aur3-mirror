# Maintainer: aksr <aksr at t-com dot me>
pkgname=diakonos-git
pkgver=v0.9.1.r46.ga7646a7
pkgrel=2
pkgdesc="A Linux console text editor for the masses."
arch=('i686' 'x86_64')
url="http://diakonos.pist0s.ca/"
#url="https://github.com/Pistos/diakonos"
license=('MIT')
groups=()
depends=('ruby')
makedepends=('git')
optdepends=()
provides=()
conflicts=('diakonos')
replaces=()
backup=()
options=()
install=
source=()
noextract=()
md5sums=('SKIP')
sha1sums=('SKIP')

_gitroot=https://github.com/Pistos/diakonos.git
_gitname=diakonos

pkgver() {
  cd "$srcdir/$_gitname"
  git describe --long | sed -E 's/([^-]*-g)/r\1/;s/-/./g'
}

build() {
  cd "$srcdir"
  msg "Connecting to GIT server...."

  if [[ -d "$_gitname" ]]; then
    cd "$_gitname" && git pull origin
    msg "The local files are updated."
  else
    git clone "$_gitroot" "$_gitname"
  fi

  msg "GIT checkout done or server timeout"
  msg "Starting build..."

  rm -rf "$srcdir/$_gitname-build"
  git clone "$srcdir/$_gitname" "$srcdir/$_gitname-build"
}

package() {
  cd "$srcdir/$_gitname-build"

  ruby=$(test -x /usr/bin/ruby && echo /usr/bin/ruby || which ruby)
  $ruby install.rb --dest-dir $pkgdir --prefix /usr --conf-dir /etc
  install -D -m644 LICENCE $pkgdir/usr/share/licenses/diakonos/LICENCE
}

# vim:set ts=2 sw=2 et:
