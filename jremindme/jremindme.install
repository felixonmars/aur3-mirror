post_upgrade() {
	echo ">>> 1.4 uses a new format for reminders."
	echo ">>> You will need to manually delete from ~/.jRemindMe/Reminders"
	echo "    and recreate them."
	echo ">>> Automatic updates are also broken."
	echo ">>> You can disable them in Edit->Preferences."
	echo ">>> Sorry for the inconvenience."
}

op=$1
shift
$op $*
