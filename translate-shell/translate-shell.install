post_upgrade() {
  echo 'If you use zsh with grml-zsh-config,'
  echo 'then run `translate-shell` instead of `trans`.'
  echo 'Alternatively, run `/usr/bin/trans` directly.'

  echo 'This release temporarily fixes a bug with gawk-4.1.2'
}

post_install() {
  post_upgrade
}
