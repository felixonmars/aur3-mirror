# Maintainer: Laszlo Papp <djszapi @ gmail at com>
# Contributor: Piotr Rogoża <rogoza dot piotr at gmail dot com>
pkgname=vim-manpageview
pkgver=23
_scriptid=14117
pkgrel=2
pkgdesc="Viewer for manpages, gnu info,  perldoc, and php (unix/linux/cygwin)"
arch=('any')
url="http://www.vim.org/scripts/script.php?script_id=489"
license=('custom')
depends=(vim)
groups=('vim-plugins')
install=vimdoc.install
source=(${pkgname}.vba.gz::http://www.vim.org/scripts/download_script.php?src_id=${_scriptid}
        license.txt)
build() {
    install -d ${pkgdir}/usr/share/vim/vimfiles/{doc,plugin,syntax,autoload}
    vim -c "UseVimball $srcdir" -c "q" ${pkgname}.vba

    for x in doc; do
		install -Dm644 ${srcdir}/$x/* ${pkgdir}/usr/share/vim/vimfiles/$x/ || return 1;
	done

    for x in plugin syntax autoload; do
		install -Dm644 ${srcdir}/$x/* ${pkgdir}/usr/share/vim/vimfiles/$x/ || return 1;
	done

    install -Dm644 ${srcdir}/license.txt ${pkgdir}/usr/share/licenses/${pkgname}/license.txt || return 1
	rm -f ${pkgdir}/usr/share/vim/vimfiles/doc/tags || return 1
}

md5sums=('09ed0bcd6f76b1d7660a30e611d4827d'
         'efbd5986e691ce8c876fb86e8f5961ea')
