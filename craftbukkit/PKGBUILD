# Maintainer: Sebastian Köhler <sebkoehler@whoami.org.uk>
pkgname=craftbukkit
pkgver=1000
pkgrel=2
pkgdesc="Current build of the Minecraft Server mod Bukkit"
arch=(any)
url="http://bukkit.org/"
license=('LGPL')
depends=('java-runtime' 'tmux')
conflicts=('bukkit')
install=craftbukkit.install
source=('http://ci.bukkit.org/job/dev-CraftBukkit/1000/artifact/target/craftbukkit-0.0.1-SNAPSHOT.jar' 'craftbukkit.rc')
md5sums=('fd263ff8ade829c0c23ded0021af8df8'
         '76c54c34d28471668dbff201b405120a')

package() {
  mkdir -p "$pkgdir/opt/craftbukkit/plugins"
  mkdir -p "$pkgdir/etc/rc.d"
  install -m 644 "$srcdir/craftbukkit-0.0.1-SNAPSHOT.jar" "$pkgdir/opt/craftbukkit/"
  install -m 755 -T "$srcdir/craftbukkit.rc" "$pkgdir/etc/rc.d/craftbukkit"
}

# vim:set ts=2 sw=2 et:
