post_install() {
    msg 'To copy files with a progressbar use the -g option, for example:'
    msg 'cp -g /path/to/file /another/path'
	msg ' '
    msg 'The new cp utility has been installed into /usr/bin,'
    msg 'so make sure that /usr/bin is before /bin in your $PATH.'
}

