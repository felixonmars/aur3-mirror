# Maintainer: boenki <boenki at gmx dot de>

pkgname=ffdiaporama
pkgver=1.2
pkgrel=1
pkgdesc="Creation of videos sequences from images, titles, music"
arch=('i686' 'x86_64')
url="http://ffdiaporama.tuxfamily.org"
license=('GPL2')
conflicts=('ffdiaporama-devel')
depends=('qt' 'ffmpeg' 'exiv2' 'taglib')
source=(http://download.tuxfamily.org/${pkgname}/Stable/${pkgver}_20120303/${pkgname}_${pkgver}.2012.0303.tar.gz)
md5sums=('a1523c901ee38eb66108438e9a65ff16')

build() {
  cd ${srcdir}
  qmake PREFIX=/usr ffDiaporama.pro
  make
}

package() {
  cd ${srcdir}
  make install INSTALL_ROOT=${pkgdir}
}