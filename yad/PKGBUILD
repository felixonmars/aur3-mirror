# Maintainer: tri6791 <trile7 at gmail dot com>
# Contributor: Ernia <monghitri@aruba.it>

pkgname=yad
pkgver=0.10.1
pkgrel=1
pkgdesc="A fork of zenity - display graphical dialogs from shell scripts or command line"
url="http://code.google.com/p/yad/"
arch=('i686' 'x86_64')
license=('GPL3')
depends=('bash' 'gtk2' 'gettext' 'intltool' )
source=(http://yad.googlecode.com/files/${pkgname}-${pkgver}.tar.xz)

build() {
  cd ${srcdir}/${pkgname}-${pkgver}
  ./configure --prefix=/usr || return 1
  make || return 1
 make prefix=${startdir}/pkg/usr install || return 1
}
md5sums=('ddde3970ce4a2ad2288011f2f1407a42')
