# Maintainer: Vladimir Gorbunov <truedaemon@gmail.com>
pkgname=hy-git
_gitname=hy
pkgver=0.9.5.44.ga729b52
pkgrel=1
pkgdesc="Python <--> Lisp layer"
arch=('any')
url="https://github.com/paultag/hy"
license=('MIT')
depends=('python')
options=(!emptydirs)
source=("git://github.com/paultag/hy.git")
md5sums=("SKIP")

pkgver() {
    cd "${_gitname}"
    git describe --always | sed 's|-|.|g'
}

package() {
    cd "${_gitname}"
    python setup.py install --root="${pkgdir}/" --optimize=1
    install -D -m644 LICENSE "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
