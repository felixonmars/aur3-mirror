# Maintainer: Roberto Mastri <rmastri@liceomalpighi.bo.it>
# Builder: lobisquit <enricolovisotto@gmail.com>

# I didn't provide an english translation to because to obtain a driver's license in Italy you have to know Italian, so there is no point in creating a multi-lingual package

pkgname=webpatente
pkgver=4.1.7
pkgrel=2
pkgdesc="Strumento per fare i quiz della patente italiana" 
arch=('i686' 'x86_64')
url="http://www.rmastri.it/"
license=(costum)
# La licenza è disponibile qui: https://www.dropbox.com/s/kgr2e1l6odjwim8/licenza%20webpatente.pdf
case $CARCH in
  "i686") depends=("gtk2" "libxt" "dbus-glib" "alsa-lib");;
  "x86_64") depends=("lib32-gtk2" "lib32-libxt" "lib32-dbus-glib" "lib32-alsa-lib");;
esac

depends+=("desktop-file-utils")
source=("$pkgname-$pkgver-i386.deb::https://www.dropbox.com/s/fb7knjuf2gs7krm/webpatente-4.1.7-i386.deb")
# Non sono riuscito ad individuare un link che permetta di scaricare il file deb dal sito ufficiale, così l'ho messo su dropbox

md5sums=('597f42de5fa52e6d2e7c36361551d533')
install=$pkgname.install

package () {
  ar x $pkgname-$pkgver-i386.deb
  tar xvf data.tar.gz -C $pkgdir
}

# vim:set ts=2 sw=2 et:
