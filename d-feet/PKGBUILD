pkgname=d-feet
pkgver=0.1.12
pkgrel=1
pkgdesc="A D-Bus debugger and explorer"
arch=(any)
url="http://live.gnome.org/DFeet/"
license=('GPL')
depends=('python2' 'pygtk' 'dbus-python')
makedepends=('python2-distribute')
source=(http://ftp.gnome.org/pub/GNOME/sources/d-feet/0.1/${pkgname}-${pkgver}.tar.bz2)
md5sums=('01b0296277b26330bc430742ad4e758a')

build() {
  cd "$srcdir/${pkgname}-${pkgver}"
  python2 setup.py install --prefix=/usr --root="$pkgdir" --optimize=1 -O2
}
