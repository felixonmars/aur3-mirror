# Maintainer: Maxim Andersson <thesilentboatman@gmail.com>

pkgname=peerflix
pkgver=0.2.3
pkgrel=2
pkgdesc="Streaming torrent client for node.js"
arch=('any')
url="https://github.com/mafintosh/peerflix"
license=('MIT')
depends=('nodejs')
source=("https://github.com/mafintosh/${pkgname}/archive/v${pkgver}.tar.gz")
sha256sums=('ffbc166cfd3248e94a0aa5aa56089f6e564b6c342e9a073e3a717f44a75c3d0d')

package() {
  rm -rf "${srcdir}/v${pkgver}.tar.gz"

	install -d "$pkgdir/usr"
  cd "${srcdir}/${pkgname}-${pkgver}"
	npm install --user root -g --prefix "${pkgdir}/usr"
}

# vim:set ts=2 sw=2 et:
