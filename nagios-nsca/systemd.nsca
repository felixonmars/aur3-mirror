[Unit]
Description=Daemon to accept passive Nagios data from remote hosts
After=network.target

[Service]
Type=forking
ExecStart=/usr/sbin/nsca -c /etc/nsca/nsca.cfg --daemon

[Install]
WantedBy=multi-user.target
