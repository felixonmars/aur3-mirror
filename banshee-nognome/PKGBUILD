# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: Oliver Sherouse <first DOT last AT gmail DOT com>
pkgname=banshee-nognome
_pkgname=banshee
pkgver=2.0.0
pkgrel=1
pkgdesc="Play your music and video with no Gnome dependencies"
arch=('i686' 'x86_64')
url="http://banshee.fm"
license=('MIT')
groups=()
depends=('sqlite3' 'gstreamer0.10-base-plugins' 'ndesk-dbus-glib' 'mono-addins' 
'taglib-sharp' 'libmtp' 'mono-zeroconf' 'boo' 'libwebkit' 'gdata-sharp' 'glib' 
 'gtk-sharp-beans' 'gudev-sharp' 'gkeyfile-sharp' 'libgpod') 
makedepends=()
checkdepends=()
optdepends=('udev: device management' 'brasero: CD burning' 
'gstreamer0.10-good-plugins: Extra media codecs' 'gstreamer0.10-ugly-plugins: 
Extra media codecs' 'gstreamer0.10-bad-plugins: Extra media codecs' 
'gstreamer0.10-ffmpeg: Extra media codecs')
provides=('banshee')
conflicts=('banshee')
replaces=()
backup=()
options=()
install=banshee-nognome.install
changelog=
source=("http://download.banshee-project.org/$_pkgname/stable/$pkgver/$_pkgname-$pkgver.tar.bz2")
noextract=()
md5sums=('ecd129a3232bda507b98c90e6f9272cf')

build() {
  cd "$srcdir/$_pkgname-$pkgver"
  ./configure --prefix=/usr \
    --disable-docs \
    --disable-gnome \
    --disable-hal \
    --disable-ipod \
    --enable-gio \
    --enable-gio-hardware \
    --with-vendor-build-id=ArchLinux-AUR
  make
}

check() {
  cd "$srcdir/$_pkgname-$pkgver"
  make -k check
}

package() {
  cd "$srcdir/$_pkgname-$pkgver"
  make DESTDIR="$pkgdir/" install
}

# vim:set ts=2 sw=2 et:
