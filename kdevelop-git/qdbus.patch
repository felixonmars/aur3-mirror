diff --git a/app/kdevelop! b/app/kdevelop!
index 35dda2d..a7cb384 100755
--- a/app/kdevelop!
+++ b/app/kdevelop!
@@ -43,7 +43,7 @@ if [ "$(echo $KDEV_ATTACHED_PID | wc -w | sed 's/ //g')" != 1 ]; then
     exit 6
 fi
 
-if ! [ "$(which qdbus)" ]; then
+if ! [ "$(which qdbus-qt4)" ]; then
     echo "Error: The qdbus utility is missing."
     exit 7
 fi
