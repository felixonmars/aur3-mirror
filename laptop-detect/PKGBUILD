# Maintainer: Bartek Piotrowski <barthalion@gmail.com>
# Contributor: cromo <dawid@klej.net>

pkgname=laptop-detect
pkgver=0.13.7
pkgrel=1
arch=('i686' 'x86_64')
pkgdesc="Attempts to detect a laptop" 
url="http://packages.debian.org/sid/laptop-detect" 
license="BSD" 
depends=('dmidecode>=2.8') 
source=(http://ftp.de.debian.org/debian/pool/main/l/laptop-detect/${pkgname}_${pkgver}.tar.gz)
md5sums=('b5b3dab84ca03b66c8501dc8a98f9c91')

package() { 
    cd $srcdir/$pkgname
    sed -e "s/@VERSION@/${pkgver}/g" < laptop-detect.in > laptop-detect
    install -D -m755 laptop-detect $pkgdir/usr/sbin/laptop-detect
}

