# Maintainer: ryooichi <ryooichi+archtrack AT gmail DOT com>

pkgname=archtrack-artwork
pkgver=0.1
pkgrel=1
pkgdesc="artwork for ArchTrack"
arch=('i686' 'x86_64')
url="http://wiki.archlinux.org/index.php/ArchTrack"
license=('GPL')
groups=()
depends=(
)
makedepends=()
optdepends=(
'archtrack'
'archtrack-dev'
)
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
source=()
noextract=()
md5sums=() #generate with 'makepkg -g'

build() {
  #cd $srcdir/$pkgname-$pkgver
  #./configure --prefix=/usr
  #make || return 1
  #make DESTDIR=$pkgdir install || return 1
  return 0
}
