build ()
{
    grep "cryptkey_device=" /etc/encrypt2.conf >> "$BUILDROOT/encrypt2.conf"
    grep "keyfiles=" /etc/encrypt2.conf >> "$BUILDROOT/encrypt2.conf"
    
    add_runscript
}
help ()
{
cat<<HELPEOF
  Shreds keyfiles on boot.
HELPEOF
}