#Mainteiner: Mikhail Kulemin <mihkulemin@gmail.com>
pkgname=pyzmq
pkgver=2.0.10
pkgrel=1
pkgdesc="Cython based Python bindings for 0MQ(zeromq)."
arch=(i686 x86_64)
url="http://www.zeromq.org/bindings:python"
license=('LGPL')
depends=(cython zeromq python2)
source=(https://github.com/zeromq/pyzmq/downloads/pyzmq-2.0.10.tar.gz)
md5sums=('b8b397b2b459c6cdf7571eca5b697bc5')

build() {
  cd "$srcdir/$pkgname-$pkgver"
  #
  # BUILD HERE
  #
  python2 setup.py build||return 1
  python2 setup.py install --root="$pkgdir"||return 1  
} 
