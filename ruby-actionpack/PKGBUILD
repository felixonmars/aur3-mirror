# Contributor: Alexsandr Pavlov <kidoz at mail dot ru>
pkgname=ruby-actionpack
pkgver=3.0.8
pkgrel=1
pkgdesc="On rails from request to response."
arch=(any)
url="http://www.rubyonrails.org/"
license=('MIT')
depends=('ruby' 'ruby-erubis' 'ruby-tzinfo' 'ruby-rack-mount' 'ruby-rack-test' \
'ruby-rack' 'ruby-i18n' 'ruby-builder' 'ruby-activemodel' 'ruby-activesupport')
source=(http://gems.rubyforge.org/gems/actionpack-$pkgver.gem)
noextract=(actionpack-$pkgver.gem)
md5sums=('ec68ff468895a72f1663e3c33079da0e')

build() {
  cd $srcdir
  # _gemdir is defined inside build() because if ruby[gems] is not installed on the system
  # makepkg will exit with an error when sourcing the PKGBUILD
  local _gemdir="$(ruby -rubygems -e'puts Gem.default_dir')"

  gem install --ignore-dependencies -i "$pkgdir$_gemdir" actionpack-$pkgver.gem
}
