# Maintainer:  Hyacinthe Cartiaux <hyacinthe.cartiaux@free.fr>
# Contributor: Tianjiao Yin <ytj000+aur@gmail.com>

_plugin_name=video-downloadhelper
_plugin_version=4.9.13
_plugin_id=3006
pkgdesc="Plugin for firefox. The easy way to download and convert Web videos from hundreds of YouTube-like sites."
license=('custom')

pkgname=firefox-extension-$_plugin_name
pkgver=$_plugin_version
pkgrel=1
arch=('any')
url="https://addons.mozilla.org/firefox/addon/$_plugin_id"
depends=("firefox")
source=("https://addons.cdn.mozilla.net/storage/public-staging/${_plugin_id}/video_downloadhelper-${_plugin_version}-sm+fx.xpi")
md5sums=('6bcd1a4392efa98c01428183ac4cbbb2')

build() {
  cd $srcdir
  emid=$(sed -n '/.*<em:id>\(.*\)<\/em:id>.*/{s//\1/p;q}' install.rdf) || return 1
  local dstdir=$pkgdir/usr/lib/firefox/extensions/${emid}
  install -d $dstdir
  rm *.xpi
  mv * $dstdir
}
