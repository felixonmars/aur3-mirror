# Maintainer: Jérôme Launay <jerome@projet-libre.org> 
pkgname=cltwit
pkgver=0.5
pkgrel=3
pkgdesc="Command line twitter utility written in Python."
arch=(any)
url="http://forge.projet-libre.org/projects/cltwit/wiki"
license=('WTFPL')
depends=('python2' 'python2-distribute' 
 'python2-tweepy'
)
optdepends=()
conflicts=()
source=(http://forge.projet-libre.org/attachments/download/10/$pkgname-$pkgver-$pkgrel.tar.gz)
md5sums=('8f664f8180f353327b356085bb338daa')

build() {
  cd $srcdir/$pkgname-${pkgver//_/-}
  python2 setup.py install --root=$pkgdir/ --optimize=1
}
