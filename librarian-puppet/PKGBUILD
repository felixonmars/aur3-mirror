# Maintainer: Jochen Schalanda <jochen+aur AT schalanda.aname>
pkgname=librarian-puppet
pkgver=0.9.0
_gemname='librarian-puppet'
pkgrel=1
pkgdesc=''
arch=(any)
url='http://librarian-puppet.com/'
license=('MIT')
depends=('ruby' 'rubygems' 'ruby-thor>=0.15')
source=(http://rubygems.org/downloads/${_gemname}-${pkgver}.gem)
noextract=(${_gemname}-${pkgver}.gem)
md5sums=('d306068926b84a8c281906860c7f5cda')

build() {
  cd "${srcdir}"
  local _gemdir="$(ruby -rubygems -e'puts Gem.default_dir')"
  
  gem install --no-user-install --ignore-dependencies \
    -i "${pkgdir}${_gemdir}" -n "${pkgdir}/usr/bin" ${_gemname}-${pkgver}.gem
}
