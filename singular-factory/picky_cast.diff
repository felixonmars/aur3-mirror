diff -ru factory-4.0.1.orig/NTLconvert.cc factory-4.0.1/NTLconvert.cc
--- factory-4.0.1.orig/NTLconvert.cc	2015-03-26 21:32:43.000000000 -0400
+++ factory-4.0.1/NTLconvert.cc	2015-03-26 21:45:17.000000000 -0400
@@ -501,7 +501,10 @@
   }
   else
   {
-    long sizeofrep= ((long *) a.rep) [1];
+    //long sizeofrep= ((long *) a.rep) [1];
+    long temp, sizeofrep;
+    memcpy(&temp, a.rep, sizeof(long));
+    sizeofrep = *(&temp + sizeof(long));
     bool lessZero= false;
     if (sizeofrep < 0)
     {
@@ -519,7 +522,8 @@
       cf_stringtemp_l= sizeofrep*sizeof(mp_limb_t)*2;
       cf_stringtemp= (unsigned char*) Alloc (cf_stringtemp_l);
     }
-    int cc= mpn_get_str (cf_stringtemp, 16, (mp_limb_t *) (((long *) (a.rep)) + 2), sizeofrep);
+    //int cc= mpn_get_str (cf_stringtemp, 16, (mp_limb_t *) (((long *) (a.rep)) + 2), sizeofrep);
+    int cc= mpn_get_str (cf_stringtemp, 16, (mp_limb_t *) (&temp + 2), sizeofrep);
 
 
     char* cf_stringtemp2;
