post_install() {
  echo ">>> The 'lejos' binary is known to be broken in this pkgrel, but firmdl, lejosdl,"
  echo ">>> and lejosjc are all working; you will need to upload tmp.bin manually."

  echo ">>> The default tty is /dev/usb/legousbtower0; if this is not what you want, you"
  echo ">>> will need to set the RCXTTY variable." 
}
