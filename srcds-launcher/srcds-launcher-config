#!/bin/sh

#Set default launch options
#############################################################################
game="cstrike" #Specifies which game to start.
ip="" #Specifies the IP when multiple network cards are installed. Required for VAC2 operation.
port="27015" #Specifies which port to open for client connections. Default is 27015.
update=false #The server is searching for updates on startup.
map="de_dust2" #Specifies which map to start.
tickrate="" #Specifies Server-Tickrate.
maxplayers="10" #Specifies how many player slots the server can contain.
secure=true #Enables Valve Anti-Cheat technology.
