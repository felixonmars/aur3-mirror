post_install() {
    echo "WARNING: this is a pre-beta release of Organism, you should"
    echo "consider it as unstable!"
    echo "Some basic plugins are included and enabled by default, but you"
    echo "will have to customize their menu entries in your configuration"
    echo "file (default is ~/.config/organism/organism.conf)."
}
