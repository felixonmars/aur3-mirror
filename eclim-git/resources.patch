--- ant/build.gant
+++ ant/build.gant
@@ -146,7 +146,7 @@ target(name: 'build.vim'){
 
 target(name: 'deploy'){
   depends('deploy.eclipse', 'deploy.vim')
-  mkdir(dir: '${user.home}/.eclim/resources/ext')
+  mkdir(dir: '${vim.files}/eclim/resources/ext')
 }
 
 target(name: 'deploy.eclipse'){
