# Maintainer: Bartlomiej Piotrowski <nospam@bpiotrowski.pl>

pkgname=pkgconf
pkgver=0.8.11
pkgrel=1
pkgdesc='pkg-config compatible utility which does not depend on glib'
url='https://github.com/nenolod/pkgconf'
arch=('i686' 'x86_64')
license=('ISC')
makedepends=('popt')
conflicts=('pkg-config')
provides=('pkg-config')
source=(http://rabbit.dereferenced.org/~nenolod/distfiles/$pkgname-$pkgver.tar.bz2)
sha256sums=('713064d9a12b3567262516109209a6958d230ff76c13b34cadc6e1d26afaa5f5')

build() {                                                                       
    cd "$srcdir"/$pkgname-$pkgver
    ./configure --prefix=/usr \
        --sysconfdir=/etc \
        --mandir=/usr/share/man \
        --infodir=/usr/share/info \
        --localstatedir=/var
    make
}

package() {
    cd "$srcdir"/$pkgname-$pkgver
    make DESTDIR="$pkgdir" install
    ln -s /usr/bin/pkgconf "$pkgdir"/usr/bin/pkg-config
}
