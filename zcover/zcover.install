post_install() {
	echo "  To use the zcover plugin (Z Folders), disable the Folders plugin first"
	echo "  To re-generate thumbnails, please delete folders within the ~/.thumbnails folder"
}

post_upgrade() {
	post_install
}
