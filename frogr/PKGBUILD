# Contributor: uastasi <uastasi_at_archlinux_dot_us>
# Maintainer: ValHue <vhuelamo_at_gmail_dot_com>

pkgname=frogr
pkgver=0.7
pkgrel=2
pkgdesc="A flickr remote organizer for GNOME"
url="http://live.gnome.org/Frogr"
arch=('i686' 'x86_64')
license=('GPL')
depends=('gtk3' 'libexif' 'hicolor-icon-theme' 'desktop-file-utils' 'libsoup-gnome')
makedepends=('intltool' 'gnome-doc-utils')
install='${pkgname}.install'
source=("http://download.gnome.org/sources/${pkgname}/${pkgver}/${pkgname}-${pkgver}.tar.xz")
sha256sums=('29f98e0db06b22cd93df6e764eec2f53d4fe0de1cb45772859fbca8c83a4e1f0')

build() {
    cd "${srcdir}/${pkgname}-${pkgver}"
    ./configure --prefix=/usr
    make
}

package() {
    cd "${srcdir}/${pkgname}-${pkgver}"
    make DESTDIR="${pkgdir}" install
}
