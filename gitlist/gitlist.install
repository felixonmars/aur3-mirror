post_install() {
	echo
  echo "==> You can now configure your (PHP compatible) web server to run GitList."
  echo "==> "
	echo "==> See https://github.com/klaussilveira/gitlist/"
	echo "==> for instructions about configuring GitList."
	echo
}

# vim:set ts=2 sw=2 et:
