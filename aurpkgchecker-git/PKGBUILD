# Mantainer: HariSeldon <gianluigi85@gmail.com>

pkgname=aurpkgchecker-git
pkgver=20121008
pkgrel=1
pkgdesc="A tiny and little program to track your currently installed AUR packages, check for updates and specific flags"
url="https://gitorious.org/aurpkgchecker"
arch=('i686' 'x86_64')
license=('GPL3')
depends=('qt')
makedepends=('git')
install=aurpkgchecker-git.install

_gitroot=git://gitorious.org/aurpkgchecker/aurpkgchecker.git
_gitname=aurpkgchecker

build() {
  cd $srcdir

  msg "Downloading sources..."

  if [ -d $srcdir/$_gitname ] ; then
    cd $_gitname && git pull --rebase
  else
    git clone $_gitroot
  fi

  msg "Compiling..."

  cd aurpkgchecker
  qmake
  make

}

package() {

  cd $srcdir
  cd aurpkgchecker
  install -Dm755 AurPkgChecker $pkgdir/usr/share/aurpkgchecker/aur-pkg-checker
  install -Dm644 APC_icon.png $pkgdir/usr/share/aurpkgchecker/icons/apc_icon.png


}
