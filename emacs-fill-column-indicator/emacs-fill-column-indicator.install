post_install() {
    cat <<EOF
==> Put this in your $HOME/.emacs file to use fill-column-indicator:

(require 'fill-column-indicator)
(add-hook 'auto-fill-mode-hook 'fci-mode)
(add-hook 'refill-mode-hook 'fci-mode)
EOF
}
