## arg 1:  the new package version
post_install() {
  echo "Adding /usr/local/lib to library path";
  ldconfig;

  cat <<EOF

	VAZNO: prati ove korake, inace vjezbe nece dobro raditi.

	Pokreni program 'arh1' u direktoriju gdje zelis da ti se spreme podaci za vjezbu.
	
	Iz tog direktorija cd-aj u atlas/vjezbaX (X je broj vjezbe)
	Pokreni 'xatlas'
EOF

}
