pkgname=nodejs-unstable
pkgver=0.6.1 # its stable, actually
pkgrel=1
pkgdesc='Evented I/O for V8 javascript, unstable version'
arch=('i686' 'x86_64' 'arm')
url='http://nodejs.org/'
license=('MIT')
#depends=('python2' 'v8-current' 'zlib' 'c-ares')
depends=('python2' 'c-ares' 'zlib')
conflicts=('nodejs')
provides=('nodejs=${pkgver}')
checkdepends=('curl') # curl used for check()
optdepends=('openssl: TLS support')
source=("http://nodejs.org/dist/v${pkgver}/node-v${pkgver}.tar.gz")
sha256sums=('b161050ed8cdb2d45f601181d146821e5535a8fcbf5978b2ff064e5476a8e606')

build() {
  cd node-v${pkgver}

  # python2 fix
  for file in $(find . -name '*.py' -print) wscript tools/waf-light tools/node-waf
  do
    sed -i 's_^#!.*/usr/bin/python_#!/usr/bin/python2_' ${file}
    sed -i 's_^#!.*/usr/bin/env.*python_#!/usr/bin/env python2_' ${file}
  done
  sed -i "s|cmd_R = 'python |cmd_R = 'python2 |" wscript
  sed -i "s|python |python2 |" Makefile
  
  #./configure --shared-v8 --shared-cares --shared-zlib --prefix=/usr
  ./configure --shared-cares --shared-zlib --prefix=/usr

  make
}

#check() {
#  cd node-v${pkgver}
#  make test
#}

package() {
  cd node-v${pkgver}
  make DESTDIR=${pkgdir} install
  install -D -m644 LICENSE ${pkgdir}/usr/share/licenses/nodejs/LICENSE
}