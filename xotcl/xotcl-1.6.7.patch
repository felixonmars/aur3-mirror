diff -ru xotcl-1.6.7-orig/configure xotcl-1.6.7/configure
--- xotcl-1.6.7-orig/configure	2011-11-03 13:20:08.000000000 +0100
+++ xotcl-1.6.7/configure	2014-03-31 23:51:02.308389110 +0200
@@ -5221,7 +5221,7 @@
 # FORCE_NO_STUBS is deactivated for now
 if ! test "$with_aol3" = "no"; then
 	echo "Pre-configuring AOL-Server (xotcl)"
-	AOL_DEFINES="-DAOL_SERVER -DUSE_TCL8X -I$with_aol3/include -I$TCL_SRC_DIR/generic"
+	AOL_DEFINES="-DAOL_SERVER -DUSE_TCL8X -I$with_aol3/include -I$TCL_SRC_DIR"
 	FORCE_NO_STUBS=1
 else
 	AOL_DEFINES=""
@@ -5234,7 +5234,7 @@
 
 #--------------------------------------------------------------------
 # check for tclCompile.h (needed, when compiled without full source)
-if test -f "$TCL_SRC_DIR/generic/tclCompile.h"; then
+if test -f "$TCL_SRC_DIR/tclCompile.h"; then
    HAVE_TCL_COMPILE_H="-DHAVE_TCL_COMPILE_H=1"
 else
    HAVE_TCL_COMPILE_H=""
@@ -5499,7 +5499,7 @@
 	    esac
 	    result="Using ${TCL_INCLUDES}"
 	else
-	    if test ! -f "${TCL_SRC_DIR}/generic/tclInt.h" ; then
+	    if test ! -f "${TCL_SRC_DIR}/tclInt.h" ; then
 		as_fn_error $? "Cannot find private header tclInt.h in ${TCL_SRC_DIR}" "$LINENO" 5
 	    fi
 	    result="Using srcdir found in tclConfig.sh: ${TCL_SRC_DIR}"
diff -ru xotcl-1.6.7-orig/library/store/XOTclGdbm/tcl.m4 xotcl-1.6.7/library/store/XOTclGdbm/tcl.m4
--- xotcl-1.6.7-orig/library/store/XOTclGdbm/tcl.m4	2011-11-01 13:24:01.000000000 +0100
+++ xotcl-1.6.7/library/store/XOTclGdbm/tcl.m4	2014-03-31 23:54:26.937759929 +0200
@@ -3668,7 +3668,7 @@
 	    esac
 	    result="Using ${TCL_INCLUDES}"
 	else
-	    if test ! -f "${TCL_SRC_DIR}/generic/tclInt.h" ; then
+	    if test ! -f "${TCL_SRC_DIR}/tclInt.h" ; then
 		AC_MSG_ERROR([Cannot find private header tclInt.h in ${TCL_SRC_DIR}])
 	    fi
 	    result="Using srcdir found in tclConfig.sh: ${TCL_SRC_DIR}"
@@ -3822,7 +3822,7 @@
 	# any *_NATIVE vars be defined in the Makefile
 	TK_INCLUDES="-I${TK_GENERIC_DIR_NATIVE} -I${TK_PLATFORM_DIR_NATIVE}"
 	# Detect and add ttk subdir
-	if test -d "${TK_SRC_DIR}/generic/ttk"; then
+	if test -d "${TK_SRC_DIR}/ttk"; then
 	   TK_INCLUDES="${TK_INCLUDES} -I\"${TK_SRC_DIR_NATIVE}/generic/ttk\""
 	fi
 	if test "${TEA_WINDOWINGSYSTEM}" != "x11"; then
@@ -3846,7 +3846,7 @@
 	    esac
 	    result="Using ${TK_INCLUDES}"
 	else
-	    if test ! -f "${TK_SRC_DIR}/generic/tkInt.h" ; then
+	    if test ! -f "${TK_SRC_DIR}/tkInt.h" ; then
 	       AC_MSG_ERROR([Cannot find private header tkInt.h in ${TK_SRC_DIR}])
 	    fi
 	    result="Using srcdir found in tkConfig.sh: ${TK_SRC_DIR}"
diff -ru xotcl-1.6.7-orig/library/store/XOTclSdbm/tcl.m4 xotcl-1.6.7/library/store/XOTclSdbm/tcl.m4
--- xotcl-1.6.7-orig/library/store/XOTclSdbm/tcl.m4	2011-11-01 13:24:01.000000000 +0100
+++ xotcl-1.6.7/library/store/XOTclSdbm/tcl.m4	2014-03-31 23:54:37.684043909 +0200
@@ -3668,7 +3668,7 @@
 	    esac
 	    result="Using ${TCL_INCLUDES}"
 	else
-	    if test ! -f "${TCL_SRC_DIR}/generic/tclInt.h" ; then
+	    if test ! -f "${TCL_SRC_DIR}/tclInt.h" ; then
 		AC_MSG_ERROR([Cannot find private header tclInt.h in ${TCL_SRC_DIR}])
 	    fi
 	    result="Using srcdir found in tclConfig.sh: ${TCL_SRC_DIR}"
@@ -3822,7 +3822,7 @@
 	# any *_NATIVE vars be defined in the Makefile
 	TK_INCLUDES="-I${TK_GENERIC_DIR_NATIVE} -I${TK_PLATFORM_DIR_NATIVE}"
 	# Detect and add ttk subdir
-	if test -d "${TK_SRC_DIR}/generic/ttk"; then
+	if test -d "${TK_SRC_DIR}/ttk"; then
 	   TK_INCLUDES="${TK_INCLUDES} -I\"${TK_SRC_DIR_NATIVE}/generic/ttk\""
 	fi
 	if test "${TEA_WINDOWINGSYSTEM}" != "x11"; then
@@ -3846,7 +3846,7 @@
 	    esac
 	    result="Using ${TK_INCLUDES}"
 	else
-	    if test ! -f "${TK_SRC_DIR}/generic/tkInt.h" ; then
+	    if test ! -f "${TK_SRC_DIR}/tkInt.h" ; then
 	       AC_MSG_ERROR([Cannot find private header tkInt.h in ${TK_SRC_DIR}])
 	    fi
 	    result="Using srcdir found in tkConfig.sh: ${TK_SRC_DIR}"
diff -ru xotcl-1.6.7-orig/library/xml/TclExpat-1.1/tcl.m4 xotcl-1.6.7/library/xml/TclExpat-1.1/tcl.m4
--- xotcl-1.6.7-orig/library/xml/TclExpat-1.1/tcl.m4	2011-11-01 13:24:01.000000000 +0100
+++ xotcl-1.6.7/library/xml/TclExpat-1.1/tcl.m4	2014-03-31 23:54:18.594723696 +0200
@@ -3668,7 +3668,7 @@
 	    esac
 	    result="Using ${TCL_INCLUDES}"
 	else
-	    if test ! -f "${TCL_SRC_DIR}/generic/tclInt.h" ; then
+	    if test ! -f "${TCL_SRC_DIR}/tclInt.h" ; then
 		AC_MSG_ERROR([Cannot find private header tclInt.h in ${TCL_SRC_DIR}])
 	    fi
 	    result="Using srcdir found in tclConfig.sh: ${TCL_SRC_DIR}"
@@ -3822,7 +3822,7 @@
 	# any *_NATIVE vars be defined in the Makefile
 	TK_INCLUDES="-I${TK_GENERIC_DIR_NATIVE} -I${TK_PLATFORM_DIR_NATIVE}"
 	# Detect and add ttk subdir
-	if test -d "${TK_SRC_DIR}/generic/ttk"; then
+	if test -d "${TK_SRC_DIR}/ttk"; then
 	   TK_INCLUDES="${TK_INCLUDES} -I\"${TK_SRC_DIR_NATIVE}/generic/ttk\""
 	fi
 	if test "${TEA_WINDOWINGSYSTEM}" != "x11"; then
@@ -3846,7 +3846,7 @@
 	    esac
 	    result="Using ${TK_INCLUDES}"
 	else
-	    if test ! -f "${TK_SRC_DIR}/generic/tkInt.h" ; then
+	    if test ! -f "${TK_SRC_DIR}/tkInt.h" ; then
 	       AC_MSG_ERROR([Cannot find private header tkInt.h in ${TK_SRC_DIR}])
 	    fi
 	    result="Using srcdir found in tkConfig.sh: ${TK_SRC_DIR}"
diff -ru xotcl-1.6.7-orig/tcl.m4 xotcl-1.6.7/tcl.m4
--- xotcl-1.6.7-orig/tcl.m4	2011-11-01 13:24:01.000000000 +0100
+++ xotcl-1.6.7/tcl.m4	2014-03-31 23:53:58.315445847 +0200
@@ -3668,7 +3668,7 @@
 	    esac
 	    result="Using ${TCL_INCLUDES}"
 	else
-	    if test ! -f "${TCL_SRC_DIR}/generic/tclInt.h" ; then
+	    if test ! -f "${TCL_SRC_DIR}/tclInt.h" ; then
 		AC_MSG_ERROR([Cannot find private header tclInt.h in ${TCL_SRC_DIR}])
 	    fi
 	    result="Using srcdir found in tclConfig.sh: ${TCL_SRC_DIR}"
@@ -3822,7 +3822,7 @@
 	# any *_NATIVE vars be defined in the Makefile
 	TK_INCLUDES="-I${TK_GENERIC_DIR_NATIVE} -I${TK_PLATFORM_DIR_NATIVE}"
 	# Detect and add ttk subdir
-	if test -d "${TK_SRC_DIR}/generic/ttk"; then
+	if test -d "${TK_SRC_DIR}/ttk"; then
 	   TK_INCLUDES="${TK_INCLUDES} -I\"${TK_SRC_DIR_NATIVE}/generic/ttk\""
 	fi
 	if test "${TEA_WINDOWINGSYSTEM}" != "x11"; then
@@ -3846,7 +3846,7 @@
 	    esac
 	    result="Using ${TK_INCLUDES}"
 	else
-	    if test ! -f "${TK_SRC_DIR}/generic/tkInt.h" ; then
+	    if test ! -f "${TK_SRC_DIR}/tkInt.h" ; then
 	       AC_MSG_ERROR([Cannot find private header tkInt.h in ${TK_SRC_DIR}])
 	    fi
 	    result="Using srcdir found in tkConfig.sh: ${TK_SRC_DIR}"
