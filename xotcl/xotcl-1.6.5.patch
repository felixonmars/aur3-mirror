diff -Naur xotcl-1.6.5-orig/configure xotcl-1.6.5/configure
--- xotcl-1.6.5-orig/configure	2009-10-24 21:17:16.000000000 +0400
+++ xotcl-1.6.5/configure	2009-11-28 11:38:44.000000000 +0300
@@ -7149,7 +7149,7 @@
 	        ;;
 	esac
     else
-	if test ! -f "${TCL_SRC_DIR}/generic/tclInt.h" ; then
+	if test ! -f "${TCL_SRC_DIR}/tclInt.h" ; then
 	    { { echo "$as_me:$LINENO: error: Cannot find private header tclInt.h in ${TCL_SRC_DIR}" >&5
 echo "$as_me: error: Cannot find private header tclInt.h in ${TCL_SRC_DIR}" >&2;}
    { (exit 1); exit 1; }; }
diff -Naur xotcl-1.6.5-orig/library/store/XOTclGdbm/tcl.m4 xotcl-1.6.5/library/store/XOTclGdbm/tcl.m4
--- xotcl-1.6.5-orig/library/store/XOTclGdbm/tcl.m4	2007-10-14 22:05:10.000000000 +0400
+++ xotcl-1.6.5/library/store/XOTclGdbm/tcl.m4	2009-11-28 11:39:09.000000000 +0300
@@ -3413,7 +3413,7 @@
 	        ;;
 	esac
     else
-	if test ! -f "${TCL_SRC_DIR}/generic/tclInt.h" ; then
+	if test ! -f "${TCL_SRC_DIR}/tclInt.h" ; then
 	    AC_MSG_ERROR([Cannot find private header tclInt.h in ${TCL_SRC_DIR}])
 	fi
     fi
diff -Naur xotcl-1.6.5-orig/library/store/XOTclSdbm/tcl.m4 xotcl-1.6.5/library/store/XOTclSdbm/tcl.m4
--- xotcl-1.6.5-orig/library/store/XOTclSdbm/tcl.m4	2007-10-14 22:05:11.000000000 +0400
+++ xotcl-1.6.5/library/store/XOTclSdbm/tcl.m4	2009-11-28 11:38:59.000000000 +0300
@@ -3413,7 +3413,7 @@
 	        ;;
 	esac
     else
-	if test ! -f "${TCL_SRC_DIR}/generic/tclInt.h" ; then
+	if test ! -f "${TCL_SRC_DIR}/tclInt.h" ; then
 	    AC_MSG_ERROR([Cannot find private header tclInt.h in ${TCL_SRC_DIR}])
 	fi
     fi
diff -Naur xotcl-1.6.5-orig/library/xml/TclExpat-1.1/tcl.m4 xotcl-1.6.5/library/xml/TclExpat-1.1/tcl.m4
--- xotcl-1.6.5-orig/library/xml/TclExpat-1.1/tcl.m4	2007-10-14 22:05:11.000000000 +0400
+++ xotcl-1.6.5/library/xml/TclExpat-1.1/tcl.m4	2009-11-28 11:39:16.000000000 +0300
@@ -3413,7 +3413,7 @@
 	        ;;
 	esac
     else
-	if test ! -f "${TCL_SRC_DIR}/generic/tclInt.h" ; then
+	if test ! -f "${TCL_SRC_DIR}/tclInt.h" ; then
 	    AC_MSG_ERROR([Cannot find private header tclInt.h in ${TCL_SRC_DIR}])
 	fi
     fi
diff -Naur xotcl-1.6.5-orig/tcl.m4 xotcl-1.6.5/tcl.m4
--- xotcl-1.6.5-orig/tcl.m4	2008-02-23 20:53:45.000000000 +0300
+++ xotcl-1.6.5/tcl.m4	2009-11-28 11:38:14.000000000 +0300
@@ -3413,7 +3413,7 @@
 	        ;;
 	esac
     else
-	if test ! -f "${TCL_SRC_DIR}/generic/tclInt.h" ; then
+	if test ! -f "${TCL_SRC_DIR}/tclInt.h" ; then
 	    AC_MSG_ERROR([Cannot find private header tclInt.h in ${TCL_SRC_DIR}])
 	fi
     fi
