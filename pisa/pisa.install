# arg 1:  the new package version
post_install() {
  echo ">>> You can also install PIL and pyPDF (for watermarks)"
}

op=$1
shift
[ "$(type -t "$op")" = "function" ] && $op "$@"

# vim:set ts=2 sw=2 et:
