# CPAN Name  : IO-Prompt
# Contributor: Caleb Cushing <xenoterracide@gmail.com>
# Generator  : CPANPLUS::Dist::Arch 1.04
# Template @ http://github.com/xenoterracide/AURpan/blob/master/perl-io-prompt/PKGBUILD.tt
# File bugs @ http://github.com/xenoterracide/AURpan/issues

pkgname='perl-io-prompt'
pkgver='0.997001'
pkgrel='1'
pkgdesc="Interactively prompt for user input"
arch=('any')
url='http://search.cpan.org/dist/IO-Prompt'
license=('PerlArtistic' 'GPL')
depends=('perl' 'perl-termreadkey' 'perl-want')


options=('!emptydirs')

source=('http://search.cpan.org/CPAN/authors/id/D/DC/DCONWAY/IO-Prompt-0.997001.tar.gz')
md5sums=('c1311dd2122cca28426394acf2aed2ba')

build() {
  DIST_DIR="${srcdir}/IO-Prompt-0.997001"
  export PERL_MM_USE_DEFAULT=1 PERL5LIB=""                 \
    PERL_AUTOINSTALL=--skipdeps                            \
    PERL_MM_OPT="INSTALLDIRS=vendor DESTDIR='$pkgdir'"     \
    PERL_MB_OPT="--installdirs vendor --destdir '$pkgdir'" \
    MODULEBUILDRC=/dev/null
  {
	cd "$DIST_DIR" &&
    perl Makefile.PL INSTALLDIRS=vendor &&
    make &&
    make test &&
    make DESTDIR="$pkgdir" install;
  } || return 1;

  find "$pkgdir" -name .packlist -o -name perllocal.pod -delete

}

