# Maintainer: Austin <doorknob60 at gmail dot com>
pkgname=pygtk-shutdown-svn
pkgver=8
pkgrel=1
pkgdesc="Dummy package, replaced by pygtk-shutdown-git"
arch=('any')
url="https://github.com/doorknob60/pygtk-shutdown"
license=('GPL')
depends=('pygtk-shutdown-git')
source=()
build() {
echo "This is a dummy package, it is safe to remove this"  
}