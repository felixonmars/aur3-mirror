# Generated by gem2arch
# Contributor: Rickey Visinski <rickeyvisinski@gmail.com>

_gemname=typhoeus
pkgname=ruby-$_gemname
pkgver=0.2.0
pkgrel=1
pkgdesc="A library for interacting with web services (and building SOAs) at blinding speed."
arch=('i686' 'x86_64')
url="http://github.com/pauldix/typhoeus"
license=('MIT')
depends=('ruby')
makedepends=('rubygems')
source=(http://gems.rubyforge.org/gems/$_gemname-$pkgver.gem)
noextract=($_gemname-$pkgver.gem)
md5sums=('b4b8c00393d153caf00698f3090d4044')

build() {
  cd $srcdir
  local _gemdir="$(ruby -e'puts Gem.default_dir')"
  gem install --ignore-dependencies -i "$pkgdir$_gemdir" $_gemname-$pkgver.gem
}
