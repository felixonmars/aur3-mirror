#!/bin/bash

post_install()
{
  echo "running 'depmod -a' ..."
  depmod -a && echo "done." || echo "failed."
}

post_upgrade()
{
  echo "running 'depmod -a' ..."
  depmod -a && echo "done." || echo "failed."
}
