# Contributor: Justin Davis <jrcd83@gmail.com>
# Generator  : CPANPLUS::Dist::Arch 1.10
pkgname='perl-moosex-has-sugar'
pkgver='0.05055616'
pkgrel='1'
pkgdesc="Sugar Syntax for moose 'has' fields"
arch=('any')
license=('PerlArtistic' 'GPL')
options=('!emptydirs')
depends=('perl-moose' 'perl-moosex-types' 'perl-namespace-autoclean' 'perl-sub-exporter')
makedepends=('perl-test-fatal')
url='http://search.cpan.org/dist/MooseX-Has-Sugar'
source=('http://search.cpan.org/CPAN/authors/id/K/KE/KENTNL/MooseX-Has-Sugar-0.05055616.tar.gz')
md5sums=('31d53c017f929083ac30f01dcd85cece')

build() {
  PERL=/usr/bin/perl
  DIST_DIR="${srcdir}/MooseX-Has-Sugar-0.05055616"
  export PERL_MM_USE_DEFAULT=1 PERL5LIB=""                 \
    PERL_AUTOINSTALL=--skipdeps                            \
    PERL_MM_OPT="INSTALLDIRS=vendor DESTDIR='$pkgdir'"     \
    PERL_MB_OPT="--installdirs vendor --destdir '$pkgdir'" \
    MODULEBUILDRC=/dev/null

  { cd "$DIST_DIR" &&
    $PERL Build.PL &&
    $PERL Build &&
    $PERL Build test &&
    $PERL Build install;
  } || return 1;

  find "$pkgdir" -name .packlist -o -name perllocal.pod -delete
}
