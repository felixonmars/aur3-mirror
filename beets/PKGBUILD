# Maintainer: Adrian Sampson <adrian@radbox.org>

python=python2

pkgname=beets
pkgver=1.0b8
pkgrel=1
pkgdesc="flexible music library manager and tagger"
depends=('python2-munkres' 'python-musicbrainz2' 'mutagen' 'python2-distribute')
arch=('any')
source=(http://pypi.python.org/packages/source/b/beets/beets-$pkgver.tar.gz)
md5sums=(796e99bb53c43bd1539a4a3afcd60dd9)
url="http://beets.radbox.org/"
license="MIT"

build() {
  cd $srcdir/$pkgname-$pkgver
  $python setup.py build || return 1
  $python setup.py install --root=$pkgdir
}
