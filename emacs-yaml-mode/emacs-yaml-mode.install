post_install () {

cat << EOF

==> Add this code to your .emacs file to use the mode:

(require 'yaml-mode)
(setq auto-mode-alist
      (append '(("\\.yml\\'" . yaml-mode))
              auto-mode-alist))

EOF
}

post_upgrade () {
	post_install $1
}

op=$1
shift
$op $*
