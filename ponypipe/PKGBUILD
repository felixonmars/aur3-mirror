# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# See http://wiki.archlinux.org/index.php/VCS_PKGBUILD_Guidelines
# for more information on packaging from GIT sources.

# Maintainer: Mattias Andrée <maandree@operamail.com>
pkgname=ponypipe
pkgver=1.1
pkgrel=4
pkgdesc="Ponify and deponify text"
arch=('any')
url="https://github.com/maandree/ponypipe"
license=('Public Domain')
depends=('java-runtime>=6')
makedepends=('java-environment>=6' 'bash')
provides=('ponypipe')
conflicts=('ponypipe')
source=(https://github.com/downloads/maandree/ponypipe/ponypipe-v1.1-1.tar.gz)
md5sums=(61aebde841e01ae09315c0b4f5d4cb83)

build() {
  cd $srcdir/maandree-$pkgname-*
  make -B
}

package() {
  cd $srcdir/maandree-$pkgname-*
  make DESTDIR="$pkgdir/" install
}
