diff --git a/config b/config
index df4b21b..43f3395 100755
--- a/config
+++ b/config
@@ -712,7 +712,7 @@ debugstatscppflagsval=
 
 echo "*************** Running configure in libarchive ****************"
 (cd $libarchive_dir
- CC=${GCC} ./configure --disable-bsdtar --disable-bsdcpio --without-zlib --without-bz2lib --without-lzmadec --without-lzma  --without-lzo2  --without-nettle --without-openssl --without-xml2 --without-expat --disable-silent-rules --enable-shared=no --enable-static=yes --enable-xattr
+ CC=${GCC} ./configure --disable-bsdtar --disable-bsdcpio --without-zlib --without-bz2lib --without-lzmadec --without-lzma  --without-lzo2  --without-nettle --without-openssl --without-xml2 --without-expat --disable-silent-rules --enable-shared=no --enable-static=yes --enable-xattr --with-pic
  [ $? -ne 0 ] && exit $?
  cp Makefile Makefile.orig
  cat Makefile | sed '
@@ -733,7 +733,7 @@ if [ "x${wavpack_dir}" != "x" ]
 then
 	echo "*************** Running configure in WavPack ****************"
 	(cd $wavpack_dir
-	 CC=${GCC} ./configure --enable-static=yes --enable-shared=no
+	 CC=${GCC} ./configure --enable-static=yes --enable-shared=no --with-pic
 	 if [ $? -ne 0 ]
 	 then
 		exit 1
