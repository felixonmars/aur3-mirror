#!/bin/bash

# Path to this script
PWD=/usr/share/java/basex

# Check: path to BaseX binary
BXPATH=$PWD/basex.jar

# Assign more memory
VM=-Xmx1g

# Run JaxRx
java -cp "$BXPATH:." $VM org.basex.BaseXGUI
