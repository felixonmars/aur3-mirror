# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Contributor: Davide Lo Re <boyska@gmail.com>
pkgname=easygit
pkgver=1.6.5.3
pkgrel=1
pkgdesc="Easy GIT is  wrapper script for git, designed to make git easy to learn and use"
arch=(i686)
url="http://people.gnome.org/~newren/eg/"
license=('GPL')
groups=()
depends=(git perl)
makedepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
source=(http://people.gnome.org/~newren/eg/download/1.6.5.3/eg)
noextract=()
md5sums=('24914c75bec7865feb7b2e896ef46a88') #generate with 'makepkg -g'

build() {
  cd "$srcdir/"

  install -D eg $pkgdir/usr/bin/eg
}

# vim:set ts=2 sw=2 et:
