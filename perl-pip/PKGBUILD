# PKGBUILD generated by pacpan
pkgname=perl-pip
_realname=pip
pkgver=0.13
pkgrel=1
pkgdesc="The Perl Installation Program, for scripted and third-party"
arch=(i686 x86_64)
license=('perl')
url="http://search.cpan.org/~adamk/pip"
options=(!emptydirs)

depends=('perl>=5.10.1' 'perl-uri' 'perl-file-pushd' 'perl-file-which' 'perl-params-util' 'perl-cpan-inject' 'perl-lwp-simple')
makedepends=('perl-test-script' 'perl' 'perl-lwp-online')

#provides=('pip')
provides=('pip=0.13' 'Module::Plan::Archive=0.12' 'perl-module-plan-archive=0.12' 'Module::Plan::Lite=0.12' 'perl-module-plan-lite=0.12' 'Module::Plan::Base=0.12' 'perl-module-plan-base=0.12' 'Module::P5Z=0.12' 'perl-module-p5z=0.12')

source=(http://search.cpan.org/CPAN/authors/id/A/AD/ADAMK/pip-0.13.tar.gz)
md5sums=('f5a47961830cf89934f6c5f0377d8936')

build() {
  _expected_dir="${srcdir}/${_realname}-${pkgver}"
  if [ -d "$_expected_dir" ]; then
    cd "$_expected_dir"
  else
    _expected_dir="${srcdir}/$(bsdtar -t -f $(basename $source) | head -n1)"
    if [ -d "$_expected_dir" ]; then
      cd "$_expected_dir"
    else
      _makefile=$(find $srcdir -iname Makefile.PL)
      if [ ! -z "$_makefile" ]; then
        _expected_dir=$(dirname $_makefile)
        if [ -d "$_expected_dir" ]; then
          cd "$_expected_dir"
        else
          echo "[1;31mERROR[0m unable to detect source directory"
          echo "[1;34m-->[0m this is often due to CPAN's lack of standard naming conventions"
          echo "[1;34m-->[0m it may be possible to fix this by adjusting the build function in the PKGBUILD"
        fi
      fi
    fi
  fi
  # install module in vendor directories.
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1
  make  || return 1
  make install DESTDIR=${pkgdir} || return 1

  # remove perllocal.pod and .packlist
  find ${pkgdir} -name perllocal.pod -delete
  find ${pkgdir} -name .packlist -delete
}

# vim:set ts=2 sw=2 et:

