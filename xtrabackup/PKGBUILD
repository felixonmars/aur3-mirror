# Maintainer: Massimiliano Torromeo <massimiliano.torromeo@gmail.com>

pkgname=xtrabackup
pkgver=1.6.2
pkgrel=2
pkgdesc="A MySQL non-blocking backup solution for InnoDB and XtraDB (Percona Server) data."
arch=("i686" "x86_64")
url="http://www.percona.com/software/percona-xtrabackup/"
license=('GPL')
depends=("perl" "libaio")

#source=("$pkgname-$pkgver-$CARCH.tar.gz::http://www.percona.com/downloads/XtraBackup/xtrabackup-$pkgver/Linux/binary/$CARCH/$pkgname-$pkgver.tar.gz")
source=("http://www.percona.com/redir/downloads/XtraBackup/$pkgname-$pkgver/RPM/rhel6/$CARCH/$pkgname-$pkgver-274.rhel6.$CARCH.rpm")

md5sums=('3cd350384c7cab83192425f2ce375885')
[ "$CARCH" = "x86_64" ] && md5sums=('7e0ae25b63063bd86773293f32718050')

package() {
	cd "$srcdir/usr/bin"
	install -d "$pkgdir/usr/bin"
	install -m0755 tar4ibd innobackupex xtrabackup* "$pkgdir/usr/bin/"
}
