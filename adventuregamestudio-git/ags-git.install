post_install() {
    echo " You will require a 'Windows' version of an AGS game to use this engine"
    echo " To play a game, run:"
	echo " ags /path/to/game.exe"
	echo " "
	echo " This is not a compatibility layer, the game's data is compressed into the executable." 
}
