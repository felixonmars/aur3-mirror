# Maintainer:Bjoern Bidar <theodorstormgrade@gmail.com>
pkgname=ryzom-data
pkgver=1200
pkgrel=3
pkgdesc="Ryzom game data sync"
arch=('any')
url="http://www.ryzom.com/"
license=('CCPL:by-sa')
depends=( 'rsync')
provides=('ryzom-data')
source=( 'ryzom_sync' )
md5sums=(  '459fa38110d3d88906cb9b91e91856e1' )




package() {
  cd "$srcdir"
  mkdir -m 664 -p ${pkgdir}/usr/share/ryzom
  chgrp games ${pkgdir}/usr/share/ryzom
  install -Dm754 -g games ryzom_sync "${pkgdir}/usr/bin"
}
