# Maintainer: sputnick <gilles.quenot <AT> gmail <DOT> com>
# Contributor: sputnick <gilles.quenot <AT> gmail <DOT> com>
pkgname=pjsua
pkgver=1.6
pkgrel=1
pkgdesc="open source command line SIP user agent"
arch=(i686 x86_64)
url="http://www.pjsip.org/pjsua.htm"
provides=pjsua
license=('GPL')
source=(http://www.pjsip.org/release/$pkgver/pjproject-$pkgver.tar.bz2)
md5sums=('8e804c4930a5ee98a7a0ed2619c09b44')
 
build() {
  cd $srcdir/pjproject-$pkgver
  ./configure --prefix=/usr
  make dep || return 1
  make || return 1
  make prefix=$pkgdir/usr install
  install -D -m755 pjsip-apps/bin/pjsua-*gnu $pkgdir/usr/bin/pjsua
}

