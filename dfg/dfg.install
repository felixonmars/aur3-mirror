post_install() {
if [ "$CARCH" = "x86_64" ] ; then
cat << EOM
-------------------------------
 You will need to install the lib32-nvidia-utils package if you have a NVIDIA
 graphics card or lib32-catalyst-utils if you have an ATI graphics card.
-------------------------------
EOM
fi
}
