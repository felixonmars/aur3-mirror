pkgname=lewm
pkgver=0.7
pkgrel=1
pkgdesc="LEt fortune's Wheel help you to choose at the Moment"
url="http://github.com/cdede/lewm/"
arch=('any')
license=('GPL')
depends=('luacrypto' )
source=(https://github.com/cdede/lewm/tarball/${pkgver})
sha1sums=('e6bc9eb18fb356330096f5d047185127f8ae57fa')

build() {
    cd ${srcdir}/*${pkgname}*
    PREFIX=${pkgdir}  ./build.sh install
}
