pkgname=lewm
pkgver=0.72
pkgrel=1
pkgdesc="GPG-based, commandline get keepassx password"
url="http://github.com/cdede/lewm/"
arch=('any')
license=('GPL')
depends=('perl-file-keepass' 'perl-term-readkey')
source=(https://github.com/cdede/lewm/tarball/${pkgver})
sha1sums=('2a1a59c9a6b02f6ce72cde9a38edda0c0f885258')

build() {
    cd ${srcdir}/*${pkgname}*
    mkdir -p $pkgdir/usr/bin/
    cp ./lewm.pl $pkgdir/usr/bin/lewm
    mkdir -p $pkgdir/usr/share/doc/lewm/
    cp ./README.md $pkgdir/usr/share/doc/lewm/
}
