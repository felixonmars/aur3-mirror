post_install()
{
  echo "dictd must be restarted in order to detect the new dictionary:"
  /etc/rc.d/dictd restart
}

post_upgrade()
{
  echo "dictd must be restarted in order to detect the new dictionary:"
  /etc/rc.d/dictd restart
}
