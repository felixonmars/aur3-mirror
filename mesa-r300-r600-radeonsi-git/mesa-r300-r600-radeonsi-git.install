## arg 1:  the new package version
post_install() {
	echo 'This package is built with experimental LLVM shader support'
	echo 'if you want to use it set R600_DEBUG=llvm to enable llvm shaders at runtime'
}

post_upgrade() {
  post_install "$1"
}
