--- src/mathgl-2.1.1/widgets/qt.cpp	2012-12-17 18:45:31.000000000 +0600
+++ src/mathgl-2.1.1/widgets/qt.cpp	2013-01-13 15:18:25.654576999 +0600
@@ -296,7 +296,7 @@
 		}
 		gr->Zoom(x1,y1,x2,y2);	gr->Perspective(per);
 		if(viewYZ)	gr->View(0,phi,tet);
-		else 		gr->View(phi,0,tet);
+        else 		gr->View(phi,tet,0);
 	}
 	mglConvertFromGraph(pic, gr, &grBuf);
 	if(pic.size()!=size())	setSize(pic.width(), pic.height());
