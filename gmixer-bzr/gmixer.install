post_install() {
cat << EOF
Use gmixer to run this, and 'gmixer -d' to run as a daemon
and show the tray icon.  You can put this command in your 
Openbox startup.sh, or Gnome/KDE startup applications.
EOF
}
