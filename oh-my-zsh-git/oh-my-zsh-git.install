post_install () {
  echo "You have to execute 'cp /usr/share/oh-my-zsh/zshrc ~/.zshrc' to use it."
  echo "You may want to add ZSH_CACHE_DIR=\"/tmp/zsh_cache\" to fix permission issues with the cache. "
}

post_remove () {
	echo "Please remove ~/.zshrc to avoid errors."
}

