post_install()
{
    echo
    echo " => If you are unable to connect to your printer change baudrate"
    echo " => in File -> Preferences to 115200 (250000 is known to not work quite often)"
    echo " => and restart Cura afterwards."
    echo
}

post_upgrade()
{
    echo
    echo " => If you are unable to connect to your printer change baudrate"
    echo " => in File -> Preferences to 115200 (250000 is known to not work quite often)"
    echo " => and restart Cura afterwards."
    echo
}
