# Maintainer: Archan Paul <arp@archan.org>
# Reference : http://aur.archlinux.org/packages/eg/egit/PKGBUILD

pkgname=eclipse-egit
pkgver=1.1.0.201109151100
pkgrel=1
pkgdesc="An Eclipse Team provider for the Git version control system"
arch=('any')
url="http://eclipse.org/egit/"
license=('custom:BSD')
depends=('eclipse>=3.6' 'eclipse-mylyn>=3.6')
source=("http://download.eclipse.org/egit/site.zip")
md5sums=('c7e654d44eb6a132cdff30642296726a')
noextract=('site.zip')

build() {
  _dest="$pkgdir/usr/share/eclipse/dropins/$pkgname/eclipse"
  install -d $_dest
  cd $srcdir
  unzip site.zip
  find site -name '*source*' -exec rm {} \;
  mv site/* $_dest
}
