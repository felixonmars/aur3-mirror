# Generated by gem2arch (https://github.com/anatol/gem2arch)
# Maintainer: Balló György <ballogyor@gmail.com>

_gemname=gir_ffi-gtk
pkgname=ruby-$_gemname
pkgver=0.7.1
pkgrel=1
pkgdesc='GirFFI-based Ruby bindings for Gtk+ 2 and 3'
arch=(any)
url='http://www.github.com/mvz/gir_ffi-gtk'
license=(LGPL-2.1)
depends=(ruby ruby-gir_ffi)
options=(!emptydirs)
source=(https://rubygems.org/downloads/$_gemname-$pkgver.gem)
noextract=($_gemname-$pkgver.gem)
sha1sums=('d788c31faab28d3805d6dbc8d640f534432cea9c')

package() {
  local _gemdir="$(ruby -e'puts Gem.default_dir')"
  gem install --ignore-dependencies --no-user-install -i "$pkgdir/$_gemdir" -n "$pkgdir/usr/bin" $_gemname-$pkgver.gem
  rm "$pkgdir/$_gemdir/cache/$_gemname-$pkgver.gem"
  install -D -m644 "$pkgdir/$_gemdir/gems/$_gemname-$pkgver/COPYING.LIB" "$pkgdir/usr/share/licenses/$pkgname/COPYING.LIB"
}
