#!/bin/sh

post_install() {
        echo "You may need to add add those lines to your configs:"
        echo 'sshd_config: AcceptEnv LANG LC_*'
        echo 'ssh_config: SendEnv LANG LC_*'
}

post_upgrade() {
        post_install
}
