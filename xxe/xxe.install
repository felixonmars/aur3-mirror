# arg 1:  the new package version
post_install() {
  echo
  echo "After the install you must relog."
}

# arg 1:  the new package version
# arg 2:  the old package version
post_upgrade() {
  echo
  echo "After the install you must relog."
}

op=$1
shift

$op $*
