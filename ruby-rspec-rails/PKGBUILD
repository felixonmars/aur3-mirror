# Contributor: Emiliano Vavassori <syntaxerrormmm(at)gmail.com>
# Maintainer: Emiliano Vavassori <syntaxerrormmm(at)gmail.com>
pkgname=ruby-rspec-rails
_gemname=rspec-rails
pkgver=2.4.1
pkgrel=1
pkgdesc="RSpec-2 for Rails-3"
arch=(any)
url="http://github.com/rspec/rspec-rails"
license=('MIT')
depends=('ruby' "ruby-rspec>=2.4.0" 'ruby-railties>=3.0' 'ruby-actionpack>=3.0' 'ruby-activesupport>=3.0')
makedepends=('rubygems')
source=(http://rubygems.org/downloads/${_gemname}-${pkgver}.gem
        LICENSE)
noextract=(${_gemname}-${pkgver}.gem)
md5sums=('2145249c67c02c7d758fcfd4399de8e6'
         'f2449e7b92d90148b7eeedb53973de29')

build() {
  cd $srcdir
  local _gemdir="$(ruby -rubygems -e'puts Gem.default_dir')"

  gem install --ignore-dependencies -i "$pkgdir$_gemdir" ${_gemname}-${pkgver}.gem
}

package() {
  install -d -m755 "${pkgdir}/usr/share/licenses/${pkgname}"
  install -m644 "${srcdir}/LICENSE" "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
