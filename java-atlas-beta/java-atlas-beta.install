# arg 1:  the new package version
post_install() {
  cat << EOF

==>   To run, just type "atlas-beta" 
==>   Download maps from Geofabrik:
==>   http://download.geofabrik.de/

EOF
}

# arg 1:  the new package version
# arg 2:  the old package version
post_upgrade() {
  cat << EOF
==>   To run, just type "atlas-beta"
==>   Download maps from Geofabrik:
==>   http://download.geofabrik.de/
EOF
}

