# Maintainer:  anonymous_user <31337h4ck3r at gmail dot com>
# Contributor: Christoph Zeiler <rabyteNOSPAM_at_gmx.dot.org>, TDY <tdy@gmx.com>

pkgname=mount-gtk
pkgver=1.2.0
pkgrel=1
pkgdesc="A front end for udisks and mount"
arch=('i686' 'x86_64')
url="http://mount-gtk.sourceforge.net/"
license=('GPL')
depends=('c++-gtk-utils>=2.0.0' 'glib2>=2.26.0' 'gtk3>=2.99' 'udisks')
makedepends=('pkgconfig>=0.9')
optdepends=('libnotify>=0.4.1: provides error message capabilities')
source=(http://downloads.sourceforge.net/$pkgname/$pkgname-$pkgver.tar.gz)
md5sums=('0681d2ce18811071c0ed204fc4c9f8fb')

build() {
  cd "$srcdir/$pkgname-$pkgver"
  # libnotify currently breaks
  #sed -i 's,define\( HAVE_LIBNOTIFY\) 1,undef\1,' configure
  ./configure --prefix=/usr
  make
}

package() {
  cd "$srcdir/$pkgname-$pkgver"
  make DESTDIR="$pkgdir" install
}
