_MSG_POSTINST="\
+ If DLV complains that libodbc.so.1 is not found, create a symbolic link by running in your shell:
  cd /usr/lib && sudo ln -s ./libodbc.so ./libodbc.so.1"

post_install() {
  echo "${_MSG_POSTINST}"
}
