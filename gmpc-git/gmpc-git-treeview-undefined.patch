--- a/src/Widgets/gmpc-metadata-similarsongs.vala       2012-11-04 13:21:57.699055990 -0500
+++ b/src/Widgets/gmpc-metadata-similarsongs.vala       2012-11-04 13:37:06.798165032 -0500
@@ -152,7 +152,7 @@
             item.activate.connect(replace_clicked);
             menu.append(item);

-            (tree as Gmpc.MpdData.TreeView).right_mouse_integration(menu);
+            //(tree as Gmpc.MpdData.TreeView).right_mouse_integration(menu);
             menu.popup(null, null, null, event.button, event.time);
             menu.show_all();
             return true;
@@ -200,7 +200,7 @@
             if(current != null) return true;
         }
         this.pchild.destroy();
-        if(item != null)
+        if(false)
         {
             var model = new Gmpc.MpdData.Model();
             item.remove_duplicate_songs();
