# Maintainer: Muflone http://www.muflone.com/contacts/english/

pkgname=gptrace
pkgver=0.0.2
pkgrel=1
pkgdesc="Trace the activities of an external application"
url="http://url.muflone.com/gptrace"
arch=('any')
license=('GPL2')
depends=('gtk3' 'gobject-introspection' 'python2-xdg' 'python2-gobject' 'python2-ptrace' 'gtk-update-icon-cache')
source=("${pkgname}-${pkgver}.tar.gz"::"https://github.com/muflone/${pkgname}/archive/${pkgver}.tar.gz")
md5sums=('59952b7a1162a488fbaa6e12c7cb5c42')
sha1sums=('3e595cf9897365113241370185a38fa95ba6b8c1')
sha256sums=('935c2c7f902d302843fbbbe4e6631fa3d2c55c8a86ab1acf4fde56a634c74432')
install="${pkgname}.install"

build() {
  cd "${pkgname}-${pkgver}"
  python2 setup.py build
}

package() {
  cd "${pkgname}-${pkgver}"
  python2 setup.py install --optimize=1 --root "${pkgdir}"
}

