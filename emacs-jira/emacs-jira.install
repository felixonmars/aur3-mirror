# -*- mode: sh; -*-

note () {
    cat << EOF

==> To start using this package, add the lines below to your .emacs:

    (add-to-list 'load-path "/usr/share/emacs/site-lisp/jira.el")
    (require 'jira)
    
EOF

}

post_install() {
    note
}

post_upgrade() {
    note
}
