# Generated by Xyne::Arch::CPAN 0.07

pkgname=perl-test-mockrandom
pkgver=1.0.0
pkgrel=1
pkgdesc="Replaces random number generation with non-random number
generation"
arch=('i686 x86_64')
url="http://search.cpan.org/dist/Test-MockRandom/"
license=('apache')
source=('http://search.cpan.org/CPAN/authors/id/D/DA/DAGOLDEN/Test-MockRandom-1.00.tar.gz')
md5sums=('e19af6bc03b1516a161c96281f5c4c3a')
sha256sums=('630bca40269d04520e39bb6579eb0399684cb17728702336ed1eb1542b7c2f97')
depends=('perl>=5.0.4')
makedepends=('perl-test-simple')
options=(!emptydirs)

build() {
  _dir=$(find $srcdir -maxdepth 2 -type f -name 'Makefile.PL')
  if [ ! -z "$_dir" ]; then
    cd $(dirname "$_dir")
    PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1
    make  || return 1
    make install DESTDIR="${pkgdir}" || return 1

  else
  _dir=$(find $srcdir -maxdepth 2 -type f -name 'Build.PL')
  if [ ! -z "$_dir" ]; then
    cd $(dirname "$_dir")
    PERL_MM_USE_DEFAULT=1 perl Build.PL INSTALLDIRS=vendor || return 1
    ./Build  || return 1
    ./Build install destdir=${pkgdir} || return 1

  else
    echo "error: failed to detect build method for $pkgname"
    echo "you may be able to fix this by editing the PKGBUILD"
    return 1
  fi fi

  # remove perllocal.pod and .packlist
  find ${pkgdir} -name perllocal.pod -delete
  find ${pkgdir} -name .packlist -delete
}

# vim:set ts=2 sw=2 et:

