# Maintainer: Michael Schubert <mschu.dev at gmail>

pkgname=picard-tools
pkgver=1.103
pkgrel=1
pkgdesc="Java-based command-line utilities that manipulate SAM files (supports BAM)"
arch=('any')
url="http://picard.sourceforge.net/"
depends=('java-environment')
license=('GPL')
source=("http://sourceforge.net/projects/picard/files/picard-tools/1.103/picard-tools-$pkgver.zip")
noextract=($pkgname-$pkgver)
md5sums=('930626e41ead20d46adccb0d3e3f5d9e')

package() {
    cd "$srcdir"
    mkdir -p "$pkgdir/usr/share/java"
    unzip -d "$pkgdir/usr/share/java/$pkgname" $pkgname-$pkgver.zip
}

