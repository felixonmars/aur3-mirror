# Contributor: Mladen Pejakovic <pejakm@gmail.com>

pkgname=qx11grab
pkgver=0.4.0
pkgdesc="Qt4 applet for recording x11 windows with ffmpeg"
pkgrel=1
arch=(i686 x86_64)
url="http://qx11grab.hjcms.de/"
license=(GPL)
makedepends=('cmake')
depends=('qt' 'ffmpeg>=20120127' 'alsa-lib' 'oxygen-icons')
options=()
source=(http://qx11grab.hjcms.de/downloads/${pkgver}/${pkgname}-${pkgver}.tar.xz)
md5sums=('2dd0807e8147d11461fcb31d2f65e7e6')

build(){
  cd ${srcdir}/${pkgname}-${pkgver}
  cmake -DCMAKE_INSTALL_PREFIX=/usr \
    -DCMAKE_BUILD_TYPE=Release
  make
}

package(){
  cd ${srcdir}/${pkgname}-${pkgver}
  make DESTDIR="${pkgdir}" install
}
