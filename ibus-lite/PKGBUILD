# Maintainer: justbrowsing <developer4linux+aur@gmail.com>
# Contributer: Felix Yan <felixonmars@gmail.com>

pkgname=ibus-lite
_pkgname=ibus
pkgver=1.5.7
pkgrel=2
pkgdesc="ibus without python3"
arch=('i686' 'x86_64')
url="http://ibus.googlecode.com"
license=('LGPL')
depends=('libibus' 'dconf' 'gtk2' 'libnotify' 'iso-codes')
makedepends=('intltool')
provides=('ibus=1.5.7')
options=('!emptydirs')
install=ibus.install
source=("https://github.com/ibus/ibus/archive/master.zip")
md5sums=('73ebfff05a3f5bfad84c41f2d571ac26')

build() {
  cd "$srcdir/${_pkgname}-master"
  ./autogen.sh \
    --prefix=/usr \
    --libexecdir=/usr/lib/ibus \
    --sysconfdir=/etc \
    --disable-gconf \
    --enable-dconf \
    --enable-wayland \
    --disable-gtk-doc \
    --disable-memconf \
    --enable-ui \
    --enable-python-library \
    --with-python=python2
  make
}

package() {
  cd "$srcdir/${_pkgname}-master"
  make DESTDIR="${pkgdir}" install
  make -C src DESTDIR="${pkgdir}" uninstall
  make -C bindings DESTDIR="${pkgdir}" uninstall
  make DESTDIR="${pkgdir}" uninstall-pkgconfigDATA
}
