#Package Generated by deteego
#Email deteego@gmail.com

pkgname=jhc
pkgver=0.7.6
pkgrel=1
pkgdesc="Haskell compiler aimed at efficiency and optimization."
arch=(i686 x86_64)
url="http://repetae.net/computer/jhc/"
license=("GPL")
depends=(ghc haskell-binary haskell-zlib haskell-utf8-string haskell-readline haskell-fgl haskell-regex-compat)

source=("http://repetae.net/dist/${pkgname}-${pkgver}.tar.gz")
md5sums=('bdb700506257dd6ffc7af1f205fb5023')

build () {
	cd ${srcdir}/${pkgname}-${pkgver}
	./configure --prefix=/usr
	make || return 1
	make install DESTDIR=${pkgdir}
}

