# Fn-F5, Internal/External display toggle
# These events all look like 0000003?, where ? is 0, 1, or 2.
# They rotate among the values with each keypress.
event=hotkey ATKD 0000003.{1} .*
# pass the event on and let the script handle it
action=/etc/acpi/display.sh %e
