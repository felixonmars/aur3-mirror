# Maintainer: Stefan Husmann <stefan-husmann@t-online.de>
pkgname=guile-reader
pkgver=0.6
pkgrel=1 
pkgdesc="A document production system, uses GNU Guile 1.8 as the underlying Scheme implementation."
url="http://www.nongnu.org/$pkgname"
arch=('i686' 'x86_64')
license=('GPL')
depends=('guile')
makedepends=('gperf')
source=(http://download.savannah.gnu.org/releases/$pkgname/$pkgname-$pkgver.tar.gz
  make.sh)
md5sums=('ee7cb5c5552ef7ac26359d08b31a25f3'
         'c523cd82cf1978c23db85d23a0516e1c')
install=guile-reader.install
build() {
  cd $srcdir/$pkgname-$pkgver
  sh $srcdir/make.sh || return 1
  make DESTDIR=$pkgdir install || return 1
  rm $pkgdir/usr/share/info/dir
}