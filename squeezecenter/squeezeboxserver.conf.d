# Configuration file for SqueezebosServer on Arch Linux.
# Save this as /etc/conf.d/squeezeboxserver

# Enter space-delimited options on the following line.
# e.g. SQUEEZECENTER_OPTS="--diag --d_scan --d_command" 
# See "/opt/squeezeboxserver/slimserver.pl --help" for a full list of possible options.
SQUEEZECENTER_OPTS=""
