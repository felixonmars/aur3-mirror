service minbif
{
        type = UNLISTED
        socket_type = stream
        protocol = tcp
        wait = no
        user = minbif
        group = minbif
        server = /usr/bin/minbif
        server_args = /etc/minbif/minbif.conf
        port = 6667
        disable = yes
}
