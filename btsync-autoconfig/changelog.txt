
1.2.0

- Remove unnecessary btsync.conf.doc
- Add license info
- Replace vim modelines with local .vimrc

1.1.0

- Change default storage_path to ~/.btsync
  - These files are application data rather than configuration
- Change default WebGUI port to 7889+$UID
  - Instead of the static 8888, so that the defaults don't collide for multiple
    users
