post_install() {
   cat << EOF
1. Create the config directory:

    mkdir -p ~/.config/dmenu-tools

2. Install the example config:

    install -Dm644  /etc/dmenu-tools/dmenu-tools.conf ~/.config/dmenu-tools/
EOF
}
