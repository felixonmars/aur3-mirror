# Maintainer: Miguel Revilla <yo@miguelrevilla.com>
# Contributor: Miguel Revilla <yo@miguelrevilla.com>

pkgname=libodb-pgsql
pkgver=2.0.1
pkgrel=1
pkgdesc="The ODB PostreSQL runtime library"
arch=('i686' 'x86_64')
depends=('libodb' 'postgresql-libs')
url="http://www.codesynthesis.com/products/odb/"
options=('!libtool')
license=('GPL')
source=("http://www.codesynthesis.com/download/odb/2.0/libodb-pgsql-${pkgver}.tar.bz2")

build() {
  cd "${srcdir}/${pkgname}-${pkgver}"

  ./configure --prefix=/usr
  make ECHO=echo
}

package() {
  cd "${srcdir}/${pkgname}-${pkgver}"

  make ECHO=echo DESTDIR="${pkgdir}" install
}

md5sums=('d8983e72fa7d58ca6b72ea9fe464a773')
