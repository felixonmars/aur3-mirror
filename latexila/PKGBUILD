# Maintainer: Bernd Prünster <bernd.pruenster@gmail.com>
pkgname=latexila
provides=('latexila')
conflicts=('latexila-beta')
pkgver=2.0.3
pkgrel=2
install=latexila.install
pkgdesc="LaTeXila, a LaTeX editor designed for the GNOME desktop"
arch=('i686' 'x86_64')
url="http://latexila.sourceforge.net"
license=("GPL")
depends=('libgee' 'libunique' 'rubber' 'hicolor-icon-theme' 'gsettings-desktop-schemas' 'gtksourceview2')
makedepends=(cmake)
source=("https://github.com/downloads/swilmet/latexila/${pkgname}-${pkgver}.tar.gz")
md5sums=('a128c443795ba0de1fa509a58d429280')

build() {
        cd ${srcdir}/${pkgname}-${pkgver}

        cmake -DCMAKE_INSTALL_PREFIX=/usr || return 1
        make  || return 1
        make DESTDIR="${pkgdir}/" install || return 1


    mkdir -p "$pkgdir"/usr/share/{applications,pixmaps}
    install -Dm644 data/images/app/icon48.png \
            "$pkgdir"/usr/share/pixmaps/$pkgname.png || return 1
    install -m644 data/$pkgname.desktop \
            "$pkgdir"/usr/share/applications/ || return 1
}
