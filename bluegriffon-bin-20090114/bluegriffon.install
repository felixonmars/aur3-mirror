pkgname=bluegriffon-bin

post_install() {
	echo " :: Updating desktop database..."
	update-desktop-database -q
	echo " >>> The next-generation Web Editor based on the "
	echo " >>> rendering engine of Firefox."
}

post_upgrade() {
	echo " :: Updating desktop database..."
	update-desktop-database -q
}

post_remove() {
	echo " :: Updating desktop database..."
	update-desktop-database -q
}
