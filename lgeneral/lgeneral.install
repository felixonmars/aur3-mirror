post_install() {
  echo " > Now you need to install Panzer General data files, for example:"
  echo " >   sudo lgc-pg -s ~/downloads/pg-data -d /usr/share/lgeneral"
}

post_upgrade() {
  post_install
}
