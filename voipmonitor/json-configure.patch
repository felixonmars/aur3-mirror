--- voipmonitor-10.1.7-src/configure	2014-10-17 12:11:53.000000000 +0300
+++ configure	2014-10-20 14:50:39.694433756 +0300
@@ -4165,13 +4165,13 @@
   as_fn_error $? "Your libssh is old. Compile from source https://red.libssh.org/projects/libssh/files (current linux distributions have old library) " "$LINENO" 5
 fi
 
-{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for main in -ljson" >&5
-$as_echo_n "checking for main in -ljson... " >&6; }
+{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for main in -ljson-c" >&5
+$as_echo_n "checking for main in -ljson-c... " >&6; }
 if test "${ac_cv_lib_json_main+set}" = set; then :
   $as_echo_n "(cached) " >&6
 else
   ac_check_lib_save_LIBS=$LIBS
-LIBS="-ljson  $LIBS"
+LIBS="-ljson-c  $LIBS"
 cat confdefs.h - <<_ACEOF >conftest.$ac_ext
 /* end confdefs.h.  */
 
@@ -4200,7 +4200,7 @@
 #define HAVE_LIBJSON 1
 _ACEOF
 
-  LIBS="-ljson $LIBS"
+  LIBS="-ljson-c $LIBS"
 
 else
   as_fn_error $? "Unable to find json-c library. apt-get install libjson0-dev | yum install json-c-devel (RPM from EPEL) | or compile from source: git clone https://github.com/json-c/json-c.git; cd json-c; sh autogen.sh; ./configure; make; make install;ldconfig" "$LINENO" 5
