diff -ur saphire-0.9.5.orig/Makefile.in saphire-0.9.5/Makefile.in
--- saphire-0.9.5.orig/Makefile.in	2010-11-16 18:49:04.000000000 +0900
+++ saphire-0.9.5/Makefile.in	2010-11-17 02:04:37.608102895 +0900
@@ -28,13 +28,14 @@
 docdir=@docdir@saphire
 
 ##########################################################
-all: saphire sash saphire.sao lib lib-install
+all: saphire sash saphire.sao
 	rm -f install
 
 lib: $(CYGWIN_DLL) $(LIBSAPHIRESO1) 
 	rm -f install
 
 lib-install:
+	mkdir -p "$(libdir)"
 	if [ $(LIBSAPHIRESO) = libsaphire.so ]; then if echo $(CFLAGS) | grep -q MDEBUG; then install -m 755 libsaphire.so.1.0 "$(libdir)"; else install -s -m 755 libsaphire.so.1.0 "$(libdir)"; fi; else install -m 755 libsaphire.1.0.dylib "$(libdir)"; fi
 	if [ $(LIBSAPHIRESO) = libsaphire.so ]; then ln -s -f libsaphire.so.1.0 "$(libdir)"/libsaphire.so.1; else ln -s -f libsaphire.1.0.dylib "$(libdir)"/libsaphire.1.dylib; fi
 	if [ $(LIBSAPHIRESO) = libsaphire.so ]; then ln -s -f libsaphire.so.1.0 "$(libdir)"/libsaphire.so; else ln -s -f libsaphire.1.0.dylib "$(libdir)"/libsaphire.dylib; fi
@@ -94,7 +95,6 @@
 install:
 	mkdir -p "$(bindir)"
 	mkdir -p "$(sysconfdir)"
-	mkdir -p "$(libdir)"
 	mkdir -p "$(includedir)"
 	mkdir -p "$(docdir)"
 	install -m 644 saphire_curses.h "$(includedir)"
diff -ur saphire-0.9.5.orig/configure saphire-0.9.5/configure
--- saphire-0.9.5.orig/configure	2010-11-16 18:49:05.000000000 +0900
+++ saphire-0.9.5/configure	2010-11-17 02:04:37.608102895 +0900
@@ -3058,7 +3058,7 @@
 then
     CFLAGS="-I. -I$myprefix/include -L$myprefix/lib"
 else
-    CFLAGS="-I. -I$myprefix/include -L$myprefix/lib -fPIC"
+    CFLAGS="-I. -L. -I$myprefix/include -L$myprefix/lib -fPIC"
 fi
 
 if test -d /usr/local/include
