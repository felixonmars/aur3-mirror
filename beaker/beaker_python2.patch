Only in beaker.new//Client: build
Only in beaker.new//Client: setup.py
Only in beaker.new//Common: setup.py
Only in beaker.new//LabController/proxy: build
Only in beaker.new//LabController/proxy: setup.py
diff -crB beaker/py_rules.mk beaker.new//py_rules.mk
*** beaker/py_rules.mk	2010-03-25 03:10:26.000000000 +0800
--- beaker.new//py_rules.mk	2010-10-19 18:08:09.816667037 +0800
***************
*** 24,39 ****
  	sed -e 's/@VERSION@/$(PKGVERSION)/g' < $< > $@
  
  py-build-ext:	$(_SETUP_PY) $(PY_SOURCES)
! 	python $(SETUP_PY) build_ext -i
  
  py-build:   $(_SETUP_PY) $(PY_SOURCES)
! 	python $(SETUP_PY) build
  
  py-install:	$(_SETUP_PY)
! 	python $(SETUP_PY) install -O1 --skip-build --root $(DESTDIR)
  
  py-clean:	$(_SETUP_PY)
! 	python $(SETUP_PY) clean; \
  	rm -f $(SETUP_PY); \
  	rm -rf build
  
--- 24,39 ----
  	sed -e 's/@VERSION@/$(PKGVERSION)/g' < $< > $@
  
  py-build-ext:	$(_SETUP_PY) $(PY_SOURCES)
! 	python2 $(SETUP_PY) build_ext -i
  
  py-build:   $(_SETUP_PY) $(PY_SOURCES)
! 	python2 $(SETUP_PY) build
  
  py-install:	$(_SETUP_PY)
! 	python2 $(SETUP_PY) install -O1 --skip-build --root $(DESTDIR)
  
  py-clean:	$(_SETUP_PY)
! 	python2 $(SETUP_PY) clean; \
  	rm -f $(SETUP_PY); \
  	rm -rf build
  
Only in beaker.new//Server: build
Only in beaker.new//Server: locale
diff -crB beaker/Server/Makefile beaker.new//Server/Makefile
*** beaker/Server/Makefile	2010-03-25 03:10:26.000000000 +0800
--- beaker.new//Server/Makefile	2010-10-22 13:23:36.796667289 +0800
***************
*** 9,18 ****
  include ../py_rules.mk
  
  build: $(_SETUP_PY)
! 	python $(SETUP_PY) build --install-data=/usr/share/
  
  install: $(_SETUP_PY)
! 	python $(SETUP_PY) install -O1 --skip-build \
              --install-data=/usr/share --root $(DESTDIR)
  
  clean:
--- 9,18 ----
  include ../py_rules.mk
  
  build: $(_SETUP_PY)
! 	python2 $(SETUP_PY) build --install-data=/usr/share/
  
  install: $(_SETUP_PY)
! 	python2 $(SETUP_PY) install -O1 --skip-build \
              --install-data=/usr/share --root $(DESTDIR)
  
  clean:
Only in beaker.new//Server: setup.py
