diff -crB beaker/py_rules.mk beaker_new//py_rules.mk
*** beaker/py_rules.mk	2011-05-05 12:09:09.000000000 +0800
--- beaker_new//py_rules.mk	2011-05-12 13:03:16.000000000 +0800
***************
*** 24,39 ****
  	sed -e 's/@VERSION@/$(PKGVERSION)/g' < $< > $@
  
  py-build-ext:	$(_SETUP_PY) $(PY_SOURCES)
! 	python $(SETUP_PY) build_ext -i
  
  py-build:   $(_SETUP_PY) $(PY_SOURCES)
! 	python $(SETUP_PY) build
  
  py-install:	$(_SETUP_PY)
! 	python $(SETUP_PY) install -O1 --skip-build --root $(DESTDIR)
  
  py-clean:	$(_SETUP_PY)
! 	python $(SETUP_PY) clean; \
  	rm -f $(SETUP_PY); \
  	rm -rf build
  
--- 24,39 ----
  	sed -e 's/@VERSION@/$(PKGVERSION)/g' < $< > $@
  
  py-build-ext:	$(_SETUP_PY) $(PY_SOURCES)
! 	python2 $(SETUP_PY) build_ext -i
  
  py-build:   $(_SETUP_PY) $(PY_SOURCES)
! 	python2 $(SETUP_PY) build
  
  py-install:	$(_SETUP_PY)
! 	python2 $(SETUP_PY) install -O1 --skip-build --root $(DESTDIR)
  
  py-clean:	$(_SETUP_PY)
! 	python2 $(SETUP_PY) clean; \
  	rm -f $(SETUP_PY); \
  	rm -rf build
  
diff -crB beaker/Server/Makefile beaker_new//Server/Makefile
*** beaker/Server/Makefile	2011-05-05 12:09:09.000000000 +0800
--- beaker_new//Server/Makefile	2011-05-12 14:01:06.000000000 +0800
***************
*** 10,19 ****
  
  build: $(_SETUP_PY)
  	env PYTHONPATH=../Common$${PYTHONPATH:+:$$PYTHONPATH} \
! 	    python $(SETUP_PY) build --install-data=/usr/share/
  
  install: $(_SETUP_PY)
! 	python $(SETUP_PY) install -O1 --skip-build \
              --install-data=/usr/share --root $(DESTDIR)
  
  clean: py-clean
--- 10,19 ----
  
  build: $(_SETUP_PY)
  	env PYTHONPATH=../Common$${PYTHONPATH:+:$$PYTHONPATH} \
! 	    python2 $(SETUP_PY) build --install-data=/usr/share/
  
  install: $(_SETUP_PY)
! 	python2 $(SETUP_PY) install -O1 --skip-build \
              --install-data=/usr/share --root $(DESTDIR)
  
  clean: py-clean
Only in beaker_new//Server: Makefile.orig
Only in beaker_new//Server: Makefile.rej
