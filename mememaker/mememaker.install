post_install(){
echo -e "there is a default api code in /etc/imgurrc"
echo -e "to get your own annonymous api code"
echo -e "go to http://imgur.com/register/api_anon"
echo -e "and register mememaker as an api and put"
echo -e "your apicode into /etc/imgurrc"
}
post_upgrade(){
	echo -e "now with split memes!"
}
