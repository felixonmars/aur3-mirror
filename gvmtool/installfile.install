post_install() {
	echo "Use the command \`gvm help\` to get help"
	echo "\`gvm install groovy\` to install groovy"
}
post_remove() {
	echo "This doesn't really uninstall unfortunately. You have to do a manual uninstall."
}
