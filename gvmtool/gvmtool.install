post_install() {
	echo 'Add this to your `rc` file (`~/.zshrc` or `~/.bashrc` or `~/.profile` or `~/.bash_profile` all work)'
	echo ""
	echo "    source \"${HOME}/bin/gvm-init.sh\""
	echo ""
	echo "Help provided at:"
	echo ""
	echo "    gvm help"
	echo ""
}
post_upgrade() {
	post_install
}
