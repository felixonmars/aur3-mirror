post_install() {
  cat <<EOT
Note: /usr/share/php must be added to open_basedir (if enabled) in php.ini
You should also add the path to your external libraries in your IDE!
EOT
}

# vim:set ts=2 sw=2 et:
