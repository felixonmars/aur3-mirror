pkgname=compizconfig-python-dev
pkgver=0.9.5.0
pkgrel=1
pkgdesc="Compizconfig bindings for python"
arch=('i686' 'x86_64')
url="http://compiz.org"
license=('GPL')
provides=('compizconfig-python')
conflicts=('compizconfig-python')
depends=('compiz-core-dev' 'libcompizconfig-dev' 'glib2' 'python2')
makedepends=('intltool' 'pkgconfig' 'cython')
source=("http://releases.compiz.org/$pkgver/compizconfig-python-${pkgver}.tar.bz2")
sha1sums=('014a9134e79c68b05366972f40a24598fbf7b31c')

build()
{
	cd "compizconfig-python-${pkgver}"
	env PKG_CONFIG_PATH="/usr/lib/pkgconfig:$PKG_CONFIG_PATH" \
		python2 setup.py build
	env PKG_CONFIG_PATH="/usr/lib/pkgconfig:$PKG_CONFIG_PATH" \
		python2 setup.py install --prefix="/usr" --root="$pkgdir"
}
