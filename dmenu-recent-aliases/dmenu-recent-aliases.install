post_install() {
    echo "Please make sure to create the following files:"
    echo "~/.cache/dmenu_run"
	echo "~/.dmenu_history"
	echo "~/.zsh_aliases"
    echo "\n Your aliases should be stored in ~/.zsh_aliases"
}
