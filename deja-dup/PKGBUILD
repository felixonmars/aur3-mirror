# Contributor: polslinux <garrett16@hotmail.it>, jijijaco <jijijaco@gmail.com>

pkgname=deja-dup
pkgver=18.0
_pkgmain=18
pkgrel=1
pkgdesc="Déjà  Dup is a simple backup program. It hides the complexity of doing backups the 'right way' (encrypted, off-site, and regular) and uses duplicity as the backend."
arch=('i686' 'x86_64')
url="https://launchpad.net/deja-dup"
license=('GPL')
depends=('duplicity' 'gnome-doc-utils' 'nautilus' 'glib2>=2.26' 'gtk2>=2.22' 'perl-locale-gettext')
source=("http://launchpad.net/${pkgname}/${_pkgmain}/${pkgver}/+download/${pkgname}-${pkgver}.tar.bz2")
md5sums=('607876fcf6d22140e368dfce822d92ba')

build() {
	cd ${srcdir}/deja-dup-${pkgver} 
	./configure --prefix=/usr
	make
	make DESTDIR="${pkgdir}" install
}
