# $Id$
# Maintainer: Heiher <admin@heiher.info>

pkgname=panasonic-hbtn
pkgver=20121125
pkgrel=1
pkgdesc="Panasonic Tablet Button driver"
arch=('i686' 'x86_64')
url="https://github.com/heiher/panasonic-hbtn"
license=('GPL')
depends=('linux')
install=module.install
source=(http://heiher.info/sftp/files/${pkgname}-${pkgver}.tar.gz)
sha1sums=('ebe1a3ea3d08428e53e8811f04a9fd85d6e2b29f')

build() {
  cd ${pkgname}-${pkgver}

  make
  gzip ${pkgname}.ko
}

package() {
  cd ${pkgname}-${pkgver}

  EDIR=${pkgdir}//usr/lib/modules/extramodules-3.6-ARCH
  install -dm755 ${EDIR}
  install -m644 ${pkgname}.ko.gz ${EDIR}/${pkgname}.ko
}
