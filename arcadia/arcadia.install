post_install() {
	# This is needed because makepkd doesn't look at AUR repositories for dependencies
	echo -n "  * Now you have to install coderay with:"
	echo -n "    yaourt -S ruby-coderay"
	echo -n "  (ruby-coderay is a dependency of arcadia)."
}
