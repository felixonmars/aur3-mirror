# Contributor: Massimiliano Torromeo <massimiliano.torromeo@gmail.com>

pkgname=docblox
pkgver=0.13.0
pkgrel=1
pkgdesc="PHP 5.3 compatible API Documentation generator aimed at larger projects and Continuous Integration"
arch=("any")
url="http://docblox-project.org"
license=('CUSTOM')
depends=('php' 'php-xsl')
source=("$pkgname-$pkgver.tar.gz::https://github.com/mvriel/Docblox/tarball/v$pkgver" docblox-bin.patch)

build() {
	cd "${srcdir}/mvriel-Docblox-b46171c"

	install -m0755 -d "$pkgdir/usr/share/pear/$pkgname"
	cp -RH data docs src README "$pkgdir/usr/share/pear/$pkgname"
	install -m0644 -D LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"

	cd bin
	patch -p0 -i "$srcdir/docblox-bin.patch"
	install -m0755 -D docblox.php "$pkgdir/usr/bin/docblox"
}

md5sums=('e4d3519a559f7ae9ab6dd8d907d6744c'
         '4d1671cf3fe6c15162d21e9e6110ebb0')
