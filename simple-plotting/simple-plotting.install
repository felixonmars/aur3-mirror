post_install() {
  cat <<END
  
plot outputs an image as binary stream to stdout.
To view it directly, create a script, e.g.
#!/bin/bash
plot \"$@\" | feh -

This provides the commands plot and pplot.

END
}