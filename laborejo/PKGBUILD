# Maintainer: SpepS <dreamspepser at yahoo dot it>

pkgname=laborejo
pkgver=0.2
pkgrel=1
pkgdesc="Music Notation Workshop"
arch=(any)
url="http://www.$pkgname.org/"
license=('GPL3')
depends=('pyqt')
optdepends=('lilypond: lilypond support'
            'python-pysmf: smf support'
            'jack: JACK output')
source=("https://github.com/nilsgey/Laborejo/tarball/$pkgver"
        "$pkgname.sh")
md5sums=('3f43c096804e9ded91d69ef6591fd27a'
         '48017430ff111cc2f89fda26507c62cf')

build() {
  return 0
}

package() {
  cd "$srcdir/nilsgey-Laborejo-"*

  # data
  install -d "$pkgdir/usr/share/$pkgname"
  cp -a * "$pkgdir/usr/share/$pkgname"

  # launcher
  install -Dm755 ../$pkgname.sh \
    "$pkgdir/usr/bin/$pkgname"
}

# vim:set ts=2 sw=2 et:
