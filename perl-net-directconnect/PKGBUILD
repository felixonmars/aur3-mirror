# Generated by Xyne::Arch::CPAN 0.07

pkgname=perl-net-directconnect
pkgver=0.60.0
pkgrel=1
pkgdesc="CPAN package (no description found)"
arch=('i686' 'x86_64')
url="http://search.cpan.org/dist/Net-DirectConnect/"
license=('unknown')
source=('http://search.cpan.org/CPAN/authors/id/P/PR/PRO/Net-DirectConnect-0.06.tar.gz')
md5sums=('c78a009d46081d24d3451229d6d0ae2c')
sha256sums=('8d5cde174e63ab160c48a77ed6fcbff53db331a2c0af038f97b0f408be3448f6')
depends=('perl-dbd-sqlite' 'perl-dbi' 'perl-io-compress-bzip2' 'perl-mime-base32' 'perl-sys-sendfile' 'perl-time-hires')
makedepends=('perl-extutils-makemaker')
provides=('perl-net-directconnect-tigerhash=0.20.0' 'perl-psconn=4400' 'perl-psmisc=4412' 'perl-pssql=4414' 'perl-psweb=4413' 'perl-statlib')
options=(!emptydirs)

build() {
  _dir=$(find $srcdir -maxdepth 2 -type f -name 'Makefile.PL')
  if [ ! -z "$_dir" ]; then
    cd $(dirname "$_dir")
    PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1
    make  || return 1
    make install DESTDIR="${pkgdir}" || return 1

  else
  _dir=$(find $srcdir -maxdepth 2 -type f -name 'Build.PL')
  if [ ! -z "$_dir" ]; then
    cd $(dirname "$_dir")
    PERL_MM_USE_DEFAULT=1 perl Build.PL INSTALLDIRS=vendor || return 1
    ./Build  || return 1
    ./Build install destdir=${pkgdir} || return 1

  else
    echo "error: failed to detect build method for $pkgname"
    echo "you may be able to fix this by editing the PKGBUILD"
    return 1
  fi fi

  # remove perllocal.pod and .packlist
  find ${pkgdir} -name perllocal.pod -delete
  find ${pkgdir} -name .packlist -delete
}

# vim:set ts=2 sw=2 et:

