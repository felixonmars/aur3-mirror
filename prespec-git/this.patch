--- src/prespec/prespec/unpack/GenericHDT.hpp	2013-01-31 22:52:34.712856192 +0100
+++ nsrc/prespec/prespec/unpack/GenericHDT.hpp	2013-01-31 22:48:57.842866219 +0100
@@ -82,7 +82,7 @@
 
 			data_pointer_ = reinterpret_cast<const DataBits*>(data_ptr);
 			
-			data_ptr += data_size(header_pointer_);
+			data_ptr += this->data_size(header_pointer_);
 
 			if (data_ptr >= data_end) // we expect this not to happen
 			{
@@ -113,14 +113,14 @@
 		size_t size() const
 		{
 			if (header_pointer_ != 0)
-				return data_size(header_pointer_);
+				return this->data_size(header_pointer_);
 				
 			return 0;
 		}
 		int channel(size_t i) const 
 		{
 			assert(data_pointer_ != 0);
-			return data_channel_access(data_pointer_, i);
+			return this->data_channel_access(data_pointer_, i);
 		}
 		int value(size_t i) const 
 		{
