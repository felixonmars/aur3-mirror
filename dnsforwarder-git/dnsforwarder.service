[Unit]
Description=A dnsforwarder designed for anti-spoofing
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/dnsforwarder -e -f /etc/dnsforwarder.conf

[Install]
WantedBy=multi-user.target
