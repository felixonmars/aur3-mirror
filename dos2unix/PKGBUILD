# Maintainer: Renato Garcia <fgar.renatoATgmailDOTcom>
# Contributor: Gerson E. Ruotolo <gersonruotolo@globo.com>

pkgname=dos2unix
pkgver=5.2
pkgrel=2
pkgdesc='Text file format converter'
arch=('i686' 'x86_64')
url='http://www.xs4all.nl/~waterlan/dos2unix.html'
license=('custom')
depends=('glibc')
makedepends=('perl')
conflicts=('hd2u')
source=("http://www.xs4all.nl/~waterlan/${pkgname}/${pkgname}-${pkgver}.tar.gz")
md5sums=('17fa59da80965954205b5d4231652aa1')

build() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  make
}

package() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  make DESTDIR="${pkgdir}" install
  install -D -m644 COPYING.txt "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
