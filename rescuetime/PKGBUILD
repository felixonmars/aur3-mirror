# Maintainer: Limao Luo <luolimao+AUR@gmail.com>

pkgname=rescuetime
pkgver=2.8.5.870
pkgrel=2
pkgdesc="Time-tracking for Linux. Stable version"
arch=(i686 x86_64)
url=https://www.rescuetime.com
license=(GPL2)
depends=(qt4 xorg-xprop xprintidle)
conflicts=($pkgname-beta)
install=$pkgname.install
changelog=$pkgname.changelog

if [ $CARCH = "x86_64" ]
then
    _suffix=amd64
    sha1sums=('bd8efaa3d1beae96d26044db9d66ee38e058b45f')
else
    _suffix=i386
    sha1sums=('b9ced733a68a328e1567581362a8c24986385925')
fi

source=($url/installers/rescuetime_current_$_suffix.deb)

package() {
    bsdtar -xf data.tar.gz
    install -Dm755 usr/bin/$pkgname "$pkgdir"/usr/bin/$pkgname
}
