# Maintainer: Limao Luo <luolimao+AUR@gmail.com>

pkgname=rescuetime
pkgver=2.8.6.1001
pkgrel=1
pkgdesc="Application time-tracking for Linux. Stable version"
arch=(i686 x86_64)
url=https://www.rescuetime.com
license=(GPL2)
depends=(qt4 xorg-xprop xprintidle)
conflicts=($pkgname-beta)
install=$pkgname.install
changelog=$pkgname.changelog

if [[ $CARCH == "x86_64" ]]
then
    _suffix=amd64
    sha1sums=('963aa63c835c6ce300e0998b1ca65aa85a3a1782')
else
    _suffix=i386
    sha1sums=('94a3af421a3e225e20d24e74e8a4f037aea88671')
fi

source=($url/installers/rescuetime_current_$_suffix.deb)

package() {
    bsdtar -xf data.tar.gz
    install -Dm755 usr/bin/$pkgname "$pkgdir"/usr/bin/$pkgname
}
