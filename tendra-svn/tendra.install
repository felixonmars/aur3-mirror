post_install() {
   echo "installed on /opt/tendra, be careful"
   echo "when adding it to the PATH because the"
   echo "name conflicts with Tiny C Compiler as"
   echo "both use tcc as compiler name"
}
