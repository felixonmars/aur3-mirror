# Contributor: Vincent Delft  <vincent_delft ][ yahoo.com>
arch=('i686')
pkgname=pyevent
pkgver='0.3'
pkgrel=1
pkgdesc="python interface to libevent"
url="http://code.google.com/p/pyevent/"
license="BSD"
depends=('pyrex' 'libevent')
source=('http://pyevent.googlecode.com/files/pyevent-0.3.tar.gz')

md5sums=('584912c92d08bf005283fb29a47a6e4d')

build() {
  cd $startdir/src/$pkgname-$pkgver
  #we just remove he generated .c files
  make distclean
  #we generate the .c files and compile it
  make || return 1
  python setup.py install --root=$startdir/pkg || return 1
}
