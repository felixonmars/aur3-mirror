post_install() {
	cat <<- EOF
		:: If you can't get the software to work
		:: add the user to the video group:
		::
		:: # gpasswd -a USER video
		::
		:: Then try again.
	EOF
}

post_upgrade() {
	post_install $1
}
