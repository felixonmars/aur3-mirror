pkgname=giplet

post_install() {
        echo ">>> It is safe to remove ~/.giplet folder from 0.16 version."
}

post_upgrade() {
        echo ">>> It is safe to remove ~/.giplet folder from 0.16 version."
}

post_remove() {
      post_install
}
