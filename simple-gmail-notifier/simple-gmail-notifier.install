post_install() {
    echo "Settings file will be created after first launching. Make sure only you have read access on ~/.config/simple-gmail-notifier/notifier.conf with chmod 600"
}

post_upgrade() {
    echo "Make sure only you have read access on ~/.config/simple-gmail-notifier/notifier.conf with chmod 600"
}
