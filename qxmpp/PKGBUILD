# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: Samir Faci <csgeek@esamir.com> 
pkgname=qxmpp
pkgver=0.3.0
pkgrel=1
pkgdesc="QXmpp is a cross-platform C++ XMPP client library. It is based on Qt."
arch=('i686'  'x86_64')
url="http://code.google.com/p/qxmpp/"
license=('LGPL')
groups=()
depends=('qt')
makedepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
source=(http://qxmpp.googlecode.com/files/$pkgname-$pkgver.tar.gz)
noextract=()
md5sums=('46a54de2fd6d77af22716d832211d3bb')


build() {
  cd "$srcdir/$pkgname-$pkgver"

  qmake 
  make
}

package() {
  cd "$srcdir/$pkgname-$pkgver"

  make INSTALL_ROOT="$pkgdir/" install
}

