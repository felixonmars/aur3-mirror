# Maintainer:   Lucky <archlinux@builds.lucky.li>
# Contributor:  Farhad Shahbazi <farhad@enthusiasm.cc>
#
# Merge stuff from: puddletag-hg
# (https://aur.archlinux.org/packages.php?ID=56050)

pkgname=puddletag
pkgver=1.0.0beta6
pkgrel=1
pkgdesc="An audio tag editor for GNU/Linux (like Mp3tag)"
url="http://puddletag.sourceforge.net"
license=("GPL")
arch=("i686" "x86_64")
depends=("python-configobj>=4.5.0" "python-musicbrainz2>=0.6.0" "python2>=2.5" "python2-pyqt>=4.5" "python2-pyparsing>=1.5.1" "mutagen>=1.20")
optdepends=("python-imaging: edit/view FLAC cover art"
            "quodlibet: edit a QuodLibet library")
source=("http://downloads.sourceforge.net/project/${pkgname}/${pkgname}-${pkgver}.tar.gz")
md5sums=("91b269a86f2277121f49a5800c481ab3")

build() {
  cd "${srcdir}/${pkgname}-${pkgver}"

  python2 setup.py config
}

package() {
  cd "${srcdir}/${pkgname}-${pkgver}"

  python2 setup.py install --prefix="${pkgdir}/usr" --optimize=1
}
