--- moblin-cursor-theme-build/Makefile	2009-08-09 10:41:01.952075672 +0100
+++ moblin-cursor-theme-build/Makefile	2009-08-09 10:42:46.776076319 +0100
@@ -5,11 +5,11 @@
 
 dist:
 	cd pngs && ./make.sh
-	mkdir -p $(PACKAGE)-$(VERSION)
-	mv xcursors $(PACKAGE)-$(VERSION)	
-	cp COPYING AUTHORS $(PACKAGE)-$(VERSION)
-	tar cjvf $(PACKAGE)-$(VERSION).tar.bz2 $(PACKAGE)-$(VERSION)
-	rm -rf $(PACKAGE)-$(VERSION)
+	mkdir -p $(PACKAGE)
+	mv xcursors $(PACKAGE)	
+	cp COPYING AUTHORS $(PACKAGE)
+	tar cjvf $(PACKAGE).tar.bz2 $(PACKAGE)
+	rm -rf $(PACKAGE)
 
 .PHONY: all dist
 
