# Contributor: Piotr Rogoża <rogoza.piotr@gmail.com>
# Created: 20/09/2010
pkgname=ogmrip-dirac
pkgver=0.1
pkgrel=1
pkgdesc='ogmrip-dirac is a plugin for OGMRip which enables transcoding to dirac'
arch=('i686' 'x86_64')
url='http://ogmrip.sourceforge.net/en/plugins.html'
license=('LGPL')
groups=('ogmrip')
depends=('ogmrip>=0.13' 'dirac>=0.9')
makedepends=('intltool>=0.35')
source=("http://downloads.sourceforge.net/sourceforge/ogmrip/${pkgname}-${pkgver}.tar.gz"
"http://downloads.sourceforge.net/project/ogmrip/${pkgname}/${pkgver}/README")

build() {
  cd "$srcdir/$pkgname-$pkgver"

  ./configure --prefix=/usr
  make || return 1
  make DESTDIR="$pkgdir/" install
  install -Dm0644 README ${pkgdir}/usr/share/ogmrip-dirac/README
}

# vim:set ts=2 sw=2 et ft=sh:

md5sums=('bf7ef6bb5ee8ae964b45dff1ebdc217a'
         'f0e57c95c50814cf7594dc128ba074d6')
