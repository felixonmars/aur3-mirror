--- a/usr/lib/python3.4/site-packages/system76driver/actions.py	2013-10-23 15:04:17.000000000 -0400
+++ b/usr/lib/python3.4/site-packages/system76driver/actions.py	2014-05-04 01:02:29.681808406 -0400
@@ -239,7 +239,11 @@
                 yield line
 
     def get_isneeded(self):
-        return self.get_cmdline() != self.cmdline
+        if path.isfile(self.filename):
+            return self.get_cmdline() != self.cmdline
+        else:
+            print('Grub bootloader is not detected! Manually update your bootloader with following option(s): %s' % ' '.join(self.extra))
+            return False
 
     def perform(self):
         content = '\n'.join(self.iter_lines())
