# Maintainer: Vladimir Chizhov <jagoterr@gmail.com>
# Contributor: Henry Tang <henryykt@gmail.com>

pkgname=phantomjs
pkgver=1.5.0
pkgrel=1
pkgdesc="Headless WebKit with JavaScript API"
url="http://www.phantomjs.org/"
license=("BSD")
arch=('i686' 'x86_64')
depends=()
makedepends=()
conflicts=()
replaces=()
backup=()
install=
source=("http://phantomjs.googlecode.com/files/${pkgname}-${pkgver}-source.tar.gz"
        "package.patch")
md5sums=('e70857923c1992da662434e8c0f7215f'
         '434abee85912e61f29196fda1f485c16')

build() {
  cd $srcdir/$pkgname-$pkgver
  # qmake && make || return 1
  ./build.sh
}

package() {
  mkdir -p $pkgdir/usr/bin/
  install -D -m755 $srcdir/$pkgname-$pkgver/bin/phantomjs $pkgdir/usr/bin/
}

