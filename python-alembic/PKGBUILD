# Maintainer: Oliver Mangold <o.mangold at gmail dot com>
pkgname=python-alembic
pkgver=0.5.0
pkgrel=1
pkgdesc="Lightweight dababase migration tool for usage with SQLAlchemy"
arch=('any')
license=('MIT')
url=('https://bitbucket.org/zzzeek/alembic')
makedepends=('python-pip' 'python-distribute')
source=('https://bitbucket.org/zzzeek/alembic/raw/5c05656a0f813f60c921669796656b8003b59d59/LICENSE')
md5sums=('c0f69f012e89999aaf67409a363ef026')
depends=('python')
provides=("${pkgname}")

package() {
  cd "${srcdir}"
  pip install --root "${pkgdir}" --src "${srcdir}/src" --no-deps -I "alembic"=="${pkgver}"
  install -m 755 -d "${pkgdir}/usr/share/licenses/${pkgname}"
  install -m 755 "${srcdir}/LICENSE" "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
