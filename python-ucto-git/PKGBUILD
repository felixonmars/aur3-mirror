_pkgname=python-ucto
pkgname=${_pkgname}-git
pkgver=9
pkgrel=2
pkgdesc='Python binding for Ucto, an advanced tokenizer (for NLP)'
arch=('i686' 'x86_64')
url="https://github.com/proycon/python-ucto"
license=('GPL')
depends=('python' 'ucto')
makedepends=('git' 'cython')
provides=("${_pkgname}")
source=("git://github.com/proycon/python-ucto.git")
_gitname=("python-ucto")
md5sums=('SKIP')

pkgver() {
    cd "$srcdir/$_gitname"
    git rev-list --count HEAD
}

build() {
    cd "$srcdir/$_gitname"
    python setup3.py build_ext build 
}

package() {
    cd "$srcdir/$_gitname"
    python setup3.py install --prefix=/usr --root="${pkgdir}"
}

