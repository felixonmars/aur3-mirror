# Contributor: Justin Davis <jrcd83@gmail.com>
# Generator  : CPANPLUS::Dist::Arch 1.06
pkgname='perl-test-exception-lessclever'
pkgver='0.005'
pkgrel='1'
pkgdesc="Test::Exception simplified"
arch=('any')
license=('PerlArtistic' 'GPL')
options=('!emptydirs')
depends=('perl' 'perl-object-quick>=0.012')
url='http://search.cpan.org/dist/Test-Exception-LessClever'
source=('http://search.cpan.org/CPAN/authors/id/E/EX/EXODIST/Test-Exception-LessClever-0.005.tar.gz')
md5sums=('5d573594d6adf90ac663408e4d0e38eb')

build() {
  PERL=/usr/bin/perl
  DIST_DIR="${srcdir}/Test-Exception-LessClever-0.005"
  export PERL_MM_USE_DEFAULT=1 PERL5LIB=""                 \
    PERL_AUTOINSTALL=--skipdeps                            \
    PERL_MM_OPT="INSTALLDIRS=vendor DESTDIR='$pkgdir'"     \
    PERL_MB_OPT="--installdirs vendor --destdir '$pkgdir'" \
    MODULEBUILDRC=/dev/null

  { cd "$DIST_DIR" &&
    $PERL Build.PL &&
    $PERL Build &&
    $PERL Build test &&
    $PERL Build install;
  } || return 1;

  find "$pkgdir" -name .packlist -o -name perllocal.pod -delete
}
