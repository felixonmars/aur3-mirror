# Mantainer: Diego
# Contributor: Aleessio Sergi

pkgname=gtk-theme-numix
pkgver=1
pkgrel=3
pkgdesc="A flat and light theme with a modern look"
arch=('any')
url="http://satya164.deviantart.com/art/Numix-GTK3-theme-360223962"
license=('GPL3')
depends=('gtk-engine-murrine')
source=(http://www.deviantart.com/download/360223962/numix___gtk3_theme_by_satya164-d5ygul6.zip)
sha1sums=('714278650cf5a5dcc436714478851e09e0c79b0d')

package() {
  cd Numix

  # remove unneeded files
  rm -f .gitignore LICENSE README

  # create theme dir
  install -d -m 755 "$pkgdir/usr/share/themes/Numix"

  # install theme
  find . -type f -exec \
    install -D -m 644 '{}' "$pkgdir/usr/share/themes/Numix/{}" \;
}
