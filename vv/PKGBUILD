# Maintainer: Ã#lie Bouttier <cube.elie@gmail.com>
pkgname=vv
pkgver=20120927
pkgrel=3
pkgdesc="VV fast and simple visualization of spatio-temporal images: 2D, 2D+t, 3D and 3D+t (or 4D) images."
arch=('i686' 'x86_64')
license=('BSD' 'CeCILL-B')
url="http://www.creatis.insa-lyon.fr/rio/vv/"
depends=('insight-toolkit>=3.20' 'vtk>=5.6' 'qt>=4.4' 'gengetopt>=2.20')
makedepends=('git' 'cmake>=2.8')
_gitroot='git://git.creatis.insa-lyon.fr/clitk'
_gitname='vv'

build() {
  cd "$srcdir"
  msg "Connecting to GIT server...."
  if [ -d $_gitname ]; then
    cd $_gitname
    git checkout master
    git pull
  else
    git clone $_gitroot $_gitname
    cd $_gitname
  fi
  git checkout '4454c06e212fb6af58d206f2d6f9b76778858967'
  msg "Git checkout done."
  mkdir -p _build
  cd _build
  cmake ..
  make
}

package() {
  cd $srcdir/vv/_build
  make install
}
