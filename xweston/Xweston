#!/bin/sh

# Xweston - launch Xwayland hosted by weston

if [ -z "$DISPLAY" ]; then
	launcher=weston-launch --
else
	launcher=weston
fi

export XWAYLAND_OPTS=$(mktemp)
echo "$*" >> $XWAYLAND_OPTS
exec $launcher $WESTON_OPTS
