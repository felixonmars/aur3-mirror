--- window.py.orig	2010-05-13 00:14:31.325681984 +0900
+++ window.py	2010-05-13 00:16:19.751514947 +0900
@@ -161,7 +161,7 @@
         atexit.register(self.signal_handler)
         signal.signal(15, self.signal_handler)
         # Read the configuration from oxc.conf file
-        self.config = ConfigObj("oxc.conf") 
+        self.config = ConfigObj( os.environ['HOME'] + "/.openxencenter/oxc.conf" )
         # Read from configuration saved servers
         if self.config['servers']['hosts']:
             self.config_hosts = self.config['servers']['hosts']
--- oxcSERVER.py.orig	2010-05-13 02:16:52.691616423 +0900
+++ oxcSERVER.py	2010-05-13 02:27:17.518219334 +0900
@@ -720,15 +720,17 @@
                         and name[:6] != "pif___":
                             ds[name[:3]].append([name, desc])
         if host:
-            if os.path.exists("host_rrds.rrd"):
-                os.unlink("host_rrds.rrd")
-            urllib.urlretrieve("http://%s/host_rrds?session_id=%s" % (ip, self.session_uuid), "host_rrds.rrd")
-            rrd = RRD("host_rrds.rrd")
+            host_rrds = os.environ['HOME'] + "/.openxencenter/host_rrds.rrd"
+            if os.path.exists(host_rrds):
+                os.unlink(host_rrds)
+            urllib.urlretrieve("http://%s/host_rrds?session_id=%s" % (ip, self.session_uuid), host_rrds)
+            rrd = RRD(host_rrds)
         else:
-            if os.path.exists("vm_rrds.rrd"):
-                os.unlink("vm_rrds.rrd")
-            urllib.urlretrieve("http://%s/vm_rrds?session_id=%s&uuid=%s" % (ip, self.session_uuid, uuid), "vm_rrds.rrd")
-            rrd = RRD("vm_rrds.rrd")
+            vm_rrds = os.environ['HOME'] + "/.openxencenter/vm_rrds.rrd"
+            if os.path.exists(vm_rrds):
+                os.unlink(vm_rrds)
+            urllib.urlretrieve("http://%s/vm_rrds?session_id=%s&uuid=%s" % (ip, self.session_uuid, uuid), vm_rrds)
+            rrd = RRD(vm_rrds)
         rrdinfo = rrd.get_data(period)
 
         def show_tic(value):
@@ -874,10 +876,11 @@
         
         time.sleep(5)
         while not self.halt_performance:
-            if os.path.exists("update.rrd"):
-                os.unlink("update.rrd")
-            urllib.urlretrieve("http://%s/rrd_updates?session_id=%s&start=%s&cf=AVERAGE&interval=5&vm_uuid=%s" % (ip, self.session_uuid, int(time.time())-10, uuid), "update.rrd")
-            rrd = XPORT("update.rrd")
+            update_rrd = os.environ['HOME'] + "/.openxencenter/update.rrd"
+            if os.path.exists(update_rrd):
+                os.unlink(update_rrd)
+            urllib.urlretrieve("http://%s/rrd_updates?session_id=%s&start=%s&cf=AVERAGE&interval=5&vm_uuid=%s" % (ip, self.session_uuid, int(time.time())-10, uuid), update_rrd)
+            rrd = XPORT(update_rrd)
             rrdinfo = rrd.get_data()
             
             for key in rrdinfo:
