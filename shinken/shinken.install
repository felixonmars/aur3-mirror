pre_install() {
	useradd -MUd /var/lib/shinken -s /bin/false shinken > /dev/null 2>&1
	usermod -L shinken
}

post_install() {
	chown -R shinken:shinken /var/lib/shinken
}

post_upgrade() {
	chown -R shinken:shinken /var/lib/shinken
}

post_remove() {
	userdel shinken
	groupdel shinken
}
