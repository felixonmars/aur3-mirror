# Maintainer: Max Roder <maxroder@web.de>
# Contributor: Nathan Owe <ndowens.aur at gmail dot com>

pkgname='libhx'
pkgver='3.12'
pkgrel='2'
pkgdesc='A library providing queue, tree, I/O and utility functions'
arch=('i686' 'x86_64')
url=('http://libhx.sourceforge.net/')
license=('GPL')
depends=('glibc')
options=('!libtool' '!docs')
source=("http://downloads.sourceforge.net/${pkgname}/libHX-${pkgver}.tar.xz")
sha256sums=('68287508eda091d822f2599aadab4eb362f8be4349d471b7c39ac209fcaecb4e')

build()
{
  cd ${srcdir}/libHX-${pkgver}

  ./configure --prefix=/usr
  make
}

package()
{
  cd ${srcdir}/libHX-${pkgver}
	
  make DESTDIR=${pkgdir} install
}

# vim:set ts=2 sw=2 et:
