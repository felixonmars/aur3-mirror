# Contributor: graysky <graysky AT archlinux dot us>
# Contributor: Dan Vratil <vratil@progdansoft.com>

pkgname=nvidia-beta-ck
pkgver=260.19.21
pkgrel=1
pkgdesc="NVIDIA beta drivers for kernel26-ck."
arch=('i686' 'x86_64')
_kernver=2.6.36-ck
ARCH=x86_64 && _srcname=NVIDIA-Linux-x86_64-${pkgver}-no-compat32 && md5sums=('9db612fb5679249627d52bfff0e16f4f')
[ "$CARCH" = "i686" ] && ARCH=x86 && _srcname=NVIDIA-Linux-x86-${pkgver} && md5sums=('2ec917f51e752802646920e9bcd747e7')
provides=("nvidia-beta-ck=${pkgver}")
url="http://www.nvidia.com/"
depends=('kernel26-ck-headers' "nvidia-utils-beta=${pkgver}")
conflicts=('nvidia-ck')
license=('custom')
install=nvidia.install
source=("ftp://download.nvidia.com/XFree86/Linux-$ARCH/${pkgver}/${_srcname}.run")

build()
{
  # Extract the nvidia drivers
  cd "${srcdir}"
  if [ -d ${_srcname} ]; then
    rm -rf ${_srcname}
  fi
  sh ${_srcname}.run --extract-only
  cd ${_srcname}/kernel
  make SYSSRC=/usr/src/linux-${_kernver} module
}

package() {
  cd "${srcdir}/${_srcname}/kernel"

  # Install kernel module
  mkdir -p "${pkgdir}/lib/modules/${_kernver}/kernel/drivers/video/"
  install -m644 nvidia.ko "${pkgdir}/lib/modules/${_kernver}/kernel/drivers/video/"
}
