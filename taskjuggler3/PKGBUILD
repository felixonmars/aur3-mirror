# Generated by gem2arch
# Contributor: Mathieu Clabaut <mathieu.clabaut@gmail.com>

_gemname=taskjuggler
pkgname=${_gemname}3
pkgver=0.0.8
pkgrel=2
pkgdesc="Project Management Software"
arch=('i686' 'x86_64')
url="http://www.taskjuggler.org"
license=('GPL')
depends=('ruby' 'ruby-mail>=2.1.3' 'ruby-open4>=1.0.1')
makedepends=('rubygems')
source=(http://gems.rubyforge.org/gems/$_gemname-$pkgver.gem)
noextract=($_gemname-$pkgver.gem)
md5sums=('9398b9ac04e9f3b1c06d842839ae9444')

build() {
  cd $srcdir
  local _gemdir="$(ruby -e'puts Gem.default_dir')"
  gem install --ignore-dependencies -i "$pkgdir$_gemdir" $_gemname-$pkgver.gem
  cd $pkgdir 
  mkdir -p usr/bin
  cd usr/bin
  ln -s ../lib/ruby/gems/*/bin/* .
  sed -i -e 's/<mail>, \["=/<mail>, \[">=/' -e 's/<open4>, \["=/<open4>, \[">=/'  ../lib/ruby/gems/*/specifications/taskjuggler-*.gemspec
  
}
