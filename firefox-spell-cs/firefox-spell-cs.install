# arg 1:  the new package version
post_install() {
cat << EOM
==> To enable czech spell check, write "about:config" to the address bar
==> in Firefox, and change the value "spellchecker.dictionary" either
==> to "cs" or to "cs-ascii" (ascii doesn't contain accents).
EOM
}
