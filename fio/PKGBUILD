# Maintainer: John Williams <jwilliams4200 liamg reverse&remove moc>
pkgname=fio
pkgstr=2.0.13
pkgver=2.0.13
pkgrel=1
pkgdesc="scriptable I/O tool for storage benchmarks and drive testing"
arch=('x86_64' 'i686')
url="http://freshmeat.net/projects/fio"
license=('GPL2')
depends=('libaio' 'bash')
source=("http://brick.kernel.dk/snaps/fio-${pkgstr}.tar.bz2")

build() {
    cd $srcdir/fio-${pkgstr}
    make
}

package() {
    cd $srcdir/fio-${pkgstr}
    make DESTDIR="${pkgdir}" prefix="/usr" mandir="/usr/share/man" install
}
sha256sums=('c4ba9aceda6b3fd2335e8579ee653a3d94735e143de498e61f4168c413de809a')
