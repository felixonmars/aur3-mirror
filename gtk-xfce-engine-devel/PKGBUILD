# Maintainer: Xavier Devlamynck <magicrhesus@ouranos.be>
# Contributor: twa022 <twa022@gmail.com>

pkgname=gtk-xfce-engine-devel
pkgver=2.8.1
pkgrel=1
pkgdesc="A port of Xfce engine to GTK+-2.0 (Development Version)"
arch=('i686' 'x86_64')
license=('GPL2')
url="http://www.xfce.org/"
groups=('xfce4-devel')
depends=("gtk2>=2.14.7")
makedepends=('pkgconfig')
options=('!libtool')
conflicts=('gtk-xfce-engine')
source=(http://archive.xfce.org/src/xfce/gtk-xfce-engine/2.8/gtk-xfce-engine-${pkgver}.tar.bz2)
md5sums=('8a6527b61b0554cda11d06f66a567314')

build() {
  cd ${srcdir}/gtk-xfce-engine-${pkgver}
  ./configure --prefix=/usr \
	--sysconfdir=/etc \
	--libexecdir=/usr/lib \
	--localstatedir=/var \
	--disable-static \
	--disable-debug
  make
}

package() {
 cd ${srcdir}/gtk-xfce-engine-${pkgver}
 make DESTDIR=${pkgdir} install
}
