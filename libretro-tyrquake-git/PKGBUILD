# Maintainer: almostalive <almostalive2003 at lavabit dot com>

pkgname=libretro-tyrquake-git
_gitname=tyrquake
pkgver=2013.04.08_cd12551
pkgrel=1
pkgdesc="libretro implementation of tyrquake. (Quake)"
arch=('i686' 'x86_64' 'arm' 'armv6h')
url="https://github.com/libretro/tyrquake"
license=('custom')
makedepends=('git')
conflicts=('libretro-super-git')
source=('git://github.com/libretro/tyrquake.git')
md5sums=('SKIP')

pkgver() {
  cd $_gitname
  echo "$(git log -1 --format="%cd" --date=short | sed 's|-|.|g')_$(git describe --always | sed 's/-/_/g')"
}

build() {
  cd $_gitname
  make -f Makefile.libretro
}

package()
{
   mkdir -p $pkgdir/usr/lib/libretro
   install -v -m644 $srcdir/$_gitname/tyrquake_libretro.so $pkgdir/usr/lib/libretro/libretro-tyrquake.so
}
