# Contributor: Anibal Pacheco <apacheco.uy@gmail.com>
pkgname="scrapy"
pkgver=0.16
pkgrel=1
pkgdesc="A fast high-level scraping and web crawling framework."
arch=('i686' 'x86_64')
license=('BSD')
url="http://scrapy.org"
depends=('python2>=2.7' 'twisted>=2.5' 'libxml2>=2.6.28' 'python2-w3lib')
optdepends=("pyopenssl: only if you want to crawl secure (HTTPS) pages")
provides=('scrapy')
conflicts=('scrapy-hg')
options=(!emptydirs)
source=('https://github.com/scrapy/scrapy/tarball/0.16')
md5sums=('40af2ca07dbc1677cb43cbf75f0a06cf')

build() {
  # installation
  cd $srcdir/scrapy-scrapy-40667cb
  python2 setup.py install --root=$pkgdir || return 1

  # copying license information
  install -D -m644 LICENSE $pkgdir/usr/share/licenses/$pkgname/LICENSE

  # copying readme information
  install -D -m644 README.rst $pkgdir/usr/share/doc/$pkgname/README.rst
  install -D -m644 INSTALL $pkgdir/usr/share/doc/$pkgname/INSTALL
}
