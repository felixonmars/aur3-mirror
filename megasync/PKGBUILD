# Maintainer: Alfonso Saavedra "Son Link" <sonlink.dourden@gmail.com>
pkgname=megasync
pkgver=1.0.34
pkgrel=2
pkgdesc="Sync your files to your Mega account. Official app"
arch=('i686' 'x86_64')
url="http://mega.co.nz"
license=('custom:The Clarified Artistic License')
conflicts=('megatools')
depends=('openssl' 'c-ares' 'libgcrypt' 'crypto++' 'qt4' 'libpng' 'qtchooser' 'sqlite')
if [ "${CARCH}" = 'x86_64' ]; then
	ARCH=amd64
	md5sums=('5d234be44f27a6dfb8891b2ef17356a7')
elif [ "${CARCH}" = 'i686' ]; then
	ARCH=i386
	md5sums=('fed8b949ad2de58777ed5815803b84b7')  
fi
source=("https://mega.co.nz/linux/MEGAsync/Debian_7.0/${ARCH}/${pkgname}_${pkgver}_${ARCH}.deb")
install="${pkgname}.install"
options=(!strip)
package (){
	cd "${srcdir}"
	pwd
	tar -xzf data.tar.gz -C ${pkgdir}
	rm -r ${pkgdir}/usr/share/doc
	mkdir -p ${pkgdir}/usr/lib
	ln -s /usr/lib/libcryptopp.so ${pkgdir}/usr/lib/libcrypto++.so.9
}
