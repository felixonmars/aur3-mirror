# Contributor: Peter Simons <simons@cryp.to>
pkgname=shhopt
pkgver=1.1.7
pkgrel=1
pkgdesc="command line parsing library (used by banner)"
arch=('i686' 'x86_64')
url='http://shh.thathost.com/pub-unix/'
license=('GPL')
groups=()
depends=()
makedepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
source=( http://shh.thathost.com/pub-unix/files/${pkgname}-${pkgver}.tar.gz )
noextract=()
md5sums=('caf24206354296e8a48771aea1f47fbd')

build() {
  cd "$srcdir/$pkgname-$pkgver" || return 1
  make OPTIM="$CFLAGS" dep || return 1
  make OPTIM="$CFLAGS" || return 1
  make OPTIM="$CFLAGS" INSTBASEDIR="$pkgdir/usr" install || return 1
}
