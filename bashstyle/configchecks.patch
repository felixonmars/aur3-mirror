--- ../../bashstyle-orig/bashstyle-ng_8.4/.configure/checks	2014-03-20 07:56:23.000000000 -0400
+++ ./.configure/checks	2014-06-01 16:19:03.662218908 -0400
@@ -171,7 +171,7 @@
 	exit 1
 else
 	PYMINVER=270
-	PYMAXVER=335
+	PYMAXVER=341
 	PYVER=$($PYTHON --version 2>&1 | gawk '{ gsub(/\./,""); gsub(/\+/,""); gsub(/rc.*/,""); print $2 }')
 	if [[ $PYVER -lt $PYMINVER ]]; then
 		fail_message p-
@@ -199,10 +199,10 @@
 
 	if [[ $SMOD != "" ]]; then
 		DEP="$SMOD ($MOD)"
-		python -c "from $MOD import $SMOD" 2>/dev/null
+		${PYTHON} -c "from $MOD import $SMOD" 2>/dev/null
 	else
 		DEP="$MOD"
-		python -c "import imp
+		${PYTHON} -c "import imp
 imp.find_module('$MOD')" 2>/dev/null
 	fi
 
