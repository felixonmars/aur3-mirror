post_install() {
	post_upgrade
	sed 's/DefaultBackend=.*/DefaultBackend=alpm/' -i /etc/PackageKit/PackageKit.conf
}

post_upgrade() {
	echo ">> If you want to use this backend as default"
	echo ">> for PackageKit set DefaultBackend=alpm in"
	echo ">> /etc/PackageKit/PackageKit.conf"
}
