--- usr/lib64/OpenCL/vendors/intel/iocgui64.sh	2013-04-03 14:31:48.363753285 +0200
+++ usr/lib64/OpenCL/vendors/intel/iocgui64.sh~	2013-04-03 14:40:38.844212759 +0200
@@ -36,7 +36,7 @@
     fi
 }
 
-export INTELOCLSDKROOT=/usr/lib64/OpenCL/vendors/intel
+export INTELOCLSDKROOT='/opt/intel/opencl-sdk/'
 export PATH=$INTELOCLSDKROOT:$PATH
 
 # Look for zenity - GUI
@@ -53,13 +53,13 @@
 then
     # Get architecture supported on this machine
 	grep -q -e "^flags.*avx" /proc/cpuinfo
-	retval = $?
+	retval=$?
 	if [ $retval -eq 0 ];
 	then
 	    architecture="avx"
 	else
 	    grep -q -e "^flags.*sse4_2" /proc/cpuinfo
-		retval = $?
+		retval=$?
 	    if [ $retval -eq 0 ];
 		then
 		    architecture="sse4_2"
@@ -68,7 +68,7 @@
 
     # Add 32 bit support when available
     java -jar "$jarpath" x64 $architecture
-	retval = $?
+	retval=$?
     if [ $retval -ne 0 ];
     then
         reporterror "could not find java runtime environment"
