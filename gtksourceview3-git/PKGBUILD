# Maintainer: Stefan Husmann <stefan-husmann@t-online.de>
pkgname=gtksourceview3-git
pkgver=3.9.4.3.g5a46af7
pkgrel=1
pkgdesc="A text widget adding syntax highlighting and more to GNOME - git version"
arch=('i686' 'x86_64')
url="http://projects.gnome.org/gtksourceview/development.html"
license=('GPL')
depends=("gtk3>=3.9.9" "glib2>=2.37.3" 'libxml2')
makedepends=('git' 'intltool' 'gobject-introspection-git' 'glade' 
'gnome-common-git' 'gtk-doc')
provides=("gtksourceview3=$pkgver")
conflicts=('gtksourceview3')
options=('!libtool')
source=('gtksourceview3::git://git.gnome.org/gtksourceview')
md5sums=('SKIP')
_gitname="gtksourceview3"

pkgver() {
  cd $_gitname
  # Git tag
  echo $(git describe --always|sed 's/-/./g')
}

build() {
  cp $srcdir/gnome-autogen.sh "$srcdir/$_gitname"
  cd "$srcdir/$_gitname"
  ./autogen.sh  --prefix=/usr --sysconfdir=/etc --localstatedir=/var \
    --disable-static --enable-glade-catalog --enable-gtk-doc
  make
}

package() {
  cd "$srcdir/$_gitname"
  make DESTDIR="$pkgdir/" install
}
