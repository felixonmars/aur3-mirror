#Maintainer: Stéphane Marguet (Stemp) <smarguet à gmail point com>

pkgname=svv
pkgver=0.0.2
pkgrel=2
pkgdesc="Simple V4L2 video viewer"
url="http://moinejf.free.fr/"
changelog=ChangeLog
license="GPL"
depends=('v4l-utils' 'gtk2')
arch=(i686 x86_64)
source=("http://moinejf.free.fr/svv.c")
md5sums=('0a2734e9f4caf9e86b3951cd84629820')

build() {
  cd $startdir/src/
  gcc -Wall -O2 svv.c -o svv $(pkg-config gtk+-2.0 libv4lconvert --cflags --libs)
  mkdir -p ${startdir}/pkg/usr/local/bin/
  cp -r ${startdir}/src/svv ${startdir}/pkg/usr/local/bin/
}
