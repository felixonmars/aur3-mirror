# Maintainer: Amr Hassan <amr.hassan@gmail.com>
pkgname=pugixml
pkgver=1.0
pkgrel=2
pkgdesc="Light-weight, simple and fast XML parser for C++ with XPath support"
url="http://pugixml.org/"
arch=('x86_64' 'i686')
license=('MIT')
makedepends=(cmake)

source=("http://pugixml.googlecode.com/files/${pkgname}-${pkgver}.zip"
        "CMakeLists.txt.patch")
        
md5sums=('8ce0b7c26f6d8dba6585bf1ddfc535e1'
         '631c4a23e0a84f26e0a36f83b5ad714a')

build() {
  cd "${srcdir}/"
  patch "${srcdir}/scripts/CMakeLists.txt" CMakeLists.txt.patch
  cmake scripts -DCMAKE_INSTALL_PREFIX=/usr
  make
}

package() {
  cd "${srcdir}"
  make DESTDIR="${pkgdir}" install
}

