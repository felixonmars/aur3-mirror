# Contributor: Meow < meow@linux.cn >

pkgname=rstudio-desktop-bin
pkgver=0.94.92
pkgrel=1
pkgdesc="A new integrated development environment (IDE) for R"
arch=('i686' 'x86_64')
license=('GPL')
url="http://www.rstudio.org/"
depends=('r>=2.11.1' 'pango>=1.14.0' 'glibc>=2.7')
conflicts=('rstudio-desktop' 'rstudio-desktop-git')
provides=("rstudio-desktop=${pkgver}")

_arch=i386
source=("https://s3.amazonaws.com/rstudio-desktop/rstudio-${pkgver}-${_arch}.deb")
md5sums=('ab0be344cc77d04019fb1296692d3ec7')

[ "$CARCH" = "x86_64" ] && {
_arch=amd64
source=("https://s3.amazonaws.com/rstudio-desktop/rstudio-${pkgver}-${_arch}.deb")
md5sums=('010b0dcb2954e40aa1f85b1fc2e5afef')
}


package() {
  msg "Converting debian package"
  cd $srcdir
  tar zxpf data.tar.gz -C $pkgdir
  mkdir -p $pkgdir/usr/bin
  cd $pkgdir/usr/bin
  ln -s -f ../lib/rstudio/bin/rstudio rstudio-bin
}
