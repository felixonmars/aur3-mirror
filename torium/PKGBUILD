# Contributor: Antonio Arias Orzanco <antonio.arias99999@gmail.com>

pkgname=torium
pkgver=0.4.2
pkgrel=2
pkgdesc="Torium is a free, open source, minimalistic torrent client for Linux."
depends=('libtorrent-rasterbar>=0.13' 'gtkmm>=2.4.0' 'boost>=1.34')
source=(http://dl.sourceforge.net/sourceforge/${pkgname}/${pkgname}-${pkgver}.tar.gz)
md5sums=('fa1980cef7e248dbd82526f247541442')
url="http://bithack.se/projects/torium/"
license=(ISC)
arch=('i686' 'x86_64')

build() {
  cd $startdir/src/$pkgname-$pkgver

  ./configure --prefix=/usr || return 1
  make DESTDIR="${pkgdir}" install || return 1

# License ISC
  install -Dm644 COPYING "$pkgdir"/usr/share/licenses/$pkgname/COPYING
}
