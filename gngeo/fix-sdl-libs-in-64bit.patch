Author: josephgbr

/usr/bin/sdl-config should be patched to avoid adding "-I/usr/lib" to $SDL_LIBS,
but while it is not fixed, let us avoid errors in the configure step.

--- gngeo-0.8/configure	2012-01-14 08:35:41.000000000 -0200
+++ gngeo-0.8-new/configure	2012-01-30 22:33:53.334248169 -0200
@@ -4251,6 +4251,9 @@
     SDL_CFLAGS=`$SDL_CONFIG $sdl_config_args --cflags`
     SDL_LIBS=`$SDL_CONFIG $sdl_config_args --libs`
 
+    # Workaround to avoid sdl-config from providing wrong library folder (-I/usr/lib)
+    SDL_LIBS=${SDL_LIBS/lib/lib32}
+
     sdl_major_version=`$SDL_CONFIG $sdl_config_args --version | \
            sed 's/\([0-9]*\).\([0-9]*\).\([0-9]*\)/\1/'`
     sdl_minor_version=`$SDL_CONFIG $sdl_config_args --version | \
