## arg 1:  the new package version
post_install() {
  echo "To use popwin, append the below line to your emacs.el"
  echo "(require 'popwin)"
  echo "(setq display-buffer-function 'popwin:display-buffer)"
}

# vim:set ts=2 sw=2 et:
