# Maintainer: Alfredo Palhares <masterkorp@masterkorp.net>

_gemname=knife-windows
pkgname=ruby-$_gemname
pkgver=0.5.14
pkgrel=2
pkgdesc="Plugin that adds functionality to Chef's Knife CLI for configuring/interacting with nodes running Microsoft Windows"
arch=('any')
url="https://github.com/opscode/knife-windows"
license=('MIT')
depends=(
  'ruby'
  'ruby-em-winrm'
)
makedepends=('rubygems')
source=(http://rubygems.org/downloads/$_gemname-$pkgver.gem)
noextract=($_gemname-$pkgver.gem)
md5sums=('61c40a3e234a71e629dc7fbb9f403265')

package() {
  cd "$srcdir"
  local _gemdir="$(ruby -e'puts Gem.default_dir')"
  gem install --ignore-dependencies --no-user-install -i "$pkgdir$_gemdir" -n "$pkgdir/usr/bin" $_gemname-$pkgver.gem
}
