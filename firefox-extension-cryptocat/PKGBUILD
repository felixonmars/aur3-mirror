# Maintainer: GI_Jack <iamjacksemail@hackermail.com>
# Based on PKGBUILD firefox-extension-httpseverywhere

_plugin_name=cryptocat
_plugin_version=2.1.20.1
pkgdesc="Web based encrypted(OTR) IM client for Firefox"
license=('GNU Affero')

pkgname=firefox-extension-$_plugin_name
pkgver=$_plugin_version
pkgrel=1
arch=('any')
url="http://crypto.cat"
depends=("firefox>=3.5")
source=("https://addons.cdn.mozilla.net/_files/398258/cryptocat-${pkgver}-fx.xpi")
md5sums=('b871a9f7fb543228aeea6810e678618a')
sha1sums=('cc6ebb3e29d18c642d39111df3a1a14978704a28')
package(){
  cd $srcdir
  emid=$(sed -n '/.*<em:id>\(.*\)<\/em:id>.*/{s//\1/p;q}' install.rdf) || return 1
  local dstdir=$pkgdir/usr/lib/firefox/browser/extensions/${emid}
  install -d $dstdir
  cp -r * $dstdir
  rm $dstdir/$_plugin_name-$pkgver-fx.xpi
  chmod -R 755 $dstdir
}
