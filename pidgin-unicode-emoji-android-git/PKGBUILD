# Maintainer: Ryon Sherman <ryon.sherman@gmail.com>

_gitname="stv0g"
_pkgname="unicode-emoji"

pkgname="pidgin-${_pkgname}-android-git"
pkgver=41e746d
pkgrel=1
pkgdesc="Android Unicode Emojis for Pidgin."

provides=("pidgin-${_pkgname}-android")

arch=('any')
license=('Apache')
url="https://github.com/${_gitname}/${_pkgname}"

depends=('pidgin')
makedepends=('git')

source=("git+https://github.com/${_gitname}/${_pkgname}.git")
md5sums=('SKIP')


pkgver() {
    cd ${srcdir}/${_pkgname}
    git describe --always | sed 's|-|.|g'
}

package() {
    cd ${srcdir}/${_pkgname}
    
    installdir=${pkgdir}/usr/share/pixmaps/pidgin/emotes

    install -d ${installdir}
    cp -r android ${installdir}
}
