# Contributor: Ondrej Kucera <ondrej.kucera@centrum.cz>

pkgname=tred
pkgver=1.4642
pkgrel=1
pkgdesc="Tree editor TrEd"
arch=("any")
url="http://ufal.mff.cuni.cz/~pajas/tred/"
license=("GPL")
depends=('perl' 'perl-tk' 'perl-file-which' 'perl-tie-ixhash' 'perl-tk-codetext' 'perl-xml-libxslt' 'perl-xml-libxml-iterator' 'perl-xml-sax-writer' 'perl-xml-writer' 'perl-io-string' 'perl-treex-pml')
source=(http://ufal.mff.cuni.cz/~pajas/tred/$pkgname-$pkgver.tar.gz)
md5sums=('950c49039c4cf4808c8f9a75f2e56253')

build() {
  mkdir -p ${pkgdir}/opt
  cp -r ${srcdir}/${pkgname} ${pkgdir}/opt
  mkdir -p ${pkgdir}/usr/bin
  cd ${pkgdir}/usr/bin
  ln -s ../../opt/tred/tred tred
  ln -s ../../opt/tred/btred btred
  ln -s ../../opt/tred/ntred ntred
}
