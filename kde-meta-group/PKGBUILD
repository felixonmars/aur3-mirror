# Contributor: zertyz <zertyz@gmail.com>

# This package was made with dependencies from the homonimous package group
# in order to correctly solve package dependencies for automated installations.

pkgname=kde-meta-group

# To keep the package dependencies updated, issue the following command:
# 	echo `pacman -Qg $pkgname | sed 's/.* //'`

# Get the correct version and URL with:
#	pacman -Qi `pacman -Qg $pkgname | sed 's/.* //' | head -n 1`

pkgver=4.8
pkgrel=2
pkgdesc="Package created from the homonimous official package group in order to allow 'kde_meta' to be listed as dependencies for other packages"
arch=('i686' 'x86_64')
url="http://www.libreoffice.org/"
license="GPL"
depends=(kde-meta-kdeaccessibility kde-meta-kdeadmin kde-meta-kdeartwork kde-meta-kdebase kde-meta-kdeedu kde-meta-kdegames kde-meta-kdegraphics kde-meta-kdemultimedia kde-meta-kdenetwork kde-meta-kdepim kde-meta-kdeplasma-addons kde-meta-kdesdk kde-meta-kdetoys kde-meta-kdeutils kde-meta-kdewebdev kde-wallpapers)
makedepends=()
conflicts=()
replaces=()
backup=()
install=
source=()
md5sums=()

build() {
  true
}
