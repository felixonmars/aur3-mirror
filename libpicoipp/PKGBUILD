# Maintainer: aAXEe <axel at brasshack dot de>
pkgname=libpicoipp
pkgver=1.1.2
pkgrel=2
pkgdesc="library for picotech oscilloscope software"
arch=('x86_64')
url="http://www.picotech.com/linux.html"
license=('custom')
groups=()
depends=(mono mono-tools)
makedepends=(dpkg)
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
source=(
  "http://labs.picotech.com/debian/pool/main/libp/libpicoipp/libpicoipp_1.1.2-4r21_amd64.deb"
  )
md5sums=('dbe145d37d45c8221b650b2dc52162eb')
noextract=()

package() {
  dpkg --extract libpicoipp_1.1.2-4r21_amd64.deb $pkgdir
  chmod -R go-w $pkgdir
  chown -R root:root $pkgdir
}

