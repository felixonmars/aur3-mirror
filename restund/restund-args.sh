# RESTUND init daemon configuration file
# sourced by /etc/rc.d/ulatencyd

# Additional options passing to the daemon.
# Type "restund -h" to get all possible options.

# Example:
# RESTUND_ARGS="-d -n -f /etc/restund.conf"

RESTUND_ARGS=""
