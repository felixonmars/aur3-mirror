#Maintainer: Xyne <ac xunilhcra enyx, backwards>
pkgname=pychrom
pkgver=2011.02.05.1
pkgrel=1
pkgdesc="An enhanced yet simple pyGTK color selection and conversion tool that can minimize to the tray."
depends=('python2' 'pygtk' 'gtk2')
arch=('any')
license=('GPL')
url="http://xyne.archlinux.ca/projects/${pkgname}"
source=(http://xyne.archlinux.ca/src/${pkgname}-${pkgver}.tar.gz)
md5sums=('2e5ab3fed7eff3e150c65214c153cea9')

build() {
  install -D -m755 $srcdir/$pkgname/$pkgname $pkgdir/usr/bin/${pkgname}
}
# vim: set ts=2 sw=2 et:
