post_install() {
	echo "Make shure your /etc/resolv.conf does not get overwritten!"
	echo "You can do so by setting USEPEERDNS=0 und /etc/ppp/ip-up"
	echo "before the scripts in /etc/ppp/ip-up.d/ are run."
}
