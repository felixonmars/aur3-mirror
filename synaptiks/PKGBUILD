# Maintainer: Sebastian Wiesner <lunaryorn@googlemail.com>
# Contributor: A Rojas <nqn1976 @ gmail.com>
# Contributor: gobeav3rs297 <vincent.t.cao@gmail.com>
# Contributor: archtux <antonio.arias99999@gmail.com>
# Contributor: neo <neo[AT]acanac[DOT]net>

pkgname=synaptiks
pkgver=0.7.0
pkgrel=2
pkgdesc="A touchpad tool for KDE"
arch=('any')
url="http://synaptiks.lunaryorn.de/"
license=('BSD')
depends=('kdebase-workspace' 'kdebindings-python' 'python2-distribute'
  'python2-udev>=0.8' 'dbus-python' 'xf86-input-synaptics')
makedepends=('kdesdk-scripts')
source=(http://pypi.python.org/packages/source/s/${pkgname}/${pkgname}-${pkgver}.tar.bz2)
md5sums=('16925f2df9d8422fb406898f36faaca6')
install=(synaptiks.install)

build() {
 cd "${srcdir}/${pkgname}-${pkgver}"
 python2 setup.py install --root "${pkgdir}"
 install -m755 -d "$pkgdir/usr/share/licenses/synaptiks/"
 install -m644 COPYING "$pkgdir/usr/share/licenses/synaptiks"
}
