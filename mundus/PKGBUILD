# generated by the gambas3 ide
# Maintainer : sebikul <sebikul@gmail.com>

pkgname=mundus
pkgdesc="Clean and backup your /home and /etc folder from old configuration files once created by uninstalled software."
pkgver=1.3.0
pkgrel=2
arch=('any')
url="https://launchpad.net/mundus"
license=('GPL')
conflicts=('mundus-bzr')
depends=('gambas3-gb-image>=3.0.90' 'gambas3-gb-image<=3.99.0' 'gambas3-gb-qt4>=3.0.90' 'gambas3-gb-qt4<=3.99.0' \
	 'gambas3-gb-form>=3.0.90' 'gambas3-gb-form<=3.99.0' 'gambas3-gb-desktop>=3.0.90' 'gambas3-gb-desktop<=3.99.0' \
	 'gambas3-gb-settings>=3.0.90' 'gambas3-gb-settings<=3.99.0' 'gambas3-gb-net>=3.0.90' 'gambas3-gb-net<=3.99.0' \
	 'gambas3-gb-net-curl>=3.0.90' 'gambas3-gb-net-curl<=3.99.0' 'gambas3-gb-web>=3.0.90' 'gambas3-gb-web<=3.99.0' \
	 'gambas3-gb-form-stock>=3.0.90' 'gambas3-gb-form-stock<=3.99.0')
makedepends=('gambas3-devel')
source=(https://launchpad.net/$pkgname/trunk/$pkgver/+download/$pkgname-$pkgver.tar.gz)
md5sums=('60ac620ca6316073a29b3994b94c3e59')

build() {
  cd ${srcdir}/${pkgname}

  make
  
}

package() {
  cd ${srcdir}/${pkgname}

  make DESTDIR=$pkgdir install
}
