# generated by the gambas3 ide
# Maintainer : sebikul <sebikul@gmail.com>

pkgname=mundus
pkgdesc="Clean and backup your /home and /etc folder from old configuration files once created by uninstalled software."
pkgver=0.0.8
pkgrel=3
arch=('any')
url="https://launchpad.net/mundus"
license=('GPL')
depends=('gambas3-gb-image>=2.90.0' 'gambas3-gb-qt4>=2.90.0' 'gambas3-gb-form>=2.90.0' 'gambas3-gb-form-stock>=2.90.0' 'gambas3-gb-desktop>=2.90.0' 'gambas3-gb-settings>=2.90.0' 'gambas3-gb-net>=2.90.0' 'gambas3-gb-net-curl>=2.90.0' 'gambas3-gb-web>=2.90.0')
makedepends=('gambas3-devel')
source=(https://launchpad.net/mundus/trunk/$pkgver/+download/$pkgname-$pkgver.tar.gz)
md5sums=('36bf77a84eb7319652dfd2bcea02f73d')

build() {
  cd ${srcdir}/${pkgname}

  make
  
}

package() {
  cd ${srcdir}/${pkgname}

  make PREFIX=$pkgdir install
}
