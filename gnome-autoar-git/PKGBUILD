_pkgname=gnome-autoar
pkgname=gnome-autoar-git
pkgver=20141105
pkgrel=1
arch=('i686' 'x86_64')
pkgdesc="Automatic archives creating and extracting library"
url="https://github.com/GNOME/gnome-autoar"
license=('GPL')
makedepends=('git')
depends=('libarchive' 'glib2' 'gtk3' 'gnome-common')
conflicts=(${_pkgname})
provides=(${_pkgname})
source=('git+https://github.com/GNOME/gnome-autoar.git')
#_gitroot=('https://github.com/GNOME/gnome-autoar.git')
#_gitname=${_pkgname}

md5sums=('SKIP')

build() {
#  cd "${srcdir}"
#
#  msg "Connecting to git server"
#  msg "1"
#  if [ -d $srcdir/$_gitname ] ; then
#    cd ${_gitname} && git pull origin
#    msg "The local files are updated."
#  else
#    msg "3"
#    git clone ${_gitroot}
#  fi

#  msg "git checkout done or server timeout"

#  msg "Building"

#  rm -rf "${srcdir}/${_gitname}-build"
#  cp -r "${srcdir}/$_gitname" "${srcdir}/${_gitname}-build"
#  cd "${srcdir}/${_gitname}-build"

  cd "${srcdir}/${_pkgname}"

  ./autogen.sh --prefix=/usr --sysconfdir=/etc \
      --localstatedir=/var --enable-gtk-doc

  make
}
 
package() {
  cd "${srcdir}/${_pkgname}"
  make DESTDIR="${pkgdir}/" install
#  cd "${srcdir}/${_gitname}-build"
#  make DESTDIR="${pkgdir}/" install
#  rm -rf "${srcdir}/${_gitname}-build"
}

# vim:set ts=2 sw=2 et:
