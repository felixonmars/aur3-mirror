# Maintainer: Thomas Dziedzic < gostrc at gmail >

pkgname=sage-mathematics-spkgs
pkgver=4.6.2
pkgrel=1
pkgdesc='SAGE: Open Source Mathematics Software, a viable free alternative to Magma, Maple, Mathematica, and Matlab. Source packages only.'
url='http://www.sagemath.org'
arch=('any')
license=('GPL')
source=("http://sage.math.washington.edu/home/release/sage-${pkgver}/sage-${pkgver}.tar")
md5sums=('0dc2c9e9cc593685bb039085900ddfeb')

package() {
  for i in base standard ; do
    install -d ${pkgdir}/opt/sage/spkg/${i}
    install -m644 ${srcdir}/sage-${pkgver}/spkg/${i}/*spkg \
      ${pkgdir}/opt/sage/spkg/${i}
  done
}
