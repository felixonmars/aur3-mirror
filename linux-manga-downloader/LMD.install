#!/bin/bash

post_install() {
    red='\033[1;31m'
    stp='\033[0m'

    echo " ";  
    echo -e "$red==> Run on console until reboot $stp";

	update-desktop-database -q
}

post_upgrade() {
    post_install "$1"
}

post_remove() {
	update-desktop-database -q
}