#!/bin/bash

post_install() {
    red='\033[1;31m'
    stp='\033[0m'

    echo " ";  
    echo -e "$red==> Run on console until reboot $stp";
}

post_upgrade() {
    red='\033[1;31m'
    stp='\033[0m'

    echo " ";  
    echo -e "$red==> Run on console until reboot $stp";
}