post_install() {
{
if [ -e /home/$USER/.LMD ] 
then
    echo ".LMD already exists"
else 
    lmd -reconf
fi
}
  cp -r -f  "${pkgdir}/usr/share/lmd/langs/" "/home/$USER/.LMD/lang/"
  echo "In some Websites, when downloading, the program says there was an error with the last page of the chapter, don't worry, coding mistake, that page was downloaded successfully"
}