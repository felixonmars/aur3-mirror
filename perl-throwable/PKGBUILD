# CPAN Name  : Throwable
# Contributor: Caleb Cushing <xenoterracide@gmail.com>
# Generator  : CPANPLUS::Dist::Arch 1.04
# Template @ http://github.com/xenoterracide/AURpan/blob/master/perl-throwable/PKGBUILD.tt
# File bugs @ http://github.com/xenoterracide/AURpan/issues

pkgname='perl-throwable'
pkgver='0.102080'
pkgrel='1'
pkgdesc="a role for classes that can be thrown"
arch=('any')
url='http://search.cpan.org/dist/Throwable'
license=('PerlArtistic' 'GPL')
depends=('perl' 'perl-devel-stacktrace>=1.21' 'perl-moose>=0.87')


options=('!emptydirs')

source=('http://search.cpan.org/CPAN/authors/id/R/RJ/RJBS/Throwable-0.102080.tar.gz')
md5sums=('f048e133a92eab91a1975001d2a55a38')

build() {
  DIST_DIR="${srcdir}/Throwable-0.102080"
  export PERL_MM_USE_DEFAULT=1 PERL5LIB=""                 \
    PERL_AUTOINSTALL=--skipdeps                            \
    PERL_MM_OPT="INSTALLDIRS=vendor DESTDIR='$pkgdir'"     \
    PERL_MB_OPT="--installdirs vendor --destdir '$pkgdir'" \
    MODULEBUILDRC=/dev/null
  {
	cd "$DIST_DIR" &&
    perl Makefile.PL &&
    make &&
    make test &&
    make DESTDIR="$pkgdir" install;
  } || return 1;

  find "$pkgdir" -name .packlist -o -name perllocal.pod -delete

}

