diff -aur gmpc-libnotify-0.20.0/src/plugin.c gmpc-libnotify-0.20.0-libnotify-0.7//src/plugin.c
--- gmpc-libnotify-0.20.0/src/plugin.c	2010-03-22 04:30:04.000000000 +0100
+++ gmpc-libnotify-0.20.0-libnotify-0.7//src/plugin.c	2011-03-20 11:40:04.000000000 +0100
@@ -30,6 +30,10 @@
 
 #define LOG_DOMAIN "LibNotifyPlugin"
 
+#ifndef NOTIFY_CHECK_VERSION
+#define NOTIFY_CHECK_VERSION(x,y,z)  0
+#endif
+
 extern GtkStatusIcon *tray_icon2_gsi;
 static NotifyNotification *not = NULL;
 static gulong data_changed = 0;
@@ -149,7 +153,7 @@
 		if(not == NULL)
 		{
 			//            notify_notification_close(not, NULL);
-			not = notify_notification_new(summary, buffer,NULL, NULL);
+			not = notify_notification_new(summary, buffer, NULL);
 		}
 		else{
 			notify_notification_update(not, summary, buffer, NULL);
