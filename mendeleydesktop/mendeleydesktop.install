post_install() {
  echo ""
  echo "----------------------------------------------------------------"
  echo "KDE users may have issues with Qt themes."
  echo "A possible workaround is to remove"
  echo "the included Qt libraries (/opt/mendeleydesktop/lib/libQt*)"
  echo "----------------------------------------------------------------"
  echo ""
}

post_upgrade() {
   post_install
}
