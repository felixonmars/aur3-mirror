# Contributor: Nicolas Quiénot <niQo @ aur>

pkgname=ginn
pkgver=0.2.5
pkgrel=2
pkgdesc=""
url="http://launchpad.net/ginn"
arch=('i686' 'x86_64')
license=('GPL')
depends=('utouch-geis' 'bamf' 'bamf-bin' 'libxtst')
source=(http://launchpad.net/ginn/0.x/${pkgver}/+download/ginn-${pkgver}.tar.gz)

build() {
  cd ${srcdir}/${pkgname}-${pkgver}

  ./configure --prefix=/usr --sysconfdir=/etc
  make || return 1
  make DESTDIR=${pkgdir} install || return 1
}

md5sums=('db99263c805033e99ace8bf23cfba4bf')
