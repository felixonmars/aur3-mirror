# Contributor: Bartłomiej Piotrowski <bpiotrowski@archlinux.org>
# Maintainer: k2s

pkgname=tox-prpl
pkgver=0.4.1
pkgrel=1
pkgdesc='Tox protocol plugin for Pidgin/libpurple'
arch=('i686' 'x86_64')
url='http://tox.dhs.org'
license=('GPL3')
depends=('tox-git' 'libpurple' 'libsodium')
source=(https://github.com/jin-eld/tox-prpl/archive/v$pkgver.tar.gz)
md5sums=('427f3ff20821a5c342a883f01173ac68')

prepare() {
  cd $pkgname-$pkgver
  autoreconf -fi
}

build() {
  cd $pkgname-$pkgver
  ./configure --prefix=/usr
  make
}

package() {
  make -C $pkgname-$pkgver DESTDIR="$pkgdir" install
}

