# Maintainer: Dan Serban
# Contributor: Moritz Wilhelmy <echo dGhlLnd1bGYuZ2FuZ0Bnb29nbGVtYWlsLmNvbQ== | base64 -d>

pkgname=yget
pkgver=2.2.2
pkgrel=1
pkgdesc="yget/yplay/yurl help download / stream / retrieve download URLs of Youtube videos"
arch=(i686 x86_64)
url="http://repo.hu/projects/yget/releases"
license=('Public domain')
depends=(wget)
optdepends=('mplayer: for yplay to stream Youtube videos')
source=(http://repo.hu/projects/$pkgname/releases/$pkgname-$pkgver.tar.gz)
md5sums=('71548c89f02d76eb4a3fa459d2a2e70e')

build()
{
  cd $srcdir/$pkgname-$pkgver
  make install install_root="$pkgdir" || return 1
}
