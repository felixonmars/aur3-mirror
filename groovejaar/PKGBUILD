# Maintainer: Kevin Woods <woods.k at live dot com>

pkgname=groovejaar
pkgver=1.0.10
pkgrel=1
pkgdesc="A Java based downloader for the GrooveShark service"
arch=('i686' 'x86_64')
url="http://code.google.com/p/groovejaar/"
license=('GPL')
depends=('java-runtime' 'bash')
source=("http://groovejaar.googlecode.com/files/${pkgname}_${pkgver}_all.zip"
        "groovejaar.sh"
        "groovejaar.desktop")
md5sums=('ebd9e6a40405c13e1f106f2f1c8a7f22'
         '29c96e42f4f087f6a0e2d1c219a3201e'
         'f53a38b05b47ed4fd549b30bd14ae9dd')

package() {
  # Copy .jar file
  mkdir -p $pkgdir/usr/share/java/groovejaar
  cp $srcdir/GrooveJaar.jar $pkgdir/usr/share/java/groovejaar
  
  # Copy config file
  mkdir $pkgdir/usr/share/groovejaar
  cp $srcdir/config.properties $pkgdir/usr/share/groovejaar

  # Install launcher script
  install -D -m 755 $srcdir/groovejaar.sh $pkgdir/usr/bin/groovejaar

  # Install desktop file
  install -D -m 644 $srcdir/groovejaar.desktop $pkgdir/usr/share/applications/groovejaar.desktop
}
