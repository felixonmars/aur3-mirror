# Maintainer: mutlu_inek@yahoo.de

pkgname=antconc
pkgver=3.2.0u
pkgrel=1
pkgdesc="AntConc is a simple Concordance program for text analysis"
arch=(i686)
url="http://www.antlab.sci.waseda.ac.jp/antconc_index.html"
license=('custom:AntConc')
depends=('java-runtime')
makedepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
options=(!strip)
source=(http://www.antlab.sci.waseda.ac.jp/software/${pkgname}${pkgver}
	http://www.antlab.sci.waseda.ac.jp/software/README_antconc3.2.0.txt)
md5sums=('c7d7ebfc8f30c0dff30416e95ea692c8'
         'b8f08b4459101558721b2b62f9777c7c')

#build() {
#  cd "$srcdir/$pkgname-$pkgver"
#
#  ./configure --prefix=/usr
#  make
#}

package() {
#  cd "$srcdir/$pkgname-$pkgver"
#  make DESTDIR="$pkgdir/" install
#   mkdir -p $pkgdir/usr/bin
   install -Dm755 ${pkgname}${pkgver} $pkgdir/usr/bin/$pkgname
   install -Dm644 ${srcdir}/README_${pkgname}3.2.0.txt ${pkgdir}/usr/share/licenses/$pkgname/AntConc
}

