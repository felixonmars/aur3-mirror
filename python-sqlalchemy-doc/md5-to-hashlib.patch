--- doc/build/builder/changelog.py_orig	2013-08-24 17:23:31.551002192 +0200
+++ doc/build/builder/changelog.py	2013-08-24 17:25:35.271006499 +0200
@@ -5,7 +5,7 @@
 import textwrap
 import itertools
 import collections
-import md5
+import hashlib
 
 def _comma_list(text):
     return re.split(r"\s*,\s*", text.strip())
@@ -162,7 +162,7 @@
 
         to_hash = "%s %s" % (self.version, text[0:100])
         targetid = "%s-%s" % (self.type_,
-                        md5.md5(to_hash.encode('ascii', 'ignore')
+                        hashlib.md5(to_hash.encode('ascii', 'ignore')
                             ).hexdigest())
         targetnode = nodes.target('', '', ids=[targetid])
         para.insert(0, targetnode)
