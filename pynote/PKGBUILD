# Maintainer: Stefan Tatschner <stefan@sevenbyte.org>

pkgname=pynote
pkgver=0.1
pkgrel=3
pkgdesc="Manage notes on the commandline."
arch=('any')
url="http://redmine.sevenbyte.org/projects/pynote"
license=('MIT')
depends=('python' 'python-prettytable')
conflicts=('pynote-git')
provides=('pynote')
source=("https://pypi.python.org/packages/source/p/${pkgname}/${pkgname}-${pkgver}.tar.gz")
md5sums=('e6b5ec55a879213b6f3674471b9c657a')
install=pynote.install


package() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  python setup.py install --prefix=/usr --root="${pkgdir}" --optimize=1
}

# This comes in later releases. :)
#check() {
#  cd "$srcdir/${_pkgname}"
#  python tests.py
#}
