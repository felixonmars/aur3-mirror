# Maintainer: Trizen <echo dHJpemVueEBnbWFpbC5jb20K | base64 -d>

pkgname=trizen
pkgver=20110804
pkgrel=1
pkgdesc="Trizen AUR Package Manager: A lightweight wrapper for AUR."
arch=('any')
url="https://github.com/trizen/trizen"
license=('GPL')
depends=('perl-libwww' 'pacman')
source="http://trizen.googlecode.com/files/$pkgname-$pkgver.tar.gz"
md5sums=('cba4664345727e87f17950660abf0270')

package() {
  install -Dm 755 $srcdir/$pkgname $pkgdir/usr/bin/$pkgname
}
