# Generated by Xyne::Arch::CPAN 0.07

pkgname=perl-test-www-selenium
pkgver=1.210.0
pkgrel=1
pkgdesc="Test applications using Selenium Remote Control"
arch=('i686 x86_64')
url="http://search.cpan.org/dist/Test-WWW-Selenium/"
license=('perl')
source=('http://search.cpan.org/CPAN/authors/id/L/LU/LUKEC/Test-WWW-Selenium-1.21.tar.gz')
md5sums=('dac6a88ecda017caabaefce578e14a11')
sha256sums=('4701a654b208b4a1f50986804591006bd32ef6a2c1287760deef7955085c0540')
depends=('perl-lwp-useragent' 'perl-uri')
makedepends=('perl-extutils-makemaker' 'perl-test-exception' 'perl-test-mock-lwp' 'perl-test-simple' 'perl-test-pod')
provides=('perl-www-selenium-util' 'perl-www-selenium-xmlparseutils' 'perl-www-selenium=0.930.0')
options=(!emptydirs)

build() {
  _dir=$(find $srcdir -maxdepth 2 -type f -name 'Makefile.PL')
  if [ ! -z "$_dir" ]; then
    cd $(dirname "$_dir")
    PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1
    make  || return 1
    make install DESTDIR="${pkgdir}" || return 1

  else
  _dir=$(find $srcdir -maxdepth 2 -type f -name 'Build.PL')
  if [ ! -z "$_dir" ]; then
    cd $(dirname "$_dir")
    PERL_MM_USE_DEFAULT=1 perl Build.PL INSTALLDIRS=vendor || return 1
    ./Build  || return 1
    ./Build install destdir=${pkgdir} || return 1

  else
    echo "error: failed to detect build method for $pkgname"
    echo "you may be able to fix this by editing the PKGBUILD"
    return 1
  fi fi

  # remove perllocal.pod and .packlist
  find ${pkgdir} -name perllocal.pod -delete
  find ${pkgdir} -name .packlist -delete
}

# vim:set ts=2 sw=2 et:

