post_install() {
  echo ">> Configuration file was installed to /etc/jirc.conf"
  echo ">> Start the bridge with 'jirc -C /etc/jirc.conf'"
}

post_upgrade() {
  echo ">> Configuration file was installed to /etc/jirc.conf"
  echo ">> Start the bridge with 'jirc -C /etc/jirc.conf'"
}

op=$1
shift
$op $*
