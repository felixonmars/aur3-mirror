# Maintainer: Ivan Sichmann Freitas <ivansichfreitas at gmail dot com>
# Contributor: Todd Partridge (Gen2ly) <toddrpartridge (at) gmail>
# Contributor: Franz Burgmann <f dot burgmann at gmail dot com>

pkgname=partclone
pkgver=0.2.16
pkgrel=2
pkgdesc="Backup and restore a partition for numerous file-systems types"
arch=('i686' 'x86_64')
url="http://partclone.org/"
license=('GPL')
depends=('e2fsprogs' 'progsreiserfs' 'dosfstools' 'ncurses')
makedepends=('gcc' 'ncurses' 'pkgconfig')
conflicts=('partclone-svn' 'partclone-testing')
source=("http://downloads.sourceforge.net/project/partclone/stable/${pkgver}/${pkgname}-${pkgver}.tar.gz")
md5sums=('b28059c89ff11e2b3a3afa45da51a738')

build() {
	cd ${srcdir}/${pkgname}-${pkgver}
	# disabling ntfs until I found what happend to ntfs/versio.h
	./configure --prefix=/usr --enable-extfs --enable-reiserfs --enable-fat --enable-ncursesw
	make || return 1
	make DESTDIR="$pkgdir/" install
}
