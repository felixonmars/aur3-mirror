# Maintainer: Andrzej Giniewicz <gginiu@gmail.com>
pkgname=scikits-learn
pkgver=0.8.1
pkgrel=1
pkgdesc="A set of python modules for machine learning and data mining"
arch=('i686' 'x86_64')
url="http://scikit-learn.sourceforge.net/"
license=('BSD')
depends=('python2' 'python2-numpy' 'python2-scipy' 'scikits-base')
makedepends=('python2-distribute' 'gcc')
options=(!emptydirs)

source=("http://downloads.sourceforge.net/project/scikit-learn/scikits.learn-${pkgver}.tar.gz")
md5sums=('f05762878b426ede6e79e48568f3ce68')

build() {
  cd "$srcdir/scikits.learn-${pkgver}"

  python2 setup.py install --root="$pkgdir"/ --optimize=1 || return 1
  rm "$pkgdir/`python2 -c 'from distutils.sysconfig import get_python_lib; \
    print get_python_lib()'`/scikits/__init__.py"
}

