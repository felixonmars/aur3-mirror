# Maintainer: Andrzej Giniewicz <gginiu@gmail.com>

pkgname=scikits-learn
pkgver=0.11
pkgrel=2
pkgdesc="A set of python modules for machine learning and data mining"
arch=('i686' 'x86_64')
url="http://scikit-learn.sourceforge.net/"
license=('BSD')
depends=('python2-scipy' 'scikits-base')
makedepends=('python2-distribute')
options=(!emptydirs)

source=("http://downloads.sourceforge.net/project/scikit-learn/scikit-learn-${pkgver}.tar.gz" 
        "LICENSE")
md5sums=('5894414f9b3b7bfcc7936ecb7beaa2ce'
         '327083d2576cc0aad1b8f10b2bcd2974')

build() {
  cd "$srcdir/scikit-learn-${pkgver}"

  unset LDFLAGS

  _PYPATH=`python2 -c 'from distutils.sysconfig import get_python_lib; \
    print get_python_lib()'`

  python2 setup.py install --root="$pkgdir"/ --optimize=1
  rm "$pkgdir/$_PYPATH/scikits/__init__.py"

  sed -i -e "s|#![ ]*/usr/bin/python$|#!/usr/bin/python2|" \
         -e "s|#![ ]*/usr/bin/env python$|#!/usr/bin/env python2|" \
    $(find "${pkgdir}" -name '*.py')

  install -D "$srcdir"/LICENSE "$pkgdir"/usr/share/licenses/scikits-learn/LICENSE

  chmod -R 644 "$pkgdir/$_PYPATH/"*egg-info
  chmod 755 "$pkgdir/$_PYPATH/"*egg-info
}

